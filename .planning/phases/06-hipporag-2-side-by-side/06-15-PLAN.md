---
phase: 06-hipporag-2-side-by-side
plan: 15
type: execute
wave: 2
gap_closure: true
closes:
  - "SC2 / MODAL-05 (b) — the real cross-modality run against the Phase 3 parity corpus, re-attempted now that the fact-score blocker is fixed"
depends_on: ["06-14"]
files_modified:
  - databasise/evidence/CROSS-MODALITY-EVIDENCE.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [MODAL-05]

estimate:
  tokens: 15000
  raw_tokens: 30000
  tasks: 1
  confidence: high

must_haves:
  truths:
    - "The owner is asked exactly once, with the 2026-09-10 refusal named, the fix that closed it named, and a projected token figure derived from the parity corpus's own measured size — and with a named decline option."
    - "Whatever the owner answers, `databasise/evidence/CROSS-MODALITY-EVIDENCE.md` gains one dated section appended below the existing `## Real run attempted — refused — 2026-09-10` record; that record and the 2026-09-09 record are never rewritten."
    - "MODAL-05 flips to Complete only if both harnesses exited 0 and produced real values read from their own returned records; a decline, a refusal, or a partial run leaves the row Pending against a named outstanding item."
    - "If either harness refuses again, the refusal message is recorded verbatim and the run is not retried with a weakened or bypassed guard."
    - "No count, spend figure, duration, or comparison result appears anywhere in the new evidence section for a run that did not complete."
  artifacts:
    - databasise/evidence/CROSS-MODALITY-EVIDENCE.md
    - .planning/REQUIREMENTS.md
  key_links:
    - "06-14's fix -> `build_hipporag_index` dispatches no query-side node -> the 2026-09-10 provider 400 is unreachable -> the run is worth re-attempting"
    - "checkpoint `approve` -> `databasise.parity.build_hipporag_index` -> `databasise.parity.run_cross_modality` -> a dated `## Real run` section carrying only harness-returned values -> MODAL-05 Complete"
    - "checkpoint `decline` or a harness refusal -> a dated section naming the outcome -> MODAL-05 stays Pending"
  prohibitions:
    - "MUST NOT report any count, spend figure, duration, or comparison result for a run that did not complete — including as an illustration, a placeholder, or an estimate presented as a measurement."
    - "MUST NOT flip MODAL-05 to Complete on a declined, refused, or partial run."
    - "MUST NOT rewrite, reorder, or delete either existing dated section of `CROSS-MODALITY-EVIDENCE.md`; a new outcome is appended below them."
    - "MUST NOT retry a harness that refused by weakening, bypassing, or deleting its own guard in order to obtain a passing result."
    - "MUST NOT ask the owner this question more than once, and MUST NOT ask a narrowed version of it after a decline."
---

<objective>
Ask the one question 06-13 already asked and had answered `approve`, now that the concrete defect
that wasted that authorization is fixed — and record the answer honestly either way.

**What changed since 2026-09-10.** The owner authorized this run. It ran. Real spend was incurred.
Then `build_hipporag_index`'s own post-run verification refused, because the harness was resolving
HippoRAG's thirteen-position *base* wiring — query side included — for an index build, so
`fact-score` dispatched in the first ready batch with no query and handed the provider a zero-length
string. 06-14 fixed that at the root (the harness now resolves the seven-position `corpus-ingest`
wiring) and added a named client-level refusal so the whole class of empty-input call names itself
instead of buying a `400`. The money spent on 2026-09-10 does not come back; what this plan
guarantees is that the same money is not spent the same way twice.

**A decline is a complete, successful outcome of this plan.** The owner's standing posture is that
expensive measurement is opt-in, and they have exercised it. Nothing here pressures a particular
answer, and a second refusal from the harness itself is likewise a recorded outcome, not a failure
to work around.

**This plan contains no code.** It runs two already-written, already-tested harnesses if authorized,
and appends one dated section to one evidence document either way.

Purpose: roadmap Success Criterion 2 and MODAL-05 — two modalities actually running on one corpus
with structurally comparable outputs — is the milestone's core-value proof point and the one
remaining thing standing between this phase and its own goal.
Output: one dated appended section in `databasise/evidence/CROSS-MODALITY-EVIDENCE.md`, and a
MODAL-05 row that matches what actually happened.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-hipporag-2-side-by-side/06-VERIFICATION.md
@.planning/phases/06-hipporag-2-side-by-side/06-14-SUMMARY.md
@databasise/evidence/CROSS-MODALITY-EVIDENCE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Spend decision — re-attempt the real cross-modality run now that the fact-score blocker is fixed (MODAL-05)</name>

  <read_first>
    - `databasise/evidence/CROSS-MODALITY-EVIDENCE.md` — the whole file, especially `## Entry criterion for the deferred run` (which names `build_hipporag_index` then `run_cross_modality`, live `v1/.env.parity` credentials, `qwen/qwen3.7-flash` extraction and `qwen/qwen3-embedding-8b` embedding), the `## Real run attempted — refused — 2026-09-10` section whose verbatim provider error this plan's predecessor closed, and the `**Limits.**` paragraph whose no-fabricated-numbers discipline both branches must keep
    - `.planning/phases/06-hipporag-2-side-by-side/06-14-SUMMARY.md` — what was actually fixed, and the observed test counts; the checkpoint question below must describe the fix as it landed, not as it was planned
    - `databasise/parity/build_hipporag_index.py` — as amended by 06-14: `_INDEX_WIRING_VARIANT`, the `consumes_documents`-driven stamping, `_assert_preconditions`, the `partial`/`degraded` refusal, the empty-extraction verification block, `IndexBuildResult`'s field set, and `main()`'s exit codes
    - `databasise/parity/run_cross_modality.py` — `preflight()`, `MissingArmIndexError`, `CrossModalityRecord`'s field set, the refusal on a `partial`/`degraded` arm, and what its summary states about retrieval differences not being defects
    - `databasise/tests/fixtures/corpus/MANIFEST.json` — the corpus this run uses: `document_count` 20, `corpus_hash` `ac55d19e…`, and exactly 2 entries under `queries`
    - `.planning/REQUIREMENTS.md` line 33 — MODAL-05's row and both existing annotations (`[Measured 2026-09-09 (06-08-PLAN.md): …]` and `[Attempted 2026-09-10 (06-13-PLAN.md): …]`), which name the outstanding item verbatim
    - `.planning/phases/05-opaque-side-admission/05-LEARNINGS.md` — the mass-revert lesson: never round a partial result up to Complete
  </read_first>

  <files>databasise/evidence/CROSS-MODALITY-EVIDENCE.md, .planning/REQUIREMENTS.md</files>

  <precondition>Three things must hold before the owner is asked, and an unmet one makes the question moot: (1) `v1/.env.parity` exists and carries every key `databasise.parity.run_arm._build_clients` requires, and the Phase 3 v1-built LightRAG index is present under the parity store root — `build_hipporag_index._assert_preconditions` asserts both and raises `MissingParityEnvError` / `MissingParityEnvKeyError` / `MissingParityLightRAGIndexError` by name; (2) 06-14 landed, so `load_wiring("hipporag", variant="corpus-ingest")` returns a 7-node wiring and `EmptyEmbeddingInputError` is importable from `databasise.clients.openai_compat`; (3) `cd databasise && uv run pytest -q` exits 0. Assert all three first; on any failure, report the unmet precondition and stop rather than asking the owner to spend against a broken tree.</precondition>

  <action>
**Before asking, state the numbers and their derivation.** Read
`databasise/tests/fixtures/corpus/MANIFEST.json` and report the corpus's own measured size —
**20 documents, 2 queries, `corpus_hash` `ac55d19ec162cc9abf51ddf8502436109b1439c5cbaea4cf41c448c11575d5bd`**
— and re-state 06-13's own committed byte figure for that corpus, **15,711 bytes / 2,119 words**,
labelled as carried forward from that plan rather than re-measured here. Do not invent a new
projection method; reuse 06-13's, which the owner has already read once.

**Present exactly this question, once, and stop.**

> **Re-authorize the real cross-modality run?** (MODAL-05, this milestone's core-value proof point)
>
> **You authorized this once, on 2026-09-10, and it cost money and produced nothing.** The run died
> at the `fact-score` node on a provider `400 too_small` over a zero-length string. The cause has
> since been found and fixed: `build_hipporag_index` was resolving HippoRAG's thirteen-position
> *base* wiring for an index build, so three query-side positions dispatched with no query in their
> config. It now resolves the seven-position `corpus-ingest` wiring instead, and an empty or
> blank embedding batch now refuses locally by name (`EmptyEmbeddingInputError`) before any request
> is constructed. Both changes are covered by tests that reproduce the exact 2026-09-10 input shape
> at zero cost.
>
> **What would run:** one invocation of `databasise.parity.build_hipporag_index` against the
> 20-document Phase 3 parity corpus using live `v1/.env.parity` credentials
> (`qwen/qwen3.7-flash` extraction, `qwen/qwen3-embedding-8b` embedding), then one invocation of
> `databasise.parity.run_cross_modality`, putting each of the corpus's 2 queries through a single
> `Databasise.compare()` call against both arms.
>
> **Projected cost, derivation carried forward from 06-13:** the corpus is 15,711 bytes of source
> text (≈3.9k input tokens at 4 chars/token). HippoRAG's OpenIE position makes roughly two LLM
> calls per chunk over that text, each carrying prompt overhead, so the index build projects to the
> low hundreds of thousands of tokens — call it **0.1–0.3M tokens**. The comparison pass is bounded
> per query by v1's own 30,000-token assembled-context ceiling (RIG §CM.2, code-verified), times
> two arms, times 2 queries. **Total projection: well under 1M tokens**, or roughly **$0.01–$1.00**
> at a $0.10–$1.00 per-million band for models of this class. Both are projections labelled as
> such; neither is a quote, and neither may be reported afterwards as a measured spend.
>
> **Options:** `approve` (run both harnesses now) · `decline` (leave MODAL-05 BLOCKED, recorded)

**On `approve`:** run `uv run python -m databasise.parity.build_hipporag_index`, then — only if it
exits 0 — `uv run python -m databasise.parity.run_cross_modality`. A non-zero exit from either is a
refusal, not a result: capture its message verbatim, stop the branch there, and take the
harness-refused branch below. Never retry with a weakened guard, never edit a guard to make a run
pass, and never report a build the harness itself declined to verify.

On a clean run, **append** a new `## Real run — <ISO date>` section to
`databasise/evidence/CROSS-MODALITY-EVIDENCE.md`, below every existing section, in this document's
own house shape — claim / method / findings / verdict / limits. Carry only real values read from
the two harnesses' own returned records: `corpus_hash`, `workspace`, `graph_node_count`,
`graph_edge_count`, `chunk_vector_count`, `entity_vector_count`, `fact_vector_count`, the
`token_spend` breakdown assembled from the run's own `TokenAccounting`, `duration_seconds`,
`partial`, `degraded`, `artifacts_overlap`, and the per-query comparison structure
`CrossModalityRecord` actually returns. State in the Limits paragraph that the 2026-09-10 attempt's
own spend is not included in and not recoverable from these figures. Then flip MODAL-05 to `- [x]`
and `| MODAL-05 | Phase 6 | Complete |` in `.planning/REQUIREMENTS.md`, **appending** a dated
`[Measured <date> (06-15-PLAN.md): …]` annotation citing the new evidence section — never replacing
the 06-08 or 06-13 annotations.

**On a harness refusal after `approve`:** append a `## Real run attempted — refused — <ISO date>`
section carrying the refusal's verbatim message, the fact that spend was incurred before it, and an
explicit statement that no count, duration, or comparison result is reported because the harness
never assembled one. File the new blocker in `.planning/WINDOWS.md` via
`node <the gsd-tools path> windows add` with the failing module and a description naming the
observed error. Leave MODAL-05 `Pending` and append a dated annotation naming the new blocker.
Report the plan as complete — an honestly recorded second refusal is a valid outcome.

**On `decline`:** append a `## Deferred — <ISO date>` section stating the date, that the owner
declined the re-authorization, the owner's stated reason verbatim if given, and that the entry
criterion is otherwise unchanged. Add one sentence recording what *did* change since 2026-09-10:
06-14 fixed the `fact-score` blocker at its root and added a named empty-input refusal, so the
concrete defect the previous attempt died on is closed. Leave MODAL-05 `Pending` and append a dated
annotation naming the spend as the single outstanding item.

Do not ask this question twice, and do not ask a narrowed version of it after a decline.
  </action>

  <instructions>
    Everything automatable is already done. `build_hipporag_index.py` and `run_cross_modality.py`
    are written and tested; 06-14 fixed the exact defect that wasted your 2026-09-10 authorization
    and pinned it with tests that reproduce the failing input shape without spending anything; and
    I have asserted the three preconditions (env keys, the Phase 3 LightRAG index, a green suite)
    before asking.

    The one thing I cannot do is authorize the spend. Reply `approve` to have me run both harnesses
    now against live `v1/.env.parity` credentials, or `decline` to leave MODAL-05 recorded as
    BLOCKED with today's date and your reason appended to the evidence document. Declining is a
    complete, valid answer — you gave it at 06-06 and 06-08, and nothing here pressures a different
    one. Note honestly: the previous authorization cost real money for no result, and while the
    named cause of that is fixed, this is a first-ever completing run of this path against live
    credentials and a further unknown is possible.
  </instructions>

  <verification>
    On `approve` and a clean run: both harness invocations exit 0, and I read back non-zero graph
    node/edge counts, non-zero entity and fact vector counts, a real duration, and `partial` /
    `degraded` both false from the harnesses' own returned records before writing anything — then
    append them and flip MODAL-05.
    On `approve` and a harness refusal: I record the verbatim message, file the blocker in
    `.planning/WINDOWS.md`, report no counts at all, and leave MODAL-05 Pending.
    On `decline`: I append a dated deferral section containing no count, spend, duration or
    comparison result, and leave MODAL-05 Pending.
    Under every branch I re-run the evidence-document structural suite.
  </verification>

  <resume-signal>Reply `approve` to re-authorize the run, or `decline` to record the deferral.</resume-signal>

  <acceptance_criteria>
    - `databasise/evidence/CROSS-MODALITY-EVIDENCE.md` still contains its original heading `# Cross-Modality Evidence — MODAL-05: BLOCKED, Not Discharged`, its original `**Date:** 2026-09-09` line, and its `## Real run attempted — refused — 2026-09-10` heading, all byte-for-byte.
    - The file contains exactly one new `## ` section dated today, appended after all pre-existing content, and its line count is strictly greater than its pre-task line count.
    - If the answer was `approve` and both harnesses exited 0: MODAL-05 reads `- [x]` and `| MODAL-05 | Phase 6 | Complete |`, and the new section carries non-zero graph node and edge counts, non-zero entity and fact vector counts, a real duration, `partial` and `degraded` both false, and a per-query comparison structure for both of the corpus's 2 queries.
    - If the answer was `decline`, or if either harness exited non-zero: MODAL-05 still reads `- [ ]` and `| MODAL-05 | Phase 6 | Pending |`, and the new section reports no count, spend, duration or comparison result of any kind.
    - If a harness exited non-zero: `.planning/WINDOWS.md` gained a new row whose `phase` is `06` and whose `status` is `open`, added through the tool rather than by hand.
    - The MODAL-05 row retains both its `[Measured 2026-09-09 (06-08-PLAN.md):` and `[Attempted 2026-09-10 (06-13-PLAN.md):` annotations under every branch.
  </acceptance_criteria>

  <verify>
    <automated>cd /home/chris/coding/Databasise-2.0-fully-agnostic-system && grep -c '^\*\*Date:\*\* 2026-09-09' databasise/evidence/CROSS-MODALITY-EVIDENCE.md && grep -c '^## Real run attempted — refused — 2026-09-10' databasise/evidence/CROSS-MODALITY-EVIDENCE.md && grep -c "^## .*$(date +%Y-%m-%d)" databasise/evidence/CROSS-MODALITY-EVIDENCE.md</automated>
    <fails_when>non-zero exit from any grep, or any of the three printed counts is 0 — a 0 on the first or second means prior history was rewritten instead of appended to; a 0 on the third means no dated section was recorded for this run</fails_when>
    <automated>cd /home/chris/coding/Databasise-2.0-fully-agnostic-system && grep -c 'Measured 2026-09-09 (06-08-PLAN.md)' .planning/REQUIREMENTS.md && grep -c 'Attempted 2026-09-10 (06-13-PLAN.md)' .planning/REQUIREMENTS.md && grep -c '| MODAL-05 | Phase 6 | \(Complete\|Pending\) |' .planning/REQUIREMENTS.md</automated>
    <fails_when>non-zero exit, or either of the first two printed counts is 0 (an earlier annotation was replaced rather than appended to), or the third printed count is not exactly 1 (the row is absent or duplicated)</fails_when>
    <automated>cd databasise && uv run pytest -q tests/evidence/test_cross_modality_record.py -x</automated>
    <fails_when>non-zero exit, or the summary line reads "no tests ran" or "0 passed" — the structural assertions over this evidence document must still hold after the append</fails_when>
    <automated>cd databasise && uv run pytest -q</automated>
    <fails_when>non-zero exit, or the summary line reports any failure or error, or reports fewer than 943 passed — a real run must not have left the tree red</fails_when>
  </verify>

  <reversibility rating="costly">A real LLM/embedding run spends money that cannot be un-spent and writes a real index into the parity store root; the index is deletable, the spend is not. This is the second such authorization for the same run — the first produced nothing — which raises the cost of a wrong answer without changing its shape. Rated `costly` rather than `one-way` deliberately: a `one-way` rating would require a preceding `checkpoint:decision`, and this task **is** a blocking human checkpoint over exactly that decision; inserting a second gate would ask the owner the same question twice, which this plan's own prohibitions forbid.</reversibility>

  <done>The owner has answered once; `CROSS-MODALITY-EVIDENCE.md` carries exactly one dated appended section recording that answer and its real consequences; MODAL-05's row matches what actually happened, with both prior annotations intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner's checkpoint answer -> a live, billed provider run | The single authorization that turns a plan into spend crosses here |
| harness-returned record -> a committed evidence document | Measured values become permanent project evidence here; a fabricated one is indistinguishable downstream |
| `v1/.env.parity` -> `_build_clients` | Real credentials enter the process here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-15-01 | Repudiation (a fabricated measurement) | the new `CROSS-MODALITY-EVIDENCE.md` section | critical | mitigate | Every reported field is named explicitly in the action and must be read from `IndexBuildResult` / `CrossModalityRecord`; the decline and harness-refusal branches are required to report no numbers at all, and the acceptance criteria assert that absence. |
| T-06-15-02 | Tampering (history rewrite) | the two pre-existing dated sections | high | mitigate | The action requires an append below all existing content; the acceptance criteria and the first `<verify>` command pin the 2026-09-09 date line and the 2026-09-10 heading byte-for-byte. |
| T-06-15-03 | Denial of service (spend exhaustion) | `build_hipporag_index` / `run_cross_modality` | high | mitigate | One invocation each, gated behind a blocking human checkpoint, against a 20-document / 2-query corpus with a stated projection; a non-zero exit stops the branch rather than triggering a retry, and retrying with a weakened guard is prohibited outright. |
| T-06-15-04 | Elevation of privilege (a plan overriding an owner decision) | the checkpoint itself | high | mitigate | The question is asked once; re-asking or asking a narrowed variant after a decline is prohibited, and a decline is defined as this plan's success state rather than its failure state. |
| T-06-15-05 | Information disclosure | credentials in `v1/.env.parity` | medium | mitigate | Credentials are reached only through the existing `run_arm._load_env_file` / `_build_clients` path; no env value is printed, refusals name the missing key rather than its value, and no credential may appear in the evidence document. |
| T-06-15-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no package and adds no dependency; it runs two already-committed modules. |
</threat_model>

<artifacts_this_phase_produces>
| Kind | Symbol / path |
|------|---------------|
| Evidence section | `## Real run — <date>` **or** `## Real run attempted — refused — <date>` **or** `## Deferred — <date>` in `databasise/evidence/CROSS-MODALITY-EVIDENCE.md` (exactly one of the three) |
| Requirement annotation | `[Measured <date> (06-15-PLAN.md): …]` or `[Deferred <date> (06-15-PLAN.md): …]` on MODAL-05 in `.planning/REQUIREMENTS.md` |
| Ledger row (conditional) | a new `.planning/WINDOWS.md` entry, added only on a harness refusal |
</artifacts_this_phase_produces>

<flagged_assumptions>
- **Edge probe, `MODAL-05` (`unclassified`, `unresolved`).** The probe could not classify this
  requirement into an edge category and it is carried forward unresolved rather than auto-resolved
  with a backstop. MODAL-05's own outcome here is decided by a real run and an owner decision, not
  by a boundary predicate, so no acceptance criterion is authored from the probe row.
- **The 06-13 cost projection is carried forward, not re-derived.** The 15,711-byte / 2,119-word
  figure and the 0.1–0.3M-token estimate come from 06-13-PLAN.md, which the owner has already read.
  Re-deriving them would produce a second, differently-rounded number for the same corpus and
  invite the reader to treat one of them as measured. The `document_count` and query count are
  re-read from `MANIFEST.json` at run time.
</flagged_assumptions>

<verification>
- The evidence document retains both prior dated sections byte-for-byte and gains exactly one new one.
- MODAL-05's row state matches the branch actually taken, with both prior annotations intact.
- `cd databasise && uv run pytest -q tests/evidence/test_cross_modality_record.py -x` passes.
- `cd databasise && uv run pytest -q` passes at or above 943 passed / 4 skipped.
</verification>

<success_criteria>
- The owner was asked once, against a stated projection and with the previous wasted authorization
  named honestly.
- Exactly one dated section was appended to `databasise/evidence/CROSS-MODALITY-EVIDENCE.md`.
- MODAL-05 reads Complete if and only if both harnesses exited 0 and produced real values.
- No number of any kind appears in the evidence document for a run that did not complete.
</success_criteria>

<output>
Create `.planning/phases/06-hipporag-2-side-by-side/06-15-SUMMARY.md` when done, recording the
branch taken, the owner's answer verbatim, and — only on a completed run — the harness-returned
values as actually observed.
</output>
