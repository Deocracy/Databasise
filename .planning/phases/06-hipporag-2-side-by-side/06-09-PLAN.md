---
phase: 06-hipporag-2-side-by-side
plan: 09
type: execute
wave: 6
depends_on: ["06-06", "06-08"]
files_modified:
  - databasise/evidence/F-07-MUTABLE-STORE-DISPOSITION.md
  - databasise/seam/compare.py
  - databasise/seam/refusals.py
  - databasise/tests/seam/test_mutable_store_exclusion.py
  - databasise/tests/evidence/test_f07_record.py
  - databasise/evidence/HIPPORAG-PORT-RECORD.md
  - .planning/REQUIREMENTS.md
  - .planning/research/STACK.md
autonomous: true
requirements: [MACH-10]

estimate:
  tokens: 30000
  raw_tokens: 60000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "Every component in this project declaring `mutates_store` is named in the F-07 record in exactly one of two dispositions — a defined snapshot/reset protocol, or a recorded permanent exclusion from §5's parity and determinism comparisons."
    - "An F-07 record enumerating zero mutable-store components is refused as incomplete rather than accepted as a discharge; the record names every such component or it is not a discharge."
    - "A comparison request resolving a wiring whose parts declare `mutates_store` is refused by name, and the refusal cites the recorded disposition rather than failing generically."
    - "Re-rendering the F-07 record from the registry on unchanged inputs produces a byte-identical document, so a second run is not a second finding."
    - "The upstream-package parity measurement that would lift HippoRAG's index side out of `opaque` effective depth is recorded as deliberately deferred, with what it consists of and what would trigger it, rather than left absent."
    - "Every one of this phase's six requirement rows in REQUIREMENTS.md carries a dated status reflecting what was actually measured, including any that did not fully close."
    - statement: "Component identity in the F-07 record is compared as the exact `name@version` string the registry holds, never a case-folded, normalised or display form."
      verification: backstop
    - statement: "The recorded disposition states what is guaranteed if a comparison run is interrupted part-way through a snapshot or reset, or if two comparison runs touch the same mutable store concurrently."
      verification: backstop
  artifacts:
    - databasise/evidence/F-07-MUTABLE-STORE-DISPOSITION.md
    - databasise/tests/seam/test_mutable_store_exclusion.py
    - databasise/tests/evidence/test_f07_record.py
  key_links:
    - "registry parts declaring mutates_store -> the F-07 record's enumeration -> the comparison-path refusal that cites it"
  prohibitions:
    - "MUST NOT record F-07 as discharged without naming which of the two dispositions applies to every mutable-store component — a discharge that leaves one component unnamed is the open finding it claims to have closed."
    - "MUST NOT let a `mutable-store` component enter a §5 parity or determinism comparison silently — §14.4 point 3 requires an explicit refusal until a snapshot/reset protocol is defined and the node re-verified against it, and a silent inclusion produces a comparison whose reset state nobody controls."
    - "MUST NOT mark a requirement Complete in REQUIREMENTS.md on the strength of a plan having run — the status must reflect what the committed evidence actually establishes, per the Phase 5 lesson where five never-defective rows were reverted en masse from a phase status rather than per-requirement from the evidence."
---

<objective>
Discharge F-07 (MACH-10): name every `mutable-store` component in exactly one disposition — a defined snapshot/reset protocol or a recorded permanent A/B exclusion — enforce that disposition as an explicit refusal in the comparison path, and close the phase's record.

Purpose: F-07 has been open because nothing forced the question. A working comparison surface forces it: a mutable-store component is now reachable through the candidate pool, so its disposition is no longer theoretical.
Output: a committed F-07 disposition record, the refusal that enforces it, the deferred-measurement record, and a reconciled requirement table.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-hipporag-2-side-by-side/06-RESEARCH.md
@.planning/phases/06-hipporag-2-side-by-side/06-06-SUMMARY.md
@.planning/phases/06-hipporag-2-side-by-side/06-07-SUMMARY.md
@.planning/phases/06-hipporag-2-side-by-side/06-08-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols created by this plan: `databasise/evidence/F-07-MUTABLE-STORE-DISPOSITION.md`, `MutableStoreComparisonExcludedError`, `render_f07_record`.

Phase-level symbol list: see `06-01-PLAN.md` § "Artifacts this phase produces".

<tasks>

<task type="auto">
  <name>Task 1: Enumerate every mutable-store component and give each exactly one disposition</name>
  <files>databasise/evidence/F-07-MUTABLE-STORE-DISPOSITION.md, databasise/tests/evidence/test_f07_record.py</files>
  <read_first>
    - docs/system-model/CONTRACT.md (§14.4 point 3 in full — a `mutates_store`-declaring node "MUST be excluded from §5's parity and determinism comparisons by an explicit refusal ... unless and until a snapshot/reset protocol is defined and the node is re-verified against it")
    - docs/system-model/MODEL-RED-TEAM.md (the F-07 finding in full, including its three named roster entries `CA-2`, `MC-1` and `CA-7`)
    - docs/system-model/PARTS.md (`## §X` — codebase-memory-mcp's mutation-surface scan, its self-contained SQLite storage location, and the `delete_project` unlink behaviour; the X1 defect row)
    - databasise/foreign/codebase_memory_mcp_adapter.py (the adapter's own mutation-surface handling and its process-lifecycle framing)
    - databasise/parts/registry.py (`default_registry` — the live source of which parts declare `mutates_store`)
    - databasise/evidence/ADMISSION-CODEBASE-MEMORY-MCP.md (the eleven-verdict house format and its verdict vocabulary wider than a boolean: `satisfied` / `confirms-the-rule` / `open-question` / `machine-side-obligation`)
    - .planning/phases/06-hipporag-2-side-by-side/06-RESEARCH.md (`## F-07 Snapshot/Reset Protocol` — the two legitimate outcomes, their costs, and assumption A5's write-quiesce caveat)
  </read_first>
  <action>
Enumerate the mutable-store population from the registry itself, not from a hand list: every `Part` in `default_registry()` whose `effects` contain `mutates_store`. Compare that live set against `MODEL-RED-TEAM.md`'s three named roster entries and state the difference explicitly — `CA-2` (codebase-memory-mcp) is admitted and running in this project today; `MC-1` (postgres-mcp's unrestricted `execute_sql`) and `CA-7` (a hypothetical memory-agent's memory-edit node) are catalogued and not built. A disposition is owed for each of the three, and the two unbuilt ones get a disposition scoped to what they are: catalogued, not registered, and inheriting the default disposition if and when they are built.

Give each component exactly one of the two dispositions §14.4 point 3 admits. Apply 06-RESEARCH's recommended default — permanent exclusion from §5's parity and determinism comparisons — for codebase-memory-mcp, on its stated grounds: it is admitted whole-engine opaque, is already excluded from the default selector, and no requirement in this milestone asks for its `manage_adr`/`delete_project` behaviour to be A/B-testable. Record the alternative that was not taken, and what would reverse the decision: a requirement asking for A/B on that behaviour, at which point the snapshot/reset protocol is a file-copy-and-restore of the engine's own SQLite database plus its WAL and SHM siblings, in the adapter that already owns that file-unlink shape.

Record 06-RESEARCH's assumption A5 as a stated limit of that alternative rather than as a solved design: a file-level snapshot assumes no other process holds an open write handle across the snapshot window, which would need a brief write-quiesce this project has not verified against the adapter's actual concurrency model. That is exactly the concurrency question a reader will have, and it is answered as "not established" rather than left unasked.

Write `databasise/evidence/F-07-MUTABLE-STORE-DISPOSITION.md` in `ADMISSION-CODEBASE-MEMORY-MCP.md`'s house format: dated header; §14.4 point 3 and the F-07 finding quoted verbatim; a per-component table carrying the exact `name@version` string as the registry holds it, its disposition, its reason, and a `[code-verified]`/`[docs-verified]`/`[inference]` tag; a verdict; and method and limits. Compare and record component identity as the exact registry string — never a display form, never case-folded — so the record and the code name the same thing.

Render the document from the registry through a `render_f07_record` function rather than writing it by hand, following `parity_report.py`'s render-from-committed-inputs convention. Re-rendering on unchanged inputs must reproduce it byte-identically: a second run of the discharge is not a second finding.

`databasise/tests/evidence/test_f07_record.py` parses the document structurally — front matter and table, never whole-file grep — and asserts: the enumerated set equals the live `mutates_store` set computed from `default_registry()` at test time, plus the two catalogued-not-built entries; the table is non-empty, so a record enumerating nothing fails rather than reading as a discharge; every row carries a disposition drawn from the two admissible values and a non-empty reason; every component string matches the registry's own `name_at_version` exactly; and re-rendering reproduces the document byte-identically.
  </action>
  <verify>
    <automated>cd databasise && uv run pytest -q tests/evidence/test_f07_record.py -x</automated>
    <fails_when>non-zero exit, or "no tests ran" in the summary line</fails_when>
    <human-check>The owner has confirmed the disposition for codebase-memory-mcp — permanent exclusion from §5 comparisons, or build the snapshot/reset protocol — and the recorded reason matches their intent. MACH-10 is not marked Complete in REQUIREMENTS.md until this confirmation is recorded.</human-check>
  </verify>
  <acceptance_criteria>
    - `databasise/evidence/F-07-MUTABLE-STORE-DISPOSITION.md` exists with a dated header and quotes §14.4 point 3 and the F-07 finding verbatim
    - Its table names all three `MODEL-RED-TEAM.md` roster entries, each with exactly one disposition and a non-empty reason
    - A test asserts the enumerated set equals the live `mutates_store` set from `default_registry()`
    - A test asserts an empty table fails the record
    - A test asserts every component string equals a registry `name_at_version` exactly
    - A test asserts re-rendering reproduces the document byte-identically
    - The limits section records the write-quiesce caveat as unestablished
  </acceptance_criteria>
  <done>Every mutable-store component has exactly one recorded disposition, rendered from the registry and bound to a test that fails on an unenumerated component.</done>
  <reversibility rating="costly">A recorded permanent A/B exclusion cedes gate-adjudicated comparison for this capability class under the current contract; reversing it is an amendment to a committed disposition record, in the same shape as the GATE-01 waiver and the Phase 3 gate amendment, not a code change.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The explicit refusal §14.4 requires, in the comparison path</name>
  <files>databasise/seam/compare.py, databasise/seam/refusals.py, databasise/tests/seam/test_mutable_store_exclusion.py</files>
  <read_first>
    - docs/system-model/CONTRACT.md (§14.4 point 3's "excluded ... by an explicit refusal" wording — a refusal, not a filter and not a silent skip)
    - databasise/seam/compare.py and `Databasise.compare` (as landed by 06-03)
    - databasise/seam/refusals.py (the `SeamRefusalError` hierarchy and 05-LEARNINGS' rule that a refusal raised inside a pydantic validator never reaches the registered handler)
    - databasise/seam/selectors.py (`_wiring_effects` — the union of a candidate wiring's registered parts' own declared effects, the same computation this refusal reads)
    - databasise/wirings/codebase-memory-mcp.json (the registered wiring whose part declares `mutates_store`, now reachable through the candidate pool)
    - databasise/tests/seam/test_rest_transport.py (`_REFUSAL_FACTORIES` — the subclass-enumerating test that fails loudly for a refusal with no registered factory)
    - databasise/evidence/F-07-MUTABLE-STORE-DISPOSITION.md (as written by Task 1 — the disposition the refusal cites)
  </read_first>
  <behavior>
    - Test 1: a comparison whose selectors resolve a wiring containing a `mutates_store`-declaring part raises `MutableStoreComparisonExcludedError`, naming the excluded component and citing the recorded disposition.
    - Test 2: the refusal fires before any arm executes — no envelope is produced and no store is written for either arm.
    - Test 3: a single-selector call resolving the same wiring is a run, not a comparison, and is not refused — §14.4 point 3 excludes the component from parity and determinism comparisons, not from being run.
    - Test 4: a comparison between two wirings neither of which declares `mutates_store` is unaffected.
    - Test 5: the refusal names the excluded component and does not name any other candidate wiring, arm name or node id.
  </behavior>
  <action>
Add `MutableStoreComparisonExcludedError` to `databasise/seam/refusals.py` as a `SeamRefusalError` subclass carrying the excluded component's `name@version` and a pointer to the recorded disposition. Embed the class name in the message, following the existing refusals' second workaround, and name only the excluded component — never another candidate wiring, arm name or node id, per §18.2's closed-set rule applied to the refusal path.

In `Databasise.compare`, after each selector resolves and before any arm executes, compute each resolved wiring's declared effect union using `selectors.py`'s own `_wiring_effects` — the same function the capability selector already uses, never a second copy of the computation — and raise when `mutates_store` is present. This is §14.4 point 3's explicit refusal: not a filter that quietly drops the arm, not a warning, and not a comparison that proceeds with an uncontrolled reset state.

The refusal is scoped to comparisons only. A single-selector call is a run under RIG §RUN.3's degenerate-width rule, and §14.4 point 3 excludes the component from §5's parity and determinism comparisons rather than from execution — so `query()` and the one-selector `compare()` path are untouched.

Register a factory for the new refusal in `databasise/tests/seam/test_rest_transport.py`'s `_REFUSAL_FACTORIES`; the subclass-enumerating test fails loudly without one, and that is the intended signal.

Write `databasise/tests/seam/test_mutable_store_exclusion.py` covering the `<behavior>` block, driving the real registered `codebase-memory-mcp` wiring through `compare` and asserting the refusal fires before execution — assert on the absence of any store write, not only on the raised exception, so a refusal that fires after an arm already ran would still fail.
  </action>
  <verify>
    <automated>cd databasise && uv run pytest -q tests/seam/test_mutable_store_exclusion.py -x</automated>
    <fails_when>non-zero exit, or "no tests ran" in the summary line</fails_when>
    <automated>cd databasise && uv run --extra rest pytest -q tests/seam/ -x</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `MutableStoreComparisonExcludedError` is a `SeamRefusalError` subclass whose message contains its own class name and the excluded component's `name@version`
    - `Databasise.compare` computes the effect union via `selectors.py`'s `_wiring_effects` and defines no second effect-union computation
    - A test asserts no store directory is written for either arm when the refusal fires
    - A test asserts a single-selector call resolving the same wiring completes without the refusal
    - `_REFUSAL_FACTORIES` has an entry for `MutableStoreComparisonExcludedError`
    - The refusal message names no candidate wiring, arm name or node id other than the excluded component
  </acceptance_criteria>
  <done>§14.4 point 3's exclusion is an explicit, tested refusal in the comparison path rather than a clause nothing enforces.</done>
</task>

<task type="auto">
  <name>Task 3: Close the record — deferred measurement, stale docs, and per-requirement status</name>
  <files>databasise/evidence/HIPPORAG-PORT-RECORD.md, .planning/REQUIREMENTS.md, .planning/research/STACK.md</files>
  <read_first>
    - databasise/evidence/HIPPORAG-PORT-RECORD.md (as landed by 06-07 — its limits section, where the deferred measurement is recorded)
    - .planning/REQUIREMENTS.md (the six Phase 6 rows and the Traceability table; and MODAL-01's own annotation style, which states what was measured, what remains, and why the row's status is what it is)
    - .planning/phases/05-opaque-side-admission/05-VERIFICATION.md (the lesson that five never-defective rows were reverted en masse from a phase status rather than per-requirement from the evidence)
    - .planning/research/STACK.md (lines 26, 27, 48, 69, 75, 76, 85, 97, 110 — every LanceDB recommendation, and line 85's claim that `nano-vectordb` should be replaced by it)
    - .claude/CLAUDE.md (the recorded correction: the live engine defaults are Cozo for graph and Faiss for vector)
    - .planning/ROADMAP.md (Phase 1 success criterion 1's own inline amendment recording the same correction, dated 2026-08-30 per CONTEXT.md D-05)
    - .planning/phases/06-hipporag-2-side-by-side/06-06-SUMMARY.md and 06-08-SUMMARY.md (what the two measured plans actually established)
  </read_first>
  <action>
**The deferred measurement, recorded with a trigger.** Extend `HIPPORAG-PORT-RECORD.md`'s limits section into a named deferral entry for the upstream-package parity measurement, so it is a tracked deferral rather than an absence. State: what it consists of — the real upstream `hipporag` package pinned by exact commit SHA in its own isolated interpreter, never imported into `databasise`'s own environment under the same D-14 import-boundary rule Phase 5 enforced for v1's `lightrag`, run end to end on the same corpus and queries, compared at the retrieval level exactly as Phase 3 did for LightRAG; what it would buy — `stage`-eligibility for the index side, and with it true shared-artifact eligibility under §3's blast-radius rule and a genuine `reuse` classification from §6's reindex planner; why it is not run here — MODAL-04's own text requires the index-side effective depth to stay `opaque` until parity is shown, so keeping it `opaque` is what this phase delivers, and 06-RESEARCH's assumption A6 records the contrary reading as this-session inference rather than a stated requirement; and its trigger — the first requirement asking for HippoRAG index-side artifact sharing or `stage`-depth eligibility. Also record the upstream package's alpha status (`2.0.0a4`/`a5`) as a standing risk to that future measurement: a pre-1.0 API is being depended on for a measurement, not for production behaviour, and an upstream break would invalidate the harness rather than the engine.

**The stale vector-store recommendation.** `.planning/research/STACK.md` recommends LanceDB as the vector primitive across lines 26, 27, 48, 69, 75, 76, 85, 97 and 110. That recommendation was adopted from a factually wrong description of the incumbent and is superseded — the live defaults are Cozo for graph and Faiss for vector, as `ROADMAP.md`'s own dated Phase 1 amendment and `.claude/CLAUDE.md` both record, and as `databasise/pyproject.toml`'s actual pins show. Correct the document in place rather than deleting it: add a dated superseded-note at the head of the vector-store section naming the correction, its authority and its date, and amend each LanceDB recommendation row to record that Faiss is the shipped default and LanceDB is not adopted. Do not rewrite the file's history or remove the original reasoning; this project's own convention is to record a supersession rather than erase the superseded claim. Correct line 85's `nano-vectordb` replacement claim in the same pass — the replacement that actually shipped is Faiss, not LanceDB.

**Per-requirement status.** Update each of the six Phase 6 rows in `.planning/REQUIREMENTS.md` — MODAL-04, MODAL-05, API-08, MACH-10, MACH-02, MACH-03 — and their Traceability rows, from the committed evidence rather than from the phase's own status. Phase 5's own verification found five never-defective rows reverted en masse from a phase status; the correction is to read each row's status from what its evidence establishes, one row at a time.

Write each row's annotation in MODAL-01's established style: dated, stating what was measured, naming the committed evidence document, and naming plainly whatever remains outstanding. A row whose evidence is complete is marked Complete. A row with an outstanding owner confirmation — MACH-03's "materially narrower" threshold, MACH-10's disposition — stays Pending with the outstanding item named, and flips only once that confirmation is recorded. Do not round a partial result up: an outstanding human judgment is not a completed measurement, and a row marked Complete on the strength of a plan having run is exactly the staleness this pass exists to remove.
  </action>
  <verify>
    <automated>cd databasise && uv run pytest -q tests/evidence/ -x</automated>
    <fails_when>non-zero exit, or "no tests ran" in the summary line</fails_when>
    <automated>cd databasise && uv run pytest -q</automated>
    <fails_when>non-zero exit</fails_when>
    <automated>grep -c 'lancedb' .planning/research/STACK.md</automated>
    <fails_when>a count of 0 — the superseded rows are corrected in place and annotated, never deleted, so the term must still appear alongside its supersession note</fails_when>
  </verify>
  <acceptance_criteria>
    - `HIPPORAG-PORT-RECORD.md` has a named deferral entry for the upstream-package parity measurement stating its content, its benefit, its reason for deferral and its trigger
    - It records the upstream package's alpha status as a risk to that future measurement
    - `.planning/research/STACK.md` carries a dated supersession note at the head of its vector-store section naming Cozo and Faiss as the shipped defaults and citing its authority
    - Every LanceDB recommendation row in that file is annotated as not adopted, and line 85's `nano-vectordb` replacement claim names Faiss
    - All six Phase 6 requirement rows in `.planning/REQUIREMENTS.md` carry a dated annotation naming the committed evidence document that establishes their status
    - Any row with an outstanding owner confirmation is Pending with that item named, not Complete
    - The Traceability table's six Phase 6 rows agree with the annotated statuses above them
  </acceptance_criteria>
  <done>The phase's record is closed honestly: the one deliberate deferral is tracked with a trigger, the stale vector-store recommendation is superseded in place, and every requirement status reflects its own evidence.</done>
</task>

</tasks>

<verification>
- `cd databasise && uv run pytest -q` exits 0
- `cd databasise && uv run --extra rest pytest -q` exits 0
- `databasise/evidence/F-07-MUTABLE-STORE-DISPOSITION.md` is committed and re-renders byte-identically
- No Phase 6 requirement row is marked Complete without a committed evidence document named in its annotation
</verification>

<success_criteria>
MACH-10 is discharged: every mutable-store component carries exactly one recorded disposition, that disposition is enforced by an explicit refusal in the comparison path, and the phase's remaining record — the deferred parity measurement, the superseded vector-store recommendation, and the six requirement statuses — is closed against committed evidence rather than against the fact that the plans ran.
</success_criteria>

<flagged_assumptions>
- **The upstream-package parity harness is deliberately not built in this phase.** 06-RESEARCH's assumption A6 records the "showing parity means running the upstream package" reading as that session's own inference, not a clause any frozen document states. MODAL-04's text requires the index-side depth to stay `opaque` until parity is shown, and this phase delivers exactly that. The deferral is recorded with a trigger rather than left as an absence.
- **codebase-memory-mcp's disposition defaults to permanent exclusion.** 06-RESEARCH recommends it as the lower-risk default and no requirement in this milestone asks for that engine's mutation behaviour to be A/B-testable. The alternative and its reversal condition are recorded; the owner confirmation is a `human-check` on Task 1.
</flagged_assumptions>

<output>
Create `.planning/phases/06-hipporag-2-side-by-side/06-09-SUMMARY.md` when done
</output>
