---
phase: 06-hipporag-2-side-by-side
plan: 13
type: execute
wave: 3
gap_closure: true
closes:
  - "SC2 / MODAL-05 (b) — the real cross-modality run against the Phase 3 parity corpus (spend-gated)"
  - "SC6 / MACH-03 (3) — one real A/A calibration producing a p95 floor at each §EV.2 target family (spend-gated)"
depends_on: ["06-11", "06-12"]
files_modified:
  - databasise/evidence/CROSS-MODALITY-EVIDENCE.md
  - databasise/evidence/FALSIFIER-5-EVIDENCE.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [MODAL-05, MACH-03, MACH-02]

estimate:
  tokens: 25000
  raw_tokens: 50000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "The owner is asked exactly once per spend, with a concrete document count and a projected token figure derived from the corpus's own measured size, and with a named declined option."
    - "Whatever the owner answers, `databasise/evidence/CROSS-MODALITY-EVIDENCE.md` gains a dated entry appended below its existing 2026-09-09 record — the 2026-09-09 content is never rewritten."
    - "Whatever the owner answers, `databasise/evidence/FALSIFIER-5-EVIDENCE.md` gains a dated entry appended below its existing 2026-09-09 record — the 2026-09-09 content is never rewritten."
    - "A requirement row flips to Complete only when the run that would satisfy its own pass criterion actually executed and produced real values; a declined or partial spend leaves the row Pending against a named outstanding item."
    - "If the A/A calibration runs, the `materially narrower` threshold is written into the evidence document before either floor is read, not chosen after seeing them."
    - "No floor, count, spend, duration or comparison result appears anywhere in either evidence document for a run that did not happen."
  artifacts:
    - databasise/evidence/CROSS-MODALITY-EVIDENCE.md
    - databasise/evidence/FALSIFIER-5-EVIDENCE.md
    - .planning/REQUIREMENTS.md
  key_links:
    - "checkpoint answer -> a dated appended entry in the matching evidence document -> the REQUIREMENTS.md row's own state"
    - "approve (cross-modality) -> databasise.parity.build_hipporag_index -> databasise.parity.run_cross_modality -> CROSS-MODALITY-EVIDENCE.md real counts"
    - "approve (A/A) -> databasise.eval.remint.main -> bundle@v2 -> databasise.eval.corpus_ingest --spend -> databasise.eval.calibration.calibrate_aa_floor -> FALSIFIER-5-EVIDENCE.md real floors"
    - "decline (either) -> a dated BLOCKED entry naming the date and the reason -> the row stays Pending"
  prohibitions:
    - "A declined spend checkpoint must not be rounded up to Complete in REQUIREMENTS.md."
    - "An evidence document must not be rewritten in place on a re-run — a dated entry is appended below the existing record."
    - "No floor, count, spend, duration or comparison result may be reported for a run that did not happen, including as an illustration or a placeholder."
    - "A partial result (one target family calibrated, not both) must not be recorded as Falsifier 5 discharged."
    - "The owner must not be asked either question more than once in this plan."
---

<objective>
Present the two real-corpus spends this phase has now twice deferred, each as one blocking
checkpoint with a concrete number attached, and record the answer honestly either way.

**A declined checkpoint is a complete, successful outcome of this plan.** The owner declined both of
these spends at 06-06 and again at 06-08, and the standing posture is explicit: build the product
first, expensive measurement is opt-in. Nothing in this plan assumes authorization. What this plan
guarantees is that the question is finally *answerable* — 06-10 gave HippoRAG a real write path
through the seam, and 06-12 turned "unbudgetable" into a printed projection and a document-count
bound — so the owner is choosing against a number instead of against an unknown.

**Everything spend-free is already done.** Gap 1(a) closed in 06-10; MACH-10 closed in 06-11; both of
Falsifier 5's named preconditions closed in 06-12. This plan contains no code. It runs two harnesses
*if* authorized, and appends to two evidence documents either way.

**No tracer task.** Both tasks are checkpoints; there is no slice to trace. TRACER_MODE's
leading-slice rule was discharged by 06-10's and 06-12's own tracer tasks, which this plan depends
on.

Purpose: MODAL-05 and MACH-03 are the two roadmap success criteria 06-VERIFICATION.md marked FAILED.
Their remaining blocker is a spend decision, not an engineering one.
Output: a dated appended entry in each of the two evidence documents, and a REQUIREMENTS.md whose
rows match what actually happened.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-hipporag-2-side-by-side/06-VERIFICATION.md
@databasise/evidence/CROSS-MODALITY-EVIDENCE.md
@databasise/evidence/FALSIFIER-5-EVIDENCE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Spend decision — the real cross-modality run against the Phase 3 parity corpus (MODAL-05)</name>

  <read_first>
    - `databasise/evidence/CROSS-MODALITY-EVIDENCE.md` — the whole file, especially "Entry criterion for the deferred run" (which names `build_hipporag_index` then `run_cross_modality`, live `v1/.env.parity` credentials, `qwen/qwen3.7-flash` extraction and `qwen/qwen3-embedding-8b` embedding) and the "Limits, stated plainly" section whose no-fabricated-numbers discipline both branches must keep
    - `databasise/parity/build_hipporag_index.py` — `main()`'s exit codes, `_assert_preconditions`, and the empty-extraction guard that refuses to report an unverified build
    - `databasise/parity/run_cross_modality.py` — `preflight()`, `MissingArmIndexError`, `CrossModalityRecord`, and what it refuses to report
    - `databasise/tests/fixtures/corpus/MANIFEST.json` — the corpus this run uses: 20 documents, 15,711 bytes on disk
    - `.planning/REQUIREMENTS.md` line 33 — MODAL-05's row and its existing `[Measured 2026-09-09 (06-08-PLAN.md): ...]` annotation, which names the outstanding item verbatim
    - `.planning/phases/05-opaque-side-admission/05-LEARNINGS.md` — the mass-revert lesson: never round a partial result up to Complete
  </read_first>

  <files>databasise/evidence/CROSS-MODALITY-EVIDENCE.md, .planning/REQUIREMENTS.md</files>

  <precondition>`v1/.env.parity` exists and carries every key `databasise.parity.run_arm._build_clients` requires, and the Phase 3 v1-built LightRAG index is present under the parity store root — `build_hipporag_index._assert_preconditions` asserts both and raises `MissingParityEnvError`/`MissingParityEnvKeyError`/`MissingParityLightRAGIndexError` by name. Assert these before asking the owner to spend: an unmet precondition makes the question moot, and the correct outcome is to report the unmet precondition rather than to ask.</precondition>

  <action>
**Before asking, compute and print the projection.** Run
`cd databasise && uv run python -m databasise.eval.corpus_ingest --limit 20 --arm hipporag` as the
shape of the projection (it reads the eval-corpus, not the parity corpus, so use it as the printed
method reference only), and independently state the parity corpus's own measured size read from
`databasise/tests/fixtures/corpus/MANIFEST.json`: **20 documents, 15,711 bytes, 2,119 words**.

**Present exactly this question, once, and stop.**

> **Authorize the real cross-modality run?** (MODAL-05, this milestone's core-value proof point)
>
> **What would run:** one invocation of `databasise.parity.build_hipporag_index` against the
> 20-document Phase 3 parity corpus using live `v1/.env.parity` credentials
> (`qwen/qwen3.7-flash` extraction, `qwen/qwen3-embedding-8b` embedding), then one invocation of
> `databasise.parity.run_cross_modality` putting each of the corpus's own queries through a single
> `Databasise.compare()` call against both arms.
>
> **Projected cost, with the derivation stated:** the corpus is 15,711 bytes of source text
> (≈3.9k input tokens at 4 chars/token). HippoRAG's OpenIE position makes roughly two LLM calls per
> chunk over that text, each carrying prompt overhead, so the index build projects to the low
> hundreds of thousands of tokens — call it **0.1–0.3M tokens**. The comparison pass is bounded per
> query by v1's own 30,000-token assembled-context ceiling (RIG §CM.2, code-verified), times two
> arms, times the corpus's own query count. **Total projection: well under 1M tokens.** At a
> $0.10–$1.00 per-million-token band for models of this class that is roughly **$0.01–$1.00**.
> Both figures are projections labelled as such; neither is a quote, and this project's own
> discipline forbids reporting either as a measured spend.
>
> **Options:** `approve` (run both harnesses now) · `decline` (leave MODAL-05 BLOCKED, recorded)

**On `approve`:** run `uv run python -m databasise.parity.build_hipporag_index`, then
`uv run python -m databasise.parity.run_cross_modality`. A non-zero exit from either is a refusal,
not a result: record the refusal's own message and stop — never retry with a weakened guard, and
never report a build the harness itself refused to verify. On success, **append** a new
`## Real run — <ISO date>` section to `databasise/evidence/CROSS-MODALITY-EVIDENCE.md` below the
existing 2026-09-09 record (never rewriting it), in the house sections claim / method / findings /
verdict / limits, carrying only real values read from the two harnesses' own returned records:
graph node and edge counts, chunk/entity/fact vector counts, token spend from the run's own
`TokenAccounting`, duration, `partial`/`degraded`, `artifacts_overlap`, and the per-query
comparison structure. Then flip MODAL-05 to `- [x]` and `| MODAL-05 | Phase 6 | Complete |` in
`.planning/REQUIREMENTS.md`, **appending** a dated `[Measured <date> (06-13-PLAN.md): ...]`
annotation citing the new evidence section — never replacing the 06-08 annotation.

**On `decline`:** **append** a `## Deferred again — <ISO date>` entry to
`databasise/evidence/CROSS-MODALITY-EVIDENCE.md` below the existing record, stating the date, that
the owner declined the spend a second time, the owner's stated reason verbatim if given, and that
the entry criterion is unchanged. Add one sentence recording what *did* change since 2026-09-09:
06-10 gave HippoRAG a real ingest path through the public seam
(`databasise/wirings/hipporag/corpus-ingest.json`), so the structural half of Gap 1 is closed and
only the spend remains. Leave MODAL-05 `Pending` in `.planning/REQUIREMENTS.md` and append a dated
annotation naming the spend as the single outstanding item. Report the plan as complete: a recorded
decline is this task's success state, not its failure state.

Do not ask this question twice, and do not ask a narrowed version of it after a decline.
  </action>

  <instructions>
    I have already done everything automatable: `databasise/parity/build_hipporag_index.py` and
    `databasise/parity/run_cross_modality.py` are written, reviewed and ready; both preconditions
    (`v1/.env.parity` keys, the Phase 3 v1-built LightRAG index) are asserted before I ask; 06-10
    gave HippoRAG a real ingest path through the public seam so the structural half of this gap is
    already closed; and the projection above is computed from the corpus's own measured size, not
    guessed.

    The one thing I cannot do is authorize the spend. Reply `approve` to have me run both harnesses
    now against live `v1/.env.parity` credentials, or `decline` to leave MODAL-05 recorded as
    BLOCKED with today's date and your reason appended to the evidence document. Declining is a
    complete, valid answer — it is the same answer you gave at 06-08, and nothing here pressures a
    different one.
  </instructions>

  <verification>
    On `approve`: both harness invocations exit 0, and I read back non-zero graph node/edge counts,
    non-zero entity/fact vector counts, a real duration and `partial`/`degraded` both false from the
    harnesses' own returned records — then append them to `CROSS-MODALITY-EVIDENCE.md` and flip
    MODAL-05.
    On `decline`: I append a dated deferral entry containing no count, spend, duration or comparison
    result, and leave MODAL-05 Pending.
    Either way I re-run `cd databasise && uv run pytest -q tests/evidence/test_cross_modality_record.py -x`
    to prove the evidence document still parses against its own structural assertions.
  </verification>

  <resume-signal>Reply `approve` to authorize the run, or `decline` to record the deferral.</resume-signal>

  <acceptance_criteria>
    - `databasise/evidence/CROSS-MODALITY-EVIDENCE.md` still contains its original heading `# Cross-Modality Evidence — MODAL-05: BLOCKED, Not Discharged` and its original `**Date:** 2026-09-09` line, byte-for-byte.
    - `databasise/evidence/CROSS-MODALITY-EVIDENCE.md` contains exactly one new `## ` section dated today, appended after the pre-existing content.
    - The file's line count is strictly greater than its pre-task line count.
    - If the answer was `approve`: MODAL-05 reads `- [x]` and `| MODAL-05 | Phase 6 | Complete |`, and the new evidence section carries non-zero graph node and edge counts, non-zero entity and fact vector counts, a real duration, and `partial`/`degraded` both false.
    - If the answer was `decline`: MODAL-05 still reads `- [ ]` and `| MODAL-05 | Phase 6 | Pending |`, and the new evidence section contains no count, spend, duration or comparison result of any kind.
    - The MODAL-05 row retains its original `[Measured 2026-09-09 (06-08-PLAN.md):` annotation under either answer.
  </acceptance_criteria>

  <verify>
    <automated>cd /home/chris/coding/Databasise-2.0-fully-agnostic-system && grep -c '^\*\*Date:\*\* 2026-09-09' databasise/evidence/CROSS-MODALITY-EVIDENCE.md && grep -c 'Measured 2026-09-09 (06-08-PLAN.md)' .planning/REQUIREMENTS.md && grep -c "^## .*$(date +%Y-%m-%d)" databasise/evidence/CROSS-MODALITY-EVIDENCE.md</automated>
    <fails_when>non-zero exit from any grep, or any of the three printed counts is 0 — a 0 on the first or second means prior history was rewritten instead of appended to; a 0 on the third means no dated entry was recorded for this run</fails_when>
    <automated>cd /home/chris/coding/Databasise-2.0-fully-agnostic-system && grep -c '| MODAL-05 | Phase 6 | \(Complete\|Pending\) |' .planning/REQUIREMENTS.md</automated>
    <fails_when>the printed count is not exactly 1 — the row must be in exactly one of the two states, never absent and never duplicated</fails_when>
    <automated>cd databasise && uv run pytest -q tests/evidence/test_cross_modality_record.py -x</automated>
    <fails_when>non-zero exit, or the summary line reads "no tests ran" or "0 passed" — the structural assertions over this evidence document must still hold after the append</fails_when>
  </verify>

  <reversibility rating="costly">A real LLM/embedding run spends money that cannot be un-spent and writes a real index into the parity store root; the index itself is deletable, the spend is not. Rated `costly` rather than `one-way` deliberately: a `one-way` rating would require a preceding `checkpoint:decision`, and this task **is** a blocking human checkpoint over exactly that decision — inserting a second gate would ask the owner the same question twice, which this plan's own prohibitions forbid.</reversibility>

  <done>The owner has answered once; `CROSS-MODALITY-EVIDENCE.md` carries a dated appended entry recording that answer and its consequences; MODAL-05's row in `.planning/REQUIREMENTS.md` matches what actually happened, with its 06-08 annotation intact.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Spend decision — the real A/A calibration at both §EV.2 target families (MACH-03, Falsifier 5)</name>

  <read_first>
    - `databasise/evidence/FALSIFIER-5-EVIDENCE.md` — the whole file: MACH-03's pass criterion quoted verbatim, the two-blocker findings table, "Two preconditions that must hold before this run can happen", and the Limits section's refusal to pre-register a threshold against floors that do not exist
    - `.planning/phases/06-hipporag-2-side-by-side/06-RESEARCH.md` — Open Question 1's standing recommended default for "materially narrower" (T1's calibrated p95 floor, in the same score units, at most half of T0's); this is the threshold to pre-register, not to re-derive
    - `databasise/eval/calibration.py` — `calibrate_aa_floor`'s signature, `NullIdentity`'s fields, `UnusableFloorError` and `StaleNullError` (the §AA.2 preconditions enforced as refusals), `correct_for_batch_width`
    - `databasise/eval/remint.py` and `databasise/eval/corpus_ingest.py` — **as built by 06-12**: `resolve_judge_identity`, `remint`, `main`; `estimate`, `ingest_documents`, `main`, and the `--limit`/`--arm`/`--spend` flags
    - `databasise/evidence/EVAL-BUNDLE-V1.md` — the §EV.2 target-family table's own cost bands (`gold_passage` ~1-5% of the full per-decision bill; `answer_level` the full ~5.1-5.2M-token query-side figure plus judging) and the split sizes (dev 17, holdout 9, sealed 4)
    - `.planning/REQUIREMENTS.md` lines 16-17 — MACH-02's and MACH-03's rows and their existing annotations
  </read_first>

  <files>databasise/evidence/FALSIFIER-5-EVIDENCE.md, .planning/REQUIREMENTS.md</files>

  <precondition>06-12's two modules exist and their tests pass (`cd databasise && uv run pytest -q tests/eval/ -x` exits 0), and `v1/.env.parity` carries every key `_build_clients` requires. Both of FALSIFIER-5-EVIDENCE.md's named preconditions are otherwise unaddressed and the question below is not yet answerable.</precondition>

  <action>
**Before asking, compute and print the projection.** Run
`cd databasise && uv run python -m databasise.eval.corpus_ingest --limit 291 --arm lightrag` and show
the owner its actual printed output — that is the real, computed ingest projection for the whole
eval-corpus, and the whole reason this question is now answerable.

**Present exactly this question, once, and stop.**

> **Authorize the real A/A calibration?** (MACH-03 / Falsifier 5 — the gate Phase 7's promotion
> evidence rides on)
>
> **What would run, in order:** (1) `databasise.eval.remint` makes one minimal live model call to
> resolve the judge identity and mints `bundle@v2` carrying it — `bundle@v1` is left byte-identical;
> (2) `databasise.eval.corpus_ingest --limit N --arm lightrag --spend` ingests the first N of the 291
> eval-corpus documents; (3) two paired runs over the bundle's dev split (17 questions) produce
> per-question differences, and `calibrate_aa_floor` bootstrap-resamples them to a p95 floor at each
> target family.
>
> **Projected cost, with the derivation stated:** the eval-corpus is 173,869 bytes across 291
> documents (≈43.5k input tokens at 4 chars/token). LightRAG's extraction pass over that projects to
> roughly **0.6–1.6M tokens** for a full 291-document ingest, and scales down linearly with `--limit`.
> The **gold-passage (T1) leg** invokes neither generator nor judge — RIG §EV.2 prices it at ~1-5% of
> the full per-decision bill — so it adds well under 0.5M tokens. The **answer-level (T0) leg**
> invokes both generator and judge, priced by RIG §CM.2 at ~5.1-5.2M tokens per adjudicated
> decision, and an A/A null needs the paired pair: **≈10M tokens**. So: **T1 leg only ≈ ≤2M tokens;
> both legs ≈ 12M tokens.** At a $0.10–$1.00 per-million-token band that is roughly **$0.20–$2 for
> T1 alone** and **$1.20–$12 for both**, with a frontier-class judge pushing the T0 figure
> materially higher. All figures are projections labelled as such.
>
> **Options:** `approve-both` (both legs; the only answer that can discharge Falsifier 5) ·
> `approve-t1-only` (the cheap retrieval-side leg; MACH-03 stays Pending against the missing T0
> floor) · `decline` (leave Falsifier 5 BLOCKED, recorded)
>
> If you pick `approve-both` or `approve-t1-only`, also state `N` — how many of the 291 documents to
> ingest. The bundle's dev split is 17 questions; ingesting fewer documents than the gold passages
> those questions point at will make the gold-passage metric unmeasurable, so N below that is not
> useful.

**On `approve-both` or `approve-t1-only`:** first **write the threshold into
`databasise/evidence/FALSIFIER-5-EVIDENCE.md` before computing either floor**, adopting
06-RESEARCH.md Open Question 1's standing recommendation verbatim (T1's calibrated p95 floor at most
half of T0's, in the same score units) and naming it as pre-registered with today's date. Only then
run the three steps. Let `UnusableFloorError` and `StaleNullError` propagate — a refused floor is a
result, never something to work around. Then **append** a `## Real calibration — <ISO date>` section
below the existing 2026-09-09 record (never rewriting it), in the house sections claim / method /
findings / verdict / limits, carrying only real computed values: the `NullIdentity` key, the p95
floor per family, `n_questions`, `n_resamples`, the seed, and the resolved `judge_instance` and
bundle version the null is keyed to. Then update `.planning/REQUIREMENTS.md`:
  - `approve-both` **and** T1's floor is at most half of T0's -> MACH-03 flips to `- [x]` /
    `| MACH-03 | Phase 6 | Complete |` with a dated annotation citing the new evidence section.
  - `approve-both` **and** the threshold is not met -> MACH-03 stays `Pending` and the annotation
    records Falsifier 5 as **failed**, which is a halting, SELECTION.md-level reversal per Phase 2's
    own waiver record — say so plainly and stop; do not soften it and do not re-run to get a
    different number.
  - `approve-t1-only` -> MACH-03 stays `Pending` against the missing T0 floor. A single-family floor
    is not Falsifier 5 discharged; record the T1 floor as real and the requirement as partial.
  - In every approved case, append a dated note to MACH-02's row recording that `bundle@v2` was
    minted with a resolved `judge_instance`, superseding `bundle@v1`'s `"unresolved"` sentinel for
    calibration purposes while leaving `bundle@v1` itself intact and its own row Complete.

**On `decline`:** **append** a `## Deferred again — <ISO date>` entry below the existing record,
stating the date, that the owner declined a second time, the stated reason verbatim if given, and
that both entry preconditions are now met — 06-12 landed the judge-identity resolver and the
cost-bounded, document-count-capped ingest path, so the only remaining blocker is the spend itself.
Leave MACH-03 `Pending` with a dated annotation naming the spend as the single outstanding item.
Report the plan as complete: a recorded decline is this task's success state.

Do not ask this question twice, and do not re-ask a cheaper variant after a decline.
  </action>

  <instructions>
    I have already done everything automatable: 06-12 closed both preconditions
    `FALSIFIER-5-EVIDENCE.md` names — `databasise/eval/remint.py` resolves the judge identity from a
    real provider response (and refuses to substitute a declared one), and
    `databasise/eval/corpus_ingest.py` turns the 291-document eval-corpus from "unbudgetable" into a
    printed projection under an enforced `--limit` cap. The calibration instrument itself
    (`databasise/eval/calibration.py`) has been built and fixture-tested since 06-06. The projection
    above is computed from the corpus's own measured size and RIG's own §EV.2/§CM.2 cost anchors.

    The one thing I cannot do is authorize the spend. Reply `approve-both` (with an `N`) to run the
    full calibration, `approve-t1-only` (with an `N`) to run only the cheap retrieval-side leg
    knowing MACH-03 stays Pending, or `decline` to leave Falsifier 5 recorded as BLOCKED with
    today's date and your reason. Declining is a complete, valid answer.
  </instructions>

  <verification>
    On either approve branch: the pre-registered threshold sentence is written into
    `FALSIFIER-5-EVIDENCE.md` before any floor is computed; then I read back real values —
    the `NullIdentity` key, the p95 floor per family, `n_questions`, `n_resamples`, the seed, and
    the resolved `judge_instance` and bundle version — and append them. MACH-03 flips to Complete
    only on `approve-both` with T1's floor at most half of T0's.
    On `decline`: I append a dated deferral entry containing no floor, spend or duration, and leave
    MACH-03 Pending.
    Either way I re-run `cd databasise && uv run pytest -q tests/evidence/test_falsifier5_record.py tests/eval/ -x`
    and re-check that `bundle@v1`'s `bundle.json` digest still matches its committed
    `bundle.sha256`.
  </verification>

  <resume-signal>Reply `approve-both N=<count>`, `approve-t1-only N=<count>`, or `decline`.</resume-signal>

  <acceptance_criteria>
    - `databasise/evidence/FALSIFIER-5-EVIDENCE.md` still contains its original heading `# Falsifier 5 Evidence — MACH-03 (A/A Calibration): BLOCKED, Not Discharged` and its original `**Date:** 2026-09-09` line, byte-for-byte.
    - The file contains exactly one new `## ` section dated today, appended after the pre-existing content, and its line count is strictly greater than its pre-task line count.
    - If any approve branch ran: the pre-registered threshold sentence appears in the file **above** the section reporting the floors, and the reported floors are real computed values with a stated `n_questions`, `n_resamples` and seed.
    - If `decline`: the new section contains no numeric floor, no token spend and no duration.
    - MACH-03 reads Complete **only** if `approve-both` ran and T1's floor is at most half of T0's; in every other case it reads Pending with a named outstanding item.
    - `bundle@v1`'s `bundle.json` digest is unchanged from its committed `bundle.sha256` under every branch.
    - MACH-02 stays Complete under every branch.
  </acceptance_criteria>

  <verify>
    <automated>cd /home/chris/coding/Databasise-2.0-fully-agnostic-system && grep -c '^\*\*Date:\*\* 2026-09-09' databasise/evidence/FALSIFIER-5-EVIDENCE.md && grep -c "^## .*$(date +%Y-%m-%d)" databasise/evidence/FALSIFIER-5-EVIDENCE.md && grep -c '| MACH-02 | Phase 6 | Complete |' .planning/REQUIREMENTS.md</automated>
    <fails_when>non-zero exit from any grep, or any of the three printed counts is 0 — a 0 on the first means prior history was rewritten; a 0 on the second means no dated entry was recorded; a 0 on the third means MACH-02 was disturbed by this plan</fails_when>
    <automated>cd /home/chris/coding/Databasise-2.0-fully-agnostic-system && grep -c '| MACH-03 | Phase 6 | \(Complete\|Pending\) |' .planning/REQUIREMENTS.md</automated>
    <fails_when>the printed count is not exactly 1 — the row must be in exactly one of the two states, never absent and never duplicated</fails_when>
    <automated>cd databasise && uv run python -c "import hashlib,pathlib; d=pathlib.Path('evidence/eval-bundles/bundle@v1'); got=hashlib.sha256((d/'bundle.json').read_bytes()).hexdigest(); want=(d/'bundle.sha256').read_text().split()[0]; assert got==want, (got, want); print('v1-intact')"</automated>
    <fails_when>the line "v1-intact" is absent from stdout, or an AssertionError traceback appears — meaning bundle@v1's committed bytes were edited in place instead of a new version being minted</fails_when>
    <automated>cd databasise && uv run pytest -q tests/evidence/test_falsifier5_record.py tests/eval/ -x</automated>
    <fails_when>non-zero exit, or the summary line reads "no tests ran" or "0 passed" — the structural assertions over this evidence document and the eval suite must still hold after the append</fails_when>
  </verify>

  <reversibility rating="costly">A real judge call, a real N-document ingest and a real paired A/A run all spend money that cannot be un-spent. The minted `bundle@v2` is itself additive and fully reversible — `bundle@v1` is untouched and every committed record citing it keeps resolving — so the artifact is two-way and only the spend is not. Rated `costly` rather than `one-way` for the same reason Task 1 is: this task is itself the blocking human gate over the decision, and a preceding `checkpoint:decision` would ask the owner the same question twice.</reversibility>

  <done>The owner has answered once; `FALSIFIER-5-EVIDENCE.md` carries a dated appended entry recording that answer and its consequences, with any threshold pre-registered above any floor it is read against; MACH-03's row matches what actually happened; MACH-02 is undisturbed; `bundle@v1` is byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner decision -> real spend against a live model endpoint | Money and irreversible index writes cross here |
| harness output -> a committed evidence document | Real measured values, or an absent run, become a durable claim here |
| evidence document -> a REQUIREMENTS.md completion claim | A partial or declined outcome could be misread as a discharged gate here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-13-01 | Elevation of privilege (unauthorized spend) | Both checkpoint tasks | critical | mitigate | Both are `gate="blocking-human"`; no harness runs before an answer; each question is asked exactly once, with a declined branch that is a complete outcome; no cheaper variant may be re-asked after a decline. |
| T-06-13-02 | Repudiation | The two evidence documents | high | mitigate | Both branches **append** a dated section and the acceptance criteria pin the original 2026-09-09 headings and date lines byte-for-byte, so history cannot be rewritten to hide a prior deferral. `tests/evidence/test_cross_modality_record.py` and `tests/evidence/test_falsifier5_record.py` re-run after the append. |
| T-06-13-03 | Spoofing (fabricated measurement) | Findings in either appended section | critical | mitigate | Declined branches are asserted to contain no count, spend, duration or floor; approved branches carry only values read from the harnesses' own returned records; a non-zero harness exit is recorded as a refusal and never retried with a weakened guard. |
| T-06-13-04 | Tampering (result shopping) | The Falsifier 5 threshold | high | mitigate | The threshold is written into the evidence document **before** either floor is computed, adopting 06-RESEARCH.md Open Question 1's standing recommendation verbatim; the acceptance criteria require the threshold sentence to appear above the floors it is read against. A missed threshold is recorded as a failure and the plan stops. |
| T-06-13-05 | Spoofing (partial rounded up) | MACH-03's row | high | mitigate | `approve-t1-only` explicitly leaves MACH-03 Pending against the missing T0 floor; the prohibition block and the acceptance criteria both state that a single-family floor is not Falsifier 5 discharged (Phase 5's mass-revert lesson). |
| T-06-13-06 | Tampering | `bundle@v1`'s committed bytes | high | mitigate | The remint path mints a new version; `BundleEditInPlaceError` and `load_bundle`'s checksum re-verification already refuse an in-place edit; the digest check in Task 2's verify block pins v1 under every branch. |
| T-06-13-07 | Information disclosure | `v1/.env.parity` credentials | medium | mitigate | Credentials are reached only through the existing `run_arm._load_env_file`/`_build_clients` path; no env value is printed and no evidence section records one. |
| T-06-13-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package and contains no code change. |
</threat_model>

<artifacts_this_phase_produces>
| Kind | Symbol / path |
|---|---|
| Evidence section (new, appended) | `## Real run — <date>` **or** `## Deferred again — <date>` in `databasise/evidence/CROSS-MODALITY-EVIDENCE.md` |
| Evidence section (new, appended) | `## Real calibration — <date>` **or** `## Deferred again — <date>` in `databasise/evidence/FALSIFIER-5-EVIDENCE.md` |
| Evidence sentence (new, conditional) | The pre-registered "materially narrower" threshold in `FALSIFIER-5-EVIDENCE.md`, written before any floor is read |
| Bundle version (conditional) | `databasise/evidence/eval-bundles/bundle@v2/` — minted only on an approved A/A spend, carrying a resolved `judge_instance` |
| Requirement-row annotations (new) | Dated `06-13-PLAN.md` annotations appended to the MODAL-05, MACH-03 and (on approve) MACH-02 rows of `.planning/REQUIREMENTS.md` |
| Requirement-row state changes (conditional) | MODAL-05 and/or MACH-03 Pending -> Complete, **only** where the run that satisfies the row's own pass criterion actually executed |

No new code symbols, no new modules, no new tests, no new CLI flags — this plan runs artifacts 06-10 and 06-12 already created.
</artifacts_this_phase_produces>

<verification>
1. The append-not-rewrite greps in both tasks' verify blocks — original headings and dates intact, exactly one new dated section per document.
2. `cd /home/chris/coding/Databasise-2.0-fully-agnostic-system && grep -c '| MODAL-05 | Phase 6 | \(Complete\|Pending\) |' .planning/REQUIREMENTS.md` and the same for MACH-03 — each row in exactly one state.
3. `cd databasise && uv run pytest -q tests/evidence/test_cross_modality_record.py tests/evidence/test_falsifier5_record.py -x` — the structural assertions over both evidence documents still hold.
4. The `bundle@v1` digest check — v1 byte-identical under every branch.
5. `cd databasise && uv sync --extra rest && uv sync --extra mcp && uv run --extra rest --extra mcp pytest -q` — the phase's full suite still passes after any real run wrote into the store root.
</verification>

<success_criteria>
- The owner was asked each of the two spend questions exactly once, each with a document count, a projected token figure and its derivation, and a named declined option.
- Both evidence documents carry a dated appended entry recording the answer; neither had its 2026-09-09 record rewritten.
- MODAL-05 and MACH-03 read Complete only where a real run produced real values against the row's own pass criterion, and Pending with a named outstanding item otherwise.
- No number appears anywhere for a run that did not happen.
- `bundle@v1` and MACH-02 are undisturbed.
</success_criteria>

<output>
Create `.planning/phases/06-hipporag-2-side-by-side/06-13-SUMMARY.md` when done.
</output>
