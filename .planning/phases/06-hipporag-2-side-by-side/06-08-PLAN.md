---
phase: 06-hipporag-2-side-by-side
plan: 08
type: execute
wave: 5
depends_on: ["06-03", "06-07"]
files_modified:
  - databasise/parity/build_hipporag_index.py
  - databasise/parity/run_cross_modality.py
  - databasise/parity/__init__.py
  - databasise/evidence/CROSS-MODALITY-EVIDENCE.md
  - databasise/tests/parity/test_cross_modality_isolation.py
  - databasise/tests/evidence/test_cross_modality_record.py
autonomous: true
requirements: [MODAL-05]

estimate:
  tokens: 40000
  raw_tokens: 80000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "HippoRAG 2's index-side chain has run for real against the Phase 3 parity corpus, producing a populated `hipporag-graph` namespace and populated `hipporag-chunks`, `hipporag-entities` and `hipporag-facts` vector namespaces, with its token spend recorded from the run's own accounting."
    - "LightRAG and HippoRAG answer the same corpus from disjoint on-disk store namespaces: no directory under the shared store root is written by both arms, and neither arm's retrieval resolves an identifier the other arm produced."
    - "The two arms' per-arm outputs are structurally comparable — every returned item on both sides conforms to the same §4 item shape, and the two envelopes carry the same field set."
    - "The KV store is shared across arms only where §3's `shared` scope admits it; because every HippoRAG position resolves to `opaque` effective depth, HippoRAG writes `quarantined` and its index is instance-scoped, never SA-1-shareable."
    - "The two arms' index recipes share no resolved input, so `artifacts_overlap` between their recipe hashes is false and each arm carries its own full index cost — RIG §RUN.2's zero-overlap case, observed rather than assumed."
    - statement: "MODAL-05's edge behaviour (unclassified by the deterministic probe) is not specified anywhere in the frozen record; the acceptance boundary for `structurally comparable` — whether it constrains item count, score scale, or only type conformance — is carried as a flagged planner assumption rather than an implied contract."
      verification: backstop
  artifacts:
    - databasise/parity/build_hipporag_index.py
    - databasise/parity/run_cross_modality.py
    - databasise/evidence/CROSS-MODALITY-EVIDENCE.md
  key_links:
    - "the Phase 3 parity corpus snapshot -> both arms' indexes -> one compare() call -> two envelopes"
    - "each wiring's declared store_namespaces -> disjoint on-disk directories -> zero artifact overlap"
  prohibitions:
    - "MUST NOT let either arm's retrieval resolve an identifier the other arm's index produced and report it as its own — retrieval sourced from a neighbouring arm's index and presented as this arm's result is fabricated provenance, and it is the exact failure per-arm isolation exists to prevent."
    - "MUST NOT present a run that degraded, halted, or completed partially as a clean side-by-side comparison — a confounded run serialised as clean reads as settled rather than unsettled, which is worse than a refusal."
    - "MUST NOT interpret a retrieval difference between the two arms as a quality finding — this is architecture-comparison mode, the two recipes share no resolved input, and no floor calibrated for either arm has been read here."
---

<objective>
Run both modalities on one corpus for real (MODAL-05): build HippoRAG 2's index over the Phase 3 parity corpus, answer one query through both arms in a single comparison call, and record that their stores are isolated and their outputs structurally comparable.

Purpose: this is the milestone's proof point observed rather than asserted. Every prior plan builds the capability; this one exercises it against real data and writes down what happened.
Output: an index-build harness, a cross-modality run harness, and a committed evidence record.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-hipporag-2-side-by-side/06-RESEARCH.md
@.planning/phases/06-hipporag-2-side-by-side/06-01-SUMMARY.md
@.planning/phases/06-hipporag-2-side-by-side/06-03-SUMMARY.md
@.planning/phases/06-hipporag-2-side-by-side/06-07-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols created by this plan: `databasise/parity/build_hipporag_index.py` (`build_index`, `IndexBuildResult`), `databasise/parity/run_cross_modality.py` (`run_cross_modality`, `CrossModalityRecord`), `databasise/evidence/CROSS-MODALITY-EVIDENCE.md`.

Phase-level symbol list: see `06-01-PLAN.md` § "Artifacts this phase produces".

<tasks>

<task type="auto">
  <name>Task 1: Build HippoRAG's index over the Phase 3 parity corpus, for real</name>
  <files>databasise/parity/build_hipporag_index.py, databasise/parity/__init__.py</files>
  <precondition>`v1/.env.parity` exists and carries every key `databasise/parity/run_arm.py`'s `_build_clients` requires, and the Phase 3 v1-built LightRAG index is present in the parity store root — the same two conditions `databasise/parity/run_comparison.py` already asserts before any measured run. Assert both before starting and halt naming the missing key or the missing store directory if either is absent.</precondition>
  <read_first>
    - databasise/parity/run_arm.py (the whole file — `DEFAULT_V1_ENV_PARITY`, `_build_clients`, `_build_stores`, `_inject_query`, `_inject_token_allowance`, the `OPENAI_LLM_EXTRA_BODY` provider-routing pin, and 03-11's own startup guard refusing a silently-empty extraction configuration)
    - databasise/parity/corpus.py (`load_snapshot`, `CorpusSnapshot`, `CorpusDocument` — the documents this indexes)
    - databasise/parity/import_index.py (the existing "build an index, then verify it" harness shape, including its verification pass before the result is trusted)
    - databasise/wirings/hipporag/base.json (the index-side node ids, their deps and their config keys)
    - databasise/parts_core/hipporag/chunk_embed.py (the `config["documents"]` shape this harness supplies)
    - databasise/parts_core/hipporag/graph_augment_persist.py (the artifact and counts it emits)
    - databasise/runner/scheduler.py (`run_wiring`'s signature and its returned `results`/`nodes`/`partial`/`degraded` map)
  </read_first>
  <action>
Create `databasise/parity/build_hipporag_index.py`, a one-time index-build harness in `import_index.py`'s own shape: build, then verify, then report — never report a build it has not verified.

It loads the Phase 3 parity corpus snapshot (`tests/fixtures/corpus/`, the corpus LightRAG's v1 index was already built and imported against — this is what makes "one corpus" true rather than two corpora that happen to be similar), resolves the HippoRAG base wiring, injects the corpus documents onto `chunk-embed`'s `config["documents"]` and a real token allowance onto every node exactly as `run_arm.py`'s own `_inject_query`/`_inject_token_allowance` do, builds real clients from `v1/.env.parity` through `run_arm.py`'s `_build_clients`, builds the stores at HippoRAG's own declared namespaces, and runs the wiring through `run_wiring`.

Adopt 03-11's own hard-won guard: refuse a run whose extraction produced nothing. A silently-empty OpenIE pass produces an index that looks built and retrieves nothing, which is exactly the failure that cost Phase 3 two gap-closure plans. After the run, verify before reporting success: the graph namespace holds a non-zero node and edge count; the entity and fact vector namespaces hold non-zero entry counts; at least one passage vertex and at least one entity vertex are present; and the run reports `partial` false and `degraded` false. Any failure is a refusal naming the specific check, never a warning attached to a returned result.

Report an `IndexBuildResult` carrying the corpus snapshot hash, the per-namespace counts, the run's own token spend read from the node traces' accounting rather than estimated, and the wall-clock duration. Print a human-readable summary and exit non-zero on refusal, mirroring `run_comparison.py`'s convention that a non-zero exit means something.

This harness is a one-time build tool checked in beside its own documentation, exactly like `build_corpus_fixture.py` — not a runtime dependency, not run by any test. Document the exact invocation command in the module docstring.

Run it once against the parity store root, and record in the plan's SUMMARY the observed counts, spend and duration. If the run refuses, fix the cause and re-run rather than lowering a check to make it pass.
  </action>
  <verify>
    <automated>cd databasise && uv run python -m databasise.parity.build_hipporag_index</automated>
    <fails_when>non-zero exit, or a printed summary reporting zero entities, zero facts, zero graph nodes, or zero graph edges</fails_when>
    <automated>cd databasise && uv run pytest -q</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `databasise/parity/build_hipporag_index.py` exists and its module docstring names the exact invocation command
    - It asserts both preconditions before starting and names the missing key or directory on failure
    - It verifies non-zero graph node, graph edge, entity and fact counts before reporting success, and exits non-zero otherwise
    - It reads token spend from the run's own node-trace accounting and does not compute an estimate
    - One real invocation has completed and its observed counts, spend and duration are recorded in `06-08-SUMMARY.md`
  </acceptance_criteria>
  <done>HippoRAG 2 has a real, verified index over the same corpus LightRAG already answers from, built by its own thirteen-position chain.</done>
</task>

<task type="auto">
  <name>Task 2: One query, both arms, and the isolation the run makes observable</name>
  <files>databasise/parity/run_cross_modality.py, databasise/tests/parity/test_cross_modality_isolation.py</files>
  <read_first>
    - docs/system-model/RIG.md (`### §RUN.1` in full — the KV-shared-only-where-`shared`-scope-admits rule, the opaque-arm-writes-`quarantined` rule and the instance-scoped-never-SA-1-shareable consequence; `### §RUN.2`'s artifact-overlap iff rule and its worked `VT-1`/`GR-1` zero-overlap pair; `### §RUN.4`'s per-arm-output alignment on the eval bundle's own question order)
    - docs/system-model/CONTRACT.md (§4's frozen item shape — `ref`, `kind`, `score`, `provenance`, `payload`, `metadata` — and its `ItemKind` union; §3's three artifact scopes)
    - databasise/namespaces.py (`derive_namespace`, `namespace_dir`, `artifacts_overlap` — the iff-on-recipe-hashes decision, never a similarity judgment)
    - databasise/seam/compare.py and `Databasise.compare` (as landed by 06-03)
    - databasise/parity/run_comparison.py (the comparison-harness shape: pre-flight verification, a `ComparisonRecord`, asymmetric result shapes with distinguishable keys, machine-readable plus human-readable output)
    - databasise/parity/build_hipporag_index.py (as written by Task 1)
    - databasise/tests/seam/test_store_isolation.py (06-01's synthetic isolation proof, which this extends to the real built indexes)
  </read_first>
  <action>
Create `databasise/parity/run_cross_modality.py`, a comparison harness in `run_comparison.py`'s own shape: pre-flight verification first, then the run, then a machine-readable `CrossModalityRecord` plus a human-readable summary, with a non-zero exit meaning something.

Pre-flight, before any query: both arms' indexes are present and non-empty at their own declared namespaces; the two arms' resolved store directory sets are disjoint; and `artifacts_overlap` between the two arms' index-recipe hashes is false. RIG §RUN.2's rule is an iff on recipe hashes and nothing else — never a similarity judgment, never a field-by-field comparison — and the expected result here is false, the same zero-overlap case §RUN.2's own worked `VT-1`/`GR-1` pair states as the norm for two unrelated recipes: no artifact sharing, two full index costs.

Then run the Phase 3 corpus's own queries through one `Databasise.compare` call per query, supplying the two caller-side selectors that resolve to the two arms, and collect the resulting per-arm envelopes, per-arm traces reached through each envelope's trace reference, and per-arm token accounting.

Record, per query and per arm: the returned items with their `§4` shape fields, the envelope's own field set, `partial`, `degraded`, `stop_reason`, `degradation_reason`, and the per-`counted_by` token accounting. A run where either arm reports `partial` or `degraded` is recorded as such and the harness exits non-zero — a confounded run serialised as a clean comparison reads as settled rather than unsettled, which this project's own trace honesty invariant already refuses at the record layer and this harness refuses at the report layer.

Structural comparability, made checkable rather than asserted: every item both arms return conforms to the same `§4` item shape and carries a `kind` drawn from the same `ItemKind` union, and the two envelopes carry identical field sets. That is a type guarantee, not a content guarantee — the two arms' actual passages and scores may differ arbitrarily, which is the point of architecture-comparison mode. Say so in the harness's own summary output so a reader of the numbers cannot mistake a retrieval difference for a defect.

`databasise/tests/parity/test_cross_modality_isolation.py` asserts the isolation properties against the real built indexes, guarded with an explicit skip-if-absent check on the parity store root so a fresh checkout skips cleanly rather than failing collection — 05-07's own recorded reason for preferring an explicit guard. Assert: no directory under the shared store root is written by both arms; every identifier LightRAG's arm retrieves resolves in a LightRAG namespace and in no HippoRAG namespace, and the converse; `artifacts_overlap` between the two recipe hashes is false; and the KV namespaces the two arms read are distinct, which is §RUN.1's KV rule holding for an arm whose positions all resolve to `opaque` effective depth and therefore write `quarantined` and never `shared`.

Add the cross-read negative control: seeding a record into one arm's namespace and querying the other arm returns nothing from it, so the isolation assertions are not passing vacuously on two empty stores.
  </action>
  <verify>
    <automated>cd databasise && uv run python -m databasise.parity.run_cross_modality</automated>
    <fails_when>non-zero exit, or a printed summary reporting `partial` or `degraded` true for either arm</fails_when>
    <automated>cd databasise && uv run pytest -q tests/parity/test_cross_modality_isolation.py -x -rs</automated>
    <fails_when>non-zero exit, or every test in the run reports as skipped</fails_when>
    <automated>cd databasise && uv run pytest -q</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `databasise/parity/run_cross_modality.py` runs its three pre-flight checks before any query and exits non-zero on any failure
    - It exits non-zero when either arm reports `partial` or `degraded`
    - `artifacts_overlap` is called with the two arms' recipe hashes and its false result is recorded
    - A test asserts no store-root directory is written by both arms
    - A test asserts each arm's retrieved identifiers resolve only in that arm's own namespaces
    - A cross-read negative control exists and asserts the isolation checks are not passing on empty stores
    - The harness summary states that a retrieval difference between the arms is expected and is not a quality finding
  </acceptance_criteria>
  <done>One comparison call answers the same corpus from two isolated indexes, with zero artifact overlap observed rather than assumed, and both arms' outputs proven type-comparable.</done>
</task>

<task type="auto">
  <name>Task 3: The MODAL-05 evidence record</name>
  <files>databasise/evidence/CROSS-MODALITY-EVIDENCE.md, databasise/tests/evidence/test_cross_modality_record.py</files>
  <read_first>
    - databasise/evidence/PARITY-EVIDENCE.md (the Phase 3 evidence document this one mirrors in structure — per-arm table, verdict, the human-authored-cause discipline, and its own honest treatment of an unresolved finding)
    - databasise/evidence/parity_report.py (the render-from-committed-inputs shape and the explicit-empty-state rule: zero deviations is a result, an absent file is not)
    - databasise/parity/run_cross_modality.py (as written by Task 2 — the `CrossModalityRecord` this renders)
    - .planning/REQUIREMENTS.md (MODAL-05's own wording, including its F-14 clause)
    - databasise/evidence/F-14-SEAM-INVARIANCE.md (as landed by 06-03 — the F-14 outcome this record cites rather than restates)
    - docs/system-model/RIG.md (`### §AA.4`'s mode-determination rule and its statement that in architecture-comparison mode an improvement is the comparison's own payoff, not a suspicious result to be explained away)
  </read_first>
  <action>
Render `databasise/evidence/CROSS-MODALITY-EVIDENCE.md` from the committed `CrossModalityRecord`, following `parity_report.py`'s own rule that the document is a function of committed inputs rather than prose written alongside them — a stale record is worse than an absent one, and an absent input is an error while a zero result is a finding.

Structure, mirroring `PARITY-EVIDENCE.md`: dated header; MODAL-05's claim quoted verbatim from `REQUIREMENTS.md`; a per-arm, per-query table of returned item counts, score ranges, envelope field sets and token spend by `counted_by`; an isolation section recording the disjoint directory sets, the false `artifacts_overlap` result and the KV-scope reasoning; a structural-comparability section recording that both arms' items conform to the same `§4` shape and both envelopes carry the same field set; a verdict; and method and limits.

State the comparison mode explicitly and early, because everything a reader concludes from the table depends on it: LightRAG and HippoRAG are two independently-registered wirings, so this is `architecture-comparison mode` per `§AA.4`'s own read-from-the-arms rule, not `decomposition-parity mode`. Parity-not-gain does not apply and a difference between the arms is not an excursion needing a cause. `§5`'s sixth refusal condition is trivially satisfied because the two recipes share no resolved input to hold constant in the first place — the same reasoning `§AA.4` performs for its own `VT-1`/`GR-1` pair, transferring by the identical structural argument.

Cite `F-14-SEAM-INVARIANCE.md` for the envelope-invariance finding rather than restating it; one fact, one home.

Under limits, state: the corpus is the two-question Phase 3 parity fixture, deliberately sized for index affordability and not for a statistical floor; no calibrated floor is read here and none applies, since 06-06's nulls are keyed to the LightRAG arm and this is not a promotion decision; HippoRAG's index-side effective depth remains `opaque` under the taint rule per 06-07's port record; and the observed retrieval difference between the arms is a difference between two architectures on the same corpus and is not evidence about either one's quality.

`databasise/tests/evidence/test_cross_modality_record.py` parses the document structurally — front matter and tables, never whole-file grep — and asserts: one table row per (query, arm) pair present in the committed record; the recorded `artifacts_overlap` value is false; the comparison mode is stated as architecture-comparison; and the document cites `F-14-SEAM-INVARIANCE.md` by name. Include the render-staleness check `parity_report.py`'s own convention implies: re-rendering from the committed record reproduces the document byte-identically, so a hand-edited record fails.
  </action>
  <verify>
    <automated>cd databasise && uv run pytest -q tests/evidence/test_cross_modality_record.py -x</automated>
    <fails_when>non-zero exit, or "no tests ran" in the summary line</fails_when>
    <automated>cd databasise && uv run pytest -q</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `databasise/evidence/CROSS-MODALITY-EVIDENCE.md` exists with a dated header and quotes MODAL-05's claim verbatim
    - It names the comparison mode as architecture-comparison and states that parity-not-gain does not apply
    - It records the disjoint directory sets, the false `artifacts_overlap` result, and the KV-scope reasoning
    - It cites `F-14-SEAM-INVARIANCE.md` rather than restating the F-14 outcome
    - Its limits section names all four stated limits
    - A test asserts re-rendering from the committed record reproduces the document byte-identically
  </acceptance_criteria>
  <done>MODAL-05's claim has a committed, re-renderable evidence record produced from a real two-arm run on one corpus.</done>
</task>

</tasks>

<verification>
- `cd databasise && uv run pytest -q` exits 0
- `cd databasise && uv run python -m databasise.parity.run_cross_modality` exits 0
- `databasise/evidence/CROSS-MODALITY-EVIDENCE.md` is committed and re-renders byte-identically
</verification>

<success_criteria>
LightRAG and HippoRAG 2 answer the same corpus from disjoint, zero-overlap indexes under RIG §RUN, one comparison call returns both, their outputs are proven type-comparable, and the run is recorded honestly including the fact that a difference between them is not a quality finding.
</success_criteria>

<flagged_assumptions>
- **MODAL-05 edge behaviour is unclassified.** The deterministic edge probe returned `unclassified` for MODAL-05 and it is carried here as `unresolved`. `RIG §RUN.4` grounds comparability in `§4`'s pooling key and the frozen item shape, but never states whether "structurally comparable" additionally constrains item counts or score scales. The type-conformance reading used here is this plan's own, stated in the evidence record rather than implied.
- **"One corpus" is the two-question Phase 3 parity fixture,** chosen because LightRAG's v1-built index already exists against it and rebuilding that index is neither free nor risk-free. The 30-question eval corpus 06-04 built is the bundle's corpus, not this run's; they are deliberately different and both are recorded by hash.
</flagged_assumptions>

<output>
Create `.planning/phases/06-hipporag-2-side-by-side/06-08-SUMMARY.md` when done
</output>
