---
phase: 06-hipporag-2-side-by-side
plan: 18
type: execute
wave: 1
gap_closure: true
closes:
  - "ROADMAP Phase 6 Success Criterion 6 carries no deferral annotation — 06-GATE-AMENDMENT.md's tracking changes reached Phase 7's `Depends on:` line and REQUIREMENTS.md's MACH-03/MODAL-01 rows, but never SC6 itself"
  - "ROADMAP Phase 6's `**Plans**:` line is factually stale — it reports 4/6, SC2 FAILED, and WINDOWS.md entry id 3 open, all three of which later work superseded"
  - "`databasise/tests/eval/test_aa_run.py`'s blanket module-level `pytestmark = pytest.mark.asyncio` marks sync test functions, emitting PytestWarnings on every full-suite run (06-VERIFICATION.md anti-pattern table, Info; named as a follow-up in 06-17-SUMMARY.md)"
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - databasise/tests/eval/test_aa_run.py
autonomous: true
requirements: [MACH-03]

estimate:
  tokens: 17000
  raw_tokens: 34000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "Phase 6's Success Criterion 6 reads with its original sentence intact, byte-for-byte, plus a dated annotation citing `06-GATE-AMENDMENT.md` — so the next re-verification sees a criterion that was re-timed by written amendment rather than one that is simply unmet and unexplained."
    - "Phase 6's `**Plans**:` line states the verification score, SC2's status, and WINDOWS.md entry id 3's status as they actually are, and names SC6 as the only criterion still FAILED."
    - "The full test suite emits zero PytestWarnings, while collecting and running exactly the same tests as before — the seven `async def test_` functions in `test_aa_run.py` still execute as coroutines, not as skipped or silently-passing sync functions."
    - "MACH-03 and MODAL-01 are still Pending with unchecked boxes, `06-VERIFICATION.md` still carries the verifier's own `status` and `score`, and `06-UAT.md` and `docs/system-model/` are untouched."
  artifacts:
    - .planning/ROADMAP.md
    - databasise/tests/eval/test_aa_run.py
  key_links:
    - "`06-GATE-AMENDMENT.md`'s `## What this authorises` -> the one tracking change never applied -> Phase 6 SC6's annotation in `.planning/ROADMAP.md`"
    - "06-15's completed cross-modality run + 06-14's close of WINDOWS.md entry id 3 -> the three corrected clauses in Phase 6's `**Plans**:` line"
    - "`databasise/pyproject.toml`'s `asyncio_mode = \"auto\"` -> the seven async tests stay collected as async once the module-level mark is removed"
  prohibitions:
    - "MUST NOT ask the owner about the MACH-03 / A/A calibration spend in any form, narrowed or otherwise — it has been declined three times and deferred a fourth by written amendment, and 06-17-SUMMARY.md binds any reopening to be new scope rather than a retry."
    - "MUST NOT flip MACH-03 or MODAL-01 to Complete, and MUST NOT touch either checkbox — `06-GATE-AMENDMENT.md`'s `## What is not amended` requires both stay Pending until discharged on their own terms."
    - "MUST NOT modify `.planning/phases/06-hipporag-2-side-by-side/06-VERIFICATION.md`, `06-UAT.md`, or anything under `docs/system-model/`."
    - "MUST NOT rewrite, reword, reflow, or renumber Success Criterion 6's original sentence — the annotation is an append to that same line and nothing else."
    - "MUST NOT change which tests run or how they run: no test may be deleted, renamed, skipped, converted between sync and async, or moved to another file."
    - "MUST NOT add a fourth item, refactor anything, or make an opportunistic cleanup — the shortest correct diff is the right one here."
---

<objective>
Three closeout corrections that nothing else in Phase 6 will fix on its own: two stale lines in
`.planning/ROADMAP.md` and one misapplied pytest marker.

**Why item 1 matters more than it looks.** `06-GATE-AMENDMENT.md` re-timed MACH-03 a fourth time
and, in its `## What this authorises` section, authorised tracking changes in both
`.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md`. Two of the three landed: Phase 7's
`**Depends on**` line records the deferral, and MACH-03's and MODAL-01's requirement rows each
gained a dated deferral note. The third never did — Phase 6's own Success Criterion 6, which the
amendment's `## What is amended` section names in its very first line, still reads as a plain
unqualified criterion. Consequence: every future re-verification scores SC6 FAILED against text
that gives a reader no way to know the owner already re-timed it. The fix is an annotation in the
house style this document already uses — Phase 3's criterion 5 on line 107 carries exactly this
shape, and Phase 7's struck criteria 4 and 5 carry its stronger variant. SC6 is *re-timed, not
withdrawn* (`06-GATE-AMENDMENT.md`: "Condition 6's substance … is re-timed a fourth time, not
withdrawn"), so it takes the Phase-3 form — an appended parenthetical — not the struck-through one.

**Item 2 is three false statements in one line.** Phase 6's `**Plans**:` line closes with a
parenthetical reporting a 4/6 score, SC2 as still FAILED, and one open filed defect. The
verification file now reads `score: 5/6`; SC2/MODAL-05 is VERIFIED after 06-15's real
cross-modality run; and `.planning/WINDOWS.md` reports `open_count: 0` with entry id 3 fixed by
06-14. SC6 is the only criterion still FAILED, and item 1 above is what makes that reading fair.

**Item 3 is eight warnings on every full-suite run.** `test_aa_run.py` is the only file in the
entire suite carrying an explicit asyncio mark of any kind, and `databasise/pyproject.toml` sets
`asyncio_mode = "auto"`, under which every `async def test_` is collected and run as a coroutine
with no mark at all. The module-level mark is therefore not just noisy but redundant: removing it
restores the convention the other test files already follow rather than inventing a new one.

Purpose: close Phase 6's paperwork honestly so the next re-verification scores what is actually
true, and remove the one cosmetic defect the phase's own record left named and unfixed.
Output: two corrected lines in `.planning/ROADMAP.md`, one deleted line in
`databasise/tests/eval/test_aa_run.py`, and a full suite that runs clean and silent.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-hipporag-2-side-by-side/06-GATE-AMENDMENT.md
@.planning/phases/06-hipporag-2-side-by-side/06-17-SUMMARY.md
</context>

<source_coverage_audit>
Gap-closure plan: the source is the orchestrator's scope brief plus the four artifacts it cites.
Every in-scope item is covered; every excluded item is excluded for a stated, sourced reason.

| Source | Item | Status | Where |
|--------|------|--------|-------|
| Brief item 1 | Phase 6 SC6 carries no deferral annotation | COVERED | Task 1 |
| Brief item 2 | Phase 6 `**Plans**:` line factually stale | COVERED | Task 2 |
| Brief item 3 | blanket `pytestmark` over sync tests | COVERED | Task 3 |
| REQ / MACH-03 | the A/A calibration spend itself | EXCLUDED | Deferred a fourth time by `06-GATE-AMENDMENT.md` to the first gate-adjudicated promotion, outside this milestone under MACH-09's default posture. Owner-originated, declined three times prior. Reopening is new scope per 06-17-SUMMARY.md. |
| REQ / MODAL-01 | two owner-only judgment items | EXCLUDED | Deferred to the parity claim itself by the same amendment; both are owner judgments a plan cannot author. |
| 06-VERIFICATION.md Gap 3 | WR-01 / WR-02 in `aa_run.py` | EXCLUDED | Already closed — `06-REVIEW-FIX.md` `status: all_fixed`, commits `a886f1d` and `7d69588`. |
| 06-VERIFICATION.md Gap 4 | CR-01 loose `floor_value_pattern` | EXCLUDED | Already closed — commit `e8612d8`, re-verified live in `06-REVIEW.md`. |
| 06-REVIEW.md CR-02 | upload 500-instead-of-422 | EXCLUDED | Already closed — commit `554e071` with its own regression test. |
| 06-UAT.md | stale `source:` covering only 06-01..06-09 | EXCLUDED | UAT answers are the owner's own judgments captured by `/gsd-verify-work`; a plan cannot author them. Surfaced to the owner as a separate command by the orchestrator. |

No item is deferred by this plan's own judgment. Every exclusion is owner-originated, already
closed in a commit, or structurally impossible for a plan to perform.
</source_coverage_audit>

<tasks>

<task type="auto">
  <name>Task 1: Annotate Phase 6's Success Criterion 6 with the fourth MACH-03 deferral, preserving its original sentence byte-for-byte</name>

  <read_first>
    - `.planning/ROADMAP.md` line 241 — Phase 6's Success Criterion 6, the line this task appends to. Read it in full before editing; its original text must survive unchanged.
    - `.planning/ROADMAP.md` line 107 — Phase 3's Success Criterion 5, which carries the house annotation shape for a criterion that was *deferred but not withdrawn*: an appended italic parenthetical `*(amended <date> per \`<path>\`: <what moved, and why>)*` with the original sentence untouched ahead of it. This is the form to match.
    - `.planning/ROADMAP.md` Phase 7's Success Criteria 4 and 5 — the *stronger* variant (`~~struck~~ — **STRUCK <date>** *(deferred per \`<path>\`: …)*`), which is **not** the form to use here, because `06-GATE-AMENDMENT.md` says condition 6's substance is re-timed rather than withdrawn.
    - `.planning/phases/06-hipporag-2-side-by-side/06-GATE-AMENDMENT.md` — the whole file, and specifically: `## What is amended` (whose first line names Phase 6's success criterion 6), `## The amendment`'s first clause (MACH-03's new point of first need), `## What is not amended` (MACH-03 and MODAL-01 stay Pending; condition 6's substance is re-timed, not withdrawn), and `## What this authorises` (the tracking changes this task completes).
    - `.planning/REQUIREMENTS.md` line 17 — MACH-03's row, whose four bracketed annotations are the established house style this annotation must read like: dated, document-citing, appended never substituted, and explicit that the row stays Pending.
    - `.planning/REQUIREMENTS.md` line 29 — MODAL-01's row and its own `[Deferred 2026-09-11 …]` annotation, the sibling tracking change that did land.
  </read_first>

  <files>.planning/ROADMAP.md</files>

  <precondition>`.planning/ROADMAP.md` line 241 still begins `  6. Owner mints an eval bundle with dev/holdout/sealed splits` and ends `carried forward from Phase 3 per \`.planning/phases/03-lightrag-query-side/03-GATE-AMENDMENT.md\`` with no annotation already present. If an annotation is already there, this item is closed and the task must report that and stop rather than appending a second one.</precondition>

  <action>
Append — and only append — an annotation to the end of Phase 6's Success Criterion 6 in
`.planning/ROADMAP.md`. The criterion's existing sentence is not reworded, not reflowed, not
renumbered and not indented differently; the annotation is added after its final backtick-quoted
path, on the same line, separated by a single space.

Append exactly this text:

`*(deferred 2026-09-11 per `.planning/phases/06-hipporag-2-side-by-side/06-GATE-AMENDMENT.md`: MACH-03 is re-timed a fourth time — from this phase's cross-modality run to the first gate-adjudicated promotion, which lies outside this milestone under MACH-09's default posture, since RIG §PR.3 and CONTRACT §7's operator-asserted path carries no verdict and consumes no floor for the gate's floor to serve. The criterion's substance — no measured claim rides on an unmeasured comparison — is re-timed, not withdrawn: MACH-03 stays Pending, Falsifier 5 stays open rather than failed, and the milestone cannot close until a real A/A run computes both floors. This annotation completes the tracking change that amendment's `## What this authorises` section named alongside the two that did land — Phase 7's `**Depends on**` line and REQUIREMENTS.md's MACH-03 and MODAL-01 rows.)*`

Three points of care, each of which is a way this task can be done wrongly while looking right:

1. **Do not strike the criterion through.** Phase 7's criteria 4 and 5 use `~~…~~` plus
   `**STRUCK**` because those were withdrawn from that phase outright. Condition 6 was not — the
   amendment says so in as many words — so it keeps its plain text and gains a parenthetical, the
   same as Phase 3's criterion 5.
2. **Do not flip a checkbox, a requirement row, or a phase status.** This task edits one line of
   `.planning/ROADMAP.md` and nothing else. MACH-03 stays `- [ ]` / `Pending`, MODAL-01 stays
   `- [ ]` / `Pending`, Phase 6 stays `In Progress`, and `06-VERIFICATION.md` is not opened.
3. **Do not touch `docs/system-model/`.** It is a verbatim upstream mirror of the ratified system
   model and is never edited; the amendment itself opens by saying so. This annotation cites into
   it and changes nothing there.
  </action>

  <acceptance_criteria>
    - `.planning/ROADMAP.md` still contains the literal substring `both §EV.2 target families (MACH-02), then runs one A/A calibration` exactly once, and that same line also contains the annotation opener `*(deferred 2026-09-11 per` — proving the original sentence survived and the annotation was appended to it rather than replacing it.
    - `.planning/ROADMAP.md` still contains `Falsifier 5, MACH-03, carried forward from Phase 3 per` exactly once — SC6's original closing clause is intact.
    - Success Criterion 6 is still numbered `6.` and still sits under Phase 6's `**Success Criteria** (what must be TRUE):` heading; no criterion was added, removed or renumbered.
    - The annotation contains no strike-through markers (`~~`) and no `**STRUCK**` token — the criterion is re-timed, not withdrawn.
    - `.planning/REQUIREMENTS.md` still shows `| MACH-03 | Phase 6 | Pending |` and `| MODAL-01 | Phase 3 | Pending |`, each exactly once, with both checkboxes unchecked.
    - `.planning/phases/06-hipporag-2-side-by-side/06-VERIFICATION.md` still carries `status: gaps_found` and `score: 5/6 must-haves verified` in its front matter, untouched by this plan.
  </acceptance_criteria>

  <verify>
    <automated>cd /home/chris/coding/Databasise-2.0-fully-agnostic-system && grep -F 'both §EV.2 target families (MACH-02), then runs one A/A calibration' .planning/ROADMAP.md | grep -cF '*(deferred 2026-09-11 per'</automated>
    <fails_when>non-zero exit, or the printed count is not exactly 1 — a non-zero exit from the first grep means SC6's original sentence was rewritten or reflowed instead of appended to, and a 0 or missing count from the second means the annotation landed somewhere other than on that same line</fails_when>

    <automated>cd /home/chris/coding/Databasise-2.0-fully-agnostic-system && grep -cF 'Falsifier 5, MACH-03, carried forward from Phase 3 per' .planning/ROADMAP.md</automated>
    <fails_when>non-zero exit, or the printed count is not exactly 1 — SC6's original closing clause must survive the append verbatim</fails_when>

    <automated>cd /home/chris/coding/Databasise-2.0-fully-agnostic-system && grep -cF '| MACH-03 | Phase 6 | Pending |' .planning/REQUIREMENTS.md && grep -cF '| MODAL-01 | Phase 3 | Pending |' .planning/REQUIREMENTS.md</automated>
    <fails_when>non-zero exit, or either printed count is not exactly 1 — a 0 means a row this plan is prohibited from touching was flipped or deleted, and a count above 1 means a row was duplicated</fails_when>

    <automated>cd /home/chris/coding/Databasise-2.0-fully-agnostic-system && grep -cE '^status: gaps_found' .planning/phases/06-hipporag-2-side-by-side/06-VERIFICATION.md && grep -cE '^score: 5/6 must-haves verified' .planning/phases/06-hipporag-2-side-by-side/06-VERIFICATION.md</automated>
    <fails_when>non-zero exit, or either printed count is not exactly 1 — the verifier's own verdict and score lines must be exactly as this plan found them; this plan records a deferral, it does not re-score the phase</fails_when>
  </verify>

  <reversibility rating="reversible">A single appended parenthetical on one markdown line. Deleting the appended text restores the file byte-for-byte, and nothing reads the ROADMAP programmatically.</reversibility>

  <done>Phase 6's Success Criterion 6 carries a dated, amendment-citing deferral annotation with its original sentence intact ahead of it; MACH-03 and MODAL-01 are still Pending and unchecked; and `06-VERIFICATION.md` is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Replace Phase 6's stale `**Plans**:` line with the current score, SC2's real status, and the closed defect ledger</name>

  <read_first>
    - `.planning/ROADMAP.md` line 243 — Phase 6's `**Plans**:` line, the only line in the file beginning `**Plans**: 17/17`, and the wave history it carries which must survive the replacement.
    - `.planning/phases/06-hipporag-2-side-by-side/06-VERIFICATION.md` front matter — `status: gaps_found` and `score: 5/6 must-haves verified`, read to confirm the score this line will state. Read only; this task does not modify that file.
    - `.planning/WINDOWS.md` front matter — `open_count: 0`, `fixed_count: 3`, `total_count: 3`, confirming entry id 3 is closed as `fixed` rather than open.
    - `.planning/REQUIREMENTS.md` — MODAL-05's row, confirming it is Complete after 06-15's real cross-modality run, which is the fact SC2's corrected status rests on.
    - `.planning/phases/06-hipporag-2-side-by-side/06-15-SUMMARY.md` and `06-14-SUMMARY.md` — the two plans whose outcomes supersede the line's stale clauses; read enough of each to confirm the run completed and the defect closed, rather than taking this plan's word for it.
  </read_first>

  <files>.planning/ROADMAP.md</files>

  <precondition>`.planning/phases/06-hipporag-2-side-by-side/06-VERIFICATION.md` front matter reads `score: 5/6 must-haves verified` and `.planning/WINDOWS.md` front matter reads `open_count: 0`. If either disagrees with the replacement text below, the replacement is wrong: stop, report the disagreement, and do not write a number the source files do not support.</precondition>

  <action>
Replace Phase 6's `**Plans**:` line — the single line in `.planning/ROADMAP.md` beginning
`**Plans**: 17/17 plans executed` — in full, with exactly this line:

```
**Plans**: 18/18 plans executed — 9 executed across 6 waves, plus 4 gap-closure plans (06-10..06-13) across 3 further waves, plus 4 more gap-closure plans (06-14..06-17) added 2026-09-10 across 3 further waves, plus 1 more (06-18) added 2026-09-12 (06-VERIFICATION.md re-verified at 5/6; SC2 is VERIFIED — 06-15's real cross-modality run completed against live credentials and MODAL-05 is Complete — and WINDOWS.md entry id 3 is closed as `fixed` with `open_count: 0` per 06-14; SC6 alone remains FAILED, deferred a fourth time per `.planning/phases/06-hipporag-2-side-by-side/06-GATE-AMENDMENT.md`)
```

What changed and why, so the executor can tell a correct replacement from a plausible one:

- **The wave history is carried over verbatim**, clause for clause, up to and including
  `plus 4 more gap-closure plans (06-14..06-17) added 2026-09-10 across 3 further waves`. That
  history is the record of how this phase actually ran and is not this task's to revise.
- **The count moves 17/17 to 18/18** because this plan is the eighteenth, and every prior
  gap-closure round in this same line bumped the count the same way (9, then 13, then 17). A line
  that reports 17 plans while 18 exist is stale in exactly the way this task exists to fix. The
  new `plus 1 more (06-18) added 2026-09-12` clause extends the history rather than rewriting it.
- **4/6 becomes 5/6**, the score `06-VERIFICATION.md`'s own front matter now carries.
- **SC2 is named VERIFIED**, because 06-15's real cross-modality run completed against live
  credentials and MODAL-05 is Complete — it is no longer FAILED and no longer waiting on anything.
- **The defect clause is replaced by its closure**: `.planning/WINDOWS.md` entry id 3 is `fixed`
  and the ledger reports `open_count: 0`, both closed by 06-14. Nothing in this phase is
  converging on an open defect.
- **SC6 is named as the sole remaining FAILED criterion, with its deferral cited**, which is what
  Task 1's annotation makes legible on the criterion itself.

Do not edit any other line of `.planning/ROADMAP.md` in this task. The `- [ ] 06-18-PLAN.md` entry
and its wave heading are already present in the Plans list below this line; leave them alone.
  </action>

  <acceptance_criteria>
    - `.planning/ROADMAP.md` contains the literal `**Plans**: 18/18 plans executed` exactly once, and contains no line beginning `**Plans**: 17/17`.
    - That same line contains `re-verified at 5/6` and `SC2 is VERIFIED`, and contains neither a claim that SC2 is failed nor a claim that any WINDOWS.md entry is open.
    - That same line still contains `plus 4 more gap-closure plans (06-14..06-17) added 2026-09-10 across 3 further waves` — the wave history survived the replacement unedited.
    - The Plans list beneath the line is unchanged: every existing `- [x] 06-NN-PLAN.md` entry and every `**Wave N**` / `**Gap-closure Wave N**` heading is present exactly as before.
    - Phase 7's section is untouched — its `**Depends on**` line still cites `06-GATE-AMENDMENT.md`, and its `**Plans**: 5 plans across 5 sequential waves` line is unchanged.
  </acceptance_criteria>

  <verify>
    <automated>cd /home/chris/coding/Databasise-2.0-fully-agnostic-system && grep -cF '**Plans**: 18/18 plans executed' .planning/ROADMAP.md</automated>
    <fails_when>non-zero exit, or the printed count is not exactly 1 — a non-zero exit means the replacement never landed, and a count above 1 means the line was duplicated instead of replaced</fails_when>

    <automated>cd /home/chris/coding/Databasise-2.0-fully-agnostic-system && ! grep -qF 'converging on one filed defect' .planning/ROADMAP.md && echo STALE_CLAUSE_GONE</automated>
    <fails_when>non-zero exit, or the literal token STALE_CLAUSE_GONE is absent from the output — either means the stale trailing parenthetical is still in the file and the line was appended to rather than replaced</fails_when>

    <automated>cd /home/chris/coding/Databasise-2.0-fully-agnostic-system && grep -F '**Plans**: 18/18 plans executed' .planning/ROADMAP.md | grep -cF 'plus 4 more gap-closure plans (06-14..06-17) added 2026-09-10 across 3 further waves'</automated>
    <fails_when>non-zero exit, or the printed count is not exactly 1 — the wave history was dropped or reworded by a replacement that rewrote the line instead of correcting its trailing clauses</fails_when>

    <automated>cd /home/chris/coding/Databasise-2.0-fully-agnostic-system && grep -F '**Plans**: 18/18 plans executed' .planning/ROADMAP.md | grep -cF 'SC2 is VERIFIED'</automated>
    <fails_when>non-zero exit, or the printed count is not exactly 1 — SC2's corrected status is the single most load-bearing clause in this line and its absence means the correction is incomplete</fails_when>

    <automated>cd /home/chris/coding/Databasise-2.0-fully-agnostic-system && grep -cF '**Plans**: 5 plans across 5 sequential waves' .planning/ROADMAP.md && grep -cF '- [x] 06-17-PLAN.md' .planning/ROADMAP.md</automated>
    <fails_when>non-zero exit, or either printed count is not exactly 1 — a 0 on the first means Phase 7's own Plans line was collaterally edited, and a 0 on the second means the Plans list beneath the replaced line was disturbed</fails_when>
  </verify>

  <reversibility rating="reversible">One markdown line replaced with another. The original is recoverable from `git show HEAD~1 -- .planning/ROADMAP.md` and is quoted in this plan's own read_first.</reversibility>

  <done>Phase 6's `**Plans**:` line reports 18/18, a 5/6 verification score, SC2 as VERIFIED, WINDOWS.md entry id 3 as closed, and SC6 as the sole remaining FAILED criterion with its deferral cited — with the wave history and the plan list beneath it untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Remove the blanket module-level asyncio mark from test_aa_run.py so the sync tests stop emitting PytestWarnings</name>

  <read_first>
    - `databasise/tests/eval/test_aa_run.py` — the whole file, and specifically: line 43's `pytestmark = pytest.mark.asyncio`, the seven `async def test_` functions at lines 239, 265, 283, 307, 330, 357 and 377, the eight `def test_` functions at lines 176, 202, 214, 227, 415, 443, 471 and 486, and the five remaining uses of the `pytest` import at lines 186, 217, 230, 267 and 362 (`pytest.approx` and four `pytest.raises`) — which are why the `import pytest` on line 13 must stay.
    - `databasise/pyproject.toml` lines 101-103 — `[tool.pytest.ini_options]` with `asyncio_mode = "auto"`. This setting is the whole justification for the fix: under auto mode pytest-asyncio collects and runs every `async def test_` as a coroutine with no mark of any kind, so the module-level mark adds nothing to the seven async tests while misapplying itself to the eight sync ones.
    - Any one other async test module in `databasise/tests/` — for example `databasise/tests/eval/test_corpus_ingest.py` — to confirm the house convention directly: no file in this suite carries `@pytest.mark.asyncio` on a function, and `test_aa_run.py` is the only file in the suite carrying a module-level `pytestmark` asyncio mark at all.
  </read_first>

  <files>databasise/tests/eval/test_aa_run.py</files>

  <precondition>`databasise/pyproject.toml` contains `asyncio_mode = "auto"` under `[tool.pytest.ini_options]`. If it does not — if the mode is `strict` or unset — then deleting the module-level mark would stop the seven async tests from running as coroutines, and this fix is the wrong mechanism: stop and report rather than proceeding, because a suite that silently stops awaiting seven tests still exits 0.</precondition>

  <action>
Delete the module-level asyncio mark from `databasise/tests/eval/test_aa_run.py`. Concretely:
remove line 43, `pytestmark = pytest.mark.asyncio`, together with one of the two blank lines that
follow it, so the region reads as the import block, two blank lines, then the `# ---` shared-stubs
banner comment — the same spacing every other test module in this suite uses between its imports
and its first top-level block.

That deletion is the entire change. Specifically do not:

- add `@pytest.mark.asyncio` to the seven async test functions as a replacement. Under
  `asyncio_mode = "auto"` that decorator is redundant, and no other file in this suite uses it;
  adding fifteen lines to remove one is the opposite of the fix.
- add an `asyncio_mode`, `filterwarnings`, or `-W` setting to `pyproject.toml`. Suppressing the
  warning is not removing its cause, and the cause is one redundant line.
- remove `import pytest` on line 13 — five call sites still use it (`pytest.approx` once,
  `pytest.raises` four times) and the module will not import without it.
- touch, rename, reorder, split or reformat any test function, stub class, docstring or import.
  The file's fifteen tests must be the same fifteen tests, in the same order, before and after.

**Baseline to preserve, measured at planning time (2026-09-12, HEAD `0451464`), not quoted from
an older document.** `cd databasise && uv run pytest -q` currently reports
`1068 passed, 1 skipped, 8 warnings in 187.19s`, and `cd databasise && uv run pytest -q
tests/eval/test_aa_run.py` reports `15 passed, 8 warnings in 1.84s`. After this change both must
report the same pass and skip counts with the warnings clause gone entirely — pytest omits that
clause from its summary line when the count is zero, which is what the verify commands below key
on. If the pass count moves in either direction, the change did more than delete one line.

Two figures in the phase's own records disagree with those measurements, and the disagreement has
a clean cause rather than being a reason to doubt the numbers: `06-VERIFICATION.md`'s anti-pattern
table and `06-17-SUMMARY.md`'s follow-up note both say **6** warnings, measured before
`06-REVIEW-FIX.md`'s two regression tests landed — and both of those tests
(`test_score_gold_passage_empty_gold_document_ids_raises_value_error_not_zero_division` at line
227, and `test_main_spend_path_catches_confounded_pass_error_and_exits_1` at line 486) are sync,
so each added one more warning: 6 + 2 = 8. Likewise the 962-passed baseline those documents carry
predates Phase 7's five plans, which is why the current figure is 1068. Neither of those documents
is corrected here — `06-VERIFICATION.md` is out of this plan's scope by prohibition, and a
superseded SUMMARY is not rewritten — but the SUMMARY this plan writes should record the measured
figures and this explanation so the next reader is not left reconciling three different numbers.
  </action>

  <acceptance_criteria>
    - `databasise/tests/eval/test_aa_run.py` contains no line matching `pytestmark`, and no line matching `@pytest.mark.asyncio`.
    - The file still contains `import pytest` and still has exactly 7 lines matching `^async def test_` and exactly 8 lines matching `^def test_` — the same fifteen tests, none converted between sync and async.
    - `cd databasise && uv run pytest -q tests/eval/test_aa_run.py` reports `15 passed` with no warnings clause in its summary line and no line containing `PytestWarning` anywhere in its output.
    - `cd databasise && uv run pytest -q` reports exactly `1068 passed, 1 skipped` with no warnings clause — the same pass and skip counts measured before the change, proving no test was dropped, skipped, or silently stopped being awaited.
    - `git diff --numstat -- databasise/tests/eval/test_aa_run.py` shows 0 insertions and 2 deletions.
  </acceptance_criteria>

<!-- planner-discipline-allow: pytestmark -->
<!-- planner-discipline-allow: @pytest\.mark\.asyncio -->
<!-- planner-discipline-allow: pytestmark|@pytest\.mark\.asyncio -->
<!-- planner-discipline-allow: PytestWarning -->

  <verify>
    <automated>cd /home/chris/coding/Databasise-2.0-fully-agnostic-system && ! grep -qE 'pytestmark|@pytest\.mark\.asyncio' databasise/tests/eval/test_aa_run.py && grep -cE '^import pytest' databasise/tests/eval/test_aa_run.py</automated>
    <fails_when>non-zero exit, or the printed count is not exactly 1 — a non-zero exit from the first grep means the mark is still in the file or was re-added as a per-function decorator, and a 0 from the second means the still-needed `import pytest` was removed along with it</fails_when>

    <automated>cd /home/chris/coding/Databasise-2.0-fully-agnostic-system && grep -cE '^async def test_' databasise/tests/eval/test_aa_run.py && grep -cE '^def test_' databasise/tests/eval/test_aa_run.py</automated>
    <fails_when>non-zero exit, or the first printed count is not exactly 7, or the second is not exactly 8 — any other pair means a test was added, deleted, or converted between sync and async, which this task forbids</fails_when>

    <automated>cd /home/chris/coding/Databasise-2.0-fully-agnostic-system/databasise && uv run pytest -q tests/eval/test_aa_run.py 2>&1 | grep -E '^15 passed in '</automated>
    <fails_when>non-zero exit — the anchored pattern matches only when all fifteen tests pass AND the summary line carries no trailing warnings clause, so a non-zero exit means either a test failed or dropped out of collection, or at least one warning is still being emitted</fails_when>

    <automated>cd /home/chris/coding/Databasise-2.0-fully-agnostic-system/databasise && ! uv run pytest -q tests/eval/test_aa_run.py 2>&1 | grep -q PytestWarning && echo NO_PYTEST_WARNINGS</automated>
    <fails_when>non-zero exit, or the literal token NO_PYTEST_WARNINGS is absent from the output — either means a PytestWarning line is still present in the run output</fails_when>

    <automated>cd /home/chris/coding/Databasise-2.0-fully-agnostic-system/databasise && uv run pytest -q 2>&1 | grep -E '^1068 passed, 1 skipped in ' && echo SUITE_BASELINE_HELD</automated>
    <fails_when>non-zero exit, or the literal token SUITE_BASELINE_HELD is absent — the anchored pattern asserts four things at once, and a miss on any of them fails: the pass count is exactly 1068, the skipped count is exactly 1, no failures or errors occurred (pytest would name them in this same line), and no warnings remain (pytest appends a `, N warnings` clause here whenever the count is non-zero, which the anchored ` in ` terminator excludes)</fails_when>
  </verify>

  <reversibility rating="reversible">A two-line deletion in one test file, with no production code touched. Restoring the line restores the previous behavior, warnings included.</reversibility>

  <done>`databasise/tests/eval/test_aa_run.py` carries no asyncio mark of any kind; the same seven async and eight sync tests run as before; and `cd databasise && uv run pytest -q` reports `1068 passed, 1 skipped` with no warnings clause at all.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a written owner amendment -> the ROADMAP's own criterion text | An annotation that overstates the amendment silently converts a re-timing into a withdrawal, and no later reader can tell |
| a stale status line -> a future re-verification's reading of the phase | A line that misreports score, criterion status, or the defect ledger causes the next verifier to re-litigate closed work or trust closed work that is open |
| a test-collection marker -> what the suite actually asserts | A change to how tests are collected can silently stop running them while the suite still exits 0 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-18-01 | Tampering (a re-timing rewritten as a withdrawal) | Phase 6 SC6 in `.planning/ROADMAP.md` | high | mitigate | The annotation text is given verbatim in Task 1's action; the criterion's original sentence is pinned by two positive greps; the action forbids strike-through and `**STRUCK**`, and an acceptance criterion asserts their absence. |
| T-06-18-02 | Elevation of privilege (a plan discharging a requirement it may not) | MACH-03 / MODAL-01 rows, SC6's checkbox | critical | mitigate | Both requirement rows are pinned Pending by a verify command in Task 1; the plan's prohibitions forbid flipping either, and no task lists `.planning/REQUIREMENTS.md` in its `<files>`. |
| T-06-18-03 | Repudiation (asking a settled question again) | the MACH-03 spend | high | mitigate | `autonomous: true` and no checkpoint task of any type exists in this plan; the prohibition is stated in `must_haves.prohibitions` and sourced to 06-17-SUMMARY.md's binding no-re-ask clause. |
| T-06-18-04 | Tampering (tests silently stop running) | `test_aa_run.py` collection | high | mitigate | Task 3's precondition asserts `asyncio_mode = "auto"` before the deletion; sync/async function counts are pinned at 7 and 8 by a verify command; the file-level and full-suite pass counts are both asserted exactly, so a test that stops being collected fails the gate rather than passing quietly. |
| T-06-18-05 | Tampering (suppressing a warning instead of removing its cause) | `pyproject.toml` | medium | mitigate | Task 3's action names `filterwarnings` and `-W` as forbidden by name, and `pyproject.toml` appears in no task's `<files>`; the diff is asserted at 0 insertions / 2 deletions in one test file. |
| T-06-18-06 | Elevation of privilege (a plan re-scoring its own phase) | `06-VERIFICATION.md` | high | mitigate | The file appears in no task's `<files>`; its `status` and `score` lines are pinned by a verify command in Task 1; the prohibition list names it, `06-UAT.md`, and `docs/system-model/` explicitly. |
| T-06-18-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no package and adds no dependency. It edits two markdown lines and deletes two lines from one test file; the only command it runs is the project's already-configured `uv run pytest`. No package-legitimacy gate is reached. |
</threat_model>

<artifacts_this_phase_produces>
| Kind | Symbol / path | Change |
|------|---------------|--------|
| ROADMAP criterion annotation | Phase 6 Success Criterion 6 in `.planning/ROADMAP.md` | appended `*(deferred 2026-09-11 per …06-GATE-AMENDMENT.md: …)*`; original sentence unchanged |
| ROADMAP status line | Phase 6's `**Plans**:` line in `.planning/ROADMAP.md` | replaced — 17/17 to 18/18, 4/6 to 5/6, SC2 FAILED to VERIFIED, open defect to closed ledger, SC6 named as sole remaining failure |
| Test module | `databasise/tests/eval/test_aa_run.py` | deleted `pytestmark = pytest.mark.asyncio` (line 43) and one adjacent blank line; no test added, removed or converted |
| Plan summary | `.planning/phases/06-hipporag-2-side-by-side/06-18-SUMMARY.md` | created on completion, recording the measured suite figures and why they differ from the 962/6 figures in the phase's earlier records |

Nothing under `databasise/` outside that one test file is touched, and no production symbol is
created, renamed or removed by this plan.
</artifacts_this_phase_produces>

<flagged_assumptions>
- **The measured suite baseline is 1068 passed / 1 skipped / 8 warnings, not the 962 / 1 / 6 the
  orchestrator's brief and `06-VERIFICATION.md` both carry.** Both figures were measured directly
  at planning time against HEAD `0451464` and both discrepancies have a clean cause:
  `06-REVIEW-FIX.md`'s two regression tests are sync and each added one warning (6 + 2 = 8), and
  Phase 7's five plans added the tests that moved 962 to 1068. The plan asserts the measured
  numbers rather than the documented ones, because a gate keyed to a stale count would fail on a
  correct change. If the executor measures something different again, that is a real signal — a
  test was added or removed between planning and execution — and the SUMMARY should say so.
- **The plan count moves 17/17 to 18/18, which the brief said to leave alone.** The brief's
  statement was true when written, before 06-18 existed; once this plan is the eighteenth, a line
  reporting seventeen is stale in exactly the way Task 2 exists to fix, and every prior
  gap-closure round in this same line bumped the count (9, 13, 17). The wave history preceding the
  count is carried over verbatim and pinned by a verify command, which is the part the brief
  protected. Reversible in one edit if the owner disagrees.
- **SC6 takes Phase 3 criterion 5's annotation form, not Phase 7 criteria 4 and 5's struck form.**
  `06-GATE-AMENDMENT.md`'s `## What is not amended` says condition 6's substance is "re-timed a
  fourth time, not withdrawn", and a strike-through would assert the opposite. Stated here so a
  later reader can check the choice rather than take it on faith.
- **No `type="tracer"` task appears despite `TRACER_MODE: true`.** This is plan 18 of a phase whose
  tracer landed at 06-01 and whose architecture has been proven end-to-end for seventeen plans.
  The scope is two markdown lines and one deleted Python line — there is no layer stack for a thin
  slice to cut through, and a tracer here would be ceremony rather than information.
- **No `D-NN` decision ids are cited.** This phase has no `CONTEXT.md` and therefore no discussed
  decisions to cite; the decision-coverage gate skips. No ids are invented to satisfy a scanner.
</flagged_assumptions>

<verification>
- Phase 6's Success Criterion 6 carries the deferral annotation with its original sentence intact.
- Phase 6's `**Plans**:` line reports 18/18, 5/6, SC2 VERIFIED, entry id 3 closed, SC6 sole failure.
- `.planning/REQUIREMENTS.md`'s MACH-03 and MODAL-01 rows both still read Pending, unchecked.
- `06-VERIFICATION.md` still reads `status: gaps_found` / `score: 5/6 must-haves verified`, and
  `06-UAT.md` and `docs/system-model/` are unmodified.
- `cd databasise && uv run pytest -q tests/eval/test_aa_run.py` reports `15 passed in <time>` with
  no warnings clause.
- `cd databasise && uv run pytest -q` reports `1068 passed, 1 skipped in <time>` with no warnings
  clause.
- No checkpoint of any type was presented to the owner, and the MACH-03 spend was not raised.
</verification>

<success_criteria>
- A future re-verification reading Phase 6's SC6 can see, on the criterion itself, that MACH-03 was
  re-timed a fourth time by written amendment — and can follow the citation to the document that
  did it.
- Phase 6's status line states nothing that is not currently true.
- The full suite runs silent: same tests, same results, zero PytestWarnings.
- MACH-03, MODAL-01, `06-VERIFICATION.md`, `06-UAT.md` and `docs/system-model/` are exactly as this
  plan found them.
</success_criteria>

<output>
Create `.planning/phases/06-hipporag-2-side-by-side/06-18-SUMMARY.md` when done, recording: the
exact annotation text appended to SC6, the exact replacement `**Plans**:` line, the measured
before/after suite figures, and the reconciliation of those figures against the 962-passed /
6-warning numbers still carried in `06-VERIFICATION.md` and `06-17-SUMMARY.md`.
</output>
