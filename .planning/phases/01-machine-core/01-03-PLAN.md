---
phase: 01-machine-core
plan: 03
type: tdd
wave: 3
depends_on: ["01-02"]
files_modified:
  - databasise/validator/cycles.py
  - databasise/validator/depth.py
  - databasise/validator/errors.py
  - databasise/validator/parse.py
  - databasise/validator/blast_radius.py
  - databasise/validator/execution_mode.py
  - databasise/tests/validator/__init__.py
  - databasise/tests/validator/test_cycles_and_depth.py
  - databasise/tests/validator/test_blast_radius.py
  - databasise/tests/validator/test_execution_mode.py
  - databasise/tests/validator/test_taint_conformance.py
autonomous: true
requirements: [MACH-05, MACH-08]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Effective depth is computed by the machine and never read from a declaration: a node's effective depth is the minimum over its own structural depth and every transitive dep's effective depth, ordered opaque < evidence < stage (D-01)."
    - "A wiring containing a cycle still computes a defined effective depth for every node, because cycle members are condensed into one SCC and resolve to the same effective depth; the cycle itself is returned as data under a `cycle` key, never raised past the validator."
    - "A node may write a `shared` artifact namespace only at effective depth `stage`; a node writing at any shallower effective depth is refused at load time with the offending node id named (D-02, load-time half)."
    - "The transient store effects `writes_kv`, `writes_vector`, `writes_graph` and `writes_lexical` are outside the blast-radius rule by design and never trigger a refusal, however shallow the writing node's effective depth (D-03 repair 2, PARTS-04 D1)."
    - "`execution_mode` is derived from a node's declared `effects[]` and its kind, never requested by the component; a node deriving `subprocess`, `confined-unit` or `long-lived-service` returns an explicit refusal that names the unimplemented placement, while the derivation is still computed and available for stamping (D-08)."
    - "The spike-005 conformance set runs all twelve cases with no case exempt from the failure count, under the repaired vocabulary, and every case matches its expected violation set exactly (D-03)."
    - statement: "A wiring whose `nodes` object is empty is refused at parse time with a named violation rather than producing an empty successful run, and a single-node wiring with no deps validates and yields exactly one node in the depth map."
      verification: backstop
  artifacts:
    - databasise/validator/cycles.py
    - databasise/validator/depth.py
    - databasise/validator/errors.py
    - databasise/validator/blast_radius.py
    - databasise/validator/execution_mode.py
    - databasise/tests/validator/test_taint_conformance.py
  key_links:
    - "parse_wiring accumulates violations into ValidationReport -> strongly_connected_components condenses cycles -> effective_depth min-reduces over the condensation DAG -> blast_radius_violations reads that depth map -> derive_execution_mode reads effects[] and refuses unimplemented placements by name"
---

<objective>
Complete the validator: cycle-safe depth computation over a Tarjan SCC condensation, the
blast-radius rule enforced at load time, `execution_mode` derived from declared effects with D-08's
named refusals, and spike 005's twelve-case laundering conformance set ported under the repaired
contract vocabulary.

Purpose: this is the mechanism D-01 assigns to Phase 1 and the deliverable Phase 2's Falsifier 2
gate computes over. Phase 1 owns the mechanism; Phase 2 owns the proof.

Output: a validator that returns every violation in one pass with JSON-Pointer paths, and a
non-skippable conformance suite proving the taint and blast-radius rules hold on the twelve wirings
spike 005 measured.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-machine-core/01-CONTEXT.md
@.planning/phases/01-machine-core/01-RESEARCH.md
@.planning/phases/01-machine-core/01-02-SUMMARY.md
@docs/system-model/CONTRACT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cycle-safe depth — Tarjan SCC condensation, taint min-reduce, one-pass violation accumulation</name>
  <files>
    databasise/validator/cycles.py,
    databasise/validator/errors.py,
    databasise/validator/depth.py,
    databasise/validator/parse.py,
    databasise/tests/validator/__init__.py,
    databasise/tests/validator/test_cycles_and_depth.py
  </files>
  <read_first>
    - databasise/validator/depth.py — the tracer's acyclic min-reduce written in plan 01-02; this
      task replaces its ordering source, not its reduction
    - databasise/validator/parse.py — the tracer's one-pass parse, to be extended not rewritten
    - docs/system-model/CONTRACT.md line 236 — §3's taint-rule sentence, quoted verbatim: effective
      depth is the minimum over a node's transitive `deps`
    - docs/system-model/CONTRACT.md lines 238-258 — §3's depth ordering, three artifact scopes, and
      the strict cross-run pin rule
    - .planning/phases/01-machine-core/01-RESEARCH.md — "### Pattern 2: SCC-condensation depth
      computation", including its explicit rejection of adding `networkx` for one algorithm
    - .claude/skills/spike-findings-rag-graph-vector-raw/references/wiring-spec-and-validation.md —
      the "all violations at once with JSON-Pointer paths" and "cycle detection returns node names
      as data" rules
  </read_first>
  <behavior>
    Tests first in `databasise/tests/validator/test_cycles_and_depth.py`:

    - Test 1: a linear chain `a -> b -> c` where `a` is structural `opaque` and `b`, `c` are
      `stage` yields effective depth `opaque` for all three.
    - Test 2: two parallel lanes with no dep edge between them do not taint each other — an
      `opaque` node in lane 2 leaves lane 1's nodes at `stage`.
    - Test 3: non-adjacent fan-in taints — a node depending on both a `stage` sibling and an
      `opaque` sibling computes `opaque`.
    - Test 4: a three-node cycle `a -> b -> c -> a` where one member is structural `evidence` and
      the others `stage` yields `evidence` for all three members, and `strongly_connected_components`
      groups exactly those three into one component.
    - Test 5: a cycle with a `stage` node downstream of it computes that downstream node's depth as
      the cycle's depth, proving the condensation DAG is traversed in dependency order.
    - Test 6: a self-loop `a -> a` is a valid one-member SCC and does not recurse infinitely.
    - Test 7: `parse_wiring` on a document with three independent defects returns all three in one
      `ValidationReport`, each carrying a distinct JSON-Pointer path, and raises nothing.
    - Test 8: a wiring whose `nodes` object is empty produces exactly one violation whose pointer is
      `/nodes` and whose code names the empty-wiring condition; a single-node wiring with an empty
      `deps` list validates clean and produces a depth map of length one.
    - Test 9: an unintended cycle is reported as data — `parse_wiring` returns a report whose
      `cycles` field lists the member node ids, and no exception escapes the validator.
  </behavior>
  <action>
    `databasise/validator/errors.py` — define `Violation` as a frozen dataclass carrying `code`
    (a stable string identifier), `pointer` (an RFC 6901 JSON Pointer into the wiring document,
    for example `/nodes/extract/effects/1`), and `message`. Define `ValidationReport` carrying
    `violations: list[Violation]`, `cycles: list[list[str]]`, and an `ok` property that is true
    when `violations` is empty. Define stable code constants at minimum for: unknown component,
    dangling dep, unknown effect member, empty wiring, and blast-radius refusal. A report is
    returned, never raised — one-error-per-run costs a repair round trip per defect and round trips
    dominate.

    `databasise/validator/cycles.py` — hand-roll Tarjan's strongly-connected-components algorithm
    as `strongly_connected_components(deps: dict[str, set[str]]) -> list[set[str]]`, returned in
    reverse topological order. Implement it **iteratively with an explicit stack**, not recursively:
    a wiring is author-supplied data and a deep chain must not exhaust the Python recursion limit.
    Do not add `networkx` — 01-RESEARCH.md rejects pulling a graph library into `databasise/` for
    this one function, and the dependency list in `databasise/pyproject.toml` is itself part of
    EMBED-01's proof. Also provide `condensation(deps, sccs) -> dict[int, set[int]]` mapping each
    SCC index to the SCC indices it depends on, excluding self-edges, which is acyclic by
    construction.

    `databasise/validator/depth.py` — replace the tracer's acyclic ordering source with the
    condensation DAG while leaving the min-reduce untouched. `effective_depth(parsed) -> dict[str, Depth]`
    now: computes SCCs, builds the condensation, orders it with
    `graphlib.TopologicalSorter(condensation).static_order()` (always safe — the condensation is
    acyclic), and min-reduces in one linear pass, where each SCC's depth is the minimum of its
    members' structural depths and its condensed dependencies' already-computed depths. Every member
    of an SCC receives that same depth, because mutually reachable nodes resolve to the same
    effective depth under the taint rule's own definition. Order the depth lattice with an explicit
    integer mapping `opaque=0, evidence=1, stage=2` so `min` is the lattice meet; do not rely on
    string comparison.

    `databasise/validator/parse.py` — extend the tracer's one-pass parse so it accumulates into a
    `ValidationReport` rather than a bare list: every component that does not resolve in the
    registry, every `deps` entry naming a node absent from `nodes`, every declared effect outside
    the seventeen frozen members, and an empty `nodes` object. Populate `report.cycles` from
    `strongly_connected_components` with every component of size greater than one plus every
    self-loop. A cycle is reported as data and does not itself make the report not-ok, because
    cyclic wirings are legal content — only an unintended cycle is a defect, and the validator
    cannot know intent.
  </action>
  <verify>
    <automated>cd databasise &amp;&amp; uv run pytest tests/validator/test_cycles_and_depth.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd databasise && uv run pytest tests/validator/test_cycles_and_depth.py -x` exits 0 with 9 tests passing.
    - `databasise/validator/cycles.py` contains `def strongly_connected_components(` and `def condensation(`.
    - `grep -v '^#' databasise/validator/cycles.py | grep -c 'networkx'` returns 0.
    - `databasise/validator/cycles.py` implements Tarjan iteratively: `grep -v '^#' databasise/validator/cycles.py | grep -cE 'setrecursionlimit'` returns 0 and the SCC function contains no self-recursive call.
    - `databasise/validator/errors.py` defines classes `Violation` and `ValidationReport`.
    - `cd databasise && uv run python -c "from databasise.validator.errors import ValidationReport; r=ValidationReport(violations=[], cycles=[]); assert r.ok"` exits 0.
    - A three-node cycle produces exactly one entry in `report.cycles` containing all three node ids.
  </acceptance_criteria>
  <done>
    Depth is computed for every node of any wiring, cyclic or not, by a machine pass over an SCC
    condensation; the validator returns every violation together with a JSON-Pointer path and never
    raises on a defect.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Blast-radius rule at load time, and execution_mode derivation with D-08 named refusals</name>
  <files>
    databasise/validator/blast_radius.py,
    databasise/validator/execution_mode.py,
    databasise/tests/validator/test_blast_radius.py,
    databasise/tests/validator/test_execution_mode.py
  </files>
  <read_first>
    - databasise/validator/depth.py — the effective-depth map this task consumes
    - databasise/validator/errors.py — the Violation/ValidationReport shapes to emit into
    - databasise/parts/schema.py — the 17-member `Effect` literal and the `Depth` literal
    - docs/system-model/CONTRACT.md lines 242-254 — §3's three-artifact-scope table, the clause "an
      opaque node MAY write `quarantined` and MUST NOT write `shared`", and the accepted-cost note
    - docs/system-model/CONTRACT.md line 238 — §3's execution_mode clause, quoted verbatim:
      execution_mode is derived from declared effects, never requested; an arm is always its own
      confined process; only pure, non-iterative nodes MAY be hosted in-process
    - docs/system-model/CONTRACT.md line 354 — §7's rule that a transient store write is not a
      registry entry at all (this is why the transient effects sit outside the blast-radius rule)
    - .planning/phases/01-machine-core/01-PATTERNS.md — "### Explicit, actionable refusal messages
      naming the missing capability"
  </read_first>
  <behavior>
    Tests first in `test_blast_radius.py`:

    - Test 1: a node declaring `writes_artifact` with artifact scope `shared` at effective depth
      `stage` produces no violation.
    - Test 2: the same node at effective depth `opaque` produces exactly one violation whose message
      names the node id and its computed effective depth.
    - Test 3: the same node at effective depth `evidence` also produces a violation — `evidence` is
      shallower than `stage`, so evidence-derived shared writes are refused too.
    - Test 4: a node declaring `writes_artifact` with artifact scope `quarantined` at effective
      depth `opaque` produces no violation.
    - Test 5: a node declaring `writes_artifact` with artifact scope `self_storage` at effective
      depth `opaque` produces no violation.
    - Test 6: a node declaring only the transient effects `writes_kv`, `writes_vector`,
      `writes_graph` or `writes_lexical` at effective depth `opaque` produces no violation, for each
      of the four effects independently.
    - Test 7: a node declaring `writes_artifact` with no artifact scope stated is judged as `shared`
      — the default is explicit and tested in both directions.

    Tests first in `test_execution_mode.py`:

    - Test 8: a node with empty `effects` and a non-iterative kind derives `in-process`.
    - Test 9: a node declaring `net` derives a placement other than `in-process`.
    - Test 10: a node declaring `fs` derives a placement other than `in-process`.
    - Test 11: a node declaring `self_storage` derives a placement other than `in-process`.
    - Test 12: a node declaring `mutates_store` derives a placement other than `in-process`.
    - Test 13: a `fixpoint`-kind node derives a placement other than `in-process` regardless of its
      effects, because it is iterative.
    - Test 14: an `opaque`-kind node derives a placement other than `in-process`.
    - Test 15: attempting to *host* a node whose derived placement is `subprocess` raises
      `UnimplementedPlacementError` whose message contains the literal placement name; the same for
      `confined-unit` and `long-lived-service`.
    - Test 16: the derivation itself still succeeds and returns the placement string for all four
      placements — only hosting refuses. A refusal never suppresses the stamp.
  </behavior>
  <action>
    `databasise/validator/blast_radius.py` — implement
    `blast_radius_violations(parsed, depth_map) -> list[Violation]`. A node violates if and only if
    it declares `writes_artifact` AND the artifact scope of that write is `shared` AND its effective
    depth is not `stage`. Three properties this predicate must have, each of which is one of D-03's
    named repairs to the spike prototype:

    First, `writes_quarantined` is NOT a member of the seventeen-member effects vocabulary and must
    not be treated as one. Scope is a property of the artifact write, carried on the part's
    declaration as an `artifact_scope` field over the three §3 scopes `shared`, `quarantined`,
    `self_storage`, defaulting explicitly to `shared` when unstated. Add `artifact_scope` to the
    `Part` dataclass in `databasise/parts/schema.py` if plan 01-02 did not already carry it.

    Second, `writes_artifact` is distinguished from the four transient store effects `writes_kv`,
    `writes_vector`, `writes_graph`, `writes_lexical`. Those four sit outside the blast-radius rule
    by design per PARTS-04 D1 and CONTRACT §7 — a transient store write carries no namespace, no
    sub-recipe stamp, no corpus and no producing-instance binding, so it is not a registry entry at
    all and the rule that governs registry entries does not reach it. The predicate must key on
    `writes_artifact` alone.

    Third, the violation message names the offending node id, its computed effective depth, and the
    scope it attempted, so a wiring author can act on it without reading the validator source.

    Wire `blast_radius_violations` into `parse_wiring`'s report so the check runs at load time — this
    is the first of D-02's two call sites; plan 01-07 adds the second at the artifact-write path.

    `databasise/validator/execution_mode.py` — implement
    `derive_execution_mode(effects, kind) -> str` returning one of `in-process`, `subprocess`,
    `confined-unit`, `long-lived-service`. The rule is CONTRACT §3's: only pure, non-iterative nodes
    may be hosted in-process. Concretely, derive `in-process` if and only if the node's kind is
    neither `fixpoint` (iterative) nor `opaque`, and its declared effects contain none of `net`,
    `fs`, `self_storage`, `mutates_store`. Assign the remaining cases to the three heavier
    placements by a documented, deterministic mapping; state the mapping in the module docstring so
    Phase 5 amends a written rule rather than reverse-engineering one. The derivation is a pure
    function of declared effects and kind and never reads a self-declared placement field.

    Implement `host(placement)` separately, raising `UnimplementedPlacementError` for `subprocess`,
    `confined-unit` and `long-lived-service`, with a message naming the specific placement that is
    unimplemented and stating that placement is earned in Phase 5 when real opaque-node admission
    forces it. Follow the actionable-message shape 01-PATTERNS.md names, not a bare
    `NotImplementedError`. The split between deriving and hosting is D-08's whole point: the
    derivation from `effects[]` is still computed and stamped on the run record; only the hosting is
    unbuilt.
  </action>
  <verify>
    <automated>cd databasise &amp;&amp; uv run pytest tests/validator/test_blast_radius.py tests/validator/test_execution_mode.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd databasise && uv run pytest tests/validator/test_blast_radius.py tests/validator/test_execution_mode.py -x` exits 0 with 16 tests passing.
    - `databasise/validator/blast_radius.py` contains `def blast_radius_violations(`.
    - D-03 repair 1 checked semantically rather than by negative grep (a docstring legitimately explains why the fabricated effect was removed, so a text search would flag the explanation): `cd databasise && uv run python -c "import typing; from databasise.parts.schema import Effect; m=typing.get_args(Effect); assert len(m)==17, len(m); assert 'writes_quarantined' not in m"` exits 0.
    - `databasise/validator/execution_mode.py` contains `def derive_execution_mode(`, `def host(`, and `class UnimplementedPlacementError`.
    - `cd databasise && uv run python -c "from databasise.validator.execution_mode import host, UnimplementedPlacementError; import sys;\nfor p in ('subprocess','confined-unit','long-lived-service'):\n  try: host(p); sys.exit(1)\n  except UnimplementedPlacementError as e: assert p in str(e), p"` exits 0.
    - `databasise/parts/schema.py` declares an `artifact_scope` field over exactly `shared`, `quarantined`, `self_storage`.
  </acceptance_criteria>
  <done>
    The blast-radius rule refuses a shallow shared write at load time with the node named, the four
    transient store effects are provably outside the rule, and every unimplemented placement refuses
    by name while its derivation still succeeds.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Port spike 005's twelve-case laundering conformance set with D-03's three repairs</name>
  <files>
    databasise/tests/validator/test_taint_conformance.py
  </files>
  <read_first>
    - .claude/skills/spike-findings-rag-graph-vector-raw/sources/005-laundering-test/taint.py — the
      full source. Lines 26-46 hold the component registry; lines 86-195 hold the twelve-case table
      with each case's expected violation set and its stated purpose. Lines 60-78 hold the two rules
      as the prototype implemented them.
    - .claude/skills/spike-findings-rag-graph-vector-raw/sources/005-laundering-test/README.md — the
      Falsifier 1 record and the three-scope repair
    - .planning/phases/01-machine-core/01-CONTEXT.md — decision D-03, which names the three
      divergences that must be repaired in the port rather than carried
    - docs/system-model/CONTRACT.md lines 242-258 — §3's scope table and the Falsifier-1 verdict note
    - databasise/validator/depth.py and databasise/validator/blast_radius.py — the implementations
      under test
  </read_first>
  <behavior>
    One parametrised test over all twelve cases, asserting the computed violation set equals the
    expected violation set exactly (not a superset, not a subset). Case ids and expected violation
    sets, after D-03's repairs:

    - Case 1 (decomposed ingest + opaque query core): expect no violations.
    - Case 2 (opaque core feeds a stage extractor that writes shared): expect `{extract}`.
    - Case 3 (real LightRAG middle state — opaque ingest writing the index, decomposed query chain
      downstream): expect `{ingest}`. **This is D-03 repair 3.** The prototype recorded `[]` here
      and its harness exempted any case whose title begins with `3.` from the failure count; the
      port carries no exemption and no case title is special. Under the taint rule the opaque
      ingest node declaring a `shared` artifact write at effective depth `opaque` is an ordinary
      refusal, which is exactly the finding that forced the `quarantined` scope amendment.
    - Case 3b (ingest as its own run): expect `{ingest}`.
    - Case 3c (query run reading that index, no runtime dep on ingest): expect no violations —
      decomposition credit survives the artifact boundary.
    - Case 3d (the repair: opaque ingest writing an instance-scoped quarantined namespace): expect
      no violations. **This is D-03 repair 1.** The prototype modelled this with a fabricated
      `writes_quarantined` effect. The port declares `effects = [calls_llm, writes_artifact]` with
      `artifact_scope = quarantined`, because scope is a property of the artifact write, not an
      effects member.
    - Case 4 (fully decomposed, every node stage): expect no violations.
    - Case 5 (fully opaque part using only self_storage): expect no violations.
    - Case 6 (opaque node writing a shared namespace directly): expect `{ingest}`.
    - Case 7 (non-adjacent fan-in — stage cache reads an opaque sibling, writes shared): expect
      `{cache}`.
    - Case 8 (parallel lanes, no dep edge between them): expect no violations.
    - Case 9 (evidence-depth part feeding a stage extractor that writes shared): expect `{extract}`.

    Plus one guard test asserting **D-03 repair 2**: adding each of `writes_kv`, `writes_vector`,
    `writes_graph` and `writes_lexical` to the opaque node in case 6 changes nothing — the violation
    set stays `{ingest}` and no additional violation appears for the transient writes.

    Plus one meta-test asserting the case table has exactly twelve entries and that no entry carries
    any skip, xfail or exemption marker.
  </behavior>
  <action>
    Port the case table into `databasise/tests/validator/test_taint_conformance.py` as a named
    conformance set, restated in frozen contract vocabulary. Build a fixture registry of the
    spike's components mapped onto this project's `Part` shape: each entry carries
    `name_at_version`, `structural_depth` over `opaque`/`evidence`/`stage`, `effects` drawn only
    from the seventeen frozen members, and `artifact_scope` where the part writes an artifact. Set
    `upstream_ref` on every ported entry to the spike source path, per D-14 and CONTRACT §7 — this
    is copied prior work and its provenance is recorded rather than implied.

    Run each case through the real `parse_wiring` plus `effective_depth` plus
    `blast_radius_violations` path — do not reimplement the two rules inside the test file. The
    prototype's own rule implementations exist only as the oracle for the expected values, which are
    already fixed in the table above.

    Mark the whole module as a mandatory suite: no `pytest.mark.skip`, no `pytest.mark.xfail`, no
    conditional exemption of any kind. D-03's third repair exists precisely because the prototype
    harness exempted a case from its own failure count, and the port must make that structurally
    impossible.
  </action>
  <verify>
    <automated>cd databasise &amp;&amp; uv run pytest tests/validator/test_taint_conformance.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `cd databasise && uv run pytest tests/validator/test_taint_conformance.py -x` exits 0.
    - `cd databasise && uv run pytest tests/validator/test_taint_conformance.py --collect-only -q | grep -c 'test_'` reports at least 14 collected tests (12 parametrised cases plus the transient-effects guard plus the meta-test).
    - `grep -v '^#' databasise/tests/validator/test_taint_conformance.py | grep -cE 'mark\.(skip|xfail)'` returns 0.
    - No ported registry fixture declares the fabricated effect, checked by value rather than by text (the case-3d docstring legitimately explains the repair): `cd databasise && uv run pytest tests/validator/test_taint_conformance.py -k repair_one -x` exits 0, where that test asserts every fixture part's `effects` is a subset of the 17 frozen members.
    - `grep -c 'upstream_ref' databasise/tests/validator/test_taint_conformance.py` is at least 1.
    - Case 3's expected violation set in the file is `{ingest}`, not empty.
  </acceptance_criteria>
  <done>
    All twelve spike-005 cases run against the real validator under the repaired vocabulary, no case
    is exempt from the failure count, and the three D-03 divergences are repaired rather than
    carried forward.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols created by THIS plan (new — not pre-existing, not drift):

**Modules:** `databasise/validator/cycles.py`, `databasise/validator/errors.py`,
`databasise/validator/blast_radius.py`, `databasise/validator/execution_mode.py`,
`databasise/tests/validator/__init__.py`, `databasise/tests/validator/test_cycles_and_depth.py`,
`databasise/tests/validator/test_blast_radius.py`,
`databasise/tests/validator/test_execution_mode.py`,
`databasise/tests/validator/test_taint_conformance.py`.

**Functions:** `strongly_connected_components`, `condensation`, `blast_radius_violations`,
`host`. Extended in place: `effective_depth`, `parse_wiring`, `derive_execution_mode`.

**Classes:** `Violation`, `ValidationReport`, `UnimplementedPlacementError`.

**Fields added to existing types:** `Part.artifact_scope` (Literal over `shared`, `quarantined`,
`self_storage`; default `shared`), `ValidationReport.cycles`.
</artifacts_this_phase_produces>

<verification>
- `cd databasise && uv run pytest tests/validator/ -x` is green across all four test modules.
- The twelve-case conformance set passes with no exemptions.
- Depth is computed for cyclic wirings without raising.
</verification>

<success_criteria>
The validator computes effective depth for any wiring, refuses shallow shared writes at load time
with the node named, derives every `execution_mode` while refusing three of the four placements by
name, and reproduces spike 005's measured result under the repaired contract vocabulary.
</success_criteria>

<output>
Create `.planning/phases/01-machine-core/01-03-SUMMARY.md` when done.
</output>
