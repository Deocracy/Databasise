---
phase: 05-opaque-side-admission
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - databasise/evidence/INJECTED-LLM-ENDPOINT-SURVEY.md
  - databasise/evidence/DR-04-DECISION.md
  - databasise/tests/evidence/__init__.py
  - databasise/tests/evidence/test_admission_docs.py
autonomous: true
requirements: [MACH-04, HARD-03]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`databasise/evidence/INJECTED-LLM-ENDPOINT-SURVEY.md` carries exactly five engine rows — codebase-memory-mcp (CA-2), postgres-mcp (MC-1), supabase-mcp (MC-2), code-graph-rag (CA-1), and a foreign-hosted LightRAG server — each with an endpoint-injection verdict, the evidence it rests on, and an explicit confidence tag; Falsifier 8's own claim is restated and adjudicated against those five rows (MACH-04)."
    - statement: "Two survey passes over the same five-engine list yield the same five engine rows and the same per-engine verdict — the survey records no run-order-dependent state and its regeneration is not a stateful operation (edge MACH-04/concurrency)."
      verification: backstop
    - "The survey's own header states that the five-engine list is an adopted recommended default inferred from CATALOG.md's `CA-`/`MC-` buckets and Falsifier 6's named pair — not a verbatim list read from any source document — and names the one-edit place to correct it (05-RESEARCH.md Assumption A2, OPEN DECISION 1)."
    - "`databasise/evidence/DR-04-DECISION.md` selects exactly one of DR-04's two named options by name, and its selection is machine-readable as a single declared value rather than inferable only from prose (HARD-03)."
    - "The DR-04 record checks each of the two coverings against live code and states, for each, whether it holds today — the artifact-registry SA-2 sub-recipe stamp and the per-ref `ChunkRef(corpus_id, recipe@version, ordinal, content_hash)` — naming Phase 4's FA-03 finding where a covering is declared but not realized, rather than asserting both hold."
  prohibitions:
    - "An inferred default must never be presented as if it were read from a source document — the five-engine survey list's and the DR-04 recommendation's provenance is stated wherever each is recorded."
  artifacts:
    - path: "databasise/evidence/INJECTED-LLM-ENDPOINT-SURVEY.md"
      provides: "MACH-04 / Falsifier 8's non-gating documentation pass: the endpoint-injection surface of the five sandbox-candidate engines"
    - path: "databasise/evidence/DR-04-DECISION.md"
      provides: "HARD-03: DR-04 decided, with the selected option named and both coverings checked against live code"
    - path: "databasise/tests/evidence/test_admission_docs.py"
      provides: "Executable assertions that both evidence documents carry their required structure, so a later edit that empties a row fails a test rather than passing unnoticed"
  key_links:
    - from: "databasise/evidence/DR-04-DECISION.md"
      to: "databasise/registry_artifact/index.py"
      via: "the SA-2 sub-recipe stamp columns (sa2_chunker / sa2_extraction / sa2_embedding) the first covering rests on"
      pattern: "sa2_"
    - from: "databasise/evidence/DR-04-DECISION.md"
      to: "databasise/seam/evidence.py"
      via: "the EvidenceRef field set Phase 4's FA-03 recorded as narrower than §4's ChunkRef shape"
      pattern: "class EvidenceRef"
---

<objective>
Close the phase's two documentation-shaped requirements honestly: survey injected-LLM-endpoint
support across the five sandbox-candidate engines (Falsifier 8, explicitly non-gating), and decide
DR-04 one way or the other.

Purpose: both are owner-facing judgments that Phase 5 is the first rung to actually face. Neither
gates the ladder, and neither may be left implicitly decided.
Output: two evidence documents under `databasise/evidence/`, each with executable structural
assertions so a later edit cannot silently hollow one out.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-opaque-side-admission/05-RESEARCH.md
</context>

## Artifacts this phase produces

Symbols and files created by **this plan** (new to the codebase — not drift):

| Symbol / file | Kind | Location |
|---|---|---|
| `databasise/evidence/INJECTED-LLM-ENDPOINT-SURVEY.md` | evidence document | new file |
| `databasise/evidence/DR-04-DECISION.md` | evidence document | new file |
| `databasise/tests/evidence/` package | test package | `databasise/tests/evidence/__init__.py` |
| `test_survey_names_five_engines` | test function | `databasise/tests/evidence/test_admission_docs.py` |
| `test_survey_states_list_provenance` | test function | `databasise/tests/evidence/test_admission_docs.py` |
| `test_dr04_selects_one_named_option` | test function | `databasise/tests/evidence/test_admission_docs.py` |
| `test_dr04_checks_both_coverings` | test function | `databasise/tests/evidence/test_admission_docs.py` |

## Open decisions adopted by this plan

`05-RESEARCH.md` carries three OPEN DECISIONs and no CONTEXT.md exists. Two are settled here:

- **OPEN DECISION 1 (MACH-04's five engines) — ADOPTED as recommended.** The researcher's default
  list is taken as-is: `codebase-memory-mcp` (CATALOG CA-2), `postgres-mcp` (MC-1), `supabase-mcp`
  (MC-2), `code-graph-rag` (CA-1), and a foreign-hosted LightRAG server (Falsifier 6's own second
  named engine). Grounds for adopting rather than checkpointing: Falsifier 8 is explicitly
  non-gating and `accepted-as-stated-risk` in `SYSTEM-MODEL.md`'s own falsifier table, the output
  is a document, and the survey states its own list provenance so a correction is a one-edit change.
  Rated `reversible`.
- **OPEN DECISION 2 (DR-04 / HARD-03) — the researcher's recommendation is the starting position,
  not the conclusion.** Task 2 checks both coverings against live code before selecting, because
  Phase 4's own FA-03 finding says one of the two coverings is narrower in this codebase than the
  model text assumes. Rated `costly` (see the task's `<reversibility>`).

`05-RESEARCH.md`'s OPEN DECISION 3 (network-namespace enforcement) is adopted in plans 05-01 and
05-06, not here.

<tasks>

<task type="auto">
  <name>Task 1: The injected-LLM-endpoint survey across five sandbox-candidate engines</name>
  <files>databasise/evidence/INJECTED-LLM-ENDPOINT-SURVEY.md, databasise/tests/evidence/__init__.py, databasise/tests/evidence/test_admission_docs.py</files>
  <reversibility rating="reversible">A documentation pass on a non-gating falsifier; a wrong engine row is corrected by editing one table row.</reversibility>
  <read_first>
    - `docs/system-model/SYSTEM-MODEL.md` line 158 — Falsifier 8's own claim, experiment wording, cost, narrowness and disposition
    - `docs/system-model/D-VARIANTS/SELECTION.md` line 133 — Falsifier 8 as originally stated
    - `docs/system-model/CATALOG.md` lines 110-118 and the `CA-1`/`CA-2`/`MC-1`/`MC-2` detail entries (around lines 569, 741, 767) — the four catalogued engines and their already-recorded capability/tool surfaces
    - `docs/system-model/PARTS.md` §X condition 3 — codebase-memory-mcp's already-run endpoint verdict, which this survey cites rather than re-derives
    - `docs/system-model/CONTRACT.md` §8 condition 3 — the injected-endpoint-or-`unbudgetable` rule the survey adjudicates against
    - `databasise/evidence/FALSIFIER-2-EVIDENCE.md` — the house format for a committed, re-readable evidence document in this repo
    - `databasise/clients/openai_compat.py` — the machine's own injectable `base_url` client, the concrete thing an engine would have to accept
  </read_first>
  <action>
Write `databasise/evidence/INJECTED-LLM-ENDPOINT-SURVEY.md`, following `FALSIFIER-2-EVIDENCE.md`'s existing committed-evidence format (a dated header, the claim under test quoted verbatim, a findings table, a verdict section).

Required structure:

1. **Header.** Date, requirement id `MACH-04`, falsifier id `Falsifier 8`, and its disposition quoted from `SYSTEM-MODEL.md`: non-gating, cheap, `accepted-as-stated-risk`. State plainly that a failure here is a comparability finding, not a ladder halt.

2. **List provenance, stated before the table.** A short paragraph recording that no document in `docs/system-model/` enumerates exactly five engines under the "sandbox-candidate" label; that the five below are an adopted recommended default composed from `CATALOG.md`'s `CA-`/`MC-` opaque-shaped entries plus Falsifier 6's own named second engine; and that correcting the list means editing this table. Cite `05-RESEARCH.md` Assumption A2 as the origin of the inference.

3. **The claim under test, quoted verbatim** from `SELECTION.md` line 133 and `SYSTEM-MODEL.md` line 158.

4. **The findings table**, one row per engine, columns: engine (with its CATALOG id), hosting shape, does it accept an injected LLM endpoint, the mechanism by which it would be injected, evidence class (`[code-verified]` / `[docs-verified]` / `[inference]`) with the concrete source, and the §8 condition 3 consequence (denied network namespace with the machine as provider, or admitted `unbudgetable`). The five rows:
   - `codebase-memory-mcp` (CA-2) — cite `PARTS.md ## §X` condition 3's already-run verdict verbatim, including that it is recorded as an open question rather than a clean yes because of the loopback-bound UI server, and that the engine has zero LLM calls anywhere so endpoint injection is vacuous for it.
   - `postgres-mcp` (MC-1) — cite `CATALOG.md`'s MC-1 entry; a SQL tool surface with no LLM in its own closure, so the same vacuous-injection shape as CA-2.
   - `supabase-mcp` (MC-2) — cite `CATALOG.md`'s MC-2 entry.
   - `code-graph-rag` (CA-1) — cite `CATALOG.md`'s CA-1 entry, which records a `generator` and a Cypher-generating `rewriter`; this is the one catalogued candidate whose own closure calls an LLM, so its endpoint-injection surface is the load-bearing row.
   - a foreign-hosted LightRAG server — the Falsifier-6-named LLM-heavy case; ground it in what this repo can actually see, `v1/lightrag/llm/openai.py`'s `base_url`/`api_key` parameters and `v1/scripts/run_parity_ingest.py`'s env-driven construction, which Phase 3 already exercises as a real injection.
   Where a row's evidence is an inference rather than a read source, the evidence class column says so — never round an unread engine up to a verified yes.

5. **Verdict.** Adjudicate Falsifier 8's claim against the five rows: state how many accept injection, how many are vacuous (no LLM to inject into), how many are unknown, and what that means for the token-comparability claim. State explicitly which rows would have to change for the falsifier to fire, and that the falsifier is recorded as surveyed rather than as refuted or confirmed if the evidence does not support either.

6. **Method and limits.** One paragraph: what was read, what was not, and that no engine other than the LightRAG case was executed for this pass.

Do not edit anything under `docs/system-model/` — that tree is a verbatim copy of the shipped system model, and the ANATOMY/PARTS reconciliation pass is Phase 7's HARD-02.
  </action>
  <verify>
    <automated>cd databasise &amp;&amp; uv run pytest -q tests/evidence/test_admission_docs.py -k survey -x</automated>
    <fails_when>non-zero exit, or the summary line reports "no tests ran"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '^| ' databasise/evidence/INJECTED-LLM-ENDPOINT-SURVEY.md` counts a findings table with a header row, a separator row and exactly five engine rows for the findings table specifically (the test asserts on the parsed table, not on a whole-file line count).
    - Each of the strings `codebase-memory-mcp`, `postgres-mcp`, `supabase-mcp`, `code-graph-rag`, `LightRAG server` appears in the findings table.
    - Each of `CA-1`, `CA-2`, `MC-1`, `MC-2` appears in the document.
    - Every findings-table row carries one of the three evidence-class tags in its evidence column, asserted by the test per row rather than by a whole-file grep.
    - The document contains a section whose heading names the list's provenance, and that section names `A2`.
    - `git show --name-only --pretty=format: HEAD` after this task's commit lists no path under `docs/system-model/`.
  </acceptance_criteria>
  <done>The survey exists, covers exactly five named engines with per-row evidence classes, states that its own engine list is an adopted default rather than a sourced list, and adjudicates Falsifier 8's claim without overstating it.</done>
</task>

<task type="auto">
  <name>Task 2: DR-04 decided, with both coverings checked against live code</name>
  <files>databasise/evidence/DR-04-DECISION.md, databasise/tests/evidence/test_admission_docs.py</files>
  <reversibility rating="costly">Selecting one of DR-04's two named options closes a defect-register row that has been open since the v1.0 milestone audit; reversing means reopening a dated, recorded selection and, if the other option is later taken, changing the opaque ingest core's own internal chunk schema.</reversibility>
  <read_first>
    - `docs/system-model/ANATOMY.md` line 756 — the DR-04 row in full: the question, the two named options, and the "Narrowed, not closed, by D9" verdict
    - `docs/system-model/PARTS.md` line 461 and lines 452-479 — Appendix A's DR-04 disposition, the dated correction note, and the three-group reasoning
    - `docs/system-model/SYSTEM-MODEL.md` line 190 — the fence naming the two coverings and stating the owner is the build, first faced at this rung
    - `docs/system-model/CONTRACT.md` §7 (registries and retention) — the SA-2 sub-recipe stamp and the partial-coverage registration refusal
    - `docs/system-model/CONTRACT.md` §4 — `ItemKind`, `ScoredItem.provenance`, and the deref-raising discipline
    - `databasise/registry_artifact/index.py` — the artifact table's `sa2_chunker`/`sa2_extraction`/`sa2_embedding` columns and `register()`'s idempotency key: covering 1, as actually built
    - `databasise/seam/evidence.py` — `EvidenceRef`'s real field set: covering 2, as actually built
    - `.planning/phases/04-the-seam/04-VERIFICATION.md` and `.planning/STATE.md`'s Phase 04 decision list — FA-03, which records that `EvidenceRef` carries `ref`/`namespace`/`kind`/`score`/`tier` and not the full §4 `ChunkRef` shape, because `corpus_id`, `recipe@version`, `ordinal` and `content_hash` are unavailable at the retrieval position today
    - `.planning/phases/05-opaque-side-admission/05-RESEARCH.md` — OPEN DECISION 2 and Assumption A3
  </read_first>
  <action>
Write `databasise/evidence/DR-04-DECISION.md` — the record that closes HARD-03.

**Step 1 — check each covering against live code before selecting.** Read
`databasise/registry_artifact/index.py` and `databasise/seam/evidence.py` and record, for each of
the two coverings the model names, whether it holds in this codebase today:

- *Covering 1 — the per-run SA-2 sub-recipe stamp at the artifact-registry level (CONTRACT §7).*
  Record the actual column names on the artifacts table, whether `register()` writes them on every
  registration, and whether §7's partial-coverage refusal is implemented or only declared. Quote
  the real identifiers, not paraphrases.
- *Covering 2 — the per-ref `ChunkRef(corpus_id, recipe@version, ordinal, content_hash)`
  (ANATOMY §C).* Record `EvidenceRef`'s real field set. Phase 4 already recorded (FA-03) that this
  covering is narrower in the built system than the model text assumes. Do not restate the model's
  claim as if it were the code's behaviour — state what the code carries and name FA-03 as the
  prior record of the gap.

**Step 2 — select an option by name.** Open the document with a machine-readable YAML front matter
block carrying `defect_row: DR-04`, `requirement: HARD-03`, `decided: <ISO date>`, and
`selected_option:` set to exactly one of the two literal values `per-chunk-provenance-stamp` or
`two-covering-rationale`, plus `selection_is_clean: true|false`. The prose then argues the
selection.

The starting position, from `05-RESEARCH.md`'s recommendation, is `two-covering-rationale`: the
existing SA-2 registration stamp plus §7's partial-coverage refusal answer the provenance question
a per-chunk field would answer, without a schema change inside the opaque ingest core's own
internals — a change CONTRACT's own framing treats as a capability not yet earned. **Adopt it only
if step 1 supports it.** If step 1 finds covering 2 does not hold as built, keep the same selection
but set `selection_is_clean: false` and add a section named for the gap that states: which covering
is short, exactly what it is short by, what closing it would require, and which requirement or
phase would own that work. A selection recorded as clean when one of its two legs is missing is the
exact rounding-up this phase's own admission discipline forbids.

**Step 3 — record the consequence for this phase.** State in one short section what this decision
means concretely for plan 05-01's ingest port: whether the opaque ingest core's chunk artifact
gains a new field (no, under `two-covering-rationale`) and what the artifact-registry registration
must therefore carry instead. State that no code change follows from this decision under the
selected option, so the record itself is the deliverable.

**Step 4 — scope fence.** State that `docs/system-model/ANATOMY.md`'s and `PARTS.md`'s own DR-04
rows are not edited by this plan: reconciling the `ANATOMY §F` rows with their PARTS Appendix A
dispositions is HARD-02, scheduled to Phase 7, and this record is the input that pass will consume.
  </action>
  <verify>
    <automated>cd databasise &amp;&amp; uv run pytest -q tests/evidence/test_admission_docs.py -k dr04 -x</automated>
    <fails_when>non-zero exit, or the summary line reports "no tests ran"</fails_when>
    <automated>cd databasise &amp;&amp; uv run pytest -q</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `cd databasise && uv run python -c "
import pathlib
t = pathlib.Path('evidence/DR-04-DECISION.md').read_text()
assert t.startswith('---'), 'no front matter'
fm = t.split('---')[1]
assert 'defect_row: DR-04' in fm
assert 'requirement: HARD-03' in fm
opts = [o for o in ('per-chunk-provenance-stamp','two-covering-rationale') if o in fm]
assert len(opts) == 1, opts
assert 'selection_is_clean:' in fm
print('ok')"` prints `ok`.
    - The document names both `sa2_` column identifiers read from `databasise/registry_artifact/index.py` and the real `EvidenceRef` field names read from `databasise/seam/evidence.py`.
    - The document contains the string `FA-03`.
    - `git show --name-only --pretty=format: HEAD` after this task's commit lists no path under `docs/system-model/`.
    - `databasise/tests/evidence/test_admission_docs.py` asserts the front-matter `selected_option` value by parsing it, not by a substring search over the whole file.
  </acceptance_criteria>
  <done>DR-04 carries a dated, named selection between its two options; both coverings are checked against the built system and the record says which one falls short and by what, rather than asserting both hold.</done>
</task>

</tasks>

<verification>
- `cd databasise && uv run pytest -q` exits 0.
- Both evidence documents are committed and readable standalone — a reader who has not read this plan can follow each from its own header.
- <human-check>Read `databasise/evidence/DR-04-DECISION.md`. This is the phase's one real policy decision and the owner is its named owner in `SYSTEM-MODEL.md`. Confirm the selected option, or say which option you want instead and re-run this task.</human-check>
- <human-check>Read the five-engine list in `databasise/evidence/INJECTED-LLM-ENDPOINT-SURVEY.md`. It is an adopted default, not a sourced list. Correct any engine that does not belong in the sandbox-candidate lineup.</human-check>
</verification>

<success_criteria>
- MACH-04's survey exists, names five engines, and adjudicates Falsifier 8 without overstating.
- HARD-03's DR-04 row carries one named selection and an honest account of each covering's real state.
- No file under `docs/system-model/` is modified.
</success_criteria>

<output>
Create `.planning/phases/05-opaque-side-admission/05-02-SUMMARY.md` when done.
</output>
