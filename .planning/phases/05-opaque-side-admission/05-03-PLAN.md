---
phase: 05-opaque-side-admission
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - databasise/parts/schema.py
  - databasise/runner/scheduler.py
  - databasise/foreign/v1_corpus_driver_script.py
  - databasise/foreign/v1_corpus_adapter.py
  - databasise/parts_core/lightrag/full_delete.py
  - databasise/parts_core/declared_only.py
  - databasise/wirings/lightrag/corpus-delete.json
  - databasise/seam/corpus.py
  - databasise/seam/engine.py
  - databasise/seam/refusals.py
  - databasise/tests/fixtures/v1_corpus_driver_stub.py
  - databasise/tests/parts_core/lightrag/test_full_delete.py
  - databasise/tests/seam/test_delete_document.py
  - databasise/tests/seam/test_mach11_event.py
autonomous: true
requirements: [API-02, MODAL-02]

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`lightrag/full-delete@0.1.0` is a second, separate part and node registration from `lightrag/full-ingest@0.1.0` — same underlying engine, different declared effects (`mutates_store` versus `writes_artifact`) — per CONTRACT §19.6's separate-port rule; neither part exposes the other's operation."
    - "`await Databasise(...).delete_document(document_id)` returns a `DeletionOutcome` whose `status` is drawn from v1's own four-value vocabulary (`success`, `not_found`, `not_allowed`, `fail`) — the machine invents no fifth status."
    - "Deleting the same document twice returns `success` then `not_found`; the second call performs no second mutation (edge API-02/idempotency)."
    - "A delete issued while the same document's ingest is mid-pipeline returns v1's own `not_allowed` status rather than racing the pipeline lock (edge API-02/concurrency)."
    - "A real delete against a real v1 working directory leaves an entity shared with a surviving document in the graph with the deleted document's chunk ids subtracted from its source set, and removes an entity whose source set the deletion empties — v1's own `subtract_source_ids` algorithm is preserved, not reimplemented (API-02)."
    - "Running the delete port through the real scheduler produces exactly one MACH-11 `SeamEvent` carrying the deleting part's registered `name@version`, its own spend, and an outcome from `ResponseEnvelope`'s closed vocabulary — the first non-fixture exercise of the correlation Phase 4 built."
    - "The MACH-11 correlation distinguishes a machine-observed store touch from a node-reported one; the deleting opaque node's touches are node-reported, because the machine holds no store handle inside the subprocess, and the code and its test say so rather than presenting the two as the same evidence."
    - "`databasise/seam/envelope.py` is unchanged by this plan — MACH-11 needed no new envelope field (Pitfall 2)."
  artifacts:
    - path: "databasise/parts_core/lightrag/full_delete.py"
      provides: "The second opaque port: lightrag/full-delete@0.1.0, its §8 admission record, and its executable body"
      contains: "async def full_delete_body"
    - path: "databasise/wirings/lightrag/corpus-delete.json"
      provides: "The one-node delete wiring the seam's delete_document() parses and runs"
    - path: "databasise/tests/seam/test_delete_document.py"
      provides: "The API-02 proof: idempotency, concurrency status, and the real graph-aware cleanup against a copied v1 working directory"
  key_links:
    - from: "databasise/seam/engine.py"
      to: "databasise/seam/engine.py"
      via: "delete_document() passes the same _recorder callable _execute() passes to run_wiring, so _mach11_events correlates the deleting part"
      pattern: "_mach11_events"
    - from: "databasise/parts_core/lightrag/full_delete.py"
      to: "databasise/foreign/v1_corpus_adapter.py"
      via: "await asyncio.to_thread(run_corpus_op, \"delete\", ...)"
      pattern: "run_corpus_op"
    - from: "databasise/foreign/v1_corpus_driver_script.py"
      to: "v1/lightrag/lightrag.py"
      via: "adelete_by_doc_id — v1's own source_id set-subtraction algorithm, preserved not reimplemented"
      pattern: "adelete_by_doc_id"
---

<objective>
Give the admitted opaque engine its second port: deletion, declared `mutates_store`, routed through
the machine's MACH-11 seam-event path for the first time against a real part rather than a fixture.

Purpose: CONTRACT §19.6 settles that ingest and delete must be two separate ports because their
declared effects differ. Phase 4 built the MACH-11 correlation but shipped it with zero production
parts declaring `mutates_store` and its own docstring asking a later phase to re-check the rule
against a real deleting part. This is that phase.
Output: a second admitted Part, a delete wiring, `Databasise.delete_document()`, and a real
graph-aware-cleanup proof against v1's own working directory.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-opaque-side-admission/05-RESEARCH.md
@.planning/phases/05-opaque-side-admission/05-PATTERNS.md
@.planning/phases/05-opaque-side-admission/05-01-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols created by **this plan** (new to the codebase — not drift):

| Symbol | Kind | File |
|---|---|---|
| `full_delete_body` | async function | `databasise/parts_core/lightrag/full_delete.py` |
| `LIGHTRAG_FULL_DELETE_PART` | constant | `databasise/parts_core/lightrag/full_delete.py` |
| `LIGHTRAG_FULL_DELETE_ADMISSION` | constant | `databasise/parts_core/lightrag/full_delete.py` |
| `DELETE_WALL_CLOCK_CEILING_SECONDS` | constant | `databasise/parts_core/lightrag/full_delete.py` |
| `NodeContext.record_store_touch` | dataclass field | `databasise/parts/schema.py` |
| `TOUCH_KIND_OBSERVED`, `TOUCH_KIND_NODE_REPORTED` | constants | `databasise/runner/scheduler.py` |
| `DeletionOutcome` | pydantic model | `databasise/seam/corpus.py` |
| `Databasise.delete_document` | method | `databasise/seam/engine.py` |
| `UnknownDocumentError` | exception | `databasise/seam/refusals.py` |
| `databasise/wirings/lightrag/corpus-delete.json` | wiring document | new file |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The delete port — a second registration, not a second operation on the first</name>
  <files>databasise/foreign/v1_corpus_driver_script.py, databasise/foreign/v1_corpus_adapter.py, databasise/parts_core/lightrag/full_delete.py, databasise/parts_core/declared_only.py, databasise/wirings/lightrag/corpus-delete.json, databasise/tests/fixtures/v1_corpus_driver_stub.py, databasise/tests/parts_core/lightrag/test_full_delete.py</files>
  <reversibility rating="costly">Splitting ingest and delete into two registered ports fixes a granularity choice §19.6 governs and that every later modality's opaque admission copies; merging them later would change two published part identities and every wiring that names them.</reversibility>
  <read_first>
    - `databasise/parts_core/lightrag/full_ingest.py` — the sibling port written by plan 05-01: its admission record shape, its ceiling constant, and its body's `asyncio.to_thread(run_corpus_op, ...)` call
    - `databasise/foreign/v1_corpus_adapter.py` — `run_corpus_op`'s signature and its two refusals
    - `databasise/foreign/v1_corpus_driver_script.py` — the `op` dispatch written by 05-01
    - `databasise/parts_core/declared_only.py` — the current registration list and `DECLARED_ONLY_PARTS`
    - `databasise/parts/admission.py` — `AdmissionRecord`, `ConditionVerdict`, `validate_admission`, `MANIFEST_SOURCE_CODE_INSPECTED`
    - `databasise/parts/schema.py` — `Effect`'s seventeen members, `Part.artifact_scope`'s "set only for a part that declares writes_artifact" rule
    - `docs/system-model/CONTRACT.md` §19.6 (the separate-port rule, quoted in 05-RESEARCH.md) and §14.4's `mutable-store` definition
    - `docs/system-model/PARTS.md` §X's mutation-surface section — the `manage_adr`/`delete_project` precedent for `mutates_store` on the same whole-engine part
    - `v1/lightrag/lightrag.py` lines 3111-3160 — `adelete_by_doc_id`'s signature and its pipeline-lock concurrency-control docstring
    - `v1/lightrag/base.py` lines 996-1006 — `DeletionResult`'s four-value status literal
    - `v1/lightrag/utils.py` line 4564 — `subtract_source_ids`, the algorithm being preserved
  </read_first>
  <behavior>
    - `LIGHTRAG_FULL_DELETE_PART.effects` contains `mutates_store` and does not contain `writes_artifact`; `LIGHTRAG_FULL_INGEST_PART.effects` contains `writes_artifact` and does not contain `mutates_store`. The two `name_at_version` values differ.
    - `LIGHTRAG_FULL_DELETE_PART.artifact_scope` is `None` — the part registers no artifact.
    - `derive_execution_mode(LIGHTRAG_FULL_DELETE_PART.effects, LIGHTRAG_FULL_DELETE_PART.kind)` returns the subprocess placement (the opaque kind decides before the `mutates_store` branch is reached), and `host` accepts it with the declared ceiling.
    - Registering the delete part without an admission record raises `UnadmittedOpaquePartError`.
    - The driver script's `delete` branch, given `{"op": "delete", "doc_id": ..., "working_dir": ...}`, returns v1's `DeletionResult` fields as JSON; an unknown `doc_id` yields the `not_found` status rather than a non-zero exit.
  </behavior>
  <action>
Add the deleting port, mirroring the ingest port's structure so a reader sees two ports of one engine rather than two designs.

**A. `databasise/foreign/v1_corpus_driver_script.py`.** Add an `op == "delete"` branch: read `{"doc_id": str, "delete_llm_cache": bool}` from the job, build the facade with the same `_build_rag(working_dir)` the ingest branch uses, `await rag.adelete_by_doc_id(doc_id, delete_llm_cache=...)`, and write `{"status": ..., "doc_id": ..., "message": ..., "status_code": ..., "file_path": ...}` from the returned `DeletionResult` to stdout. Do not reimplement or wrap the reference-counting logic: `adelete_by_doc_id` already subtracts the deleted document's chunk ids from each affected entity's and relation's `source_id` set and deletes the entity or edge only when the remaining set is empty, rebuilding it from surviving sources otherwise. A v1-side status of `not_found` or `not_allowed` is a *successful* driver run reporting that status — exit 0 and let the machine map it; only an exception exits 1.

Also add an `op == "entities"` branch used only by Task 3's proof: given `{"doc_id": str}`, return `{"entities": {<entity_name>: [<source_id>, ...]}}` for the entity names the named document contributes to, read through v1's own graph-storage and entity-info APIs. Decide the exact calls after reading `v1/lightrag/lightrag.py`'s `get_entity_info` and `v1/lightrag/base.py`'s `BaseGraphStorage` abstract methods; prefer v1's own public accessors over reaching into a storage implementation.

**B. `databasise/foreign/v1_corpus_adapter.py`.** No signature change is expected — `run_corpus_op` already dispatches on `op`. If the delete branch needs a different default working directory or a different env key than ingest, add it as an explicit keyword argument with a default, never as a silent branch inside the launcher.

**C. `databasise/parts_core/lightrag/full_delete.py` (new).** `DELETE_WALL_CLOCK_CEILING_SECONDS = 300.0` with a stated basis (v1's own deletion path performs graph and vector surgery plus a bounded rebuild over surviving sources; the machine declares the ceiling, v1 self-reports none — §8 condition 4). `LIGHTRAG_FULL_DELETE_ADMISSION: AdmissionRecord` built the same way `LIGHTRAG_FULL_INGEST_ADMISSION` is, with `part_name_at_version="lightrag/full-delete@0.1.0"`, `storage="machine"`, `feed_tier="n/a — this port consumes a document id, never a machine-produced feed"`, and its own eleven `ConditionVerdict` entries. Where a verdict is identical to the ingest port's, state it in its own words with its own evidence rather than importing the ingest record's tuple — two ports, two records, per §19.6. `LIGHTRAG_FULL_DELETE_PART: Part` with `name_at_version="lightrag/full-delete@0.1.0"`, `kind="opaque"`, `structural_depth="opaque"`, `effects=["mutates_store", "reads_kv", "reads_graph"]`, `upstream_ref="v1/lightrag/lightrag.py"`, `artifact_scope=None`, `body=full_delete_body`, `admission=LIGHTRAG_FULL_DELETE_ADMISSION`. `async def full_delete_body(ctx)` reads `doc_id` from `ctx.config`, calls `await asyncio.to_thread(run_corpus_op, "delete", payload, timeout=DELETE_WALL_CLOCK_CEILING_SECONDS, working_dir=...)`, and returns the driver's payload unchanged. Task 2 adds the store-touch reporting call to this body.

**D. `databasise/parts_core/declared_only.py`.** Import `LIGHTRAG_FULL_DELETE_PART` and add it to `DECLARED_ONLY_PARTS`, or — preferably — move both LightRAG corpus-side parts into a small tuple this module re-exports, keeping `DECLARED_ONLY_PARTS`'s existing name and membership contract intact so `default_registry()` and the Falsifier-2 evidence wirings keep resolving. Update the module docstring: this file's name is now partly historical, and it should say so rather than mislead.

**E. `databasise/wirings/lightrag/corpus-delete.json` (new).** A one-node wiring mirroring `corpus-ingest.json`: `wiring_id` `lightrag-corpus-delete`, one node keyed `full-delete` with `component` `lightrag/full-delete@0.1.0`, `kind` `opaque`, the part's own three effects, empty `deps`, empty `harnesses`, `provides` naming the single node. No derived field is declared.

**F. `databasise/tests/fixtures/v1_corpus_driver_stub.py`.** Add stub branches for `delete` (echo a configurable status from the job, defaulting to `success`) and `entities` (echo a configurable entity map), so Task 2 and Task 3 can drive both without a real v1.

**G. `databasise/tests/parts_core/lightrag/test_full_delete.py` (new).** One test per `<behavior>` bullet, asserting on real declared values.
  </action>
  <verify>
    <automated>cd databasise && uv run pytest -q tests/parts_core/lightrag/test_full_delete.py -x</automated>
    <fails_when>non-zero exit, or the summary line reports "no tests ran"</fails_when>
    <automated>cd databasise && uv run python -m databasise.tools.check_import_boundary</automated>
    <fails_when>non-zero exit, or any stdout line reporting an `[import-boundary]` violation</fails_when>
    <automated>cd databasise && uv run pytest -q tests/validator/ tests/parts/ -x</automated>
    <fails_when>non-zero exit — the Falsifier-2 wirings resolve against `DECLARED_ONLY_PARTS` and must keep resolving after the registration change</fails_when>
  </verify>
  <acceptance_criteria>
    - `cd databasise && uv run python -c "
from databasise.parts.registry import default_registry
r = default_registry()
i = r.get('lightrag/full-ingest@0.1.0'); d = r.get('lightrag/full-delete@0.1.0')
assert 'writes_artifact' in i.effects and 'mutates_store' not in i.effects
assert 'mutates_store' in d.effects and 'writes_artifact' not in d.effects
assert d.artifact_scope is None
assert len(d.admission.verdicts) == 11
print('ok')"` prints `ok`.
    - `cd databasise && uv run python -c "
from databasise.validator.execution_mode import derive_execution_mode, host
from databasise.parts.registry import default_registry
d = default_registry().get('lightrag/full-delete@0.1.0')
m = derive_execution_mode(d.effects, d.kind); assert m == 'subprocess', m
host(m, wall_clock_ceiling_seconds=d.admission.wall_clock_ceiling_seconds)
print('ok')"` prints `ok`.
    - `grep -c 'adelete_by_doc_id' databasise/foreign/v1_corpus_driver_script.py` is at least 1, and `grep -c 'subtract_source_ids' databasise/` across the `databasise/` tree is 0 — the algorithm is called on the v1 side, never copied onto the machine side.
    - `cd databasise && uv run python -c "import json,pathlib; d=json.loads(pathlib.Path('wirings/lightrag/corpus-delete.json').read_text()); assert list(d['nodes'])==['full-delete']; print('ok')"` prints `ok`.
  </acceptance_criteria>
  <done>Two ports of one engine are registered and resolvable, differing exactly in declared effects; the delete port derives the subprocess placement and hosts under its own declared ceiling; the driver's delete branch calls v1's own deletion algorithm and returns its own status vocabulary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: delete_document() at the seam, and MACH-11's first real correlation</name>
  <files>databasise/parts/schema.py, databasise/runner/scheduler.py, databasise/parts_core/lightrag/full_delete.py, databasise/seam/engine.py, databasise/seam/corpus.py, databasise/seam/refusals.py, databasise/tests/seam/test_mach11_event.py, databasise/tests/seam/test_delete_document.py</files>
  <read_first>
    - `databasise/seam/engine.py` — the module docstring's MACH-11 section in full (`_mach11_events`, `_accounted_store_keys`, the FA-08 note that no production part declares `mutates_store` and that a later phase must re-check the rule), plus `_execute`'s `_recorder`/`touches` plumbing and its `ingest()` sibling from 05-01
    - `databasise/runner/scheduler.py` — `_ScopedStoresView` and the `recorder(node_id, kind, key)` call shape it emits
    - `databasise/parts/schema.py` — `NodeContext`'s current five fields and the "defaults to an empty dict so every existing construction site keeps working" precedent
    - `databasise/seam/envelope.py` — `SeamEvent`'s closed field set and `ResponseEnvelope`'s outcome vocabulary
    - `databasise/tests/seam/test_mach11_event.py` — the whole file, including its module docstring's explanation of why the fixture parts never complete real dispatch and how the test reaches a genuine touch tuple
    - `docs/system-model/CONTRACT.md` §14.4's `mutable-store` paragraph and §18.2's envelope shape
  </read_first>
  <behavior>
    - `NodeContext` gains an optional `record_store_touch` callable defaulting to a no-op, so every existing construction site and every existing test keeps working unchanged.
    - The scheduler binds `record_store_touch` for each dispatched node to the run's recorder with the node-reported touch kind; machine-observed touches from `_ScopedStoresView` keep the observed kind.
    - `_mach11_events` treats both touch kinds as touches for a `mutates_store` part, and its docstring states which is evidence and which is a self-report.
    - Running the delete wiring through `Databasise.delete_document()` produces exactly one `SeamEvent` whose `component` is `lightrag/full-delete@0.1.0`, whose `spend` carries a real `counted_by`, and whose `outcome` is a member of the envelope's closed vocabulary.
    - A node declaring `mutates_store` whose only touched key is accounted for by a declared dependency still produces no event — the existing in-deps case from Phase 4 is unchanged.
    - `delete_document` on an unknown document id returns a `DeletionOutcome` with the `not_found` status; a second delete of a previously deleted id returns the same.
  </behavior>
  <action>
Make the deleting port's mutation visible at the seam, and prove the correlation against it rather than against a fixture.

**A. `databasise/parts/schema.py`.** Add `record_store_touch: Callable[[str], None] = field(default=lambda _key: None)` to `NodeContext`, documented as the channel by which a node whose real store mutation happens outside any handle the machine holds — an opaque node hosted in its own OS process — reports that mutation for MACH-11 correlation. State in the field's docstring that a node-reported touch is a self-report and is recorded as such, never merged with a machine-observed one.

**B. `databasise/runner/scheduler.py`.** Define module constants `TOUCH_KIND_OBSERVED = "store"` (the existing value `_ScopedStoresView` already emits — do not change it, `_mach11_events` and `test_mach11_event.py` both read it) and `TOUCH_KIND_NODE_REPORTED = "store-declared"`. When building each node's `NodeContext`, bind `record_store_touch` to a closure calling the run's `recorder(node_id, TOUCH_KIND_NODE_REPORTED, key)`. When no recorder is supplied for the run, bind the existing no-op default.

**C. `databasise/parts_core/lightrag/full_delete.py`.** After a successful driver call whose reported status is `success`, call `ctx.record_store_touch("graph")` and `ctx.record_store_touch("vector")` and `ctx.record_store_touch("kv")` — the three store kinds v1's own deletion path mutates inside the opaque boundary. Do not report a touch for a `not_found` or `not_allowed` status: nothing was mutated, and reporting one would fabricate an event.

**D. `databasise/seam/engine.py`.** In `_mach11_events`, accept a touch whose kind is either `TOUCH_KIND_OBSERVED` or `TOUCH_KIND_NODE_REPORTED`, importing both constants from the scheduler rather than repeating their string values. Extend the function's docstring and the module docstring's MACH-11 section with the finding this plan is required to record: the correlation as Phase 4 built it keys on store touches the scheduler's own `_ScopedStoresView` observes, and a `mutates_store` node hosted at the `subprocess` placement mutates stores the machine holds no handle to, so the machine can observe nothing for it; the node therefore reports its own touches through `NodeContext.record_store_touch`, and the two kinds stay distinct in the recorded tuples so a reader can tell observed evidence from a self-report. Delete or rewrite the stale sentence claiming no part registered anywhere declares `mutates_store` — two now do.

**E. `databasise/seam/corpus.py`.** `DeletionOutcome(BaseModel)` frozen with `extra="forbid"`: `document_id: str`, `status: Literal["success", "not_found", "not_allowed", "fail"]`, `message: str`, `seam_events: list[SeamEvent]`. The status literal is v1's own vocabulary, imported conceptually rather than re-invented; state that in the model's docstring.

**F. `databasise/seam/refusals.py`.** Add `UnknownDocumentError(SeamRefusalError)` carrying `document_id`. Use it only for an id that fails the machine's own token discipline — a document v1 reports as `not_found` is a normal outcome carried in `DeletionOutcome.status`, not a refusal, because a caller deleting an already-deleted document has asked a legitimate question and received a legitimate answer.

**G. `databasise/seam/engine.py`.** Add `async def delete_document(self, document_id: str) -> DeletionOutcome`: validate the id against the same token discipline `databasise/seam/corpus.py`'s `generated_on_disk_name` enforces (raising `UnknownDocumentError` on a malformed id), load `corpus-delete.json`, stamp `doc_id` on the `full-delete` node's config, `_inject_token_allowance`, `parse_wiring`, build stores and finalize them in a `finally` block, and `await _scheduler.run_wiring(...)` passing **the same `_recorder` closure shape `_execute` passes** — this is the load-bearing line, without it MACH-11 sees nothing. After the run, build `node_by_id` from the `RunRecord` exactly as `_execute` does, call `_mach11_events(parsed, touches, node_by_id)`, and return a `DeletionOutcome` carrying the driver's status/message and the correlated events. Do not construct a `ResponseEnvelope` and do not touch `databasise/seam/envelope.py`.

**H. `databasise/tests/seam/test_mach11_event.py`.** Add a test class for the real deleting part, driving the stub driver: assert exactly one `SeamEvent`, assert its `component` equals the registered `name@version` and not a node id, assert its `spend.counted_by` is a real value rather than the `unknown` sentinel, and assert the recorded touch tuples for that node carry the node-reported kind. Leave every existing fixture-based test in the file untouched — the in-deps no-event case must keep passing.

**I. `databasise/tests/seam/test_delete_document.py` (new).** The idempotency and concurrency-status behaviours, driven by the stub: two deletes of one id yield `success` then `not_found`; a stub configured to report `not_allowed` (v1's pipeline-busy answer) surfaces that status unchanged rather than being remapped or raised.
  </action>
  <verify>
    <automated>cd databasise && uv run pytest -q tests/seam/test_mach11_event.py tests/seam/test_delete_document.py -x</automated>
    <fails_when>non-zero exit, or the summary line reports "no tests ran", or fewer tests are collected from `test_mach11_event.py` than before this task ran</fails_when>
    <automated>cd databasise && uv run pytest -q</automated>
    <fails_when>non-zero exit — `NodeContext` gains a field, so every construction site in the suite is the regression surface</fails_when>
  </verify>
  <acceptance_criteria>
    - `cd databasise && uv run python -c "
from databasise.parts.schema import NodeContext
c = NodeContext(node_id='n', config=None, inputs={}, stores={})
c.record_store_touch('graph')
print('ok')"` prints `ok` — the new field defaults to a working no-op.
    - `grep -c 'TOUCH_KIND_NODE_REPORTED' databasise/seam/engine.py databasise/runner/scheduler.py databasise/parts_core/lightrag/full_delete.py` reports a non-zero count for the scheduler and the seam; the string value itself appears in exactly one module.
    - `databasise/seam/envelope.py` appears in no commit of this plan (`git log --name-only --pretty=format: 05-03-start..HEAD` does not list it).
    - The MACH-11 section of `databasise/seam/engine.py`'s module docstring no longer asserts that no registered part declares `mutates_store`.
    - The `SeamEvent` produced for a delete run carries `component == "lightrag/full-delete@0.1.0"`, asserted by equality in the test.
  </acceptance_criteria>
  <done>A real deleting part fires MACH-11 for the first time; the correlation rule was re-checked against it rather than assumed, and the finding — that a subprocess-hosted mutator's touches are node-reported, not machine-observed — is recorded in the code that implements it.</done>
</task>

<task type="auto">
  <name>Task 3: Graph-aware cleanup proved against a real v1 working directory</name>
  <files>databasise/tests/seam/test_delete_document.py, databasise/parts_core/lightrag/full_delete.py</files>
  <precondition>`v1/.parity_working_dir/` exists and holds Phase 3's committed ingest build (created by `v1/scripts/run_parity_ingest.py`). The test copies it; it never mutates the original, because 03-02's D-01 pins every recorded parity number to that one build.</precondition>
  <read_first>
    - `v1/lightrag/lightrag.py` `adelete_by_doc_id`, especially lines 3532-3552 (the entity path) and 3608-3627 (the relation path) — `remaining_sources`, `entities_to_delete`, `entities_to_rebuild`, `graph_references_deleted_chunks`
    - `v1/lightrag/utils.py` line 4564 — `subtract_source_ids`
    - `v1/scripts/run_parity_ingest.py` — which documents are in `v1/.parity_working_dir` and how it was built (the 20-document HotpotQA fixture, run exactly once)
    - `databasise/tests/fixtures/corpus/MANIFEST.json` — the document ids in that corpus
    - `databasise/tests/parity/test_graph_arm_real_index.py` — this repo's existing precedent for a test gated on the real v1 build being present
    - `databasise/foreign/v1_corpus_driver_script.py` — the `delete` and `entities` branches from Task 1
    - `.planning/phases/03-lightrag-query-side/03-VERIFICATION.md` — what the Phase 3 build actually contains and what was measured against it
  </read_first>
  <action>
Prove API-02's central claim against real data rather than a stub: an entity shared with a surviving document survives the deletion with its source set reduced, and an entity the deletion orphans is removed.

**A. Test shape (`databasise/tests/seam/test_delete_document.py`, added to the file Task 2 created).** A single test that:
1. Skips with a loud, specific reason when `v1/.parity_working_dir` is absent, mirroring `tests/parity/test_graph_arm_real_index.py`'s existing guard.
2. Copies the whole working directory into `tmp_path` with `shutil.copytree` and points the adapter's `working_dir` override at the copy. The original is never opened for writing.
3. Chooses a target document from the corpus manifest and calls the driver's `entities` op for it, then partitions the returned entity map into two sets: entities whose `source_id` set names chunks from more than one document (shared) and entities whose set names only this document's chunks (orphan-only). Skip with a loud reason naming the document if either set is empty — a corpus with no shared entity cannot prove this claim, and pretending otherwise is the failure this whole phase's discipline exists to prevent.
4. Deletes the document through `Databasise.delete_document()`.
5. Re-reads both entity sets through the `entities` op against the same copied directory and asserts: every shared entity still exists, with the deleted document's chunk ids absent from its source set and at least one surviving chunk id still present; every orphan-only entity is gone.

**B. Cost gate.** v1's own deletion path rebuilds partially-affected entities, which may reach the LLM when no extraction cache is present. Guard the real leg additionally on an opt-in environment variable (`DATABASISE_RUN_REAL_DELETE`), and make the skip message state both that the leg is opt-in and what it costs. When the variable is set and the working directory is present, the test runs and its assertions are hard — a failure here is a real API-02 failure, never a skip. Record in the plan summary what the run actually cost the first time it was executed, so the phase's own evidence says whether this is affordable to re-run.

**C. Adapter timeout.** Pass `DELETE_WALL_CLOCK_CEILING_SECONDS` through unchanged; if the real run exceeds it, do not raise the ceiling to make the test pass — record the measured duration and raise the ceiling deliberately in `full_delete.py` with the measurement as its stated basis, since §8 condition 4's ceiling is the machine's own declaration and must be grounded in something.

**D. Evidence.** Append a short section to the plan's own SUMMARY (not a new evidence document) recording: the document deleted, the counts of shared and orphan-only entities, the before/after source-set sizes for one named shared entity, and the measured wall-clock duration. This is what makes the API-02 claim checkable by a reader who does not re-run the test.
  </action>
  <verify>
    <automated>cd databasise && DATABASISE_RUN_REAL_DELETE=1 uv run pytest -q tests/seam/test_delete_document.py -x -rs</automated>
    <fails_when>non-zero exit, or the `-rs` skip report names the real-deletion test while `v1/.parity_working_dir` exists on disk (a present build must not be skipped past)</fails_when>
    <automated>cd databasise && uv run pytest -q</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - The real-deletion test calls `shutil.copytree` and the string `.parity_working_dir` never appears as a write target in the test — asserted by reading the test, and by the original directory's `git status` remaining clean after the run.
    - The test's skip messages name the missing precondition explicitly (which directory, or which environment variable), never a bare `skip`.
    - The shared-entity assertion checks both halves: the deleted document's chunk ids are absent AND at least one surviving chunk id is still present. An assertion that only checks absence would pass on a fully deleted entity.
    - `05-03-SUMMARY.md` contains the measured wall-clock duration of the real deletion run and the before/after source-set sizes for one named entity.
  </acceptance_criteria>
  <done>API-02's no-orphaning claim is proved against v1's real graph, with the numbers recorded, or the run's blocking precondition is named loudly enough that nobody mistakes a skip for a pass.</done>
</task>

</tasks>

<verification>
- `cd databasise && uv run pytest -q` exits 0.
- `cd databasise && uv run python -m databasise.tools.check_import_boundary` exits 0.
- `databasise/seam/envelope.py` is untouched across the whole plan — MACH-11 required no envelope change (05-RESEARCH.md Pitfall 2).
- <human-check>Run `cd databasise && DATABASISE_RUN_REAL_DELETE=1 uv run pytest -q tests/seam/test_delete_document.py -rs` once against the real Phase 3 build and read the recorded before/after entity source sets in `05-03-SUMMARY.md`. Confirm the surviving entity's remaining sources are the ones you expect.</human-check>
</verification>

<success_criteria>
- Ingest and delete are two separate admitted ports of one engine, differing exactly in declared effects (§19.6).
- `Databasise.delete_document()` returns v1's own deletion status vocabulary and the correlated MACH-11 events.
- The MACH-11 correlation was re-checked against a real deleting part and its limitation for subprocess-hosted mutators is recorded in the code, not assumed away.
- API-02's graph-aware cleanup is proved against real v1 data with the numbers recorded.
</success_criteria>

<output>
Create `.planning/phases/05-opaque-side-admission/05-03-SUMMARY.md` when done.
</output>
