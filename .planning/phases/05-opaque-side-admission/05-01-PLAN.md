---
phase: 05-opaque-side-admission
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - databasise/parts/admission.py
  - databasise/parts/schema.py
  - databasise/parts/registry.py
  - databasise/parts_core/lightrag/__init__.py
  - databasise/validator/execution_mode.py
  - databasise/runner/scheduler.py
  - databasise/foreign/__init__.py
  - databasise/foreign/v1_corpus_driver_script.py
  - databasise/foreign/v1_corpus_adapter.py
  - databasise/parts_core/lightrag/full_ingest.py
  - databasise/parts_core/declared_only.py
  - databasise/wirings/lightrag/corpus-ingest.json
  - databasise/seam/corpus.py
  - databasise/seam/engine.py
  - databasise/seam/refusals.py
  - databasise/tools/check_import_boundary.py
  - databasise/pyproject.toml
  - databasise/tests/fixtures/v1_corpus_driver_stub.py
  - databasise/tests/parts_core/lightrag/test_full_ingest.py
  - databasise/tests/parts/test_admission.py
autonomous: true
requirements: [MODAL-02, API-01]

estimate:
  tokens: 92000
  raw_tokens: 92000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A caller can `await Databasise(...).ingest(IngestDocument(text=...))` and receive an `IngestJob` carrying v1's own `track_id` — the ~1,786-line ingest core ran as a `kind=\"opaque\"` node dispatched through `scheduler.run_wiring`, in a subprocess under v1's own interpreter, never imported in-process (MODAL-02, API-01)."
    - "`databasise/tools/check_import_boundary.py` exits 0 with `v1_corpus_driver_script.py` as the only entry added to `_SUBPROCESS_ENTRY_POINT_EXCLUSIONS` by this plan, and no other module under `databasise/` imports `lightrag`."
    - "A subprocess-placed opaque node whose run exceeds its declared `wall_clock_ceiling_seconds` is refused by a named timeout refusal carrying the ceiling value; a run that completes inside the ceiling returns its payload normally (edge MODAL-02/boundary, §8 condition 4)."
    - "The ingest node's token spend is reported with `counted_by` naming its real source; when the subprocess reports no usage at all the node reports the existing `unbudgetable` sentinel and the seam raises rather than substituting an estimated, rounded, or zero number (edge MODAL-02/precision, §9, D-08)."
    - "Two ingests submitted concurrently each receive a distinct `job_id`; an ingest interrupted mid-flight leaves its documents in v1's own `PENDING`/`FAILED` status, never in `PROCESSED` (edge API-01/concurrency)."
    - "`PartRegistry.register` refuses an executable (`body is not None`) part whose `kind` is `opaque` and whose `admission` is `None` — an opaque node cannot be smuggled into the registry without a §8 admission record."
    - "`AdmissionRecord` for `lightrag/full-ingest@0.1.0` carries exactly eleven `ConditionVerdict` entries, one per §8 condition, each with a non-empty `evidence` string, and `manifest_source` is the code-inspected value — the engine's own self-report is never the manifest source (§8 condition 1)."
    - "An opaque part declaring `artifact_scope=\"shared\"` is refused by name at admission validation; `quarantined` is accepted (§8 condition 5, §3)."
    - "A raw-upload ingest never joins a caller-supplied filename into a filesystem path — the on-disk name is generated server-side, and a document exceeding the declared byte cap is refused before any bytes reach the subprocess."
  artifacts:
    - path: "databasise/parts/admission.py"
      provides: "The §8 admission record: ConditionVerdict, AdmissionRecord, validate_admission, and the named admission refusals"
      contains: "class AdmissionRecord"
    - path: "databasise/foreign/v1_corpus_driver_script.py"
      provides: "The v1-side leaf subprocess entry point for the corpus-side operations (ingest today; status/delete added by 05-03/05-04), JSON over stdin/stdout"
      contains: "def main"
    - path: "databasise/foreign/v1_corpus_adapter.py"
      provides: "The machine-side launcher for the corpus driver — the §17 foreign-part adapter: process lifecycle, wall-clock ceiling, non-zero exit as a named refusal"
      contains: "def run_corpus_op"
    - path: "databasise/parts_core/lightrag/full_ingest.py"
      provides: "The real executable body for lightrag/full-ingest@0.1.0, replacing the body=None stub"
      contains: "async def full_ingest_body"
    - path: "databasise/wirings/lightrag/corpus-ingest.json"
      provides: "The one-node ingest wiring the seam's ingest() parses and runs"
    - path: "databasise/seam/corpus.py"
      provides: "The corpus-side seam DTOs: IngestDocument, IngestJob"
      contains: "class IngestDocument"
    - path: "databasise/tests/parts_core/lightrag/test_full_ingest.py"
      provides: "The end-to-end tracer test: ingest one document through the admitted opaque node against a stub driver, and against the real v1 interpreter when present"
  key_links:
    - from: "databasise/seam/engine.py"
      to: "databasise/runner/scheduler.py"
      via: "await scheduler.run_wiring(parsed, registry, stores, ...) for the one-node ingest wiring"
      pattern: "run_wiring\\("
    - from: "databasise/parts_core/lightrag/full_ingest.py"
      to: "databasise/foreign/v1_corpus_adapter.py"
      via: "await asyncio.to_thread(run_corpus_op, ...)"
      pattern: "run_corpus_op"
    - from: "databasise/runner/scheduler.py"
      to: "databasise/validator/execution_mode.py"
      via: "host(execution_mode, wall_clock_ceiling_seconds=...) — the ceiling sourced from the resolved Part's admission record"
      pattern: "host\\("
    - from: "databasise/tools/check_import_boundary.py"
      to: "databasise/foreign/v1_corpus_driver_script.py"
      via: "_SUBPROCESS_ENTRY_POINT_EXCLUSIONS names the driver script by filename"
      pattern: "v1_corpus_driver_script"
---

<objective>
Admit LightRAG's ingest core as a real, executable opaque node and prove one document flows from
the seam, through the machine's own scheduler, into v1's pipeline — in a subprocess under v1's own
interpreter — and back out as a job handle.

Purpose: this is the phase's load-bearing claim. Every prior phase declared `lightrag/full-ingest`
without a body; Phase 1 refused to host anything but `in-process`, with its own refusal message
naming Phase 5 as the phase that earns `subprocess` hosting. Nothing else in this phase can run
until an opaque node can actually execute.
Output: a §8 admission record type, `subprocess` placement hosting, the v1 corpus driver script and
its machine-side adapter, a real `full_ingest` body, a one-node ingest wiring, and
`Databasise.ingest()`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-opaque-side-admission/05-RESEARCH.md
@.planning/phases/05-opaque-side-admission/05-PATTERNS.md
@.claude/skills/spike-findings-rag-graph-vector-raw/SKILL.md
</context>

## Artifacts this phase produces

Symbols created by **this plan** (new to the codebase — not drift):

| Symbol | Kind | File |
|---|---|---|
| `ConditionVerdict` | dataclass | `databasise/parts/admission.py` |
| `AdmissionRecord` | dataclass | `databasise/parts/admission.py` |
| `validate_admission` | function | `databasise/parts/admission.py` |
| `AdmissionConditionMissingError` | exception | `databasise/parts/admission.py` |
| `SelfReportedManifestError` | exception | `databasise/parts/admission.py` |
| `MissingWallClockCeilingError` | exception | `databasise/parts/admission.py` |
| `ForbiddenSharedScopeError` | exception | `databasise/parts/admission.py` |
| `UnadmittedOpaquePartError` | exception | `databasise/parts/registry.py` |
| `Part.admission` | dataclass field | `databasise/parts/schema.py` |
| `run_corpus_op` | function | `databasise/foreign/v1_corpus_adapter.py` |
| `CorpusOpTimeoutError` | exception | `databasise/foreign/v1_corpus_adapter.py` |
| `CorpusOpSubprocessError` | exception | `databasise/foreign/v1_corpus_adapter.py` |
| `MissingV1InterpreterError` (re-export) | exception | `databasise/foreign/v1_corpus_adapter.py` |
| `DEFAULT_V1_INTERPRETER`, `DEFAULT_V1_DRIVER_SCRIPT`, `DEFAULT_V1_ENV_PARITY`, `DEFAULT_V1_WORKING_DIR` | constants | `databasise/foreign/v1_corpus_adapter.py` |
| `full_ingest_body` | async function | `databasise/parts_core/lightrag/full_ingest.py` |
| `LIGHTRAG_FULL_INGEST_ADMISSION` | constant | `databasise/parts_core/lightrag/full_ingest.py` |
| `INGEST_WALL_CLOCK_CEILING_SECONDS` | constant | `databasise/parts_core/lightrag/full_ingest.py` |
| `IngestDocument` | pydantic model | `databasise/seam/corpus.py` |
| `IngestJob` | pydantic model | `databasise/seam/corpus.py` |
| `MAX_DOCUMENT_BYTES` | constant | `databasise/seam/corpus.py` |
| `Databasise.ingest` | method | `databasise/seam/engine.py` |
| `OversizedDocumentError` | exception | `databasise/seam/refusals.py` |
| `AmbiguousIngestPayloadError` | exception | `databasise/seam/refusals.py` |
| `ForeignEngineRefusalError` | exception | `databasise/seam/refusals.py` |
| `databasise/foreign/` package | package | `databasise/foreign/__init__.py` |
| `databasise/wirings/lightrag/corpus-ingest.json` | wiring document | new file |
| `databasise/tests/fixtures/v1_corpus_driver_stub.py` | test fixture script | new file |

## Flagged assumptions (spec-less probe fallback, §A/§C)

No SPEC exists for this phase. The deterministic edge probe surfaced 14 rows; this plan authors
the MODAL-02 and API-01 rows into `must_haves.truths` above (four rows: MODAL-02/boundary,
MODAL-02/precision, API-01/concurrency, plus the admission-record invariants). No row assigned to
this plan is dropped.

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end — one document ingested through an admitted opaque subprocess node</name>
  <reversibility rating="costly">Hosting the `subprocess` placement changes a Phase-1 refusal contract (`host()` refuses everything but `in-process` today) that Phase 6's HippoRAG index side and every later opaque node inherit. Undoing it means reverting the gate and every opaque Part that depends on it, across modules.</reversibility>
  <precondition>`v1/.venv/bin/python` exists (built by Phase 3's plan 03-02). If absent, the real-interpreter half of this task's test skips and the stub-driver half still runs; the executor must not silently treat a missing interpreter as a pass for the real half.</precondition>
  <files>databasise/parts/admission.py, databasise/parts/schema.py, databasise/validator/execution_mode.py, databasise/runner/scheduler.py, databasise/foreign/__init__.py, databasise/foreign/v1_corpus_driver_script.py, databasise/foreign/v1_corpus_adapter.py, databasise/parts_core/lightrag/full_ingest.py, databasise/parts_core/lightrag/__init__.py, databasise/parts_core/declared_only.py, databasise/wirings/lightrag/corpus-ingest.json, databasise/seam/corpus.py, databasise/seam/engine.py, databasise/seam/refusals.py, databasise/tools/check_import_boundary.py, databasise/pyproject.toml, databasise/tests/fixtures/v1_corpus_driver_stub.py, databasise/tests/parts_core/lightrag/test_full_ingest.py</files>
  <read_first>
    - `databasise/parity/v1_driver_script.py` — the query-side leaf script this plan's corpus driver mirrors verbatim in protocol, error handling, imports and boundary framing
    - `databasise/parity/v1_arm.py` — `run_v1_arm`, `_load_env_file`, `MissingV1InterpreterError`, `V1ArmSubprocessError`, `DEFAULT_V1_*` constants: the exact launch shape `run_corpus_op` copies
    - `databasise/validator/execution_mode.py` — `derive_execution_mode`, `host`, `_UNIMPLEMENTED_PLACEMENTS`, `UnimplementedPlacementError`
    - `databasise/runner/scheduler.py` — the `host(execution_mode)` call site and `NodePlacementRefusedError`
    - `databasise/parts/schema.py` — `Part`, `NodeContext`, `Effect`, `ArtifactScope`
    - `databasise/parts_core/declared_only.py` — `LIGHTRAG_FULL_INGEST_PART`, the stub being given a body
    - `databasise/seam/engine.py` — `_build_stores`, `_execute`, `_inject_token_allowance`, the store lifecycle and `run_wiring` call shape
    - `databasise/tools/check_import_boundary.py` — `_SUBPROCESS_ENTRY_POINT_EXCLUSIONS` and the module docstring's subprocess-entry-point rationale
    - `databasise/wirings/lightrag/base.json` — the wiring document shape (`wiring_id`, `wiring_version`, `nodes`, `recipe`, `harnesses`, `provides`)
    - `databasise/validator/parse.py` — `parse_wiring`, `ParsedWiring`
    - `v1/scripts/run_parity_ingest.py` — the v1-side `initialize_rag` construction and `_extra_body` env handling the corpus driver reuses
    - `v1/lightrag/pipeline.py` lines 232-262 — `apipeline_enqueue_documents` signature (`input`, `ids`, `file_paths`, `track_id`, `docs_format`)
    - `v1/lightrag/lightrag.py` lines 1428-1470 — `ainsert` signature and its returned `track_id`
    - `docs/system-model/CONTRACT.md` §8 (the eleven conditions, lines 368-390) and §17
  </read_first>
  <action>
Wire one document end-to-end through every layer this phase touches, one path only, no second call site.

**A. `databasise/parts/admission.py` (new).** Frozen dataclasses `ConditionVerdict(condition: int, verdict: str, evidence: str)` and `AdmissionRecord` with fields `part_name_at_version: str`, `entry_path: str`, `storage: str`, `wall_clock_ceiling_seconds: float`, `wall_clock_ceiling_basis: str`, `feed_tier: str`, `ttl_days: int`, `network_namespace: str`, `environment_hash: str`, `manifest_source: str`, `verdicts: tuple[ConditionVerdict, ...]`. Define module constants `MANIFEST_SOURCE_CODE_INSPECTED = "code-inspected"` and `ADMISSION_CONDITION_IDS = tuple(range(1, 12))`. Define `validate_admission(part: Part, record: AdmissionRecord) -> None` raising, by name, `AdmissionConditionMissingError` (the verdict set is not exactly conditions 1-11 with no duplicate, or any `evidence` is blank), `SelfReportedManifestError` (`manifest_source` is not `MANIFEST_SOURCE_CODE_INSPECTED` — §8 condition 1 forbids trusting the engine's self-report), `MissingWallClockCeilingError` (`wall_clock_ceiling_seconds` is absent or not greater than zero — §8 condition 4), and `ForbiddenSharedScopeError` (`part.structural_depth` is the opaque value and `part.artifact_scope` is the shared value — §8 condition 5 restating §3). Every refusal stores its offending value on a named attribute and names only what was wrong, matching `databasise/seam/refusals.py`'s house style. Do not subclass `SeamRefusalError` here — these are wire-time machine refusals, not consumer-facing seam refusals.

**B. `databasise/parts/schema.py`.** Add `admission: "AdmissionRecord | None" = None` as the last field on the `Part` dataclass, with a docstring sentence stating it is required for an executable opaque part and enforced at registration. Import `AdmissionRecord` under `typing.TYPE_CHECKING` so `parts/admission.py` may import `Part` without a cycle.

**C. `databasise/validator/execution_mode.py`.** Narrow `_UNIMPLEMENTED_PLACEMENTS` to the `confined-unit` and `long-lived-service` members. Change `host` to `host(placement: str, *, wall_clock_ceiling_seconds: float | None = None) -> None`; return for `in-process` unchanged; add a `subprocess` branch that returns when `wall_clock_ceiling_seconds` is a positive number and otherwise raises `MissingWallClockCeilingError` imported from `databasise.parts.admission`. Rewrite the module docstring's D-08 paragraph to state what `subprocess` hosting now means: the containment is the node body's own launch of an OS process under a foreign interpreter, and the machine's contribution is the declared wall-clock ceiling §8 condition 4 requires — the ceiling is the gate, so a subprocess node with no ceiling is refused rather than hosted.

**D. `databasise/runner/scheduler.py`.** At the existing `host(execution_mode)` call site, pass `wall_clock_ceiling_seconds` read from the resolved `Part`'s `admission` record (`None` when the part carries no record). Leave `NodePlacementRefusedError` wrapping untouched so a ceiling-less subprocess node still surfaces as a placement refusal on that node's trace.

**E. `databasise/foreign/` (new package).** `__init__.py` re-exports `run_corpus_op` and the adapter's refusals, with a module docstring naming this package as CONTRACT §17's foreign-part adapter layer — process lifecycle, the RPC channel, health — and stating why it is not `databasise/parity/` (parity is Phase 3's comparison harness; this layer is shared by ingest, delete and status, and gains a second engine in plan 05-06).

**F. `databasise/foreign/v1_corpus_driver_script.py` (new leaf).** Copy `databasise/parity/v1_driver_script.py`'s module-docstring framing verbatim in structure — leaf status, why it lives under `databasise/` but runs under v1's interpreter, the stdin/stdout protocol, and the "any exception prints a traceback to stderr and exits 1, nothing partial on stdout" rule. Protocol: read one JSON object `{"op": str, "working_dir": str, ...}` from stdin. This task implements only `op == "ingest"`, whose payload carries `{"documents": [{"id": str, "text": str}], "track_id": str | null}`; any other `op` value exits 1 with a named message rather than a silent no-op (plans 05-03 and 05-04 add the delete and status branches). Build v1's `LightRAG` facade with `_build_rag(working_dir)` copied from `v1/scripts/run_parity_ingest.py`'s `initialize_rag` construction shape (same env-var-driven `kv_storage`/`graph_storage`/`vector_storage`/`doc_status_storage`/`enable_llm_cache` reads, same `EmbeddingFunc`/`openai_embed.func` unwrap, same `_extra_body("OPENAI_LLM_EXTRA_BODY")` handling), then `await rag.initialize_storages()`. Run the ingest by calling `apipeline_enqueue_documents(input=[...texts], ids=[...ids], track_id=...)` followed by `apipeline_process_enqueue_documents()` — not `ainsert`, because the two-call form is the path that accepts an explicit `track_id` the seam hands back to the caller as its job id. Write one JSON object to stdout: `{"track_id": str, "enqueued": int, "usage": {...} | null}`. `usage` carries whatever token accounting v1 exposes for the run, or `null` when v1 reports none — never a fabricated zero.

**G. `databasise/foreign/v1_corpus_adapter.py` (new).** `run_corpus_op(op: str, payload: dict[str, Any], *, timeout: float, interpreter: Path | None = None, driver_script: Path | None = None, env_path: Path | None = None, working_dir: Path | None = None) -> dict[str, Any]`. Copy `databasise/parity/v1_arm.py`'s `run_v1_arm` launch shape exactly: resolve the interpreter and raise `MissingV1InterpreterError` when absent, build `env` from `dict(os.environ)` updated with `_load_env_file(env_path or DEFAULT_V1_ENV_PARITY)`, `env.setdefault("ENABLE_LLM_CACHE", "false")`, `subprocess.run([str(interpreter), str(driver_script)], input=json.dumps(job), capture_output=True, text=True, cwd=str(_REPO_ROOT / "v1"), env=env, timeout=timeout)`, non-zero return code raising `CorpusOpSubprocessError(returncode, stderr)`, then `json.loads(proc.stdout)`. `timeout` is required, not optional, and a `subprocess.TimeoutExpired` is caught and re-raised as `CorpusOpTimeoutError(op, timeout)` carrying both on named attributes — the §8 condition 4 refusal. All caller-controlled data travels in the JSON job on stdin; nothing caller-controlled is ever interpolated into argv or into a shell string. Duplicate `_load_env_file` from `v1_arm.py` rather than importing its private helper, following that module's own stated duplication rule.

**H. `databasise/parts_core/lightrag/full_ingest.py` (new).** `INGEST_WALL_CLOCK_CEILING_SECONDS = 900.0` with a comment stating the basis (one document at a time through v1's own extraction pipeline over the 20-document HotpotQA fixture; the machine declares it, v1 never self-reports one — §8 condition 4). `LIGHTRAG_FULL_INGEST_ADMISSION: AdmissionRecord` with `part_name_at_version="lightrag/full-ingest@0.1.0"`, `entry_path="in-process opaque core, hosted as a subprocess under v1's pinned interpreter"`, `storage="machine"`, the ceiling and its basis, `feed_tier="document"` (the node receives whole documents, never machine-produced chunks — §8 condition 6), `ttl_days=90`, `network_namespace="denied-by-construction: the machine's own OpenAI-compatible base_url and key are injected into the subprocess env; the subprocess configures no independent outbound endpoint"`, `environment_hash` computed at import time as `"sha256:" + sha256(canonicalise({...})).hexdigest()` over a dict naming v1's own closure inputs (the resolved interpreter path, the text of `v1/uv.lock`, and the text of `v1/.python-version`) — §8 condition 2's whole-resolved-runtime-closure rule, `manifest_source=MANIFEST_SOURCE_CODE_INSPECTED`, and eleven `ConditionVerdict` entries, one per §8 condition, each `evidence` string citing the concrete source it rests on. Write verdicts 1-11 from CONTRACT §8's own text plus what this plan actually builds; conditions 7, 8 and the counting half of 9 are machine-side obligations and their verdicts say so plainly rather than being rounded up to a clean yes. `async def full_ingest_body(ctx: NodeContext) -> dict[str, Any]` reads `documents` and `track_id` from `ctx.config`, calls `await asyncio.to_thread(run_corpus_op, "ingest", payload, timeout=INGEST_WALL_CLOCK_CEILING_SECONDS, working_dir=...)` (the `asyncio.to_thread` wrap is required — `run_corpus_op` is blocking, per `v1_arm.py`'s own docstring note), and returns `{"track_id": ..., "enqueued": ..., "usage": ...}`.

**I. `databasise/parts_core/declared_only.py`.** Give `LIGHTRAG_FULL_INGEST_PART` a real `body=full_ingest_body` and `admission=LIGHTRAG_FULL_INGEST_ADMISSION`, importing both from `databasise.parts_core.lightrag.full_ingest`. Leave `name_at_version`, `kind`, `structural_depth`, `effects` and `artifact_scope` exactly as declared today — `databasise/evidence/wirings/w3-lightrag-half-decomposed.json` and the Falsifier-2 validator tests resolve against those values and must keep passing unchanged. Update the module docstring to record that this entry is no longer declaration-only. `CODEBASE_MEMORY_MCP_PART` is untouched by this plan.

**J. `databasise/wirings/lightrag/corpus-ingest.json` (new).** A one-node wiring: `wiring_id` `lightrag-corpus-ingest`, `wiring_version` `"1"`, one node keyed `full-ingest` with `component` `lightrag/full-ingest@0.1.0`, `kind` `opaque`, the same four `effects` the Part declares, empty `deps`, `harnesses` empty, `provides` naming the single node. Do not declare `effective_depth`, `execution_mode`, `structural_depth`, `artifact_scope`, `depth` or `blast_radius` — `parse_wiring` refuses a self-declared derived field by name.

**K. `databasise/seam/corpus.py` (new).** `MAX_DOCUMENT_BYTES = 25 * 1024 * 1024`. `IngestDocument(BaseModel)` frozen with `extra="forbid"`: `text: str | None`, `document_id: str | None`, `file_name: str | None` (advisory metadata only, never used to build a path). `IngestJob(BaseModel)` frozen with `extra="forbid"`: `job_id: str`, `enqueued: int`. Task 3 extends both.

**L. `databasise/seam/refusals.py`.** Add `ForeignEngineRefusalError(SeamRefusalError)` carrying `operation` and `cause` on named attributes — the seam-facing wrapper for a `CorpusOpSubprocessError`/`CorpusOpTimeoutError` so `rest.py`'s existing generic `SeamRefusalError` handler maps it without a new endpoint-level branch. The message must not enumerate this machine's own wirings, arms or node ids, per this module's own docstring rule.

**M. `databasise/seam/engine.py`.** Add module constants `_INGEST_WIRING_PATH` (resolved from `databasise/wirings/lightrag/corpus-ingest.json` via `importlib.resources` or a `Path(__file__)`-relative walk consistent with how `wirings/resolve.py` already loads wiring documents) and `_INGEST_NODE_ID = "full-ingest"`. Add `async def ingest(self, document: IngestDocument) -> IngestJob`: load the ingest wiring, stamp `documents` and a freshly minted `track_id` (a `uuid4` hex string) onto the `full-ingest` node's `config`, apply `_inject_token_allowance`, `parse_wiring`, build stores with `_build_stores` and finalize them in a `finally` block exactly as `_execute` does, `await _scheduler.run_wiring(...)` with the same `determinism_setting`/`concurrency_setting`/`clients` arguments `_execute` passes, then read the node's own result out of `scheduled["results"][_INGEST_NODE_ID]` and return `IngestJob(job_id=..., enqueued=...)`. Catch `CorpusOpSubprocessError`/`CorpusOpTimeoutError` surfacing from the run and re-raise as `ForeignEngineRefusalError`. Do not call `_execute`, do not build a `ResponseEnvelope`, and do not add a field to `databasise/seam/envelope.py` — ingest is a distinct §18 operation, not a query, and §18.2's field set is closed. Extend the module docstring with a short paragraph naming ingest as the fourth §18 operation this seam exposes and why it is an operation rather than a selector (no selector can express "put this document into the corpus").

**N. `databasise/tools/check_import_boundary.py`.** Add `"v1_corpus_driver_script.py"` to `_SUBPROCESS_ENTRY_POINT_EXCLUSIONS` and extend the module docstring's subprocess-entry-point paragraph to name it alongside `v1_driver_script.py`, stating which operations it serves. This is the one deliberate hole this plan opens in an enforced boundary and it stays a one-line, auditable addition.

**O. `databasise/pyproject.toml`.** Add `"databasise.foreign"` to `[tool.setuptools] packages`.

**P. `databasise/tests/fixtures/v1_corpus_driver_stub.py` (new).** A standard-library-only script that reads one JSON job from stdin and writes one JSON result to stdout, honouring the same protocol the real driver does: for `op == "ingest"` echo back `{"track_id": <job's track_id>, "enqueued": len(documents), "usage": null}`; sleep for the job's optional `_stub_sleep_seconds` before replying so a timeout can be provoked; exit 1 with a traceback on stderr for an unknown `op`. It imports nothing from `lightrag` and nothing from `databasise`.

**Q. `databasise/tests/parts_core/lightrag/test_full_ingest.py` (new).** The tracer test, asserting real values rather than shapes (this codebase's own house rule, 04-CONTEXT.md). Build a `Databasise` against `tmp_path` with a registry whose `lightrag/full-ingest@0.1.0` entry is the production Part with `body` bound to a `full_ingest_body` variant pointed at the stub driver via the adapter's `driver_script`/`interpreter` override points, and assert: (1) `await engine.ingest(IngestDocument(text=...))` returns an `IngestJob` whose `job_id` equals the `track_id` the stub echoed and whose `enqueued` is 1; (2) `derive_execution_mode(LIGHTRAG_FULL_INGEST_PART.effects, LIGHTRAG_FULL_INGEST_PART.kind)` returns the subprocess placement and `host` accepts it with the declared ceiling; (3) `host` with the subprocess placement and no ceiling raises `MissingWallClockCeilingError`; (4) a stub job configured to outlast a deliberately tiny ceiling raises `ForeignEngineRefusalError` out of `ingest()` whose `cause` is a `CorpusOpTimeoutError` carrying that ceiling; (5) a second, separately-minted ingest returns a different `job_id` than the first. Add a real-interpreter test guarded by `pytest.mark.skipif` on the absence of `v1/.venv/bin/python` that launches the real driver script with an empty document list and asserts the subprocess exits 0 and returns a JSON object with a `track_id` key — proving the real leaf script parses, builds v1's facade and speaks the protocol, without paying for a full LLM extraction.
  </action>
  <verify>
    <automated>cd databasise &amp;&amp; uv run pytest -q tests/parts_core/lightrag/test_full_ingest.py -x</automated>
    <fails_when>non-zero exit, or the summary line reports "no tests ran", or any test is reported as skipped that is not the `v1/.venv`-guarded real-interpreter test</fails_when>
    <automated>cd databasise &amp;&amp; uv run python -m databasise.tools.check_import_boundary</automated>
    <fails_when>non-zero exit, or any line of stdout naming a file under `databasise/` with an `[import-boundary]` violation</fails_when>
    <automated>cd databasise &amp;&amp; uv run pytest -q tests/validator/ tests/parts/ -x</automated>
    <fails_when>non-zero exit — the Falsifier-2 validator suite and the parts-registry suite must still pass unchanged after the `host()` signature change and the `Part.admission` field addition</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n 'wall_clock_ceiling_seconds' databasise/validator/execution_mode.py` returns at least one line inside the `host` signature.
    - `grep -c 'confined-unit' databasise/validator/execution_mode.py` is non-zero and `_UNIMPLEMENTED_PLACEMENTS` no longer contains the subprocess member.
    - `cd databasise && uv run python -c "from databasise.parts_core.declared_only import LIGHTRAG_FULL_INGEST_PART as p; assert p.body is not None; assert p.admission is not None; assert len(p.admission.verdicts) == 11; assert sorted(v.condition for v in p.admission.verdicts) == list(range(1,12)); assert all(v.evidence.strip() for v in p.admission.verdicts); print('ok')"` prints `ok`.
    - `cd databasise && uv run python -c "from databasise.parts_core.lightrag.full_ingest import LIGHTRAG_FULL_INGEST_ADMISSION as a; assert a.wall_clock_ceiling_seconds > 0; assert a.manifest_source == 'code-inspected'; assert a.environment_hash.startswith('sha256:'); print('ok')"` prints `ok`.
    - `git ls-files databasise/foreign/v1_corpus_driver_script.py` is non-empty after the commit, and `grep -c 'v1_corpus_driver_script' databasise/tools/check_import_boundary.py` is at least 1.
    - `cd databasise && uv run python -c "import json,pathlib; d=json.loads(pathlib.Path('wirings/lightrag/corpus-ingest.json').read_text()); assert list(d['nodes'])==['full-ingest']; assert d['provides']==['full-ingest']; print('ok')"` prints `ok`.
    - `databasise/seam/envelope.py` is unchanged by this task: `git show --name-only --pretty=format: HEAD` after the task commit lists no `databasise/seam/envelope.py` line.
  </acceptance_criteria>
  <done>A caller in a test process ingests one document through `Databasise.ingest()`; the opaque node is dispatched by the real scheduler at the `subprocess` placement, the adapter launches the driver under a foreign interpreter, and an `IngestJob` carrying v1's own track id comes back. The import boundary check still exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The machine refuses an unadmitted or dishonest opaque part</name>
  <files>databasise/parts/registry.py, databasise/parts/admission.py, databasise/tests/parts/test_admission.py</files>
  <read_first>
    - `databasise/parts/admission.py` — as written by Task 1
    - `databasise/parts/registry.py` — `PartRegistry.register`, `DuplicatePartError`, `DeclarationOnlyPartError`, `default_registry`
    - `databasise/seam/selectors.py` — `_is_default_eligible`, `_resolve_default` (§8 condition 7's existing implementation)
    - `databasise/registry_artifact/index.py` — `ArtifactRegistry.register`/`discover` and the scope CHECK constraint (§8 conditions 5 and 10's existing implementation)
    - `databasise/seam/query.py` — `QueryObject`'s five frozen members and `check_consumable` (§8 condition 11's existing surface)
    - `docs/system-model/CONTRACT.md` §8 conditions 5, 7, 10, 11
  </read_first>
  <behavior>
    - Registering an executable part with `kind` opaque and `admission=None` raises `UnadmittedOpaquePartError` naming the part; the same part with a valid record registers cleanly.
    - Registering a declaration-only opaque part (`body is None`, no admission) still succeeds — `CODEBASE_MEMORY_MCP_PART` must keep loading in `default_registry()` until plan 05-06 admits it.
    - `validate_admission` raises `ForbiddenSharedScopeError` for an opaque part declaring the shared artifact scope and returns for the quarantined scope.
    - `validate_admission` raises `SelfReportedManifestError` when `manifest_source` is any value other than the code-inspected constant.
    - `validate_admission` raises `AdmissionConditionMissingError` for a ten-verdict record, for a duplicated condition id, and for a record whose eleventh verdict carries a blank `evidence`.
    - `_is_default_eligible` returns False for the corpus-ingest wiring — the opaque node is excluded from the default selector (§8 condition 7) — while the naive arm stays eligible.
    - `QueryObject` has no `as_of` member and rejects one at construction, and no registered part declares a temporal capability (§8 condition 11 satisfied by construction).
  </behavior>
  <action>
Add the enforcement half of admission, so a record that lies is refused rather than filed.

**A. `databasise/parts/registry.py`.** Define `UnadmittedOpaquePartError(RuntimeError)` carrying `name_at_version`, with a message stating that an executable opaque part requires a §8 admission record and naming `databasise.parts.admission.AdmissionRecord` as where to supply it. In `PartRegistry.register`, after the duplicate check, refuse when `part.body is not None` and `part.structural_depth` is the opaque value and `part.admission is None`; when a record is present, call `validate_admission(part, part.admission)` so an invalid record is refused at registration rather than at run time. Extend the module docstring with a sentence naming this as the point where §8's "an opaque node's self-report is not trusted at face value" becomes machine-enforced.

**B. `databasise/parts/admission.py`.** Add `cross_check_conditions(part, record, *, registry, resolved_wiring)` — the three machine-side conditions the record alone cannot prove. Condition 7: assert `databasise.seam.selectors._is_default_eligible(resolved_wiring, registry)` is False for a wiring whose provides node resolves to this opaque part, raising `AdmissionConditionMissingError` naming condition 7 when it is not. Condition 10: assert the part's `artifact_scope` is not the shared value, so nothing it registers can be enumerated as an SA-1-shareable artifact by `ArtifactRegistry.discover`. Condition 11: assert `"as_of" not in QueryObject.model_fields` and that no registered part's declared effects include a temporal capability, raising when either fails. Import `_is_default_eligible` and `QueryObject` lazily inside the function so `parts/admission.py` does not take a module-scope dependency on `databasise.seam` (which would invert the existing one-way parts→seam direction).

**C. `databasise/tests/parts/test_admission.py` (new).** One test per `<behavior>` bullet. Each refusal test asserts on the raised exception's own named attribute, not on the message text. The condition-7 test resolves the real `corpus-ingest.json` wiring and the real naive arm through the same `_is_default_eligible` call, so it fails if a later change makes the opaque node default-eligible. The condition-11 test asserts `QueryObject(as_of="2026-01-01")` raises `pydantic.ValidationError`, proving the refusal is structural rather than a runtime branch.
  </action>
  <verify>
    <automated>cd databasise &amp;&amp; uv run pytest -q tests/parts/test_admission.py -x</automated>
    <fails_when>non-zero exit, or the summary line reports "no tests ran"</fails_when>
    <automated>cd databasise &amp;&amp; uv run pytest -q</automated>
    <fails_when>non-zero exit — `default_registry()` is constructed by most of the suite, so an over-strict registration refusal breaks it here rather than silently later</fails_when>
  </verify>
  <acceptance_criteria>
    - `cd databasise && uv run python -c "from databasise.parts.registry import default_registry; r=default_registry(); assert 'codebase-memory-mcp@0.1.0' in r.keys(); assert 'lightrag/full-ingest@0.1.0' in r.keys(); print('ok')"` prints `ok`.
    - `cd databasise && uv run python -c "
from databasise.parts.registry import PartRegistry, UnadmittedOpaquePartError
from databasise.parts.schema import Part
p = Part(name_at_version='t/x@1.0.0', kind='opaque', structural_depth='opaque', effects=[], upstream_ref=None, body=lambda ctx: None)
try:
    PartRegistry(seed_tracer_parts=False).register(p)
except UnadmittedOpaquePartError as e:
    assert e.name_at_version == 't/x@1.0.0'; print('ok')
"` prints `ok`.
    - `grep -c 'as_of' databasise/parts/admission.py` is at least 1, and the condition-11 check reads `QueryObject.model_fields` rather than a hardcoded member list.
    - `cd databasise && uv run pytest -q tests/seam/test_selectors.py -x` exits 0 — the default-selector behaviour Phase 4 proved is unchanged by the cross-check.
  </acceptance_criteria>
  <done>An executable opaque part cannot enter the registry without a valid eleven-verdict §8 record; a record claiming the shared scope, a self-reported manifest source, or an incomplete verdict set is refused by a named exception, and the whole existing suite still passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Raw upload and structured payload, both refused safely at the seam boundary</name>
  <files>databasise/seam/corpus.py, databasise/seam/refusals.py, databasise/seam/engine.py, databasise/foreign/v1_corpus_driver_script.py, databasise/tests/parts_core/lightrag/test_full_ingest.py</files>
  <read_first>
    - `databasise/seam/corpus.py` — `IngestDocument`, `IngestJob`, `MAX_DOCUMENT_BYTES` as written by Task 1
    - `databasise/seam/engine.py` — `Databasise.ingest` as written by Task 1
    - `databasise/namespaces.py` — `_NAMESPACE_TOKEN_RE` and `namespace_dir`'s bare-token refusal, the directly reusable server-side-naming precedent
    - `databasise/stores/blob.py` — the existing blob store's own on-disk path construction
    - `databasise/seam/refusals.py` — the refusal house style and the no-machine-identity-in-messages rule
    - `v1/lightrag/pipeline.py` lines 232-262 — `docs_format` and `file_paths`, the two-shape distinction v1 itself draws
    - `databasise/foreign/v1_corpus_driver_script.py` — the ingest branch written by Task 1
  </read_first>
  <behavior>
    - `IngestDocument(text="...")` validates; `IngestDocument(raw=b"...", content_type="application/pdf")` validates; a document with both `text` and `raw` set raises `AmbiguousIngestPayloadError`; a document with neither raises the same refusal naming what was missing.
    - A `raw` payload larger than `MAX_DOCUMENT_BYTES` raises `OversizedDocumentError` carrying the actual byte count and the cap, before any bytes are written to disk and before the subprocess is launched.
    - A `file_name` containing a path separator or a parent-directory token never appears in the generated on-disk path; the on-disk name is derived server-side from the minted document id.
    - A raw-payload ingest reaches the driver with a `file_paths` entry and the deferred-parse document format; a text-payload ingest reaches it with inline text and the raw document format. Both return an `IngestJob`.
  </behavior>
  <action>
Give ingest its two input shapes without letting either become a filesystem or memory hazard.

**A. `databasise/seam/corpus.py`.** Extend `IngestDocument` with `raw: bytes | None = None` and `content_type: str | None = None`, and add a Pydantic `model_validator(mode="after")` that raises `AmbiguousIngestPayloadError` when both `text` and `raw` are set or when neither is, and `OversizedDocumentError(actual_bytes, MAX_DOCUMENT_BYTES)` when `raw` exceeds the cap or when `text`'s UTF-8 encoding does. Add `def generated_on_disk_name(document_id: str) -> str` returning a name built only from the server-minted `document_id` and a fixed suffix, refusing any `document_id` that does not match the same bare-token discipline `databasise/namespaces.py`'s `_NAMESPACE_TOKEN_RE` enforces. `file_name` and `content_type` stay advisory metadata carried into the job payload for citation; neither is ever a path component.

**B. `databasise/seam/refusals.py`.** Add `AmbiguousIngestPayloadError(SeamRefusalError)` carrying `set_members: list[str]` and `OversizedDocumentError(SeamRefusalError)` carrying `actual_bytes: int` and `limit_bytes: int`.

**C. `databasise/seam/engine.py`.** In `ingest()`, mint the document id server-side as a `uuid4` hex string when `document.document_id` is absent, and validate a caller-supplied one through `generated_on_disk_name`'s own token rule so a caller-supplied id can never widen the path. For a `raw` payload, write the bytes to `self.store_root / "corpus-inbox" / generated_on_disk_name(document_id)` using the machine's own directory construction, and put that path plus the deferred-parse document format on the node config; for a `text` payload put the inline text and the raw document format on the node config. Both shapes then take the identical `run_wiring` path Task 1 built — one operation, two input shapes, never two execution paths.

**D. `databasise/foreign/v1_corpus_driver_script.py`.** Extend the ingest branch to read an optional `file_paths` list and `docs_format` string from the job and pass them through to `apipeline_enqueue_documents` unchanged, so the deferred-parse shape reaches v1's own parse worker rather than being flattened to inline text on the machine side.

**E. `databasise/tests/parts_core/lightrag/test_full_ingest.py`.** Add one test per `<behavior>` bullet, driving the stub driver. The path-safety test asserts on the resolved `Path` the engine actually built — that its parent is exactly the corpus-inbox directory under `store_root` and that its name equals `generated_on_disk_name(document_id)` — never on the absence of a substring in a message.
  </action>
  <verify>
    <automated>cd databasise &amp;&amp; uv run pytest -q tests/parts_core/lightrag/test_full_ingest.py -x</automated>
    <fails_when>non-zero exit, or the summary line reports fewer tests collected than before this task ran (the four new behaviour tests must be added, not substituted)</fails_when>
    <automated>cd databasise &amp;&amp; uv run pytest -q</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `cd databasise && uv run python -c "
from databasise.seam.corpus import IngestDocument
from databasise.seam.refusals import AmbiguousIngestPayloadError, OversizedDocumentError
for kwargs in ({'text':'a','raw':b'b'}, {}):
    try: IngestDocument(**kwargs); raise SystemExit('not refused')
    except Exception as e: assert 'AmbiguousIngestPayload' in repr(e), repr(e)
print('ok')"` prints `ok`.
    - `cd databasise && uv run python -c "
from databasise.seam.corpus import generated_on_disk_name
import pathlib
n = generated_on_disk_name('deadbeef')
assert pathlib.Path(n).name == n
for bad in ('../x','a/b','a\\\\b','.'):
    try: generated_on_disk_name(bad); raise SystemExit('accepted '+bad)
    except ValueError: pass
print('ok')"` prints `ok`.
    - An `OversizedDocumentError` raised for an over-cap payload carries `actual_bytes` and `limit_bytes` as integers, asserted in the test rather than inferred from the message.
    - `databasise/seam/envelope.py` remains unchanged across all three tasks of this plan.
  </acceptance_criteria>
  <done>Both ingest input shapes work end to end against the stub driver; an ambiguous, empty, or oversized payload is refused by name before any subprocess launch; and no caller-supplied string ever reaches a filesystem join.</done>
</task>

</tasks>

<verification>
- `cd databasise && uv run pytest -q` exits 0.
- `cd databasise && uv run python -m databasise.tools.check_import_boundary` exits 0 and prints nothing.
- `git diff --stat` on `databasise/tools/check_import_boundary.py` shows exactly one added filename in `_SUBPROCESS_ENTRY_POINT_EXCLUSIONS` plus docstring prose — this is the phase's one deliberate hole in an enforced boundary and it must stay reviewable at a glance.
- <human-check>Read the added `_SUBPROCESS_ENTRY_POINT_EXCLUSIONS` entry and the docstring paragraph naming it. Confirm the new driver script genuinely runs only under v1's interpreter and is imported by nothing under `databasise/`.</human-check>
</verification>

<success_criteria>
- `lightrag/full-ingest@0.1.0` is an executable, admitted opaque part: real body, eleven-verdict §8 record, `quarantined` artifact scope, externally-declared wall-clock ceiling.
- The machine hosts the `subprocess` placement and refuses it without a declared ceiling.
- `Databasise.ingest()` accepts both a structured text payload and a raw byte payload and returns a job handle carrying v1's own track id.
- The Phase 2 Falsifier-2 validator suite and the Phase 3/4 suites pass unchanged.
</success_criteria>

<output>
Create `.planning/phases/05-opaque-side-admission/05-01-SUMMARY.md` when done.
</output>
