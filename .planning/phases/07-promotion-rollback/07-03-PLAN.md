---
phase: 07-promotion-rollback
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - databasise/seam/rest.py
  - databasise/mcp/tools.py
  - databasise/mcp/server.py
  - databasise/mcp/__init__.py
  - databasise/evidence/PROMOTION-LEDGER-EVIDENCE.md
  - databasise/tests/seam/test_rest_transport.py
  - databasise/tests/seam/test_dual_transport.py
  - databasise/tests/mcp/test_tool_growth_invariant.py
  - databasise/tests/evidence/test_promotion_ledger_record.py
  - .planning/phases/05-opaque-side-admission/COVERAGE.md
autonomous: true
requirements: [API-09]

estimate:
  tokens: 40000
  raw_tokens: 80000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "A caller reaches promote, rollback and retire identically in-process, over REST, and over MCP — the same alias, the same semver, the same trace ids and the same change_origin produce the same ledger record and the same response whichever transport is used."
    - "Every new refusal Phase 7 added maps to a non-2xx REST response and to an MCP ToolError without any per-route or per-tool exception handling — the existing generic SeamRefusalError handlers pick them up, and the transport-level refusal test covers each new subclass by a registered factory."
    - "The MCP tool roster grew by exactly three names for three genuinely new §18 operations, not by one name per modality — §18.5's per-operation growth rule, still enforced by the same invariant test."
    - "No promote, rollback or retire response returned by any transport contains a wiring name, arm name, wiring id, node id, instance hash, or mutation class — §18.2's closed set for this operation is alias, minted semver, record kind, provenance and generation ordinal."
    - "The §18.5 operations record in COVERAGE.md and the code agree: every tool name has a coverage row and every MCP-transported row maps to a known tool, with no row describing an operation the code does not have."
    - "`databasise/evidence/PROMOTION-LEDGER-EVIDENCE.md` states, from the real committed code and tests rather than from the plan text, which of ROADMAP Phase 7's success criteria 1-3 hold and by which test, and names anything that does not."
  artifacts:
    - databasise/evidence/PROMOTION-LEDGER-EVIDENCE.md
    - databasise/tests/evidence/test_promotion_ledger_record.py
  key_links:
    - "POST /promote, /rollback, /retire -> the identical Databasise.promote/rollback/retire methods, with no logic in the route bodies"
    - "the promote/rollback/retire MCP tools -> the same three engine methods through the existing _refusal_mapped wrapper"
    - "every new SeamRefusalError subclass -> rest.py's single add_exception_handler and mcp/server.py's single _refusal_mapped, with a matching _REFUSAL_FACTORIES entry so the existing parametrized transport test covers it"
  prohibitions:
    - statement: "A transport MUST NOT add a per-route or per-tool try/except for a Phase 7 refusal — both transports already map the SeamRefusalError base class generically, and a second handler is where one refusal quietly stops being mapped."
      status: active
      verification: unverified
    - statement: "rollback and retire MUST NOT be folded into one scope-dispatched endpoint or tool — Phase 4 D-03 requires one entry per §18 operation, and the status/resolve scope-dispatch precedent is explicitly not the pattern for these three verbs."
      status: active
      verification: unverified
    - statement: "A transport adapter MUST NOT reshape, enrich, or add a field to the promotion response — §18.2's closed set is the same on every transport, and a transport-only field is a consumer-visible difference the parity test exists to catch."
      status: active
      verification: unverified
    - statement: "The evidence record MUST NOT claim a success criterion holds without naming the committed test that shows it — this project's evidence documents carry code-verified tags, and an unsourced claim is the vacuous pass HARD-01 was filed against."
      status: active
      verification: unverified
---

<objective>
Make API-09's operator path reachable over REST and MCP in exact parity with the in-process call,
and record what Phase 7 actually delivered against ROADMAP success criteria 1-3.

Purpose: the three verbs exist and are tested in-process after 07-01 and 07-02. API-09 is a claim
about what a *caller* can do, and this project has proven three-transport parity by conformance test
since Phase 4 D-17 and Phase 5's MCP parity work. This plan is thin adapters plus the tests that
make the parity claim real — no logic lands in a route body or a tool body.

Output: three REST routes, three MCP tools, the extended transport and roster tests, the §18.5
coverage record brought back into agreement with the code, and the phase's evidence document.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-promotion-rollback/07-CONTEXT.md
@.planning/phases/07-promotion-rollback/07-01-SUMMARY.md
@.planning/phases/07-promotion-rollback/07-02-SUMMARY.md
@databasise/seam/rest.py
@databasise/mcp/server.py
@databasise/mcp/tools.py
</context>

<artifacts_this_phase_produces>
See `07-01-PLAN.md`'s "Artifacts this phase produces" section for the full Phase 7 symbol list. The
subset this plan lands:

REST routes `POST /promote`, `POST /rollback`, `POST /retire`; request models `PromoteRequest`,
`RollbackRequest`, `RetireRequest`; MCP tools `promote`, `rollback`, `retire`; arg models
`PromoteToolArgs`, `RollbackToolArgs`, `RetireToolArgs`; `TOOL_NAMES` grows from six entries to
nine; `databasise/evidence/PROMOTION-LEDGER-EVIDENCE.md`;
`databasise/tests/evidence/test_promotion_ledger_record.py`; three new rows in
`.planning/phases/05-opaque-side-admission/COVERAGE.md`'s §18.5 operations table and three new
entries in that test module's `_OPERATION_TO_TOOLS` mapping.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Three REST routes, and every new refusal mapped by the handler that already exists</name>
  <files>databasise/seam/rest.py, databasise/tests/seam/test_rest_transport.py</files>
  <read_first>
    - databasise/seam/rest.py — the whole file: `_RequestModel`, the existing `IngestRequest`/`DeleteRequest` shapes, `_refusal_response`, the single `app.add_exception_handler(SeamRefusalError, _refusal_response)` registration, and the `post_documents` route shape every write route follows
    - databasise/tests/seam/test_rest_transport.py — `_REFUSAL_FACTORIES` (every entry is a zero-argument callable constructing one refusal) and the parametrized test that walks `SeamRefusalError.__subclasses__()` transitively and asserts each maps to a non-2xx response
    - databasise/seam/engine.py — the final signatures of `promote`, `rollback` and `retire` as 07-01/07-02 landed them
    - databasise/seam/refusals.py — the nine refusals Phase 7 added, and their exact constructor keyword names
    - .planning/phases/05-opaque-side-admission/05-LEARNINGS.md — the recorded REST defect where a refusal raised inside a Pydantic model construction in a route body is wrapped into `ValidationError` and 500s instead of 422ing, and the `_checked_page` helper added to fix it
  </read_first>
  <behavior>
    - Test `test_post_promote_returns_the_same_result_as_the_in_process_call`: the same alias, trace ids and change_origin through `POST /promote` and through `Databasise.promote` produce identical response bodies apart from the generation ordinal, and identical ledger records apart from `created_at`.
    - Test `test_post_rollback_and_post_retire_round_trip`: the same for `POST /rollback` and `POST /retire`.
    - Test `test_every_new_refusal_maps_to_non_2xx`: the existing parametrized refusal walk now includes all nine Phase 7 refusal subclasses and each returns a non-2xx status with the refusal class name in the body.
    - Test `test_promote_route_body_carries_no_logic`: `POST /promote`'s response for a refusing input is produced by the registered handler, not by a route-local except block — asserted by driving a refusing call and checking the response shape matches `_refusal_response`'s own output shape exactly.
    - Test `test_rest_response_carries_no_internal_identity`: the serialised REST response for each of the three verbs contains no arm name, wiring id, node id or 64-hex instance hash, with the forbidden-token set derived live from the registry and the resolved wiring.
  </behavior>
  <action>
Add three `_RequestModel` subclasses and three routes to `databasise/seam/rest.py`, each a thin adapter in the exact shape `post_documents` already uses — deserialize, await the engine method, return.

`PromoteRequest` carries `alias`, `trace_ids`, `change_origin`, and `verb` defaulting to `"operator-asserted"`. `RollbackRequest` and `RetireRequest` each carry `alias`, `version`, `trace_ids`, `change_origin`. Field names match the engine parameter names exactly, so a reader comparing the REST body to the in-process call sees the same vocabulary.

Routes: `POST /promote` → `await engine.promote(body.alias, body.trace_ids, body.change_origin, verb=body.verb)`; `POST /rollback` → `await engine.rollback(body.alias, body.version, body.trace_ids, body.change_origin)`; `POST /retire` → `await engine.retire(body.alias, body.version, body.trace_ids, body.change_origin)`. Each returns the `PromotionResult` the engine returns, unchanged. No route body constructs a refusal, catches an exception, reshapes a result, or adds a field.

Register no new exception handler. `app.add_exception_handler(SeamRefusalError, _refusal_response)` is already registered once for the whole app and dumps `vars(exc)` generically, so every Phase 7 refusal subclass is picked up with zero transport-layer code. Adding a second handler is the failure mode this instruction exists to prevent.

Heed 05-LEARNINGS' recorded defect: do not construct any object inside a route body whose constructor can raise a `SeamRefusalError`, because Pydantic wraps it into a `ValidationError` that Starlette's exception middleware cannot match against the registered handler, turning a 422 into a 500. All three of these routes hand raw scalars straight to the engine, so the shape is already safe — keep it that way rather than adding validation into the request models.

Extend `databasise/tests/seam/test_rest_transport.py`'s `_REFUSAL_FACTORIES` with one zero-argument factory entry per Phase 7 refusal subclass: `EmptyPromotionTraceIdsError`, `DisagreeingPromotionTraceIdsError`, `InvalidChangeOriginError`, `MeasurementPostureRefusalError`, `UncalibratedFloorRefusalError`, `GateVerbNotBuiltError`, `UnknownGenerationVersionError`, `TombstonedGenerationError`, `ActiveGenerationRetirementError`. The subclass walk is automatic; the factory entries are not, and a missing entry fails that test with its own "no test factory registered" assertion rather than silently skipping.

Add the three round-trip and identity tests from the `<behavior>` block to the same module, following its existing app-construction and client conventions.
  </action>
  <verify>
    <automated>cd databasise &amp;&amp; uv sync --extra rest</automated>
    <fails_when>non-zero exit</fails_when>
    <automated>cd databasise &amp;&amp; uv run --extra rest pytest -q tests/seam/test_rest_transport.py -x</automated>
    <fails_when>non-zero exit, or "no tests ran" / "0 passed" in the summary line</fails_when>
    <automated>cd databasise &amp;&amp; uv run --extra rest pytest -q tests/seam/test_rest_transport.py -k refusal -x</automated>
    <fails_when>non-zero exit, or "no tests ran" in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - `databasise/seam/rest.py` declares `PromoteRequest`, `RollbackRequest` and `RetireRequest` as `_RequestModel` subclasses
    - `grep -v '^\s*#' databasise/seam/rest.py | grep -c 'add_exception_handler'` returns 1
    - Each of the three new route bodies is a single `return await engine.<verb>(...)` statement with no surrounding try/except
    - `_REFUSAL_FACTORIES` in `databasise/tests/seam/test_rest_transport.py` has one entry for each of the nine Phase 7 refusal classes
    - `cd databasise && uv run --extra rest pytest -q tests/seam/test_rest_transport.py::test_rest_response_carries_no_internal_identity -x` exits 0
    - The forbidden-token set in the identity test is built from `registry.keys()` and the resolved wiring at test time, not from a literal list in the test file
  </acceptance_criteria>
  <done>All three operator verbs are reachable over REST with the same vocabulary as the in-process call, and every Phase 7 refusal returns a non-2xx refusal response through the one handler that was already there.</done>
  <reversibility rating="costly">REST paths and request-body field names are consumer-visible surface. Renaming `/promote` or a body field later breaks every HTTP caller, and the dual-transport conformance test pins the shape.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Three MCP tools, the roster growth invariant, and the §18.5 coverage record</name>
  <files>databasise/mcp/tools.py, databasise/mcp/server.py, databasise/mcp/__init__.py, databasise/tests/mcp/test_tool_growth_invariant.py, .planning/phases/05-opaque-side-admission/COVERAGE.md</files>
  <read_first>
    - databasise/mcp/tools.py — `_ToolArgs`, `TOOL_NAMES` (six entries today), `DeleteToolArgs` and `IngestToolArgs` as the field-reuse pattern, and `__all__`
    - databasise/mcp/server.py — the `@server.tool(name=...)` registrations, `_refusal_mapped` and its single-base-class catch, and `delete_tool` as the write-tool shape to copy
    - databasise/mcp/__init__.py — the `TOOL_NAMES` re-export
    - databasise/tests/mcp/test_tool_growth_invariant.py — the whole file: `_TOOL_ARG_MODELS`, the `len(names) == len(TOOL_NAMES) == 6` assertion that this task changes, `_COVERAGE_PATH` (which points at Phase 5's COVERAGE.md, not this phase's), `_OPERATION_TO_TOOLS`, `_parse_markdown_table`, the forbidden-vocabulary test, and the second-modality-adds-nothing test
    - .planning/phases/05-opaque-side-admission/COVERAGE.md — the `| operation | transports | plan |` table this test parses, and the "Operations deliberately absent" table below it
    - .planning/phases/04-the-seam/04-CONTEXT.md — D-03, one entry per §18 operation
    - docs/system-model/CONTRACT.md §18.5 — the growth rule: the tool roster grows per operation, never per modality
  </read_first>
  <behavior>
    - Test (extend existing roster test): the registered tool names equal `TOOL_NAMES` and both have nine entries.
    - Test (existing, must keep passing): registering a second modality adds no tool name.
    - Test (existing, must keep passing): every tool name has a COVERAGE.md row and every MCP-transported row maps to a known tool — now including `promote`, `rollback` and `retire`.
    - Test `test_mcp_promote_matches_the_in_process_call`: the `promote` tool invoked with the same arguments as `Databasise.promote` returns the same serialised mapping and writes the same ledger record.
    - Test `test_mcp_rollback_and_retire_match_the_in_process_call`: the same for the other two tools.
    - Test `test_mcp_refusals_surface_as_tool_errors`: each of the nine Phase 7 refusals raised through a tool body surfaces as a `ToolError` whose payload names the refusal class, through the existing `_refusal_mapped` wrapper and no per-tool handling.
  </behavior>
  <action>
Add `PromoteToolArgs`, `RollbackToolArgs` and `RetireToolArgs` to `databasise/mcp/tools.py` as `_ToolArgs` subclasses, reusing the seam's own field vocabulary exactly as `DeleteToolArgs` does rather than declaring parallel types: `PromoteToolArgs` carries `alias`, `trace_ids`, `change_origin` and `verb` (defaulting to `"operator-asserted"`); the other two carry `alias`, `version`, `trace_ids`, `change_origin`. Add all three to `__all__`.

Grow `TOOL_NAMES` from its six entries to nine by appending `"promote"`, `"rollback"`, `"retire"`. Three names for three genuinely new §18 operations — Phase 4 D-03 requires one entry per operation, and RESEARCH.md's Open Question 3 confirms the `status`/`resolve` scope-dispatch precedent is available but explicitly not the intended pattern here. Do not fold rollback and retire into one lifecycle tool with a scope field.

Register three tools in `databasise/mcp/server.py` in the exact `delete_tool` shape: the `@server.tool(name=...)` decorator, then `@_refusal_mapped`, then an async body that awaits the engine method and returns `outcome.model_dump()`. No per-tool try/except — `_refusal_mapped` already catches the `SeamRefusalError` base class generically and re-raises as `ToolError` with the refusal detail, so every Phase 7 subclass is covered by construction.

Update `databasise/tests/mcp/test_tool_growth_invariant.py`:

- Change the hardcoded roster-length assertion from six to nine, and add the three new arg models to `_TOOL_ARG_MODELS`.
- Add three entries to `_OPERATION_TO_TOOLS` mapping each new operation-row label to its tool name. The test asserts every parsed COVERAGE.md row has a mapping entry and fails with its own "update the mapping" message otherwise, so this is required, not optional.

Update `.planning/phases/05-opaque-side-admission/COVERAGE.md` — and note carefully that this is the file the invariant test reads (`_COVERAGE_PATH` resolves to Phase 5's record, which every later phase has extended rather than replaced; 06-03 set that precedent for `compare`). Add three rows to its `| operation | transports | plan |` table, each naming in-process, the REST path and the MCP tool, with `07-03` as the plan, and each stating in the final column why it is an operation and not a §18.4 selector. The reason for all three is the same and should be said once per row rather than cross-referenced: a selector picks which fitted modality answers a question; promote, rollback and retire each write a generation record to the ledger and repoint an alias, which no selector value can express. Each is a separate operation under Phase 4 D-03's one-entry-per-operation rule and under §19.6's own precedent that a differently-effecting mutation is its own port.

Do **not** create a `COVERAGE.md` for Phase 7. The API-coverage detector returned no external-API-integration signal for this phase's scope — Phase 7 consumes no third-party surface, it extends this machine's own seam — so the seal-time gate treats it as a non-API phase. The edits above are to the existing §18.5 operations record, which is a different artifact serving a different test.

Add the three parity and refusal tests from the `<behavior>` block, following the module's existing MCP-availability guard convention (the shadow-safe resolver pattern 05-07 established, never a bare `import mcp` or `pytest.importorskip("mcp")`, both of which can resolve to this project's own `databasise/mcp/` package or the `tests/mcp/` namespace shadow instead of skipping cleanly).
  </action>
  <verify>
    <automated>cd databasise &amp;&amp; uv sync --extra mcp</automated>
    <fails_when>non-zero exit</fails_when>
    <automated>cd databasise &amp;&amp; uv run --extra mcp pytest -q tests/mcp/ -x -rs</automated>
    <fails_when>non-zero exit, or "no tests ran" in the summary line, or every test in tests/mcp reported as skipped</fails_when>
    <automated>cd databasise &amp;&amp; uv run --extra mcp pytest -q tests/mcp/test_tool_growth_invariant.py -x</automated>
    <fails_when>non-zero exit, or "no tests ran" in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - `databasise/mcp/tools.py`'s `TOOL_NAMES` has exactly nine entries and includes `promote`, `rollback` and `retire`
    - `grep -v '^\s*#' databasise/mcp/server.py | grep -c '@server.tool'` returns 9
    - Each of the three new tool registrations in `databasise/mcp/server.py` carries the `@_refusal_mapped` decorator directly beneath its `@server.tool(...)` decorator, matching `delete_tool`'s stacking order
    - No tool body in `databasise/mcp/server.py` contains a `try:` statement
    - `_OPERATION_TO_TOOLS` in `databasise/tests/mcp/test_tool_growth_invariant.py` has one entry per row of COVERAGE.md's §18.5 operations table
    - `.planning/phases/05-opaque-side-admission/COVERAGE.md`'s `| operation | transports | plan |` table contains three new rows whose `transports` column each names `MCP`
    - No file named `COVERAGE.md` exists under `.planning/phases/07-promotion-rollback/`
    - `cd databasise && uv run --extra mcp pytest -q tests/mcp/test_tool_growth_invariant.py -x` exits 0
  </acceptance_criteria>
  <done>The MCP roster carries nine tools for nine §18 operations, the three new ones reach the same engine methods through the existing refusal wrapper, and the §18.5 coverage record and the code state the same current thing.</done>
  <reversibility rating="costly">MCP tool names are consumer-visible and pinned by the growth-invariant test; renaming one later breaks every MCP client and forces a COVERAGE.md row rewrite alongside it.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Three-transport conformance and the phase's evidence record</name>
  <files>databasise/tests/seam/test_dual_transport.py, databasise/evidence/PROMOTION-LEDGER-EVIDENCE.md, databasise/tests/evidence/test_promotion_ledger_record.py</files>
  <read_first>
    - databasise/tests/seam/test_dual_transport.py — the whole file: how it drives the same call in-process and over REST and asserts envelope equality, and which fields it already excludes from comparison
    - databasise/tests/mcp/ — the MCP-side parity test 06-03 landed, for the third leg's conventions
    - databasise/evidence/F-14-SEAM-INVARIANCE.md — the house shape for an evidence document: `[code-verified]` tags, a findings table, a verdict that names what does not hold
    - databasise/evidence/CROSS-MODALITY-EVIDENCE.md — the second example of that shape, including how a BLOCKED or partial outcome is recorded honestly
    - databasise/tests/evidence/test_f14_record.py — the pattern for a test that asserts an evidence document's own claims against the code, rather than letting the document drift
    - .planning/ROADMAP.md — Phase 7's success criteria 1, 2 and 3 (criteria 4 and 5 are struck; see `07-GATE-AMENDMENT.md`)
    - .planning/phases/07-promotion-rollback/07-GATE-AMENDMENT.md — what this phase does not deliver and why, which the evidence record must restate rather than omit
  </read_first>
  <behavior>
    - Test `test_promote_parity_across_three_transports`: the same promote call in-process, over REST, and over MCP produces three responses that are equal field-for-field, and three ledger records equal on every column except `id` and `created_at`.
    - Test `test_rollback_parity_across_three_transports` and `test_retire_parity_across_three_transports`: the same for the other two verbs.
    - Test `test_refusal_parity_across_three_transports`: one refusing input — a disagreeing trace-id pair — refuses on all three transports, and all three name the same refusal class.
    - Test `test_promotion_ledger_record_claims_match_the_code` (new module `tests/evidence/test_promotion_ledger_record.py`): every success criterion the evidence document marks as holding names a test path that exists and is collected by pytest; every criterion it marks as not holding is absent from the holding list. The document cannot claim a criterion without a live test reference.
  </behavior>
  <action>
Extend `databasise/tests/seam/test_dual_transport.py` with the three parity cases and the refusal-parity case from the `<behavior>` block, following the module's existing equality conventions and its existing exclusion list. Exclude only `id` and `created_at` from the ledger-record comparison — every other column, including `minted_version`, `mutation_class`, `change_origin`, `record_kind` and `targets_version`, must be equal across the three transports, because a transport that produced a different class or a different minted version would be the exact divergence this test exists to catch.

Drive the three transports through the same seeded store and the same trace records, with a fresh store root per transport so the three promotions are independent first promotions rather than a sequence in which the second and third mint different versions than the first. If the module's existing fixtures make three independent store roots awkward, assert on the response objects and the record columns rather than restructuring the module's fixtures — the parity claim is about field equality, not about sharing one store.

Write `databasise/evidence/PROMOTION-LEDGER-EVIDENCE.md` in this project's established evidence shape (`F-14-SEAM-INVARIANCE.md` and `CROSS-MODALITY-EVIDENCE.md` are the two nearest examples). It states, per ROADMAP Phase 7 success criterion 1, 2 and 3, whether the criterion holds and **by which committed test** — a test node id, not a prose assertion. Every claim carries a `[code-verified]` tag naming the file it was read from. Anything that does not hold is named in the same table rather than omitted.

The document must also restate, not omit, what this phase does not deliver: criteria 4 and 5 are struck and their requirements (HARD-04, HARD-01, HARD-02) are deferred per `07-GATE-AMENDMENT.md`; every promotion this milestone can make is operator-asserted, provisional and carries no verdict; and the gate-adjudicated path stays unreachable under MACH-09's default posture, which is what makes the deferral sound. Do not restate the amendment record's argument in full — cite it and summarise in one paragraph.

Write `databasise/tests/evidence/test_promotion_ledger_record.py` asserting the document's own claims against the code, following `test_f14_record.py`'s pattern: parse the criteria table, and for each row marked as holding, assert the named test node id is collectible by pytest. This is what keeps the document from drifting into a claim no test supports — the exact vacuous-pass failure mode HARD-01 was filed against, applied here to this phase's own record.

Do not mark MACH-07 or API-09 complete in `.planning/REQUIREMENTS.md` from this plan. Requirement state is `/gsd-verify-work`'s to change against the verification it runs; a plan marking its own requirement complete is the plan grading its own homework.
  </action>
  <verify>
    <automated>cd databasise &amp;&amp; uv run --extra rest pytest -q tests/seam/test_dual_transport.py -x</automated>
    <fails_when>non-zero exit, or "no tests ran" / "0 passed" in the summary line</fails_when>
    <automated>cd databasise &amp;&amp; uv run pytest -q tests/evidence/test_promotion_ledger_record.py -x</automated>
    <fails_when>non-zero exit, or "no tests ran" in the summary line</fails_when>
    <automated>cd databasise &amp;&amp; uv run pytest -q</automated>
    <fails_when>non-zero exit, or a lower passed-count than the count recorded at the end of 07-02</fails_when>
    <automated>cd databasise &amp;&amp; uv run python -m databasise.tools.check_import_boundary</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `databasise/evidence/PROMOTION-LEDGER-EVIDENCE.md` contains a criteria table with one row per ROADMAP Phase 7 success criterion 1, 2 and 3
    - Every row of that table marked as holding names a pytest node id, and every such node id is collectible by `pytest --collect-only`
    - The document cites `.planning/phases/07-promotion-rollback/07-GATE-AMENDMENT.md` and states that criteria 4 and 5 are struck and their requirements deferred
    - The document states that every promotion in this milestone is operator-asserted, provisional, and carries no verdict
    - `cd databasise && uv run pytest -q tests/evidence/test_promotion_ledger_record.py -x` exits 0
    - `cd databasise && uv run --extra rest pytest -q tests/seam/test_dual_transport.py -k promot -x` exits 0
    - The ledger-record comparison in the parity tests excludes only `id` and `created_at`
    - `git diff --stat .planning/REQUIREMENTS.md` shows no change from this plan
  </acceptance_criteria>
  <done>The same three verbs behave identically in-process, over REST and over MCP — on success and on refusal — and the phase's evidence document states which success criteria hold, by which test, and what it does not deliver.</done>
  <reversibility rating="reversible">Tests and an evidence document. Nothing consumer-visible is created; the document is appendable and the parity assertions tighten rather than constrain the surface.</reversibility>
</task>

</tasks>

<verification>
- `cd databasise && uv run pytest -q` green at or above the count 07-02 recorded.
- `cd databasise && uv sync --extra rest && uv run --extra rest pytest -q tests/seam/test_rest_transport.py tests/seam/test_dual_transport.py -x` exits 0.
- `cd databasise && uv sync --extra mcp && uv run --extra mcp pytest -q tests/mcp/ -x -rs` exits 0 with no whole-module skip.
- `cd databasise && uv run python -m databasise.tools.check_import_boundary` exits 0.
- No `<action>` in this plan contains a fenced code block.
</verification>

<success_criteria>
- API-09 holds as a claim about what a caller can do: promote, rollback and retire reachable in-process, over REST, and over MCP, with `operator_asserted` provenance, non-empty `promotion_trace_ids`, and no verdict or tier-of-decision on any transport.
- ROADMAP success criterion 3's refusal is reachable and identical on all three transports.
- The §18.5 coverage record and the tool roster agree, and the MCP roster grew by operation rather than by modality.
</success_criteria>

<output>
Create `.planning/phases/07-promotion-rollback/07-03-SUMMARY.md` when done.
</output>
