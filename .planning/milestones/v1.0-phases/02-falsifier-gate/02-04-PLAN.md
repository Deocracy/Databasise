---
phase: 02-falsifier-gate
plan: 04
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - .planning/phases/02-falsifier-gate/02-GATE-01-WAIVER.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [GATE-01, MACH-02, MACH-03, HARD-01, HARD-02]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The GATE-01 waiver record cites `docs/system-model/D-VARIANTS/SELECTION.md` by name and reproduces the two clauses it amends — Conditions on the selection item 6 (the eval instrument precedes every claim, and the eval-bundle-first sequencing) and Falsifiers items 2 and 5"
    - "The waiver is scoped rather than a blanket skip: it records that Falsifier 2 is satisfied now with its committed evidence artifact named, that Falsifier 5 moves to its point of first need, and that a Falsifier 2 failure still halts the ladder as a SELECTION.md-level reversal rather than a repairable defect (GATE-01, D-02)"
    - "The waiver names the standing condition under which it holds — no promotion or parity claim is taken before the A/A floor exists — and names Phase 3's substitute gate, deterministic retrieval-level comparison plus human spot-checks, with its accepted risk stated in the owner's own terms (D-05)"
    - "ROADMAP.md and REQUIREMENTS.md both map MACH-02 and MACH-03 to Phase 3 and HARD-01 and HARD-02 to Phase 7, and ROADMAP.md's Requirement Coverage table still totals 34 requirements across seven phases with every requirement mapped to exactly one phase"
    - "ROADMAP.md's Ordering Constraints no longer assert that the eval bundle and A/A floor exist before Phase 3 moves a node boundary; the replacement constraint cites the waiver record and keeps a Falsifier 2 failure as a halt"
    - "ROADMAP.md's Phase 2 entry, overview sentence and success criteria describe the phase that was actually planned, so no reader is left with a roadmap that contradicts the delivered scope"
  prohibitions:
    - statement: "MUST NOT let the waiver read as a blanket gate skip, or as the ladder proceeding unconditionally — the amendment is scoped to Falsifier 5's timing alone, and Falsifier 2 stays a halting condition"
      status: unverified
    - statement: "MUST NOT edit anything under `docs/system-model/` — it is a verbatim upstream mirror of the ratified system model; the amendment is recorded in this project's own layer with a citation into the frozen document, never by rewriting it"
      status: unverified
    - statement: "MUST NOT record a deferral without a named point of first need — an item deferred to no particular trigger is an item silently dropped"
      status: unverified
  artifacts:
    - path: ".planning/phases/02-falsifier-gate/02-GATE-01-WAIVER.md"
      provides: "The written owner decision amending the ratified §VD verdict's stated condition — GATE-01's deliverable"
      contains: "SELECTION.md"
    - path: ".planning/ROADMAP.md"
      provides: "Requirement coverage and ordering constraints matching the 2026-08-31 re-scope"
      contains: "02-GATE-01-WAIVER.md"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability rows matching the re-scope"
  key_links:
    - from: ".planning/phases/02-falsifier-gate/02-GATE-01-WAIVER.md"
      to: "databasise/evidence/FALSIFIER-2-EVIDENCE.md"
      via: "the waiver names the committed Falsifier 2 evidence it rests on"
      pattern: "FALSIFIER-2-EVIDENCE"
    - from: ".planning/ROADMAP.md"
      to: ".planning/phases/02-falsifier-gate/02-GATE-01-WAIVER.md"
      via: "the amended rung-ordering constraint cites the waiver record as its authority"
      pattern: "02-GATE-01-WAIVER"
---

<objective>
Record GATE-01's deliverable — the written owner decision amending the ratified §VD verdict's stated
condition — and move requirement tracking to match the re-scope so coverage stops describing a phase
that was not planned.

Purpose: the ratified verdict named Falsifiers 2 and 5 as its conditions. The owner deferred
Falsifier 5 to its point of first need (D-01). D-02 requires that this waiver is recorded and never
silent: GATE-01's deliverable therefore becomes the amendment document itself, plus the tracking
changes it authorises. A Falsifier 2 failure still halts the ladder.

Output: the waiver record, and ROADMAP.md and REQUIREMENTS.md re-scoped so MACH-02 and MACH-03 sit
with Phase 3's parity comparison and HARD-01 and HARD-02 sit with Phase 7's hardening pass.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-falsifier-gate/02-CONTEXT.md
@.planning/phases/02-falsifier-gate/02-RESEARCH.md
@.planning/phases/02-falsifier-gate/02-01-SUMMARY.md
@.planning/phases/02-falsifier-gate/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the GATE-01 waiver record</name>
  <files>.planning/phases/02-falsifier-gate/02-GATE-01-WAIVER.md</files>
  <read_first>
    - docs/system-model/D-VARIANTS/SELECTION.md — `## The verdict`, `## Conditions on the selection` (items 6 and 7), and `## Falsifiers` (items 1, 2 and 5); read these sections in full so the waiver quotes the conditions rather than paraphrasing them
    - .planning/phases/02-falsifier-gate/02-CONTEXT.md — D-01 through D-05, the owner's own wording and reversibility ratings
    - databasise/evidence/FALSIFIER-2-EVIDENCE.md — the evidence the waiver rests on, produced by plans 02-01 and 02-03
    - .planning/phases/02-falsifier-gate/02-MACH-09-POSTURE.md — the sibling record whose format this one follows
    - .planning/REQUIREMENTS.md — GATE-01's requirement text and the traceability table
  </read_first>
  <action>
Write `.planning/phases/02-falsifier-gate/02-GATE-01-WAIVER.md` as the amendment record. Do not edit
anything under `docs/system-model/` — that directory is a verbatim upstream mirror of the ratified
system model, and the amendment lives in this project's own layer with a citation into the frozen
document.

Required sections and content:

**What is amended.** Name `docs/system-model/D-VARIANTS/SELECTION.md` as the governing document and
reproduce, as quoted text, the two clauses this record amends: `## Conditions on the selection`
item 6 (the eval instrument precedes every claim this architecture makes, with its settled
sequencing of eval bundle and gate before isolation before architecture, and its closing rule that
benchmarks never settle decisions and only local measurement on our own corpus does), and
`## Falsifiers` item 5 (per-tier A/A nulls not materially narrower at T1 than T0). Also quote
`## Falsifiers` item 2 (depth cannot be computed statically over the three named parts) as the
condition this record does **not** amend.

**The amendment.** State it in three clauses. First: Falsifier 2 is satisfied now — name
`databasise/evidence/FALSIFIER-2-EVIDENCE.md` as the committed evidence, name the three wirings it
covers, and state its verdict. Second: Falsifier 5 moves to its point of first need, which is
Phase 3's parity comparison at the earliest, calibrated against the real pre-decomposition LightRAG
rather than a stand-in arm; reproduce D-01's own rationale, that Phase 1 has only canned
deterministic parts so a floor calibrated now would be calibrated on a stand-in. Third: the ladder
proceeds to rung 2.

**What is not amended.** State plainly that a Falsifier 2 failure still halts the ladder as a
SELECTION.md-level reversal rather than a repairable defect, and that condition 6's substance — no
promotion or parity claim rides on an unmeasured comparison — is re-timed, not withdrawn.

**The standing condition.** State the condition under which this waiver holds: no promotion decision
and no parity claim is taken before the A/A floor exists. Name Phase 3's substitute gate from D-05 —
deterministic, zero-token retrieval-level comparison of retrieved chunk sets and rankings between the
original and the decomposed query side, plus human spot-checks of answers — and state its accepted
risk in the owner's own terms: subtle answer-quality drift is unmeasured until a floor is calibrated,
judged low because Phase 3 keeps the same prompts and models, so decomposition bugs surface at the
retrieval level.

**Reversibility.** Record D-01's own rating: reversible — the bundle and calibration design is fully
specified in RIG §EV.1-§EV.3 and §AA.1-§AA.3 and can be stood up whenever wanted.

**Banked decisions.** List each already-settled eval-run decision as one line naming its id, so
whoever executes the calibration inherits the decision and its rating rather than re-deriving either.
Take the content from 02-CONTEXT.md, do not restate the rationale: D-06 corpus; D-07 generator, with
its free-model restriction to drafts only because a re-routed provider changes identity mid-run;
D-08 judge, never free, provider-pinned and prompt-hashed, with its identity derived from the
provider and model the response actually returns — rated one-way, since changing the judge mints a
new bundle version and voids every null calibrated under it; D-09 embedder, local via Ollama so tier
one stays genuinely free; D-10 declared concurrency, picked once and held — rated costly, since
changing it requires recalibrating the floor; D-11 the three calibration preconditions that must
hold before a recorded run counts — cache bypassed and checkable per node, temperature honoured by
the pinned provider, and no invisible upstream prompt caching; D-12 account prep.

Add one line for D-13, the machine-side fact that constrains D-10: the v1 three-runs-at-a-time cap
is gone in the v2 machine — concurrency is a per-node semaphore sized by that node's own config with
no process-wide cap — so a declared concurrency setting is a per-node config value and the binding
limit is the API provider, not the runner.

**What this authorises.** State that this record authorises the tracking changes Tasks 2 and 3 make:
MACH-02 and MACH-03 move to Phase 3, HARD-01 and HARD-02 move to Phase 7's hardening pass, and each
carries a named point of first need rather than an open-ended deferral.

**Decision and date.** Close with the decision line, the owner, and the date 2026-08-31, matching how
02-CONTEXT.md records the re-scope.
  </action>
  <verify>
    <automated>test -f .planning/phases/02-falsifier-gate/02-GATE-01-WAIVER.md &amp;&amp; grep -q 'SELECTION.md' .planning/phases/02-falsifier-gate/02-GATE-01-WAIVER.md &amp;&amp; grep -q 'FALSIFIER-2-EVIDENCE' .planning/phases/02-falsifier-gate/02-GATE-01-WAIVER.md</automated>
    <human-check>
      <test>Read `.planning/phases/02-falsifier-gate/02-GATE-01-WAIVER.md` end to end.</test>
      <expected>It reads as a scoped amendment, not a gate skip: the reader can see exactly which ratified condition moved, which one did not, what evidence the move rests on, what still halts the ladder, and what has to stay true for the waiver to keep holding.</expected>
      <why_human>This is a governance record amending a ratified verdict. Whether it reads as scoped rather than as permission to skip the gate is a judgment about the document's effect on a future reader, which no test can make.</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/02-falsifier-gate/02-GATE-01-WAIVER.md` exists
    - it contains `docs/system-model/D-VARIANTS/SELECTION.md`
    - it contains each of `Falsifier 2`, `Falsifier 5`, `Conditions on the selection`
    - it contains `databasise/evidence/FALSIFIER-2-EVIDENCE.md`
    - it contains the phrase `halts the ladder` and the word `reversal`
    - it contains `retrieval-level` and `accepted risk`
    - it contains references to `D-01`, `D-02` and `D-05`
    - its banked-decisions section names every id from `D-06` through `D-13`, each on its own line
    - it records `D-08` as one-way and `D-10` as costly
    - it contains `2026-08-31`
    - `git status --porcelain docs/system-model/` reports no modified files
  </acceptance_criteria>
  <done>GATE-01's deliverable exists as a written, cited, scoped amendment naming the evidence it rests on, what still halts the ladder, and the condition under which it keeps holding.</done>
</task>

<task type="auto">
  <name>Task 2: Re-scope ROADMAP.md to the phase that was actually planned</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md — the Overview paragraph, the Phase 2 and Phase 3 detail blocks, the Requirement Coverage table, and the Ordering Constraints list
    - .planning/phases/02-falsifier-gate/02-GATE-01-WAIVER.md — the authority for every change in this task
    - .planning/phases/02-falsifier-gate/02-CONTEXT.md — the domain block's in-scope and moved-out lists
  </read_first>
  <action>
Amend `.planning/ROADMAP.md` with scoped `Edit` operations only — never rewrite the file whole, since
every phase entry outside the edited region must survive untouched.

Six edits:

1. **Overview paragraph.** The sentence describing rung 1 currently names both the depth and
   execution_mode validator (Falsifier 2) and the calibrated A/A noise floor (Falsifier 5) as rung
   1's gate. Amend it so rung 1's gate is Falsifier 2, and the A/A floor is named as standing up at
   its point of first need with Phase 3's parity comparison, citing the waiver record.

2. **Phase 2 requirements line.** Change the `**Requirements:**` line for Phase 2 to
   `GATE-01, MACH-01, MACH-09`.

3. **Phase 2 goal and success criteria.** The Phase 2 `**Goal**:` line currently names both the
   computed depth/execution-mode derivation and the measured noise floor. Amend it so the goal is
   the derivation being computed rather than declared, and the gate decision being recorded — the
   noise floor is no longer this phase's goal. Then, in the success criteria: keep criterion 1 (the Falsifier 2 validator run over the three named
   wirings with the §19.10 enumeration) and criterion 4 (measurement off by default, degraded
   labelling). Remove criterion 2 (eval bundle mint) and criterion 3 (A/A calibration) and add them
   verbatim to Phase 3's success criteria. Rewrite criterion 5 so it keeps only the clause this phase
   delivers — the rung-1 to rung-2 decision is recorded, and a Falsifier 2 failure halts the ladder
   as a SELECTION.md-level reversal — and move its gate-script and ANATOMY §F clauses to Phase 7.
   Renumber the surviving criteria.

4. **Phase 3.** Change its `**Requirements:**` line to `MODAL-01, MACH-02, MACH-03`. Change its
   `**Depends on:**` line so it reads that Phase 3 depends on Phase 2's rung-1 gate with Falsifier 2
   passed and Falsifier 5 deferred per the waiver record. Add the two moved success criteria, and add
   one criterion for D-05's substitute parity gate — the parity band is checked at the retrieval
   level with deterministic zero-token comparisons plus human spot-checks until the A/A floor exists.

5. **Phase 7.** Change its `**Requirements:**` line to `MACH-07, API-09, HARD-04, HARD-01, HARD-02`
   and add a success criterion covering the moved hardening clauses: the gate scripts fail on missing
   extraction instead of passing vacuously, and every ANATOMY §F row points at its landed repair with
   stale cross-document rows reconciled.

6. **Requirement Coverage table and Ordering Constraints.** Update the coverage table rows to
   Phase 2 with 3 requirements, Phase 3 with 3, Phase 7 with 5, leaving the other rows unchanged and
   the stated total at 34 mapped requirements. In Ordering Constraints, amend the GATE-01 rung
   ordering bullet so it reads that Phase 3 does not start until Falsifier 2 passes, citing
   `.planning/phases/02-falsifier-gate/02-GATE-01-WAIVER.md`, and keeping the sentence that a
   Falsifier 2 failure halts the ladder as a SELECTION.md-level reversal. Replace the "Eval
   infrastructure before decomposition" bullet with one stating that the bundle and A/A floor stand
   up at Phase 3's parity comparison, before any promotion or parity claim rides on a measured
   number, and naming the waiver record as the authority for the change.

After editing, verify by arithmetic that the coverage table's per-phase counts sum to 34 and that
every requirement id in `.planning/REQUIREMENTS.md` appears in exactly one phase row.
  </action>
  <verify>
    <automated>grep -c '02-GATE-01-WAIVER' .planning/ROADMAP.md</automated>
    <automated>grep -n 'MACH-02' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` Phase 2 `**Requirements:**` line reads `GATE-01, MACH-01, MACH-09`
    - the Phase 2 `**Goal**:` line no longer claims the rig's own noise floor is measured in this phase
    - `.planning/ROADMAP.md` Phase 3 `**Requirements:**` line contains `MACH-02` and `MACH-03`
    - `.planning/ROADMAP.md` Phase 7 `**Requirements:**` line contains `HARD-01` and `HARD-02`
    - the Requirement Coverage table rows read 3 for Phase 2, 3 for Phase 3 and 5 for Phase 7, and the per-phase counts sum to 34
    - `.planning/ROADMAP.md` contains `02-GATE-01-WAIVER.md` at least once, inside the Ordering Constraints section
    - the Ordering Constraints section no longer states that the bundle and A/A floor exist before Phase 3 moves a node boundary
    - every `### Phase N:` heading present before this task is still present afterwards, with all seven phases intact
  </acceptance_criteria>
  <done>ROADMAP.md describes the phase that was planned and executed, every requirement still maps to exactly one phase, and the two amended ordering constraints cite the waiver record as their authority.</done>
</task>

<task type="auto">
  <name>Task 3: Re-scope the REQUIREMENTS.md traceability table</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md — the traceability table and the requirement text for MACH-02, MACH-03, HARD-01, HARD-02
    - .planning/ROADMAP.md — the coverage table Task 2 just amended, which this table must agree with
    - .planning/phases/02-falsifier-gate/02-GATE-01-WAIVER.md — the authority for the move
  </read_first>
  <action>
Amend `.planning/REQUIREMENTS.md` with scoped `Edit` operations only.

Change the traceability table rows so `MACH-02` and `MACH-03` map to `Phase 3` and `HARD-01` and
`HARD-02` map to `Phase 7`, leaving their `Pending` status unchanged. Leave `GATE-01`, `MACH-01` and
`MACH-09` mapped to Phase 2.

HARD-01's and HARD-02's requirement text each state a timing — first touched at rung 1, and a doc
pass alongside rung 1. Do not delete that text. Append a short bracketed amendment note to each of
the four moved requirement lines naming the new phase, the point of first need, and the waiver record
as the authority — for MACH-02 and MACH-03 the point of first need is Phase 3's parity comparison;
for HARD-01 and HARD-02 it is Phase 7's hardening pass, and the note must say that the rung-1 timing
in the original text is superseded by the 2026-08-31 owner decision.

Then confirm the two documents agree: every requirement id's phase in this table matches its phase in
ROADMAP.md's Requirement Coverage table, and no id appears under two phases.
  </action>
  <verify>
    <automated>grep -E '^\| (MACH-02|MACH-03|HARD-01|HARD-02) \|' .planning/REQUIREMENTS.md</automated>
    <automated>grep -c '02-GATE-01-WAIVER' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - the `MACH-02` and `MACH-03` traceability rows read `Phase 3`
    - the `HARD-01` and `HARD-02` traceability rows read `Phase 7`
    - the `GATE-01`, `MACH-01` and `MACH-09` traceability rows still read `Phase 2`
    - each of the four moved requirement lines carries an amendment note naming `02-GATE-01-WAIVER.md` and a point of first need
    - no requirement id appears under two different phases in the traceability table
    - the phase assignments in `.planning/REQUIREMENTS.md` and the Requirement Coverage table in `.planning/ROADMAP.md` agree for all 34 ids
  </acceptance_criteria>
  <done>Requirement tracking matches the recorded re-scope in both documents, each moved requirement carries its point of first need, and neither deferral is open-ended.</done>
</task>

</tasks>

## Artifacts this phase produces

Symbols and paths created by this plan. Treat every entry as newly created, not as drift against
existing code.

**New files**

- `.planning/phases/02-falsifier-gate/02-GATE-01-WAIVER.md`

**Amended files (scoped edits, never whole-file rewrites)**

- `.planning/ROADMAP.md` — Overview rung-1 sentence, Phase 2 requirements and success criteria,
  Phase 3 requirements, depends-on and success criteria, Phase 7 requirements and success criteria,
  Requirement Coverage table, and two Ordering Constraints bullets
- `.planning/REQUIREMENTS.md` — traceability rows for `MACH-02`, `MACH-03`, `HARD-01`, `HARD-02`,
  each gaining an amendment note

No source code is modified by this plan.

## Flagged Assumptions (unresolved edge-probe rows)

The spec-less edge probe returned five of this plan's requirements as `unclassified`, so those rows
stay **unresolved** rather than being auto-resolved with a backstop. They are surfaced here rather
than dropped:

1. **GATE-01 — unclassified, unresolved.** GATE-01 is an ordering and governance constraint, not a
   computation with an input domain, so the shape taxonomy found no edge. The plan resolves it as a
   document with checkable content instead. If a reviewer identifies a genuine edge in GATE-01 —
   for example the exact behaviour at the moment a rung's gate flips — it is not covered here.
2. **MACH-02 — unclassified, unresolved.** Deferred to Phase 3 by owner decision D-01; no edge
   analysis was performed because no bundle is built in this phase.
3. **MACH-03 — unclassified, unresolved.** Same as MACH-02; the A/A calibration's own edges
   (bootstrap resample count, p95 boundary, tie handling at the floor) are Phase 3's to author.
4. **HARD-01 — unclassified, unresolved.** Deferred to Phase 7's hardening pass; the vacuous-pass
   sites in `docs/system-model/parts-check.sh` and `anatomy-check.sh` were not analysed this phase.
5. **HARD-02 — unclassified, unresolved.** Deferred to Phase 7's hardening pass; the eight ANATOMY
   §F rows and the stale cross-document rows were not analysed this phase.

Further assumptions carried by this plan:

6. **HARD-01 and HARD-02 land in Phase 7.** 02-CONTEXT.md defers them to "a later doc pass" without
   naming a phase. Phase 7 is chosen because it already carries HARD-04 and is the milestone's
   hardening bucket. This supersedes the "at rung 1" timing in their own requirement text, which is
   why Task 3 requires that supersession to be written down rather than left implicit.
7. **The waiver record lives in `.planning/`, not `docs/system-model/`.** 02-RESEARCH.md Open
   Question 2 flags this as unconfirmed. The plan takes the `.planning/` option because
   `docs/system-model/` is a verbatim upstream mirror per the project CLAUDE.md, and adds an explicit
   acceptance criterion that the mirror is left unmodified.

<verification>
- The waiver record cites the governing document and the evidence artifact it rests on
- `git status --porcelain docs/system-model/` is empty — the upstream mirror is untouched
- ROADMAP.md and REQUIREMENTS.md agree on every requirement's phase, and all seven phase entries survive
- The Requirement Coverage table still totals 34 with every requirement mapped exactly once
</verification>

<success_criteria>
- GATE-01's deliverable exists: a written, cited, scoped amendment to the ratified verdict's condition
- A Falsifier 2 failure is still recorded as a halt, not a repairable defect
- Every deferred requirement carries a named point of first need and a phase
- Requirement tracking in both planning documents matches the delivered scope
</success_criteria>

<output>
Create `.planning/phases/02-falsifier-gate/02-04-SUMMARY.md` when done
</output>
