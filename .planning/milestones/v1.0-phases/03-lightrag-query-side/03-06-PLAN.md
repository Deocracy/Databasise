---
phase: 03-lightrag-query-side
plan: 06
type: execute
wave: 5
depends_on: ["03-05"]
files_modified:
  - databasise/parts_core/lightrag/join_roundrobin.py
  - databasise/parts_core/lightrag/truncator_token_budget.py
  - databasise/parts_core/lightrag/chunk_sel_kg.py
  - databasise/parts_core/lightrag/__init__.py
  - databasise/tests/parts_core/lightrag/test_transform_parts.py
  - databasise/tests/parity/test_arm_conformance.py
autonomous: true
requirements: [MODAL-01]

estimate:
  tokens: 76000
  raw_tokens: 76000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All eighteen §L.1 positions resolve against registered parts: the `mix` base wiring parses with zero violations, and so does every one of the five arms (criterion 1)."
    - "Each of the five arm patches resolves against the base to exactly the node id set its own published patch file declares."
    - "The three `join-*` positions are one component wired three times with different config, and the two `budget-*` positions are one component wired twice — a wiring node id is a position, never an identity."
    - "`chunk-sel-kg` declares `reads_vector` even on a weight-only configuration, per §19.9's fallback-reachability rule."
    - "A fan-in node emits each item either unchanged or as authored evidence carrying `derived_from`; a merge that re-projects survivors into a narrower shape is authoring and is declared as such."
  artifacts:
    - databasise/parts_core/lightrag/join_roundrobin.py
    - databasise/parts_core/lightrag/truncator_token_budget.py
    - databasise/parts_core/lightrag/chunk_sel_kg.py
    - databasise/tests/parity/test_arm_conformance.py
  key_links:
    - "`join-entities`, `join-relations`, and `join-chunks` all resolve to the one `lightrag/join-roundrobin` component; their differing `arm_precedence` and `arity` live in each node's config, which is what puts them in different `config_hash` partitions."
    - "`budget-entities` and `budget-relations` both resolve to `lightrag/truncator-token-budget`, each naming itself as its own `apportioning_node`."
    - "The base wiring, previously unparseable because eleven components were unregistered, parses clean once this plan lands — which is the check CONTRACT requires of a base independently of any arm."
  prohibitions:
    - "The parity harness must not emit a pass or fail verdict when the index-identity preconditions failed; it must emit `inconclusive`."
    - "A variance band must not be presented without its run count and cache status recorded alongside it, and must not be computed from cache-served re-runs."
    - "The declared-deviation record must not carry a blanket or catch-all category; every excursion must be named individually with its own cause."
    - "A node that made a real LLM, embedding, or rerank call must not report zero spend; an unmetered real call is a refusal, not a free node."
    - "The v1 original arm must not be presented with a RIG §TR.1-shaped run record it did not produce; wherever its numbers appear, the trace asymmetry must be stated."
    - "The MACH-02/MACH-03 deferral must not be open-ended or silent, and must never be recorded as passed, satisfied, or not-applicable."
    - "The storage-ownership audit must not report a node that touched no store as audited-compliant; a no-touch node must be reported distinctly from a node whose touches matched its declarations."
    - "Regenerating Falsifier 2's committed evidence must not silently change its recorded verdicts; a changed verdict is a gate regression to surface, never an updated file."
---

<objective>
Close the node set: the three join positions, the two budget positions, and the KG chunk selector —
then prove the `mix` base and all five arms parse and resolve to exactly the node id sets the
published patch files declare.

Purpose: criterion 1 is not "most positions run"; it is all eighteen. Several positions exist in only
some arms — `chunk-vector` in `mix` and `naive` only, `entity-lookup` in `local`, `hybrid`, and `mix`
only — so porting one arm would leave positions unexercised and the criterion unmet in substance
(D-13). This plan is where the count closes and where the arm mechanism gets proved against the whole
family rather than the two arms the tracer could reach.

Output: three more registered components (six more positions), a parseable base, and a conformance
test covering all five arms.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-lightrag-query-side/03-CONTEXT.md
@.planning/phases/03-lightrag-query-side/03-RESEARCH.md
@.planning/phases/03-lightrag-query-side/03-PATTERNS.md
@databasise/parts_core/passthrough.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `join-roundrobin` — one component, three positions</name>
  <read_first>
    - docs/system-model/wirings/lightrag-base.json — the three `join-*` nodes: their shared component
      `lightrag/join-roundrobin`, kind `join`, empty effects, and their differing config
      (`socket_shape`, `arm_precedence`, `arity` on `join-chunks`, and the
      `apportionment_policy` stating that a non-effectful join spends no budget)
    - docs/system-model/wirings/lightrag-arm-local.json-patch.json,
      lightrag-arm-global.json-patch.json, lightrag-arm-hybrid.json-patch.json,
      lightrag-arm-naive.json-patch.json — every `replace` operation touching a join node's `deps` or
      `arm_precedence`, which is the full set of configurations this one body must handle
    - docs/system-model/PARTS.md §L.2 — the fan-in ordering position and the `rank_position` emitted
      semantics at all three joins, and the E/R/C source-labelling correction
    - docs/system-model/CONTRACT.md §4 — `ScoredItem`, `provenance`, `derived_from`, and the clause
      stating a fan-in must either pass an item through unchanged or emit it as authored evidence
      carrying `derived_from`
    - databasise/parts_core/passthrough.py — the zero-effect transform shape
    - databasise/parts_core/lightrag/entity_hydrate_expand.py — the upstream items and their
      `derived_from`, written in plan 03-05
    - v1/lightrag/operate.py — the upstream merge logic for each of the three joins; locate each by
      searching for the function name, not by a PARTS.md line citation
  </read_first>
  <files>databasise/parts_core/lightrag/join_roundrobin.py, databasise/parts_core/lightrag/__init__.py, databasise/tests/parts_core/lightrag/test_transform_parts.py</files>
  <behavior>
    - Merging two input lists interleaves them round-robin, with the arm named by
      `config.arm_precedence` contributing first.
    - `join-relations`' order-insensitive dedup configuration collapses two entries with the same
      endpoint pair in either order into one.
    - `join-chunks`' `arity` of 2 is honoured, and a run supplying a different number of inputs than
      the declared arity raises rather than silently merging what arrived.
    - Every emitted item carries a `rank_position` reflecting its position in the merged order.
    - An item passed through unchanged keeps its original `derived_from`; an item re-projected into a
      narrower shape carries a `derived_from` naming the items it was computed from.
    - The body reaches no store and no client, and its Part declares no effects.
  </behavior>
  <action>
    Write `databasise/parts_core/lightrag/join_roundrobin.py` exporting one module-level `Part`
    constant for component `lightrag/join-roundrobin`, kind `join`, effects empty, `upstream_ref` set
    to the v1 file the merge logic came from and located by search.

    One body serves all three positions; the differences are config, not code. Read
    `config.arm_precedence` to decide which input contributes first, `config.socket_shape` as the
    declared shape contract, and `config.arity` where present as the required input count. The
    order-insensitive dedup `join-relations` configures is a config-selected behaviour of this same
    body, not a second component.

    Emit `rank_position` on every item, with the semantics §L.2 fixes for all three joins — read that
    subsection before choosing what the value counts from, since the three joins are stated to share
    one semantics and getting it wrong at one of them is a silent ranking difference the parity
    comparison would attribute to the decomposition.

    Honour CONTRACT §4's fan-in rule explicitly: pass an item through unchanged, or emit it as
    authored evidence carrying `derived_from` naming its contributors. Discarding a contributing item's
    origin field while re-projecting survivors into a narrower shape is authoring, and the contract
    names that case out specifically. Do not let a merge quietly drop provenance.

    A supplied input count that disagrees with a declared `arity` is a refusal, not a best-effort
    merge — this codebase's stated house style, and a silently short merge here is indistinguishable
    downstream from a genuinely short retrieval.

    Extend `LIGHTRAG_PARTS` and add per-behaviour tests to
    `databasise/tests/parts_core/lightrag/test_transform_parts.py`. No network, no stores — these
    bodies are pure.
  </action>
  <verify>
    <automated>cd databasise &amp;&amp; uv run pytest -q tests/parts_core/lightrag/test_transform_parts.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd databasise && uv run pytest -q tests/parts_core/lightrag/test_transform_parts.py -x` passes.
    - The suite covers all three join configurations drawn from the committed wiring files, not from
      hand-written config dicts — read the config out of `databasise/wirings/lightrag/base.json` and
      its patches so a config drift breaks the test.
    - The suite includes a case asserting an arity mismatch raises.
    - The suite includes a case asserting a re-projected item carries `derived_from`.
    - `cd databasise && uv run python -c "
      from databasise.parts_core.lightrag import LIGHTRAG_PARTS
      print(len(LIGHTRAG_PARTS), 'parts registered')"` reports 13 parts registered.
  </acceptance_criteria>
  <done>One join component serves all three positions, ordering semantics match §L.2, and provenance
  survives every merge.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `truncator-token-budget` and `chunk-selector-kg`</name>
  <read_first>
    - docs/system-model/wirings/lightrag-base.json — `budget-entities` and `budget-relations`
      (shared component `lightrag/truncator-token-budget`, kind `grader-filter`, empty effects,
      `apportionment_policy` of first-come-over-joined-list, each naming itself as `apportioning_node`)
      and `chunk-sel-kg` (kind `retriever`, effects `reads_kv` and `reads_vector`, config
      `cardinality_bound` unbounded, `pick_method` and `fallback_pick_method`)
    - docs/system-model/CONTRACT.md §9 — budget as a splittable token, metering at declared
      boundaries, and merge-side apportionment; this is what `apportionment_policy` implements
    - docs/system-model/CONTRACT.md §19 — §19.4's rule that a numeric threshold or cap is config and
      not a knob, and §19.9's fallback-reachability rule that `chunk-sel-kg` declares `reads_vector`
      against
    - docs/system-model/PARTS.md §L.1 — the three re-derived cuts, specifically that the chunk-token
      budget was absorbed into `assemble` and `min_rerank_score` into `rerank`; neither is resurrected
      here as a separate node
    - databasise/runner/budget.py — `meter()` and how a node's declared effects gate spend accrual
    - databasise/parts_core/lightrag/join_roundrobin.py — written in Task 1, the upstream of both
      budget nodes
    - databasise/stores/kv.py and databasise/stores/vector.py — the read surfaces `chunk-sel-kg` uses
    - v1/lightrag/operate.py — the upstream token-truncation and KG chunk-selection logic, located by
      search; and `v1/lightrag/constants.py`, where §L.2's two-lever ruling was code-verified
  </read_first>
  <files>databasise/parts_core/lightrag/truncator_token_budget.py, databasise/parts_core/lightrag/chunk_sel_kg.py, databasise/parts_core/lightrag/__init__.py, databasise/tests/parts_core/lightrag/test_transform_parts.py</files>
  <behavior>
    - `truncator-token-budget` truncates its joined input list to the configured token allowance,
      first-come over the joined list, and reports how much of the allowance it consumed.
    - Truncation preserves input order; it removes from the tail, never re-sorts.
    - An allowance smaller than the first item's token cost yields an empty list and a reported
      consumption of zero rather than a partial item.
    - `chunk-sel-kg` selects chunks by the configured `pick_method`, falls back to
      `fallback_pick_method` when the primary method yields nothing, and reports which method it used.
    - `chunk-sel-kg` reaches both `ctx.stores["kv"]` and `ctx.stores["vector"]` under its two declared
      effects, and its Part declares `reads_vector` even in a weight-only configuration.
    - Neither budget node reaches any store or client.
  </behavior>
  <action>
    Write `databasise/parts_core/lightrag/truncator_token_budget.py` exporting one module-level `Part`
    for component `lightrag/truncator-token-budget`, kind `grader-filter`, effects empty. One body
    serves both `budget-entities` and `budget-relations`; the difference is config. Implement
    first-come-over-joined-list apportionment against the token allowance from config, and report the
    consumed share in the body's return so `databasise/runner/budget.py`'s metering sees a real number
    rather than inferring one. Truncate from the tail and never re-sort — a re-sort here changes
    ranking, and ranking is exactly what the retrieval-level comparison measures.

    Write `databasise/parts_core/lightrag/chunk_sel_kg.py` exporting one module-level `Part` for
    component `lightrag/chunk-selector-kg`, kind `retriever`, effects `reads_kv` and `reads_vector`.
    Implement both pick methods v1 implements, selected by `config.pick_method` with
    `config.fallback_pick_method` used when the primary yields nothing, and report which method
    actually ran so a fall-through is visible in the run record rather than silent. The Part declares
    `reads_vector` unconditionally, including under a weight-only configuration — §19.9's
    fallback-reachability rule, and the precedent §L.2 states in as many words. Do not make the
    declaration conditional on the config: a config-conditional declaration is the failure mode the
    rule exists to prevent.

    Do not add a separate node for the chunk-token budget or for a minimum rerank score. §L.1's three
    re-derived cuts absorbed the first into `assemble` and the second into `rerank`'s guarded scope,
    the verdict was blind-confirmed, and re-deriving the cut is out of scope.

    Extend `LIGHTRAG_PARTS` and extend the transform test module with the per-behaviour cases, drawing
    configuration from the committed wiring files rather than hand-written dicts.
  </action>
  <verify>
    <automated>cd databasise &amp;&amp; uv run pytest -q tests/parts_core/lightrag/ -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd databasise && uv run pytest -q tests/parts_core/lightrag/ -x` passes.
    - The suite includes a case asserting truncation removes from the tail and preserves input order.
    - The suite includes a case asserting an allowance below the first item's cost yields an empty list.
    - The suite includes a case asserting the `chunk-sel-kg` Part declares `reads_vector` when built
      with a weight-only config.
    - The suite includes a case asserting the fall-through path reports which pick method ran.
    - `cd databasise && uv run python -c "
      from databasise.parts_core.lightrag import LIGHTRAG_PARTS
      print(len(LIGHTRAG_PARTS), 'parts registered')"` reports 15 parts registered.
  </acceptance_criteria>
  <done>All fifteen components are registered with real bodies, covering all eighteen §L.1 positions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Base and all five arms — parse clean and conform to their published node id sets</name>
  <read_first>
    - databasise/wirings/lightrag/base.json and all five `arm-*.json-patch.json` files, written in
      plan 03-04
    - docs/system-model/wirings/lightrag-arm-*.json-patch.json — each patch's own
      `resulting_node_id_set` field, which is the published expectation this test compares against
    - databasise/wirings/resolve.py — `resolve_arm` and `resolved_node_ids`, written in plan 03-04
    - databasise/validator/parse.py — `parse_wiring`'s violation codes and the `ParsedWiring.report`
      shape a conformance test reads
    - databasise/validator/depth.py, execution_mode.py, blast_radius.py — the derived properties every
      arm must compute cleanly
    - databasise/evidence/falsifier2.py — the "load named wirings, compute through the real validator
      functions, never a reimplementation" style this test follows
  </read_first>
  <files>databasise/tests/parity/test_arm_conformance.py</files>
  <behavior>
    - The unpatched `mix` base parses with zero violations against `default_registry()`.
    - Each of the five arms resolves to exactly the node id set its own published patch file declares
      in `resulting_node_id_set`.
    - Each of the five arms parses with zero violations.
    - Every node in every arm derives an `execution_mode` of `in-process` and an `effective_depth` of
      `stage`, except `embedder-index`, whose effective depth is `opaque`.
    - `blast_radius_violations` reports nothing for the base or for any arm.
    - The union of node ids across all five arms plus the base equals the eighteen positions §L.1
      publishes.
  </behavior>
  <action>
    Write `databasise/tests/parity/test_arm_conformance.py` covering every behavior above. Compute
    every derived property through the real validator functions —
    `databasise.validator.depth.effective_depth`, `databasise.validator.execution_mode.derive_execution_mode`,
    `databasise.validator.blast_radius.blast_radius_violations` — never a reimplementation, exactly the
    discipline `databasise/evidence/falsifier2.py` already follows for the Falsifier 2 evidence.

    Read each arm's expected node id set out of the committed patch file's own `resulting_node_id_set`
    field rather than hard-coding it in the test, so the test compares the mechanism against the
    published expectation instead of against a second hand-transcription of it.

    Assert the base parses clean with no arm applied. CONTRACT requires the base be independently
    validated, and until this plan landed it could not be — eleven of its components were unregistered.
    This assertion is the first time criterion 1 is checkable in full.

    Assert the eighteen-position union. This is criterion 1's own count, and it is what distinguishes
    "the arms run" from "every published position is exercised by some arm".

    If any arm fails to parse, do not adjust the wiring content to make it pass. The wiring content was
    copied from a published, mechanically-checked set; a parse failure means either a part's declared
    effects disagree with the wiring's, or a derived field leaked into a node — both of which are fixed
    on the part or the copy edit, not by re-deriving the published table.
  </action>
  <verify>
    <automated>cd databasise &amp;&amp; uv run pytest -q tests/parity/test_arm_conformance.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd databasise && uv run pytest -q tests/parity/test_arm_conformance.py -x` passes.
    - `cd databasise && uv run python -c "
      from databasise.parts.registry import default_registry
      from databasise.validator.parse import parse_wiring
      from databasise.wirings.resolve import resolve_arm, load_base
      r = parse_wiring(load_base(), default_registry())
      assert r.report.ok, [v.code for v in r.report.violations]
      ids = set()
      for arm in ('mix','hybrid','local','global','naive','bypass'):
          p = parse_wiring(resolve_arm(arm), default_registry()) if arm != 'mix' else r
          assert p.report.ok, (arm, [v.code for v in p.report.violations])
          ids |= set(p.nodes)
      assert len(ids) == 18, sorted(ids)
      print('18 positions, 5 arms + base, all parse clean')"` prints the success line.
    - `cd databasise && uv run pytest -q` shows no new failures against the pre-plan baseline.
  </acceptance_criteria>
  <done>Every published position resolves against a registered part, and every arm produces the node
  id set its own patch file declares.</done>
</task>

</tasks>

## Artifacts this phase produces

New symbols and files created by this plan:

- `databasise/parts_core/lightrag/join_roundrobin.py`, `truncator_token_budget.py`, `chunk_sel_kg.py`,
  each exporting a module-level `Part` constant
- Component names registered here: `lightrag/join-roundrobin`, `lightrag/truncator-token-budget`,
  `lightrag/chunk-selector-kg`
- `databasise/tests/parts_core/lightrag/test_transform_parts.py`
- `databasise/tests/parity/test_arm_conformance.py`

## Flagged planner assumptions

- **Unresolved probe edge — MODAL-01.** Classified `unclassified`, therefore `unresolved`. Recorded,
  not auto-resolved and not dropped.
- **`resolved_node_ids` and `load_base` are assumed to exist on `databasise/wirings/resolve.py`** from
  plan 03-04. If the tracer named them differently, adjust this plan's verify commands to the real
  names rather than adding aliases.
- **PARTS defects D1, D2, D7, and D12 stay unrepaired** per CONTEXT.md. The join and budget bodies are
  the most likely places to trip over D1 (write-effect asymmetry) and D12 (the priority axis with no
  home in §14.3); note either in the summary if it surfaces, do not repair it here.

<verification>
- `cd databasise && uv run pytest -q` green.
- The base and all five arms parse with zero violations.
- The eighteen-position union is asserted, not claimed.
- Fifteen components are registered with real bodies.
</verification>

<success_criteria>
- All eighteen §L.1 positions resolve against registered primitive parts.
- Every arm resolves to exactly its published node id set.
- One join component serves three positions and one truncator serves two — positions, not identities.
</success_criteria>

<output>
Create `.planning/phases/03-lightrag-query-side/03-06-SUMMARY.md` when done.
</output>
