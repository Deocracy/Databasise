---
phase: 03-lightrag-query-side
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03-lightrag-query-side/03-GATE-AMENDMENT.md
  - .planning/phases/03-lightrag-query-side/COVERAGE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [MACH-02, MACH-03]

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The MACH-02 / MACH-03 deferral is recorded in a written, owner-visible amendment naming the receiving phase, its point of first need, and the residual risk in D-11's own terms — never a silent slide (D-11)."
    - "The residual risk is stated plainly: answer-level drift originating in `keywords` and `generate` stays unmeasured until an A/A floor exists (D-11)."
    - "REQUIREMENTS.md and ROADMAP.md follow the amendment, so requirement coverage tracking does not read Phase 3 as owing an eval bundle it is not building (D-11)."
    - "The external model API's capability surface is enumerated with a decision per capability, and every opt-out carries a one-line reason — including D-09's rerank opt-out."
  artifacts:
    - .planning/phases/03-lightrag-query-side/03-GATE-AMENDMENT.md
    - .planning/phases/03-lightrag-query-side/COVERAGE.md
  key_links:
    - "The amendment cites `docs/system-model/D-VARIANTS/SELECTION.md` as the governing document, exactly as `02-GATE-01-WAIVER.md` does — `docs/system-model/` is a verbatim upstream mirror and is never edited."
    - "ROADMAP.md Phase 3 criteria 4 and 5, and REQUIREMENTS.md's MACH-02 / MACH-03 rows plus their phase-assignment table entries, all point at the same amendment file."
  prohibitions:
    - "The parity harness must not emit a pass or fail verdict when the index-identity preconditions failed; it must emit `inconclusive`."
    - "A variance band must not be presented without its run count and cache status recorded alongside it, and must not be computed from cache-served re-runs."
    - "The declared-deviation record must not carry a blanket or catch-all category; every excursion must be named individually with its own cause."
    - "A node that made a real LLM, embedding, or rerank call must not report zero spend; an unmetered real call is a refusal, not a free node."
    - "The v1 original arm must not be presented with a RIG §TR.1-shaped run record it did not produce; wherever its numbers appear, the trace asymmetry must be stated."
    - "The MACH-02/MACH-03 deferral must not be open-ended or silent, and must never be recorded as passed, satisfied, or not-applicable."
    - "The storage-ownership audit must not report a node that touched no store as audited-compliant; a no-touch node must be reported distinctly from a node whose touches matched its declarations."
    - "Regenerating Falsifier 2's committed evidence must not silently change its recorded verdicts; a changed verdict is a gate regression to surface, never an updated file."
---

<objective>
Record the second deferral of MACH-02 and MACH-03 as a written, owner-visible amendment, propagate it
into ROADMAP.md and REQUIREMENTS.md, and enumerate the external model API's capability surface with a
decision per capability.

Purpose: GATE-01's own precedent exists to prevent exactly the failure mode a silent deferral is —
`02-GATE-01-WAIVER.md` moved MACH-02 and MACH-03 into Phase 3 by a written record naming a point of
first need. D-10 moves them once more, to Phase 6's side-by-side run, on a technical argument rather
than a scheduling one. REQUIREMENTS.md and ROADMAP.md still assign both to Phase 3 by number, so
without this plan the phase either appears to owe an eval bundle it is not building or quietly drops
one (D-11).

Output: `03-GATE-AMENDMENT.md`, the two tracking files amended to match, and a `COVERAGE.md` matrix
for the OpenAI-compatible surface this phase integrates.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-lightrag-query-side/03-CONTEXT.md
@.planning/phases/02-falsifier-gate/02-GATE-01-WAIVER.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the MACH-02 / MACH-03 amendment record</name>
  <read_first>
    - .planning/phases/02-falsifier-gate/02-GATE-01-WAIVER.md — the precedent this record mirrors:
      its section order (what is amended, quoting the governing clauses in full; the amendment; what
      is not amended; the standing condition; reversibility; banked decisions; what this authorises;
      decision and date)
    - .planning/phases/03-lightrag-query-side/03-CONTEXT.md — D-10's technical argument, D-11's
      residual-risk wording, and the Deferred Ideas entry
    - .planning/REQUIREMENTS.md — MACH-02's and MACH-03's full text, including the amendment note
      GATE-01 already added to each
    - docs/system-model/RIG.md — §EV.1 to §EV.3 (bundle versioning, both target families,
      target-MDE derivation) and §AA.1 to §AA.3 (calibration procedure, cache-bypass precondition,
      batch-width correction); locate each subsection by search, the file is long
  </read_first>
  <files>.planning/phases/03-lightrag-query-side/03-GATE-AMENDMENT.md</files>
  <action>
    Write `.planning/phases/03-lightrag-query-side/03-GATE-AMENDMENT.md` following
    `02-GATE-01-WAIVER.md`'s structure section for section. It cites into `docs/system-model/` and
    never edits it — that directory is a verbatim upstream mirror of the ratified system model.

    Content, in that order:

    **What is amended.** Quote in full ROADMAP.md Phase 3's success criteria 4 and 5, and name
    `docs/system-model/D-VARIANTS/SELECTION.md` as the governing document on disagreement, exactly as
    GATE-01 did. Quote the specific RIG clauses being re-timed rather than paraphrasing them: §EV.1's
    bundle-versioning rule, §EV.2's both-target-families requirement, and §AA.1's bootstrap-resampled
    p95 floor keyed to `(bundle@v, tier, metric)`. Note that GATE-01 already moved both requirements
    from rung 1 into Phase 3; this is the second move, and say so.

    **The amendment.** MACH-02 and MACH-03 move to Phase 6's side-by-side run. State D-10's reason as
    the technical argument it is, not as a schedule note: with one shared index (D-01) and both arms
    pinned to one model identity (D-07, D-08), everything downstream of keyword extraction is a
    deterministic function of the query embedding and the store contents, so the retrieval-level
    comparison is a sharper instrument for what rung 2 actually tests than an answer-level A/A floor
    would be. Name the point of first need explicitly: Phase 6's cross-modality run, where no
    retrieval-level identity exists to lean on.

    **What is substituted.** Criterion 6's deterministic, zero-token retrieval-level gate is Phase 3's
    parity instrument, joined by D-12's N-run variance band over `keywords`' own output — which is how
    criterion 2's "N-run variance band rather than a single diff" is satisfied without an eval bundle.

    **Residual risk, named plainly and in D-11's own words:** answer-level drift originating in
    `keywords` and `generate` stays unmeasured until a floor exists. Do not soften this and do not
    bury it in a subordinate clause.

    **What is not amended.** Condition 6's substance — no promotion or parity claim rides on an
    unmeasured comparison — is re-timed, not withdrawn, and GATE-01's standing condition continues to
    hold: no promotion decision and no parity claim is taken before the floor exists.

    **Reversibility.** D-10's own rating: reversible. RIG §EV and §AA fully specify the bundle and
    calibration, and Phase 2's banked decisions D-06 through D-13 hold every parameter, so standing it
    up in Phase 6 costs nothing that standing it up now would have saved. Reproduce the banked
    decisions by reference to `02-GATE-01-WAIVER.md`'s own list rather than restating them, and carry
    forward D-08's `one-way` rating on the judge and D-10's `costly` rating on declared concurrency.

    **What this authorises.** The tracking changes Task 2 makes.

    **Decision and date**, in GATE-01's own format, attributing the decision to the provenance
    CONTEXT.md records: the owner directed "no area needs covered, go with recommendations" on
    2026-08-31, so D-10 and D-11 are Claude's calls made under that direction — state this, so a later
    reader does not mistake the deferral for an owner-originated position.
  </action>
  <verify>
    <automated>test -f .planning/phases/03-lightrag-query-side/03-GATE-AMENDMENT.md &amp;&amp; grep -q "Phase 6" .planning/phases/03-lightrag-query-side/03-GATE-AMENDMENT.md &amp;&amp; grep -q "SELECTION.md" .planning/phases/03-lightrag-query-side/03-GATE-AMENDMENT.md &amp;&amp; grep -qi "residual risk" .planning/phases/03-lightrag-query-side/03-GATE-AMENDMENT.md &amp;&amp; echo AMENDMENT_OK</automated>
  </verify>
  <acceptance_criteria>
    - The file exists and the verify command prints `AMENDMENT_OK`.
    - It quotes ROADMAP.md Phase 3 criteria 4 and 5 verbatim.
    - It names Phase 6 as the receiving phase and Phase 6's cross-modality run as the point of first
      need — no open-ended language.
    - It states the residual risk naming both `keywords` and `generate`.
    - It cites `docs/system-model/D-VARIANTS/SELECTION.md`, and `git status --short docs/` shows no
      modification under `docs/system-model/`.
    - It records the deferral as deferred; the words "passed", "satisfied", and "not applicable" do
      not appear as the disposition of either requirement.
  </acceptance_criteria>
  <done>A second author reading only this file knows what was required, what is substituted, why, where
  it moved to, and what is left unmeasured until it lands.</done>
  <reversibility rating="reversible">D-10's own rating. RIG §EV and §AA fully specify the deferred
  work and Phase 2 banked every parameter, so the deferral costs nothing that building it now would
  have saved.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Propagate the amendment into ROADMAP.md and REQUIREMENTS.md</name>
  <read_first>
    - .planning/phases/03-lightrag-query-side/03-GATE-AMENDMENT.md — written in Task 1
    - .planning/ROADMAP.md — the Phase 3 section, its six success criteria, and the Overview
      paragraph's sentence placing the A/A floor at Phase 3
    - .planning/REQUIREMENTS.md — the MACH-02 and MACH-03 bullet text with GATE-01's existing
      amendment note, and the phase-assignment table rows near the end of the file
  </read_first>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <action>
    Use scoped `Edit` replacements only. A whole-file rewrite of either document destroys entries
    outside the change window, and both files carry content for six other phases.

    In `.planning/ROADMAP.md`: mark Phase 3's success criteria 4 and 5 as amended in place, in the
    same style the ROADMAP already uses for Phase 1 criterion 1 (`*(amended YYYY-MM-DD per ...)*`),
    citing `.planning/phases/03-lightrag-query-side/03-GATE-AMENDMENT.md` and naming Phase 6 as the
    receiving phase. Keep the original criterion text visible rather than deleting it — the ROADMAP's
    existing amendment style preserves what was superseded. Update the Overview paragraph's sentence
    that currently places the calibrated A/A noise floor at Phase 3's parity comparison so it names
    Phase 6 and cites the new amendment alongside the GATE-01 waiver it already cites. Add a criterion
    to Phase 6's success criteria carrying MACH-02 and MACH-03's obligation forward, so the
    requirement lands somewhere concrete rather than merely leaving Phase 3.

    In `.planning/REQUIREMENTS.md`: extend MACH-02's and MACH-03's existing bracketed amendment notes
    with the second move — same format as the existing `[Amended 2026-08-31: moved to Phase 3 ...]`
    note, now naming Phase 6 and citing `03-GATE-AMENDMENT.md`. Change both rows in the
    phase-assignment table from Phase 3 to Phase 6, leaving their status column as `Pending`.

    Do not change MODAL-01 anywhere: it stays assigned to Phase 3 and this phase delivers it.
  </action>
  <verify>
    <automated>grep -n "03-GATE-AMENDMENT" .planning/ROADMAP.md .planning/REQUIREMENTS.md &amp;&amp; grep -E "^\| MACH-0[23] \| Phase 6 \|" .planning/REQUIREMENTS.md &amp;&amp; grep -E "^\| MODAL-01 \| Phase 3 \|" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "03-GATE-AMENDMENT" .planning/ROADMAP.md` returns at least 3 (criteria 4, 5, and the
      Overview sentence).
    - `grep -c "03-GATE-AMENDMENT" .planning/REQUIREMENTS.md` returns at least 2.
    - The phase-assignment table rows for MACH-02 and MACH-03 read `Phase 6`; MODAL-01 still reads
      `Phase 3`.
    - Phase 6's success criteria in ROADMAP.md now carry the eval-bundle and A/A-calibration
      obligation.
    - `git diff --stat .planning/ROADMAP.md` shows a bounded diff confined to the Phase 3 section,
      the Phase 6 section, and the Overview paragraph — no other phase's entries changed.
  </acceptance_criteria>
  <done>Requirement tracking reads the same story the amendment tells: MACH-02 and MACH-03 land in
  Phase 6, MODAL-01 lands here.</done>
</task>

<task type="auto">
  <name>Task 3: The external model API coverage matrix</name>
  <read_first>
    - .planning/phases/03-lightrag-query-side/03-CONTEXT.md — D-06 through D-09: one client shape,
      OpenAI-compatible chat-completions plus embeddings, provider-pinned generator, local embedder,
      rerank off in the parity arm
    - .planning/phases/03-lightrag-query-side/03-RESEARCH.md — the Standard Stack entry recording
      that OpenRouter and Ollama both speak the same OpenAI-compatible shape and that v1 is
      configured through env vars that speak it too
  </read_first>
  <files>.planning/phases/03-lightrag-query-side/COVERAGE.md</files>
  <action>
    Write `.planning/phases/03-lightrag-query-side/COVERAGE.md` as a markdown table with the header
    `# API Coverage — OpenAI-compatible model endpoint (OpenRouter, Ollama)` and columns
    `capability | decision | reason`, where `decision` is either `INTEGRATE` or `OPT-OUT` and every
    `OPT-OUT` carries a one-line reason. Full coverage is the default; the table is the subtraction
    record.

    Enumerate the capability surface of an OpenAI-compatible endpoint and decide each one. The
    decisions this phase's own settled context already fixes:

    - `chat.completions` (non-streaming) — INTEGRATE; the `keywords` and `generate` nodes call it.
    - `embeddings` — INTEGRATE; `embedder-query` and `embedder-index` call it.
    - response `usage` / token accounting — INTEGRATE; MACH-05 meters spend at the declared boundary
      and a node reporting zero spend for a real call is a refusal.
    - provider routing / provider pinning — INTEGRATE; Phase 2 D-07 pins the provider because a
      re-routed provider changes identity mid-run.
    - deterministic sampling parameters (temperature, seed) — INTEGRATE; both arms are handed the
      same sampling parameters from one pinned record (D-07).
    - `chat.completions` streaming — OPT-OUT; the parity comparison reads completed outputs, and the
      streaming transport is Phase 4's §18 seam concern.
    - rerank — OPT-OUT; D-09 keeps the node and its `calls_rerank` declaration but configures it as a
      pass-through, because standing up a rerank provider adds a cost source and a variance source to
      the one comparison meant to isolate decomposition.
    - model catalogue listing — OPT-OUT; model identity is derived from the provider and model the
      response actually returns, never from a catalogue lookup (Phase 1 D-12, Phase 2 D-08).
    - tool / function calling — OPT-OUT; no §L.1 query-side position calls a tool.
    - structured-output / JSON mode — OPT-OUT; the ported keyword extractor parses v1's own output
      shape, and matching v1 is what parity requires.
    - logprobs — OPT-OUT; nothing in the retrieval-level comparison reads them.

    Decide the remaining capabilities the endpoint family exposes and that this list does not already
    name — images, audio, files, fine-tuning, batch, moderations, and the responses API — rather than
    omitting them; each is an `OPT-OUT` and each needs its own one-line reason. An opt-out without a
    reason is the un-decided hole this matrix exists to close.
  </action>
  <verify>
    <automated>cd . &amp;&amp; python3 -c "
import re,sys
rows=[l for l in open('.planning/phases/03-lightrag-query-side/COVERAGE.md') if l.strip().startswith('|') and ('INTEGRATE' in l or 'OPT-OUT' in l)]
assert rows, 'no decision rows found'
bad=[r for r in rows if 'OPT-OUT' in r and len(r.split('|')[3].strip())&lt;10]
print('rows',len(rows),'unreasoned opt-outs',len(bad))
sys.exit(1 if bad else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0 and reports at least 15 decision rows and 0 unreasoned opt-outs.
    - Every row's `decision` cell is exactly `INTEGRATE` or `OPT-OUT`.
    - The rerank row is `OPT-OUT` and its reason cites D-09.
    - `chat.completions` (non-streaming), `embeddings`, and response `usage` are all `INTEGRATE`.
  </acceptance_criteria>
  <done>Every capability of the external model surface carries a recorded decision, and every opt-out
  carries a reason a later reader can weigh.</done>
</task>

</tasks>

## Artifacts this phase produces

New files created by this plan:

- `.planning/phases/03-lightrag-query-side/03-GATE-AMENDMENT.md` — the MACH-02 / MACH-03 deferral
  record
- `.planning/phases/03-lightrag-query-side/COVERAGE.md` — the OpenAI-compatible capability matrix

No new code symbols. `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md` are edited in place with
scoped replacements.

## Flagged planner assumptions

- **Unresolved probe edge — MACH-02.** Classified `unclassified` by the deterministic edge probe,
  therefore `unresolved`. Recorded here, not auto-resolved and not dropped. Note the tension this
  plan resolves: the ROADMAP still lists MACH-02 as a Phase 3 success criterion while CONTEXT.md D-10
  defers it — the amendment is what makes the two agree.
- **Unresolved probe edge — MACH-03.** Same disposition, same resolution path.
- **Unresolved probe edge — MODAL-01.** Recorded in every plan of this phase; delivered by plans
  03-01, 03-02, and 03-04 through 03-09.
- **The API-coverage detector returned `detected: false`** when run over CONTEXT.md plus the ROADMAP
  phase section at planning time. Task 3 produces the matrix anyway, because the phase genuinely does
  integrate an external model API (D-06 and D-07) and the seal-time gate re-runs the detector over the
  written plan bodies, which name the surface explicitly. Producing the matrix is both the honest call
  and the one that cannot block the seal.

<verification>
- The amendment file exists and names Phase 6, the point of first need, and the residual risk.
- ROADMAP.md and REQUIREMENTS.md both cite it, and neither carries MACH-02 or MACH-03 against Phase 3
  any more.
- `COVERAGE.md` has no unreasoned opt-out.
- `git status --short docs/` is empty — the upstream mirror was not edited.
</verification>

<success_criteria>
- The second deferral of MACH-02 and MACH-03 is a written, owner-visible record, not a silent slide.
- Requirement tracking follows the amendment.
- The external model API's capability surface is enumerated and decided.
</success_criteria>

<output>
Create `.planning/phases/03-lightrag-query-side/03-03-SUMMARY.md` when done.
</output>
