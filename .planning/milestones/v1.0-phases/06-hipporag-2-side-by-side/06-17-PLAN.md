---
phase: 06-hipporag-2-side-by-side
plan: 17
type: execute
wave: 3
gap_closure: true
closes:
  - "SC6 / MACH-03 — the A/A calibration spend itself, now that both preconditions and the driver exist"
  - "06-VERIFICATION.md's stale rows — five findings the report records as open that later commits closed"
depends_on: ["06-15", "06-16"]
files_modified:
  - databasise/evidence/FALSIFIER-5-EVIDENCE.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/06-hipporag-2-side-by-side/06-VERIFICATION.md
autonomous: false
requirements: [MACH-03, MACH-02]

estimate:
  tokens: 20000
  raw_tokens: 40000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "The owner is asked exactly once, with a real call-count projection produced by `aa_run --split dev` rather than an estimate written into the question by hand, and with a named decline option."
    - "The `materially narrower` threshold is written into `databasise/evidence/FALSIFIER-5-EVIDENCE.md` before either floor is computed, never chosen after seeing them."
    - "Whatever the owner answers, `databasise/evidence/FALSIFIER-5-EVIDENCE.md` gains one dated section appended below its existing records; the 2026-09-09 and 2026-09-10 sections are never rewritten."
    - "MACH-03 flips to Complete only if both target families produced a real floor from a completed pair of passes and the pre-registered threshold was read afterwards; one family alone, a refused calibration, or a decline leaves the row Pending."
    - "`06-VERIFICATION.md` gains a dated correction note naming, per row, the commit that closed it — and its `status`, `score` and every existing gap body are left exactly as the verifier wrote them."
    - "Falsifier 5 is recorded as open rather than failed unless a real comparison ran and returned an adverse result; a run that did not happen produces no verdict of any kind."
  artifacts:
    - databasise/evidence/FALSIFIER-5-EVIDENCE.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/06-hipporag-2-side-by-side/06-VERIFICATION.md
  key_links:
    - "06-16's `aa_run --split dev` -> a real call-count projection -> the checkpoint question the owner prices"
    - "checkpoint `approve` -> `databasise.eval.remint` -> the next bundle version -> `databasise.eval.corpus_ingest --spend` -> `databasise.eval.aa_run --spend` -> two `CalibrationResult` floors -> the pre-registered threshold -> Falsifier 5's verdict"
    - "checkpoint `decline` -> a dated section naming the date and reason -> MACH-03 stays Pending, Falsifier 5 stays open"
    - "commits 5827165 / 2cb437a / 3de871b and 06-14's own commits -> the dated correction note in 06-VERIFICATION.md"
  prohibitions:
    - "MUST NOT report any floor, count, spend figure, duration, or comparison result for a run that did not complete — including as an illustration, a placeholder, or an estimate presented as a measurement."
    - "MUST NOT choose or adjust the `materially narrower` threshold after either floor has been read."
    - "MUST NOT flip MACH-03 to Complete on a declined, refused, or single-family run, and MUST NOT record Falsifier 5 as failed for a comparison that never ran."
    - "MUST NOT rewrite, reorder, or delete any existing dated section of `FALSIFIER-5-EVIDENCE.md`, nor alter `06-VERIFICATION.md`'s `status`, `score`, or any existing gap body."
    - "MUST NOT ask the owner this question more than once, and MUST NOT ask a narrowed version of it after a decline."
---

<objective>
Ask for the A/A calibration spend a third time — this time with a driver that can actually consume
it — and leave the phase's own record honest either way.

**Why the previous two asks were premature, stated plainly.** At 06-06 the owner declined against
two named preconditions. At 06-13 they declined again, reasoning that the `fact-score` defect
should be fixed first so the same class of failure would not waste the larger spend. Both
declines were correct, and there was a third reason nobody had noticed: **there was no A/A run
driver.** `calibrate_aa_floor` had no caller outside its own tests, and nothing in the repository
produced the per-question score mappings it consumes. An `approve` at 06-13 would have funded a
run with no code to perform it. 06-14 fixed the defect; 06-16 built the driver; this plan asks the
question for the first time with everything behind it real.

**The threshold is pre-registered before the floors are read.** MACH-03's pass criterion is a
comparison — T1's null materially narrower than T0's — and a threshold chosen after seeing two
numbers is not a test. `06-RESEARCH.md`'s Open Question 1 carries the standing recommended default
and it goes into the evidence document *before* `aa_run --spend` is invoked, not after.

**A decline is a complete, successful outcome.** So is a refusal from `calibrate_aa_floor`'s own
cache-bypass precondition. Falsifier 5 stays *open* in either case — not failed, because no
comparison ran and there is no adverse verdict to record.

**The second task is bookkeeping the phase owes itself.** `06-VERIFICATION.md` was written against
commit `cc3abf3` and records five findings as open that three later commits closed. Correcting that
is an append of a dated note naming the closing commit per row; it is not a re-verification, and it
does not touch the report's own verdict or score — `/gsd-verify-work` owns those.

Purpose: roadmap Success Criterion 6 and MACH-03 / Falsifier 5 — the gate Phase 7's promotion
evidence rides on.
Output: one dated appended section in `databasise/evidence/FALSIFIER-5-EVIDENCE.md`, a MACH-03 row
that matches what actually happened, and a verification report that no longer reads as open on
things that are closed.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-hipporag-2-side-by-side/06-VERIFICATION.md
@.planning/phases/06-hipporag-2-side-by-side/06-REVIEW-FIX.md
@databasise/evidence/FALSIFIER-5-EVIDENCE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Spend decision — the real A/A calibration at both §EV.2 target families (MACH-03, Falsifier 5)</name>

  <read_first>
    - `databasise/evidence/FALSIFIER-5-EVIDENCE.md` — the whole file: MACH-03's pass criterion quoted verbatim, the two-blocker findings table, `## Two preconditions that must hold before this run can happen`, the `**Limits, stated plainly:**` list (including its refusal to pre-register a threshold against floors that do not exist, which this plan now discharges), and the `## Deferred again — 2026-09-10` section
    - `.planning/phases/06-hipporag-2-side-by-side/06-16-SUMMARY.md` — what the driver actually does, the committed bundle's own `dev` split size as observed, and the thirteen tests' results; the checkpoint question below must describe the driver as it landed, not as it was planned
    - `databasise/eval/aa_run.py` — as built by 06-16: `estimate_calls`, `calibrate_family`, `main`'s flags and exit codes, `SplitNotCalibratableError`, `UnparseableJudgeVerdictError`, and the metric constants that key every `NullIdentity`
    - `databasise/eval/remint.py` and `databasise/eval/corpus_ingest.py` — `main`'s flags and exit codes on both, `resolve_judge_identity`'s refusal to substitute a declared id, `estimate`'s self-labelling projection fields, and `LimitExceedsCorpusError`
    - `databasise/eval/calibration.py` — `calibrate_aa_floor`'s §AA.2 cache-bypass refusal (`UnusableFloorError`) and `CalibrationResult.floor` / `n_questions` / `n_resamples` / `seed`, which are the only fields a recorded floor may cite
    - `.planning/phases/06-hipporag-2-side-by-side/06-RESEARCH.md` — Open Question 1's standing recommended default for "materially narrower" (T1's calibrated p95 floor, in the same score units, at most half of T0's); this is the threshold to pre-register, not to re-derive
    - `databasise/evidence/EVAL-BUNDLE-V1.md` — the §EV.2 target-family table's cost bands and the split sizes (dev 17, holdout 9, sealed 4)
    - `.planning/REQUIREMENTS.md` line 17 — MACH-03's row and its three existing annotations, and line 16 — MACH-02's row, which stays Complete and untouched
  </read_first>

  <files>databasise/evidence/FALSIFIER-5-EVIDENCE.md, .planning/REQUIREMENTS.md</files>

  <precondition>Four things must hold before the owner is asked, and an unmet one makes the question moot: (1) `cd databasise && uv run pytest -q tests/eval/ -x` exits 0, so both 06-16 suites and the four pre-existing ones are green; (2) `cd databasise && uv run python -m databasise.eval.aa_run --split dev` exits 0 and prints a real projection, proving the driver runs and the committed bundle's `dev` split is readable; (3) `v1/.env.parity` exists and carries every key `databasise.parity.run_arm._build_clients` requires; (4) `databasise/evidence/eval-bundles/` holds a minted bundle whose `dev` split has at least two question ids. Assert all four first; on any failure, report the unmet precondition and stop rather than asking the owner to spend against a tree that cannot consume it.</precondition>

  <action>
**Before asking, pre-register the threshold, in writing, in the evidence document.** Append a
`## Pre-registered threshold — <ISO date>` section to `databasise/evidence/FALSIFIER-5-EVIDENCE.md`
stating the threshold adopted from `06-RESEARCH.md`'s Open Question 1 — **T1's calibrated p95 floor
is at most half of T0's, both read in the same score units** — and stating that both floors are
fractions on the closed unit interval (`gold_passage_recall` is a fraction of the gold document
set; `answer_level_correctness` maps the judge's three verdicts to `1.0` / `0.5` / `0.0`), so the
"same score units" condition holds by construction rather than by assumption. Commit this section
**before** any spend flag is passed to anything. This discharges the outstanding item
`FALSIFIER-5-EVIDENCE.md`'s own Limits list names: a threshold pre-registered against floors that
do not yet exist. Do not adjust it afterwards under any branch.

**Then compute and print the real projection.** Run
`cd databasise && uv run python -m databasise.eval.aa_run --split dev` and show the owner its actual
printed output — the question count, the two passes, and the per-family engine-query and judge-call
counts. Also run `cd databasise && uv run python -m databasise.eval.corpus_ingest --limit 291 --arm
lightrag` and show its actual printed ingest projection. Both are computed output; neither may be
paraphrased into a rounder number in the question below.

**Present exactly this question, once, and stop.**

> **Authorize the real A/A calibration?** (MACH-03 / Falsifier 5 — the gate Phase 7's promotion
> evidence rides on)
>
> **You have declined this twice, and both declines were right.** At 06-06 two preconditions were
> open; at 06-13 the `fact-score` defect was open. Both are now closed — and a third blocker,
> unnoticed until this round, is closed too: **there was no A/A run driver.**
> `calibrate_aa_floor` had no caller outside its own tests and nothing produced the per-question
> scores it consumes, so an `approve` at 06-13 would have funded a run with no code to perform it.
> `databasise/eval/aa_run.py` now exists, is tested, and runs dry by default.
>
> **What would run, in order:** (1) `databasise.eval.remint` makes one minimal live model call to
> resolve the judge identity and mints the next bundle version carrying it — `bundle@v1` is left
> byte-identical; (2) `databasise.eval.corpus_ingest --limit N --arm lightrag --spend` ingests the
> first N of the 291 eval-corpus documents; (3) `databasise.eval.aa_run --split dev --spend` runs
> the same arm twice over the dev split at both target families and bootstrap-resamples a p95 floor
> for each.
>
> **Projected cost — the printed output above, not a paraphrase of it.** The gold-passage leg makes
> engine queries and zero judge calls; the answer-level leg makes one judge call per question per
> pass on top of the same queries, which is why it is the expensive one. The ingest projection is
> `corpus_ingest`'s own printed figure, carrying its own `chars_per_token` divisor and labelled a
> projection rather than a bill. No currency figure is quoted here because this project has no
> measured per-question token figure to derive one from, and inventing a band would be exactly the
> fabricated number these documents exist to refuse.
>
> **What you get for it:** two calibrated floors, and Falsifier 5's first real verdict against the
> threshold already written into the evidence document above.
>
> **Options:** `approve` (run all three now) · `decline` (leave MACH-03 BLOCKED, recorded)

**On `approve`:** run the three commands in the order above, each only if the previous exited 0. A
non-zero exit from any of them is a refusal, not a result: capture its message verbatim, stop the
branch there, and take the refusal branch below. Never retry with a weakened guard, never edit a
guard to make a run pass, and in particular never work around an `UnusableFloorError` — a null
whose cache-bypass status is unknown or positive is not usable as a floor, and that refusal is the
whole point of the instrument.

On a clean run, **append** a `## Real A/A calibration — <ISO date>` section below every existing
section, in this document's own house shape — claim / method / findings / verdict / limits. The
findings table carries one row per target family with only real values read from each
`CalibrationResult`: `identity.bundle_at_v`, `identity.tier`, `identity.metric`,
`identity.determinism_setting`, `identity.concurrency_setting`, `floor`, `n_questions`,
`n_resamples`, `seed`. Then, and only then, read the two floors against the threshold pre-registered
above and state Falsifier 5's verdict. Record honestly what the numbers say, including an adverse
result: **a Falsifier 5 failure is a SELECTION.md-level ladder halt, not a defect to repair**, and
if that is what the numbers show, say so plainly and stop rather than softening it. Then flip
MACH-03 to `- [x]` and `| MACH-03 | Phase 6 | Complete |` in `.planning/REQUIREMENTS.md`,
**appending** a dated `[Measured <date> (06-17-PLAN.md): …]` annotation citing the new evidence
section — never replacing any of the three existing annotations. Leave MACH-02's row exactly as it
is; it is already Complete and this plan does not touch it.

**On a refusal after `approve`:** append a `## Real A/A calibration attempted — refused — <ISO
date>` section carrying the refusal's verbatim message, which command produced it, whether spend
was incurred before it, and an explicit statement that no floor is reported because none was
computed. File the blocker in `.planning/WINDOWS.md` via `node <the gsd-tools path> windows add`.
Leave MACH-03 `Pending` with a dated annotation naming the new blocker, and state that Falsifier 5
remains open rather than failed.

**On `decline`:** append a `## Deferred a third time — <ISO date>` section stating the date, that
the owner declined, the owner's stated reason verbatim if given, and what changed since 2026-09-10:
06-14 closed the `fact-score` defect at its root, and 06-16 built the A/A driver that did not
previously exist. Leave MACH-03 `Pending` with a dated annotation naming the spend as the single
outstanding item, and state that Falsifier 5 remains open rather than failed. The pre-registered
threshold section stays in the document under this branch — it is now a standing commitment for
whichever plan runs the calibration next.

Do not ask this question twice, and do not ask a narrowed version of it after a decline.
  </action>

  <instructions>
    Everything automatable is now genuinely done, which was not true the last two times you were
    asked. 06-12 closed both named preconditions; 06-14 fixed the `fact-score` defect that wasted
    your 2026-09-10 authorization on the sibling run; and 06-16 built the A/A driver that — I have
    to report plainly — did not exist at all when you were asked at 06-13. I have pre-registered the
    "materially narrower" threshold in the evidence document before asking, so neither of us can
    choose it after seeing the numbers, and I have printed the driver's own real call-count
    projection rather than an estimate I wrote by hand.

    The one thing I cannot do is authorize the spend. Reply `approve` to have me run remint, then
    the bounded corpus ingest, then the paired calibration, against live `v1/.env.parity`
    credentials; or `decline` to leave MACH-03 recorded as BLOCKED with today's date and your reason
    appended to the evidence document. Declining is a complete, valid answer — you have given it
    twice and both times it was the right call.
  </instructions>

  <verification>
    Before asking: the pre-registered threshold section is committed to
    `FALSIFIER-5-EVIDENCE.md`, and the two projection commands have been run and their real output
    shown.
    On `approve` and a clean run: all three commands exit 0, and I read back a `floor`,
    `n_questions`, `n_resamples` and a full five-field `NullIdentity` for each of the two families
    from the driver's own returned records before writing anything — then append them, read them
    against the pre-registered threshold, and flip MACH-03.
    On `approve` and a refusal: I record the verbatim message and which command produced it, file
    the blocker, report no floor at all, and leave MACH-03 Pending with Falsifier 5 open.
    On `decline`: I append a dated deferral section containing no floor, count, spend or duration,
    and leave MACH-03 Pending with Falsifier 5 open.
    Under every branch I re-run the evidence-document structural suite.
  </verification>

  <resume-signal>Reply `approve` to authorize the calibration, or `decline` to record the deferral.</resume-signal>

  <acceptance_criteria>
    - `databasise/evidence/FALSIFIER-5-EVIDENCE.md` still contains its original heading `# Falsifier 5 Evidence — MACH-03 (A/A Calibration): BLOCKED, Not Discharged`, its original `## Owner decision: defer-run` heading, and its `## Deferred again — 2026-09-10` heading, all byte-for-byte.
    - The file contains a `## Pre-registered threshold` section dated today, and that section's commit precedes the commit carrying any floor value — verifiable in `git log` order.
    - The file contains exactly one new outcome section dated today (`## Real A/A calibration`, `## Real A/A calibration attempted — refused`, or `## Deferred a third time`), appended after all pre-existing content.
    - If the answer was `approve` and all three commands exited 0: MACH-03 reads `- [x]` and `| MACH-03 | Phase 6 | Complete |`, and the new section carries a findings table with exactly two rows — one per target family — each carrying a `floor`, an `n_questions`, an `n_resamples` and all five `NullIdentity` fields.
    - If the answer was `decline`, or if any command exited non-zero: MACH-03 still reads `- [ ]` and `| MACH-03 | Phase 6 | Pending |`, and the new outcome section reports no floor of any kind.
    - Under every branch the document states that Falsifier 5 is open rather than failed, unless a completed comparison actually returned an adverse result.
    - MACH-02's row is unchanged and still reads Complete.
    - MACH-03 retains all three of its existing annotations under every branch.
  </acceptance_criteria>

  <verify>
    <automated>cd /home/chris/coding/Databasise-2.0-fully-agnostic-system && grep -c '^## Owner decision: defer-run' databasise/evidence/FALSIFIER-5-EVIDENCE.md && grep -c '^## Deferred again . 2026-09-10' databasise/evidence/FALSIFIER-5-EVIDENCE.md && grep -c "^## Pre-registered threshold . $(date +%Y-%m-%d)" databasise/evidence/FALSIFIER-5-EVIDENCE.md</automated>
    <fails_when>non-zero exit from any grep, or any of the three printed counts is 0 — a 0 on the first or second means prior history was rewritten instead of appended to; a 0 on the third means the threshold was never pre-registered</fails_when>
    <automated>cd /home/chris/coding/Databasise-2.0-fully-agnostic-system && git log --oneline -20 -- databasise/evidence/FALSIFIER-5-EVIDENCE.md</automated>
    <fails_when>non-zero exit, or the pre-registered-threshold commit does not appear strictly older than any commit whose subject names a floor or a calibration result — a threshold committed after the numbers is not pre-registered</fails_when>
    <automated>cd /home/chris/coding/Databasise-2.0-fully-agnostic-system && grep -c '| MACH-03 | Phase 6 | \(Complete\|Pending\) |' .planning/REQUIREMENTS.md && grep -c '| MACH-02 | Phase 6 | Complete |' .planning/REQUIREMENTS.md</automated>
    <fails_when>non-zero exit, or the first printed count is not exactly 1 (the MACH-03 row is absent or duplicated), or the second printed count is not exactly 1 (MACH-02 was disturbed by a plan that had no business touching it)</fails_when>
    <automated>cd databasise && uv run pytest -q tests/evidence/test_falsifier5_record.py -x</automated>
    <fails_when>non-zero exit, or the summary line reads "no tests ran" or "0 passed" — the structural assertions over this evidence document must still hold after the append</fails_when>
  </verify>

  <reversibility rating="costly">A real run makes one judge-identity call, ingests up to 291 documents through a live arm, and then makes two full passes over the dev split at both target families — the answer-level leg alone is one judge call per question per pass. That money cannot be un-spent; the minted bundle version and the ingested index are deletable. Rated `costly` rather than `one-way` deliberately: a `one-way` rating would require a preceding `checkpoint:decision`, and this task **is** a blocking human checkpoint over exactly that decision; inserting a second gate would ask the owner the same question twice, which this plan's own prohibitions forbid.</reversibility>

  <done>The threshold was pre-registered and committed before any number existed; the owner has answered once; `FALSIFIER-5-EVIDENCE.md` carries exactly one dated outcome section recording that answer and its real consequences; MACH-03's row matches what actually happened with all three prior annotations intact; and Falsifier 5 is recorded as open unless a completed comparison genuinely returned an adverse result.</done>
</task>

<task type="auto">
  <name>Task 2: Make the phase's own verification record honest about what later commits closed</name>

  <read_first>
    - `.planning/phases/06-hipporag-2-side-by-side/06-VERIFICATION.md` — the whole file: its front matter (`verified`, `status: gaps_found`, `score: 4/6`, `covered_files`, `covered_digest`, `re_verification`), the `gaps:` block's two truth entries with their `reason` / `artifacts` / `missing` bodies, and — further down — the Required Artifacts, Key Links, Data-Flow and Anti-Pattern tables that carry the five stale rows
    - `.planning/phases/06-hipporag-2-side-by-side/06-REVIEW-FIX.md` — the whole file: `status: all_fixed`, the CR-01 and WR-01 entries naming commits `5827165`, `2cb437a` and `3de871b`, the files each touched, and the regression tests each added
    - `.planning/phases/06-hipporag-2-side-by-side/06-REVIEW.md` — the CR-01 and WR-01 findings as originally written, and IN-01 / IN-02 / IN-03, which remain open Info-severity items and are explicitly **not** closed by this note
    - `.planning/phases/06-hipporag-2-side-by-side/06-14-SUMMARY.md` — the commits that closed `.planning/WINDOWS.md` entry id 3 and the `fact-score` blocker named in gap 1's first `missing` item
    - `databasise/seam/refusals.py` and `databasise/seam/engine.py` — `NoRawUploadPathForModalityError` and the guard in `ingest()` that raises it, so the note names what the code actually does rather than what the fix report says it does
  </read_first>

  <files>.planning/phases/06-hipporag-2-side-by-side/06-VERIFICATION.md</files>

  <precondition>Every commit this note will cite is present in `git log` — `5827165`, `2cb437a`, `3de871b`, and 06-14's own commits. Verify each with `git log --oneline` and read `NoRawUploadPathForModalityError` in `databasise/seam/refusals.py` directly. A correction note citing a commit that is not in history, or a refusal that is not in the code, is worse than no note at all.</precondition>

  <action>
Append one `## Correction — findings closed after this report was written (<ISO date>)` section to
the very end of `.planning/phases/06-hipporag-2-side-by-side/06-VERIFICATION.md`. Nothing above it
is touched: not the front matter, not `status`, not `score`, not `covered_files`, not
`covered_digest`, not either `gaps:` entry's `reason` / `artifacts` / `missing` body, and not any
of the finding tables. This is an append and only an append.

The section opens with one sentence stating plainly what it is: this report was written against
commit `cc3abf3`; commits landing after it closed several of the findings recorded here as open;
this note records which, and is **not** a re-verification — `/gsd-verify-work` owns the verdict,
and the `status` and `score` above deliberately still read as the verifier left them.

Then a table with columns `Section | Row | Closed by | What now holds`, carrying exactly six rows:

1. Required Artifacts — the `databasise/seam/engine.py (raw-bytes → HippoRAG safety)` row, recorded
   `✗ MISSING` — closed by `5827165` and `2cb437a`. `Databasise.ingest()` now raises
   `NoRawUploadPathForModalityError` before any node config is stamped when a raw-bytes document
   targets a corpus wiring whose consuming node is not `kind: "opaque"`.
2. Key Links — the `Databasise.ingest(document, selector=<HippoRAG>)` raw-bytes row, recorded
   `✗ NOT_WIRED (silent)` — closed by the same commits. The path now refuses by name rather than
   returning a normal-looking `IngestJob` over zero writes.
3. Data-Flow — the `chunk_embed.py (raw= payload)` row, recorded `✗ HOLLOW_PROP` — closed by the
   same commits, by unreachability: the refusal fires before `chunk-embed` is ever dispatched with
   an empty-text document from a raw upload.
4. Anti-Pattern row 1 — silent content loss, 🛑 Blocker — closed by the same commits, with the
   regression test `06-REVIEW-FIX.md` names.
5. Anti-Pattern row 3 — the `rest.py` mutable-singleton default, ⚠️ Warning — closed by `3de871b`,
   which set `frozen=True` on `_RequestModel`.
6. Gap 1's first `missing` item — the `fact-score` empty-string fix — closed by 06-14, which
   resolved the seven-position `corpus-ingest` wiring for index builds and added
   `EmptyEmbeddingInputError` at the client boundary; `.planning/WINDOWS.md` entry id 3 is `fixed`.

Close the section with two paragraphs. The first states what is **not** closed: gap 1's remaining
`missing` item (a real, completing cross-modality run) and gap 2 in full, each carrying whichever
outcome 06-15 and this plan's own Task 1 actually recorded — read those two SUMMARY files and
report their real branches, never an assumed `approve`. The second states that `06-REVIEW.md`'s
IN-01, IN-02 and IN-03 remain open Info-severity items, deliberately untouched by this round
because none of them is implicated in the `fact-score` blast radius: IN-02 concerns
`FaissVectorStore.self_knn`'s top-k window, which is the synonymy-edges path, while the
`fact-score` defect ran through `score_all` and the wiring-resolution choice — a different method
and a different call site.

Use no emoji beyond the two status glyphs the report's own tables already use, and match the
document's existing heading depth and table style.
  </action>

  <acceptance_criteria>
    - `.planning/phases/06-hipporag-2-side-by-side/06-VERIFICATION.md` front matter still reads `status: gaps_found` and `score: 4/6 must-haves verified`, byte-for-byte.
    - Every line of the file that existed before this task still exists: the front matter's `covered_digest: "v1:sha256:502da9da38e78ba113e9d0a312c0234f5795e0ef2553b9c4c543b5adea6a726e"` line, both `gaps:` truth entries, and every finding table are present unchanged, and the new section is the file's last heading.
    - The new section's table has exactly six data rows, and each cites at least one commit hash present in `git log` or the plan id `06-14`.
    - Every commit hash cited resolves: `git cat-file -e <hash>` succeeds for `5827165`, `2cb437a` and `3de871b`.
    - The section names IN-01, IN-02 and IN-03 as still open, and gives the reason IN-02 is not implicated.
    - The section reports 06-15's and Task 1's real recorded branches, matching those two SUMMARY files.
  </acceptance_criteria>

  <verify>
    <automated>cd /home/chris/coding/Databasise-2.0-fully-agnostic-system && grep -c '^status: gaps_found' .planning/phases/06-hipporag-2-side-by-side/06-VERIFICATION.md</automated>
    <fails_when>non-zero exit, or the printed count is not exactly 1 — the verifier's own verdict line must survive this append untouched</fails_when>
    <automated>cd /home/chris/coding/Databasise-2.0-fully-agnostic-system && grep -c '^score: 4/6 must-haves verified' .planning/phases/06-hipporag-2-side-by-side/06-VERIFICATION.md</automated>
    <fails_when>non-zero exit, or the printed count is not exactly 1 — the score line must survive this append untouched; this task records what closed, it does not re-score the phase</fails_when>
    <automated>cd /home/chris/coding/Databasise-2.0-fully-agnostic-system && git cat-file -e 5827165 && git cat-file -e 2cb437a && git cat-file -e 3de871b && echo COMMITS_RESOLVE</automated>
    <fails_when>non-zero exit, or the literal token COMMITS_RESOLVE is absent from the output — a correction note citing a commit that is not in history is worse than no note</fails_when>
    <automated>cd /home/chris/coding/Databasise-2.0-fully-agnostic-system && grep -c 'covered_digest: "v1:sha256:502da9da38e78ba113e9d0a312c0234f5795e0ef2553b9c4c543b5adea6a726e"' .planning/phases/06-hipporag-2-side-by-side/06-VERIFICATION.md</automated>
    <fails_when>non-zero exit, or the printed count is not exactly 1 — the verifier's own coverage digest must survive the append, and a 0 means content above the append point was rewritten</fails_when>
    <automated>cd /home/chris/coding/Databasise-2.0-fully-agnostic-system && grep -c '^## Correction . findings closed after this report was written' .planning/phases/06-hipporag-2-side-by-side/06-VERIFICATION.md</automated>
    <fails_when>non-zero exit, or the printed count is not exactly 1 — 0 means the note was never written, and more than 1 means a re-run appended a duplicate instead of being idempotent</fails_when>
  </verify>

  <reversibility rating="reversible">A pure append to one planning document, citing commits that already exist. Removing the appended section restores the file byte-for-byte; nothing in the codebase depends on it.</reversibility>

  <done>`06-VERIFICATION.md` carries one dated correction section naming, per row, the commit that closed it, with the report's own `status`, `score` and every existing gap body untouched, and with the three still-open Info findings named as still open.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner's checkpoint answer -> a live, billed multi-stage run | The single authorization that turns a plan into spend crosses here |
| `CalibrationResult.floor` -> a committed evidence document -> Phase 7's promotion gate | A measured number becomes the threshold future promotion claims are priced against |
| a later commit's outcome -> the phase's own verification record | A correction note can quietly become a self-issued passing verdict if it is allowed to touch `status` or `score` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-17-01 | Repudiation (a threshold chosen to fit the numbers) | the pre-registered threshold | critical | mitigate | The threshold section is written and committed before any spend flag is passed; the second `<verify>` command checks `git log` order so a threshold committed after a floor is detectable, and adjusting it afterwards is prohibited under every branch. |
| T-06-17-02 | Repudiation (a fabricated floor) | the new `FALSIFIER-5-EVIDENCE.md` section | critical | mitigate | Every reportable field is enumerated in the action and must be read from a `CalibrationResult`; the decline and refusal branches are required to report no floor at all, and the acceptance criteria assert that absence. |
| T-06-17-03 | Elevation of privilege (a plan issuing its own verdict) | `06-VERIFICATION.md` | high | mitigate | Task 2 is an append only: `status`, `score` and every gap body are pinned by acceptance criteria and by a `git diff --numstat` deletions check, and the note itself must state that it is not a re-verification. |
| T-06-17-04 | Denial of service (spend exhaustion) | remint / corpus_ingest / aa_run | high | mitigate | One invocation each, in order, each conditional on the previous exiting 0, gated behind a single blocking human checkpoint against a real printed call-count projection; a non-zero exit stops the branch, and retrying with a weakened guard is prohibited. |
| T-06-17-05 | Tampering (working around a refusal to obtain a number) | `UnusableFloorError` | critical | mitigate | The action names this refusal specifically and forbids working around it; a null whose cache-bypass status is unknown or positive is not usable as a floor, and the refusal branch records that outcome rather than retrying. |
| T-06-17-06 | Information disclosure | credentials in `v1/.env.parity` | medium | mitigate | Reached only through the existing `run_arm._load_env_file` / `_build_clients` path; no env value is printed, and no credential may appear in either document. |
| T-06-17-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no package and adds no dependency; it runs three already-committed modules and appends to two documents. |
</threat_model>

<artifacts_this_phase_produces>
| Kind | Symbol / path |
|------|---------------|
| Evidence section | `## Pre-registered threshold — <date>` in `databasise/evidence/FALSIFIER-5-EVIDENCE.md` (written under every branch) |
| Evidence section | `## Real A/A calibration — <date>` **or** `## Real A/A calibration attempted — refused — <date>` **or** `## Deferred a third time — <date>` (exactly one of the three) |
| Requirement annotation | `[Measured <date> (06-17-PLAN.md): …]` or `[Deferred <date> (06-17-PLAN.md): …]` on MACH-03 in `.planning/REQUIREMENTS.md` |
| Verification section | `## Correction — findings closed after this report was written (<date>)` in `06-VERIFICATION.md` |
| Ledger row (conditional) | a new `.planning/WINDOWS.md` entry, added only on a refusal after `approve` |
</artifacts_this_phase_produces>

<flagged_assumptions>
- **Edge probe, `MACH-02` (`unclassified`, `unresolved`).** The probe could not classify this
  requirement into an edge category and it is carried forward unresolved rather than auto-resolved
  with a backstop. MACH-02 is already verified Complete (`bundle@v1` minted with both §EV.2
  families); this plan reads that bundle and does not rebuild it, so no acceptance criterion is
  authored from the probe row beyond pinning the row's Complete state against disturbance.
- **`06-RESEARCH.md`'s Open Question 1 default is adopted as the threshold, not re-derived.** RIG
  §AA.1 names no numeric value for "materially narrower". The at-most-half rule is a recommendation
  this plan promotes to a commitment by writing it down first; a future plan wanting a different
  threshold must pre-register it the same way, against floors that do not yet exist.
- **Both floors are treated as being in the same score units.** `gold_passage_recall` is a fraction
  of a gold document set and `answer_level_correctness` maps three verdicts onto `1.0` / `0.5` /
  `0.0`; both are therefore on the closed unit interval. This is stated in the pre-registration
  section as a construction, so a later reader can check it rather than take it on faith.
</flagged_assumptions>

<verification>
- The pre-registered threshold section exists and its commit precedes any commit carrying a floor.
- `FALSIFIER-5-EVIDENCE.md` retains both prior dated sections byte-for-byte and gains exactly one new outcome section.
- MACH-03's row state matches the branch actually taken, with all three prior annotations intact; MACH-02's row is untouched.
- `06-VERIFICATION.md`'s `status` and `score` are unchanged and its diff shows zero deletions.
- `cd databasise && uv run pytest -q tests/evidence/test_falsifier5_record.py -x` passes.
- `cd databasise && uv run pytest -q` passes with no failures.
</verification>

<success_criteria>
- The "materially narrower" threshold was written down before any number existed.
- The owner was asked once, against a real printed call-count projection, with the previously
  missing driver named honestly.
- Exactly one dated outcome section was appended to `databasise/evidence/FALSIFIER-5-EVIDENCE.md`.
- MACH-03 reads Complete if and only if both target families produced a real floor from completed
  passes; Falsifier 5 is recorded as open, never failed, for a comparison that did not run.
- `06-VERIFICATION.md` no longer reads as open on five findings that later commits closed, while
  its verdict and score remain the verifier's own.
</success_criteria>

<output>
Create `.planning/phases/06-hipporag-2-side-by-side/06-17-SUMMARY.md` when done, recording the
branch taken, the owner's answer verbatim, the pre-registered threshold and the commit that carried
it, and — only on a completed calibration — the two floors as actually returned.
</output>
