---
phase: 06-hipporag-2-side-by-side
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - databasise/seam/compare.py
  - databasise/seam/engine.py
  - databasise/seam/refusals.py
  - databasise/seam/rest.py
  - databasise/seam/__init__.py
  - databasise/mcp/tools.py
  - databasise/mcp/server.py
  - databasise/mcp/__init__.py
  - databasise/evidence/F-14-SEAM-INVARIANCE.md
  - databasise/tests/seam/test_compare.py
  - databasise/tests/seam/test_rest_transport.py
  - databasise/tests/mcp/test_tool_growth_invariant.py
  - databasise/tests/mcp/test_dual_transport_parity.py
  - databasise/tests/evidence/test_f14_record.py
  - .planning/phases/05-opaque-side-admission/COVERAGE.md
  - .planning/phases/06-hipporag-2-side-by-side/COVERAGE.md
autonomous: true
requirements: [API-08, MODAL-05]

estimate:
  tokens: 40000
  raw_tokens: 80000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "A caller sends one query object and two selectors and receives a mapping keyed by exactly those two caller-supplied selector values, each holding one §18.2 ResponseEnvelope."
    - "A comparison call carrying exactly one selector returns a bare ResponseEnvelope — the same object `query()` returns — never a one-key mapping."
    - "A comparison call carrying zero selectors is refused by name rather than silently defaulting to the default selector."
    - "No comparison response contains an arm id, a wiring name, a wiring id, a node id, an instance hash, a modality name, or a `provenance` key at any nesting depth."
    - "No comparison response contains any member of §5's eight-verdict vocabulary — promote, reject, inconclusive, insufficient-depth, below-floor, unconfirmed, regression-veto, straddle — as a field name or a field value."
    - "Selector values key the response exactly as the caller supplied them: two selectors differing only in letter case are two distinct keys, and no key is normalised, case-folded or reordered."
    - "The comparison surface is reachable identically in-process, over REST, and over MCP, and the MCP tool roster grew by exactly one name for a genuinely new operation rather than by one name per modality."
    - "F-14's outcome is recorded either way: `databasise/evidence/F-14-SEAM-INVARIANCE.md` states, from a real cross-modality comparison call, either that no consumer-visible envelope field changed, or names the field that did."
  artifacts:
    - databasise/seam/compare.py
    - databasise/evidence/F-14-SEAM-INVARIANCE.md
    - databasise/tests/seam/test_compare.py
    - .planning/phases/06-hipporag-2-side-by-side/COVERAGE.md
  key_links:
    - "Databasise.compare -> the same Databasise._execute per arm -> the same envelope assembly and redaction path query() already uses"
    - "POST /compare and the `compare` MCP tool -> the identical Databasise.compare method, no logic in either transport"
  prohibitions:
    - "MUST NOT return a verdict, a winner, a ranking across arms, or any aggregate score in a comparison response — API-08 is inspection-only, and an aggregate presented at an inspection surface reads as an adjudication no gate authorised."
    - "MUST NOT key a per-arm result by an arm id, wiring name, node id or modality name — §18.2 and §18.4 forbid the machine's internal identity crossing the seam in either direction, and a key is as visible as a field."
    - "MUST NOT build a second scheduler, a second store-resolution path or a second envelope-assembly function for the comparison case — a divergent second path is where a redaction rule silently stops applying."
    - "MUST NOT record F-14 as holding without having made a real cross-modality seam call — the falsifier's whole finding is that nothing had ever run; asserting the outcome from the type definitions would reproduce that failure exactly."
---

<objective>
Land API-08: one query against two or more modalities in one call, returning per-arm results keyed by the caller's own selectors, inspection-only, reachable identically in-process, over REST and over MCP — and record F-14's outcome from the first genuine cross-modality seam call.

Purpose: this is the surface the milestone's core value statement is about. Everything else in the phase exists so this call has two real arms to fan out over.
Output: `Databasise.compare`, a `POST /compare` route, a sixth MCP tool, and a committed F-14 evidence record.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-hipporag-2-side-by-side/06-RESEARCH.md
@.planning/phases/06-hipporag-2-side-by-side/06-PATTERNS.md
@.planning/phases/06-hipporag-2-side-by-side/06-01-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols created by this plan: `databasise/seam/compare.py` (`compare_arms`), `Databasise.compare`, `EmptyComparisonRequestError`, `CompareRequest` (REST DTO), `CompareToolArgs` (MCP DTO), the `compare` MCP tool name, `POST /compare`, `databasise/evidence/F-14-SEAM-INVARIANCE.md`, `.planning/phases/06-hipporag-2-side-by-side/COVERAGE.md`.

Phase-level symbol list: see `06-01-PLAN.md` § "Artifacts this phase produces".

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Databasise.compare — a fan-out over the one existing execution path</name>
  <files>databasise/seam/compare.py, databasise/seam/engine.py, databasise/seam/refusals.py, databasise/seam/__init__.py, databasise/tests/seam/test_compare.py</files>
  <read_first>
    - docs/system-model/CONTRACT.md (§18.2's closed envelope, §18.3's seam-invariance clause, §18.4's four selectors and its "MUST NOT name a wiring, a node id, or an instance hash" rule, §4's `provenance` redaction clause)
    - docs/system-model/RIG.md (§RUN.3's degenerate-width rule — a one-arm fan-out is a valid run and never a valid comparison; §RUN.4's inspection-is-free clause; §AA.4's architecture-comparison mode and why parity-not-gain does not apply here)
    - databasise/seam/engine.py (`Databasise.query`, `Databasise.query_stream`, `_execute`, and this module's docstring on why `_execute` is split out)
    - databasise/seam/selectors.py (`Selector`'s frozen at-most-one-member shape and the instance-hash refusal validator)
    - databasise/seam/refusals.py (the `SeamRefusalError` hierarchy, and 05-LEARNINGS' warning that a refusal raised inside a pydantic validator is converted to `ValidationError` and never reaches the registered handler)
    - databasise/seam/redact.py (the leak gate's own forbidden-token derivation)
    - databasise/tests/seam/test_leak.py (the two-tier leak gate over a complete real envelope — the shape the comparison leak test mirrors)
    - databasise/tests/seam/conftest.py
    - databasise/tests/parts_core/hipporag/conftest.py (`seeded_hipporag_store`, as landed by 06-01)
  </read_first>
  <behavior>
    - Test 1 (`test_keyed_by_selector`): two selectors in, a mapping out whose key set equals the two caller-supplied selector values exactly, each value a `ResponseEnvelope`.
    - Test 2 (`test_single_selector_is_a_run`): one selector in, a bare `ResponseEnvelope` out — `isinstance(result, ResponseEnvelope)` is true and the result is not a mapping.
    - Test 3 (`test_zero_selectors_is_refused`): an empty selector list raises `EmptyComparisonRequestError`, and the refusal message names the empty input without naming any candidate wiring.
    - Test 4 (`test_no_verdict_leaks`): the serialised comparison response contains no member of §5's eight-verdict vocabulary as a key or a value.
    - Test 5 (`test_no_internal_identity_leaks`): the serialised comparison response contains no `provenance` key, no node id from either resolved wiring, no wiring id, and no 64-hex-character instance hash — derived live from the registry and the two resolved wirings, never a hand-written literal list.
    - Test 6 (`test_selector_keys_are_not_normalised`): two selectors whose values differ only in letter case produce two distinct keys and two separate runs.
    - Test 7 (`test_unsatisfiable_selector_refuses_the_whole_comparison`): one satisfiable and one unsatisfiable selector raises the same `UnsatisfiableSelectorError` the single-arm path raises, rather than returning a partial mapping with one arm missing.
  </behavior>
  <action>
Create `databasise/seam/compare.py` holding `compare_arms(execute, query_object, selectors)` — a pure async fan-out taking the bound `_execute` callable so the module has no import back into `engine`. It awaits `execute(query_object, selector)` once per supplied selector, in the caller's own order, and returns `{selector_key: envelope}`. There is no second scheduler, no second store-namespace resolution and no second envelope assembly: every arm goes through the identical `_execute` `query()` already calls, which is what makes the §4 `provenance` redaction and the §18.2 closed-envelope rule apply to the comparison case by construction rather than by a second copy of the rule.

The key for each arm is the caller's own selector value, rendered deterministically from the `Selector` model: the string for an `alias` or `harness` selector, and for a `capability` selector the requested capability rendered in the caller's own supplied order, joined by a separator declared as a module constant. Never normalise, case-fold, sort or deduplicate the caller's value — two selectors differing only in case are two distinct keys and two separate runs. Where two supplied selectors render to the same key, refuse by name rather than silently collapsing them into one entry.

Add `EmptyComparisonRequestError` to `databasise/seam/refusals.py` as a `SeamRefusalError` subclass carrying only the fact that zero selectors were supplied — never a candidate wiring, arm name or node id, per §18.2's closed-set rule applied to the refusal path. Follow 05-LEARNINGS' rule: this refusal is raised in the seam method, never inside a pydantic validator, so Starlette's exception middleware can match it; embed the class name in the message as the second workaround the existing refusals already use.

Add `async def compare(self, query_object, selectors, *, debug=False)` to `Databasise`. It runs `check_consumable(query_object, self.registry)` once before any arm, refuses a zero-length `selectors` sequence with `EmptyComparisonRequestError`, returns `await self._execute(query_object, selectors[0])` unchanged when exactly one selector is supplied — RIG §RUN.3's degenerate-width rule, a run and never a comparison — and otherwise delegates to `compare_arms`. `debug` is accepted and discarded for signature stability with `query`, exactly as `query` and `query_stream` already do. Arms run sequentially in the caller's supplied order, matching the engine's own declared `_CONCURRENCY_SETTING`; do not introduce parallel arm execution in this plan.

An unsatisfiable selector anywhere in the request refuses the whole comparison. A partial mapping with a silently absent arm is the one shape a consumer cannot distinguish from a modality that legitimately returned nothing.

Export `compare_arms` from `databasise/seam/__init__.py` alongside the existing exports.

Write `databasise/tests/seam/test_compare.py` covering the `<behavior>` block above, driving a real two-arm comparison against `synthetic_naive_store` (LightRAG) and `seeded_hipporag_store` (HippoRAG) with stub clients. Derive the forbidden-token set for Tests 4 and 5 live — the eight verdict strings from a module-level constant in the test, and the identity tokens from the two resolved wirings' own node ids plus `registry.keys()` — mirroring `test_tool_growth_invariant.py`'s own live-derivation convention rather than a hand-maintained literal list that drifts.
  </action>
  <verify>
    <automated>cd databasise && uv run pytest -q tests/seam/test_compare.py -x</automated>
    <fails_when>non-zero exit, or "no tests ran" in the summary line</fails_when>
    <automated>cd databasise && uv run pytest -q tests/seam/ -x</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `databasise/seam/compare.py` exists and contains no `import databasise.seam.engine` and no `RunRecord` construction
    - `Databasise.compare` exists and its one-selector branch returns the value of `self._execute(...)` directly
    - `EmptyComparisonRequestError` is a `SeamRefusalError` subclass and its message contains its own class name
    - `pytest tests/seam/test_compare.py::test_single_selector_is_a_run -x` exits 0
    - `pytest tests/seam/test_compare.py::test_keyed_by_selector -x` exits 0
    - `pytest tests/seam/test_compare.py::test_no_verdict_leaks -x` exits 0
    - The leak test derives its forbidden-identity token set from `registry.keys()` and the two resolved wirings at test time, not from a literal list in the test file
  </acceptance_criteria>
  <done>One query object and N selectors return N envelopes keyed by the caller's own selector values, through the one execution path, with no verdict and no internal identity anywhere in the response.</done>
  <reversibility rating="costly">The comparison response shape — a mapping keyed by rendered selector value — is consumer-visible and becomes the contract every API-08 consumer codes against; changing the key rendering later breaks every caller, and the rendering of a capability-set selector in particular has no precedent in the frozen record to fall back on.</reversibility>
</task>

<task type="auto">
  <name>Task 2: the same comparison over REST and MCP, with the tool-growth rule re-proven</name>
  <files>databasise/seam/rest.py, databasise/mcp/tools.py, databasise/mcp/server.py, databasise/mcp/__init__.py, databasise/tests/seam/test_rest_transport.py, databasise/tests/mcp/test_tool_growth_invariant.py, databasise/tests/mcp/test_dual_transport_parity.py, .planning/phases/05-opaque-side-admission/COVERAGE.md, .planning/phases/06-hipporag-2-side-by-side/COVERAGE.md</files>
  <read_first>
    - databasise/seam/rest.py (`create_app`, `_RequestModel`, `QueryRequest`, `post_query`, `_refusal_response`, `_checked_page`)
    - databasise/mcp/server.py (`create_server`, the five `@server.tool` bodies, `_refusal_mapped`, the dict-dispatch scope pattern)
    - databasise/mcp/tools.py (`TOOL_NAMES`, the per-tool args models)
    - databasise/tests/mcp/test_tool_growth_invariant.py (the three sites asserting `len(TOOL_NAMES) == 5`, `_OPERATION_TO_TOOLS`, `_COVERAGE_PATH`, `test_every_tool_has_a_coverage_row_and_every_both_transport_row_has_a_tool`, and `test_compare_is_absent_from_tool_names_if_coverage_records_it_as_an_absent_operation`)
    - databasise/tests/mcp/test_dual_transport_parity.py (`assert len(TOOL_NAMES) == 5`, the parametrised per-tool sweep, and the shadow-safe `try: import databasise.mcp` skip guard)
    - databasise/tests/seam/test_rest_transport.py (`_REFUSAL_FACTORIES` — the subclass-enumerating refusal-mapping test that fails loudly for a `SeamRefusalError` subclass with no registered factory)
    - .planning/phases/05-opaque-side-admission/COVERAGE.md (the §18.5 operations table and the deliberately-absent table `compare` currently sits in)
    - docs/system-model/CONTRACT.md (§18.5's admission rule — a capability expressible as a §18.4 selector is never a new tool, and the surface never grows per modality)
  </read_first>
  <action>
Both transports are thin adapters over the identical `Databasise.compare` method — deserialize, await, return — with no logic of their own, per D-17's already-AST-enforced rule.

**REST.** Add `CompareRequest(_RequestModel)` carrying the query object and a list of selectors, `extra="forbid"` inherited from `_RequestModel`. Add a `POST /compare` route whose body is exactly one awaited call to `engine.compare(body.query, body.selectors)` and a return. The existing app-level `SeamRefusalError` handler maps `EmptyComparisonRequestError` and `UnsatisfiableSelectorError` to the documented refusal status with no per-route handling. Register a factory for `EmptyComparisonRequestError` in `databasise/tests/seam/test_rest_transport.py`'s `_REFUSAL_FACTORIES` — the subclass-enumerating test already fails loudly for a refusal with no factory, and that is the intended signal, not an obstacle to route around.

**MCP.** Add `CompareToolArgs` to `databasise/mcp/tools.py`, add `"compare"` to `TOOL_NAMES`, and register a sixth `@server.tool(name="compare")` body in `databasise/mcp/server.py` wrapped in the existing `_refusal_mapped`. This is a legitimate sixth tool under §18.5 and the plan must say why in the tool's own docstring: comparison is not expressible as a §18.4 selector — it takes N selectors in one call, where a selector selects one arm — and the roster grows by one for one genuinely new operation, not by one name per modality. Registering HippoRAG as a second modality adds zero tools, which is the invariant `test_tool_growth_invariant.py` exists to hold.

**The pinned counts and the record.** Three assertions currently pin five tools: two in `databasise/tests/mcp/test_tool_growth_invariant.py` and one in `databasise/tests/mcp/test_dual_transport_parity.py`. Update all three to six. Add a `compare` entry to `_OPERATION_TO_TOOLS`. In `.planning/phases/05-opaque-side-admission/COVERAGE.md`, move `compare` out of the deliberately-absent table into the §18.5 operations table with both transports named and this plan cited as the one that landed it — `test_every_tool_has_a_coverage_row_and_every_both_transport_row_has_a_tool` reads that file and `test_compare_is_absent_from_tool_names_if_coverage_records_it_as_an_absent_operation` reads the absent table, so the record and the code must move together. Update that second test so it asserts the current state rather than the retired one: `compare` is present in `TOOL_NAMES` and absent from the deliberately-absent table.

Create `.planning/phases/06-hipporag-2-side-by-side/COVERAGE.md` recording this phase's own §18.5 operations table with the `compare` row, so the phase carries its own record rather than only amending Phase 5's.

Extend `databasise/tests/mcp/test_dual_transport_parity.py`'s existing per-tool sweep so `compare` is exercised through both transports and both results are asserted equal, exactly as the five existing tools are.
  </action>
  <verify>
    <automated>cd databasise && uv sync --extra rest</automated>
    <fails_when>non-zero exit</fails_when>
    <automated>cd databasise && uv run --extra rest pytest -q tests/seam/test_rest_transport.py tests/seam/test_dual_transport.py -x</automated>
    <fails_when>non-zero exit, or "no tests ran" in the summary line</fails_when>
    <automated>cd databasise && uv sync --extra mcp</automated>
    <fails_when>non-zero exit</fails_when>
    <automated>cd databasise && uv run --extra mcp pytest -q tests/mcp/ -x -rs</automated>
    <fails_when>non-zero exit, or every test in the run reports as skipped</fails_when>
  </verify>
  <acceptance_criteria>
    - `databasise/mcp/tools.py`'s `TOOL_NAMES` has exactly six members and includes `compare`
    - `grep -n 'len(TOOL_NAMES) == 5' databasise/tests/mcp/test_tool_growth_invariant.py databasise/tests/mcp/test_dual_transport_parity.py` prints no line numbers — every pinned five-tool assertion has been updated to six
    - `databasise/seam/rest.py` defines a `POST /compare` route whose body is a single awaited `engine.compare(...)` call and a return
    - `_REFUSAL_FACTORIES` in `databasise/tests/seam/test_rest_transport.py` has an entry for `EmptyComparisonRequestError`
    - `.planning/phases/05-opaque-side-admission/COVERAGE.md`'s §18.5 operations table has a `compare` row naming both REST and MCP
    - `.planning/phases/06-hipporag-2-side-by-side/COVERAGE.md` exists with a §18.5 operations table containing the `compare` row
    - The growth-invariant test that registers a second modality still passes with the tool name set byte-identical before and after
  </acceptance_criteria>
  <done>The comparison is reachable identically three ways, the tool roster grew by exactly one for one new operation, and the coverage record and the code state the same thing.</done>
</task>

<task type="auto">
  <name>Task 3: F-14's outcome, recorded from a real cross-modality call</name>
  <files>databasise/evidence/F-14-SEAM-INVARIANCE.md, databasise/tests/evidence/test_f14_record.py</files>
  <read_first>
    - docs/system-model/MODEL-RED-TEAM.md (the F-14 finding: "no wiring has run against a live seam implementation anywhere in the record ... the falsifier both clauses name as the actual test has not fired, in either direction, because nothing has run")
    - docs/system-model/CONTRACT.md (§18.2's field set and §18.3's invariance clause — the thing F-14 falsifies)
    - databasise/evidence/FALSIFIER-4-EVIDENCE.md (the evidence-document house format: dated header, claim-under-test quoted verbatim, findings table with a per-row `[code-verified]`/`[docs-verified]`/`[inference]` tag, verdict, method and limits)
    - databasise/tests/evidence/test_admission_docs.py (the convention of parsing the markdown table and front matter structurally rather than whole-file grep)
    - databasise/seam/envelope.py (`ResponseEnvelope`'s declared field set — the population being diffed)
    - databasise/tests/seam/test_compare.py (as written by Task 1 — the real two-arm call this record is generated from)
  </read_first>
  <action>
Run one real comparison call — the LightRAG arm and the HippoRAG base wiring, one query, through `Databasise.compare` against the two seeded fixtures — and diff the two arms' consumer-visible envelope field sets. Generate `databasise/evidence/F-14-SEAM-INVARIANCE.md` from that run rather than from the type definitions: the whole content of F-14's finding is that nothing had ever run, so a record derived from `ResponseEnvelope`'s declaration would reproduce the failure it is meant to close.

The document follows `FALSIFIER-4-EVIDENCE.md`'s house format exactly: dated header; the F-14 claim quoted verbatim from `MODEL-RED-TEAM.md`; a findings table with one row per consumer-visible envelope field carrying its per-arm value shape and a `[code-verified]` tag; a verdict; a method-and-limits section.

Both outcomes are acceptable and the criterion is that the outcome is recorded. If the two field sets are identical, the verdict states F-14 holds against this pair of modalities and names the pair. If a field differs, name the field explicitly — that is a §18.2 defect requiring a repair, and the record says so rather than noting it in passing.

Record, in the same document's findings table, the content-level difference already known from 06-01: HippoRAG 2's base wiring has no generator position among its thirteen, so its `answer` field carries assembled context where LightRAG's carries a generated completion. That is a difference in what a field contains, not in which fields exist, and the record must state that distinction rather than letting a reader infer it. Include the `depth_label` row explicitly too: HippoRAG's positions all resolve to `opaque` effective depth under the taint rule while LightRAG's provides node is `stage`, so the two arms legitimately report different values in the same field.

Under method-and-limits, state what this run does not establish: it is one query against two seeded fixtures, not the real-corpus run plan 06-08 performs; and the comparison is `architecture-comparison mode` per RIG §AA.4, so parity-not-gain does not apply and no field difference here is evidence about retrieval quality.

`databasise/tests/evidence/test_f14_record.py` parses the document structurally — front matter and the findings table, never a whole-file grep — and asserts every field declared on `ResponseEnvelope` has exactly one row, so a field added to the envelope later fails this test as an unrecorded widening rather than passing silently by omission. Derive the field list from `ResponseEnvelope.model_fields` at test time, never a hand-written list.
  </action>
  <verify>
    <automated>cd databasise && uv run pytest -q tests/evidence/test_f14_record.py -x</automated>
    <fails_when>non-zero exit, or "no tests ran" in the summary line</fails_when>
    <automated>cd databasise && uv run pytest -q</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `databasise/evidence/F-14-SEAM-INVARIANCE.md` exists, carries a dated header, and quotes the F-14 claim verbatim from `MODEL-RED-TEAM.md`
    - Its findings table has one row per field name in `ResponseEnvelope.model_fields`, each carrying a `[code-verified]` tag
    - Its verdict section states either that no consumer-visible field changed across the modality swap, or names the field that did
    - The document contains a row distinguishing a field-content difference from a field-set difference, covering `answer` and `depth_label`
    - `test_f14_record.py` derives its expected field list from `ResponseEnvelope.model_fields` at test time
    - Full suite `cd databasise && uv run pytest -q` exits 0
  </acceptance_criteria>
  <done>F-14 has fired, in one direction or the other, from a real seam call, and the outcome is committed evidence bound to a test that fails on an unrecorded envelope widening.</done>
</task>

</tasks>

<verification>
- `cd databasise && uv run pytest -q` exits 0
- `cd databasise && uv run --extra rest pytest -q` exits 0
- One comparison call returns two envelopes keyed by the caller's selectors, with no verdict and no internal identity in the response
- `databasise/evidence/F-14-SEAM-INVARIANCE.md` is committed
</verification>

<success_criteria>
API-08's comparison surface exists on all three transports, is inspection-only, keys results by caller-supplied selectors alone, degenerates to a run at one arm, and has produced F-14's first recorded outcome.
</success_criteria>

<flagged_assumptions>
- **Selector-key rendering for a capability set has no precedent.** §18.4 names the four selectors but the frozen record never states how a multi-capability selector renders as a response key. The joined-in-caller-order rendering chosen here is this plan's own decision, declared as a module constant so a later change is one edit rather than a search.
- **Arms run sequentially.** The engine already declares `_CONCURRENCY_SETTING` as sequential and RIG §RUN.4 prices comparison as "nothing beyond the runs themselves"; parallel arm execution would change the declared concurrency setting the calibration in 06-06 is keyed to, so it is deliberately not introduced here.
</flagged_assumptions>

<output>
Create `.planning/phases/06-hipporag-2-side-by-side/06-03-SUMMARY.md` when done
</output>
