---
phase: 06-hipporag-2-side-by-side
plan: 11
type: execute
wave: 1
gap_closure: true
closes:
  - "06-VERIFICATION.md human_verification item 1 — MACH-10's permanent-exclusion disposition, discharged by the owner in 06-UAT.md Test 1"
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [MACH-10]

estimate:
  tokens: 8000
  raw_tokens: 16000
  tasks: 1
  confidence: high

must_haves:
  truths:
    - "MACH-10 reads Complete in both places REQUIREMENTS.md records it — the checklist item and the coverage table — with a dated annotation citing 06-UAT.md Test 1 and databasise/evidence/F-07-MUTABLE-STORE-DISPOSITION.md."
    - "The annotation states what the owner actually affirmed (permanent exclusion of codebase-memory-mcp from §5 parity/determinism comparisons) and names the recorded reversal condition, so a later reader can tell an affirmed disposition from an unexamined one."
    - "No other requirement row changes state in this plan — MACH-03 and MODAL-05 remain Pending."
    - statement: "MACH-10 adjacency edge (two things exactly equal or just touching — merge, collide, or separate): two components declaring mutates_store in the same comparison still produce one refusal naming the first offending component, unchanged — tests/seam/test_mutable_store_exclusion.py exits 0."
      verification: backstop
    - statement: "MACH-10 empty-input edge: a comparison whose resolved wirings declare no mutates_store effect raises nothing and runs normally — tests/seam/test_mutable_store_exclusion.py's negative control exits 0."
      verification: backstop
    - statement: "MACH-10 ordering edge (elements comparing equal — output order specified and stable): the F-07 disposition record's component rows are enumerated from the registry, and tests/evidence/test_f07_record.py pins that every mutates_store component has exactly one disposition with a non-empty reason regardless of row order."
      verification: backstop
    - statement: "MACH-10 encoding edge (whose definition of equality applies): a component is identified by its registered name@version string, and MutableStoreComparisonExcludedError names that exact string — pinned by tests/evidence/test_f07_record.py and tests/seam/test_mutable_store_exclusion.py."
      verification: backstop
    - statement: "MACH-10 idempotency edge (what happens if this runs twice on the same input): the exclusion is a pre-execution refusal, so a repeated comparison raises identically and mutates no store — tests/seam/test_mutable_store_exclusion.py exits 0."
      verification: backstop
    - statement: "MACH-10 concurrency edge (what is guaranteed if interrupted or run in parallel): every selector is resolved and effect-checked before any arm executes, so an excluded component halts the comparison before the first store write — the ordering asserted in engine.compare()'s own docstring and exercised by tests/seam/test_mutable_store_exclusion.py."
      verification: backstop
  artifacts:
    - .planning/REQUIREMENTS.md
  key_links:
    - "REQUIREMENTS.md MACH-10 row -> 06-UAT.md Test 1 (result: pass, 2026-09-10) -> databasise/evidence/F-07-MUTABLE-STORE-DISPOSITION.md"
    - "REQUIREMENTS.md coverage table row `| MACH-10 | Phase 6 | ... |` -> the same checklist item's state"
  prohibitions:
    - "A requirement row must not be marked Complete without a dated annotation citing a committed artifact."
    - "This plan must not change the state of any requirement row other than MACH-10."
    - "The owner must not be re-asked a question 06-UAT.md already records an answer to."
---

<objective>
Close the one human-verification item 06-VERIFICATION.md left open. MACH-10's substance was already
discharged in code by 06-09 (`F-07-MUTABLE-STORE-DISPOSITION.md` plus the enforcing
`MutableStoreComparisonExcludedError`); the only thing outstanding was the owner's confirmation that
`codebase-memory-mcp@0.1.0`'s `permanent-exclusion` disposition matched their intent. **06-UAT.md
Test 1 records that answer: `result: pass`, dated 2026-09-10.** So this is a one-task documentation
change: flip the row and cite the confirmation. Do not re-ask.

**No tracer task.** This plan has no layers to slice — its whole deliverable is one dated annotation
against an already-committed evidence document. TRACER_MODE's leading-slice rule applies to the
Gap 1 code plan (06-10); a doc-only reconciliation has nothing to trace through.

Purpose: 06-VERIFICATION.md scored 3/6 with MACH-10 marked "NEEDS HUMAN" purely on this
confirmation. Leaving it Pending after the owner answered would misreport the phase.
Output: `.planning/REQUIREMENTS.md` with MACH-10 Complete in both its checklist item and its
coverage-table row.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-hipporag-2-side-by-side/06-UAT.md
@.planning/phases/06-hipporag-2-side-by-side/06-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip MACH-10 from Pending to Complete, citing the owner's recorded confirmation</name>

  <read_first>
    - `.planning/REQUIREMENTS.md` — line 24 (the `**MACH-10**` checklist item and its existing `[Measured 2026-09-10 (06-09-PLAN.md): ...]` annotation, whose final sentence currently reads that the row stays Pending until the owner's confirmation lands) and line 94 (the coverage-table row `| MACH-10 | Phase 6 | Pending |`)
    - `.planning/phases/06-hipporag-2-side-by-side/06-UAT.md` — Test 1 in full: its `expected` text (which names the `CA-2` row, its `permanent-exclusion` disposition and its stated reversal condition), `result: pass`, `coverage_id: 06-09/D1`, and the UAT front matter's `updated:` timestamp, which is the date to cite
    - `databasise/evidence/F-07-MUTABLE-STORE-DISPOSITION.md` — the `codebase-memory-mcp@0.1.0` (`CA-2`) row and its recorded reversal condition, so the annotation states what was actually affirmed rather than paraphrasing it
    - `.planning/phases/05-opaque-side-admission/05-LEARNINGS.md` — the mass-revert lesson: never round a partial result up, and never state more than the cited artifact establishes
  </read_first>

  <files>.planning/REQUIREMENTS.md</files>

  <action>
Edit `.planning/REQUIREMENTS.md` with two scoped replacements — never a whole-file rewrite, and
never touching any other requirement row.

1. Change the MACH-10 checklist marker from `- [ ] **MACH-10**` to `- [x] **MACH-10**`, and
   **append** (do not rewrite or delete the existing `[Measured 2026-09-10 (06-09-PLAN.md): ...]`
   annotation) a second bracketed annotation of the form
   `[Confirmed 2026-09-10 (06-11-PLAN.md): ...]`. That new annotation must state, in this order:
   that the owner affirmed `codebase-memory-mcp@0.1.0`'s (`CA-2`) `permanent-exclusion` disposition
   from §5 parity/determinism comparisons as matching their intent; that the confirmation is
   recorded in `.planning/phases/06-hipporag-2-side-by-side/06-UAT.md` Test 1 (`result: pass`,
   `coverage_id: 06-09/D1`); that the disposition itself is committed at
   `databasise/evidence/F-07-MUTABLE-STORE-DISPOSITION.md` and enforced by
   `MutableStoreComparisonExcludedError`; and the reversal condition that record carries verbatim
   (a future A/B-testability requirement, at which point the snapshot/reset protocol is a
   file-copy-and-restore of the engine's SQLite DB plus its WAL/SHM files). End with
   `Nothing outstanding.` — the same closing phrase the other Complete rows in this file use.
2. Change the coverage-table row `| MACH-10 | Phase 6 | Pending |` to
   `| MACH-10 | Phase 6 | Complete |`.

Change nothing else. `MACH-03` and `MODAL-05` stay `Pending`; their rows are 06-13's business, and
only if the owner authorizes the spend there.
  </action>

  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains the literal `- [x] **MACH-10**`.
    - `.planning/REQUIREMENTS.md` contains the literal `| MACH-10 | Phase 6 | Complete |`.
    - The MACH-10 line contains the literals `06-UAT.md`, `F-07-MUTABLE-STORE-DISPOSITION.md` and `Confirmed 2026-09-10`.
    - The MACH-10 line still contains its original `[Measured 2026-09-10 (06-09-PLAN.md):` annotation — the new annotation is appended, not substituted.
    - `.planning/REQUIREMENTS.md` still contains the literals `| MACH-03 | Phase 6 | Pending |` and `| MODAL-05 | Phase 6 | Pending |`.
    - `git diff --stat .planning/REQUIREMENTS.md` reports exactly one file changed and no more than 2 changed lines.
  </acceptance_criteria>

  <verify>
    <automated>cd /home/chris/coding/Databasise-2.0-fully-agnostic-system && grep -c '^- \[x\] \*\*MACH-10\*\*' .planning/REQUIREMENTS.md && grep -c '| MACH-10 | Phase 6 | Complete |' .planning/REQUIREMENTS.md && grep -c '| MACH-03 | Phase 6 | Pending |' .planning/REQUIREMENTS.md && grep -c '| MODAL-05 | Phase 6 | Pending |' .planning/REQUIREMENTS.md</automated>
    <fails_when>non-zero exit from any grep, or any of the four printed counts is 0</fails_when>
    <automated>cd /home/chris/coding/Databasise-2.0-fully-agnostic-system && grep '^- \[x\] \*\*MACH-10\*\*' .planning/REQUIREMENTS.md | grep -c 'Confirmed 2026-09-10' && grep '^- \[x\] \*\*MACH-10\*\*' .planning/REQUIREMENTS.md | grep -c 'F-07-MUTABLE-STORE-DISPOSITION.md' && grep '^- \[x\] \*\*MACH-10\*\*' .planning/REQUIREMENTS.md | grep -c 'Measured 2026-09-10 (06-09-PLAN.md)'</automated>
    <fails_when>non-zero exit from any grep, or any of the three printed counts is 0 — a 0 on the third means the pre-existing 06-09 annotation was overwritten instead of appended to</fails_when>
    <automated>cd databasise && uv run pytest -q tests/seam/test_mutable_store_exclusion.py tests/evidence/test_f07_record.py -x</automated>
    <fails_when>non-zero exit, or the summary line reads "no tests ran" or "0 passed"</fails_when>
  </verify>

  <reversibility rating="reversible">A two-line documentation edit in a git-tracked file.</reversibility>

  <done>MACH-10 reads Complete in both places REQUIREMENTS.md records it, with an appended dated annotation citing 06-UAT.md Test 1 and the F-07 disposition record; MACH-03 and MODAL-05 are untouched and still Pending; the F-07 enforcement tests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner confirmation -> requirement-row state | A policy judgment recorded in one file becomes a completion claim in another |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-11-01 | Repudiation | The MACH-10 annotation in `.planning/REQUIREMENTS.md` | medium | mitigate | The new annotation cites the exact recorded artifact and coverage id (`06-UAT.md` Test 1, `coverage_id: 06-09/D1`) and is dated, so the claim traces to a committed record rather than to this plan's own assertion. The 06-09 annotation is appended to, never overwritten, so the history of the row survives. |
| T-06-11-02 | Tampering | Collateral edits to unrelated requirement rows | medium | mitigate | The acceptance criteria pin `MACH-03` and `MODAL-05` as still `Pending` and cap the diff at two changed lines; the edit is performed as scoped replacements, never a whole-file write. |
| T-06-11-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package and touches no dependency manifest. |
</threat_model>

<artifacts_this_phase_produces>
| Kind | Symbol / path |
|---|---|
| Requirement-row state change | `MACH-10`: Pending -> Complete, in both the checklist item and the coverage table of `.planning/REQUIREMENTS.md` |
| Annotation (new) | `[Confirmed 2026-09-10 (06-11-PLAN.md): ...]` on the MACH-10 row |

No new code symbols, no new files, no new tests, no CLI flags.
</artifacts_this_phase_produces>

<verification>
1. `grep` assertions in Task 1's verify block — the row reads Complete in both places, cites both artifacts, and retains its 06-09 annotation.
2. `cd databasise && uv run pytest -q tests/seam/test_mutable_store_exclusion.py tests/evidence/test_f07_record.py -x` — the enforcement the row now claims Complete still holds in code.
3. `git diff --stat .planning/REQUIREMENTS.md` — one file, no more than two changed lines.
</verification>

<success_criteria>
- MACH-10 is Complete in `.planning/REQUIREMENTS.md`, with a dated annotation citing 06-UAT.md Test 1 and `databasise/evidence/F-07-MUTABLE-STORE-DISPOSITION.md`.
- The pre-existing 06-09 annotation survives intact.
- MACH-03 and MODAL-05 are unchanged and still Pending.
- The F-07 enforcement tests pass.
</success_criteria>

<output>
Create `.planning/phases/06-hipporag-2-side-by-side/06-11-SUMMARY.md` when done.
</output>
