---
phase: 06-hipporag-2-side-by-side
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - databasise/pyproject.toml
  - databasise/stores/graph.py
  - databasise/stores/vector.py
  - databasise/parts_core/hipporag/__init__.py
  - databasise/parts_core/hipporag/fact_score.py
  - databasise/parts_core/hipporag/fact_filter.py
  - databasise/parts_core/hipporag/reset_vector_join.py
  - databasise/parts_core/hipporag/ppr.py
  - databasise/parts_core/hipporag/assemble_result.py
  - databasise/parts/registry.py
  - databasise/wirings/__init__.py
  - databasise/wirings/hipporag/__init__.py
  - databasise/wirings/hipporag/base.json
  - databasise/wirings/resolve.py
  - databasise/wirings/lightrag/base.json
  - databasise/seam/engine.py
  - databasise/seam/selectors.py
  - databasise/parity/run_arm.py
  - databasise/tests/parts_core/hipporag/__init__.py
  - databasise/tests/parts_core/hipporag/conftest.py
  - databasise/tests/parts_core/hipporag/test_registration.py
  - databasise/tests/parts_core/hipporag/test_ppr.py
  - databasise/tests/stores/test_graph_bulk_export.py
  - databasise/tests/stores/test_vector_score_all.py
  - databasise/tests/seam/test_cross_modality_run.py
  - databasise/tests/seam/test_store_isolation.py
autonomous: false
requirements: [MODAL-04, MODAL-05]

estimate:
  tokens: 50000
  raw_tokens: 100000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "A caller submits one QueryObject with a capability selector and receives a §18.2 ResponseEnvelope whose retrieval came from HippoRAG's own five-node query-side chain, not from any LightRAG arm."
    - "The `ppr` node reaches the whole graph through exactly two Cozo queries and one native igraph `personalized_pagerank(..., implementation=\"prpack\")` call — never an N-call pointwise walk."
    - "HippoRAG's resolved graph and KV store directories are disjoint from LightRAG's on the same store_root and workspace, and LightRAG's own resolved directories are unchanged from before this plan."
    - "Every registered HippoRAG part resolves to `opaque` effective depth, so the default selector never returns a HippoRAG wiring."
    - "The seam injects the query text and mints evidence references using positions the resolved wiring declares, so neither operation names a LightRAG node id in seam source."
  artifacts:
    - databasise/parts_core/hipporag/ppr.py
    - databasise/parts_core/hipporag/reset_vector_join.py
    - databasise/parts_core/hipporag/fact_score.py
    - databasise/wirings/hipporag/base.json
    - databasise/tests/seam/test_cross_modality_run.py
  key_links:
    - "resolve_selector's candidate pool -> every registered wiring, both modalities (not the five LightRAG arms)"
    - "CozoGraphStore.export_to_igraph -> igraph.Graph -> ppr node's personalized_pagerank call"
    - "resolved wiring's store_namespaces -> _build_stores -> per-arm graph/KV directory"
  prohibitions:
    - "MUST NOT declare any HippoRAG part's structural_depth as `stage` or `evidence` in this phase — no parity measurement against a trusted baseline exists, and an earned depth stated without one launders an unmeasured claim as a measured one."
    - "MUST NOT satisfy whole-graph PPR by calling an existing pointwise store method in a loop — §14.2 requires the store expose a bulk-export path or refuse the wiring, and a loop that produces plausible numbers hides a capability the store has not earned."
    - "MUST NOT let a HippoRAG node read a store namespace LightRAG built, and MUST NOT let a LightRAG node read one HippoRAG built — retrieval sourced from the other arm's index and reported as this arm's own is fabricated provenance."
---

<objective>
Land the phase's tracer: HippoRAG 2's query-side chain answers one real query end to end through the §18 seam, over its own isolated stores, using the native igraph/prpack whole-graph PPR call reached through §14.2's Graph bulk-export capability.

Purpose: this is the phase's architectural de-risking pass. It proves in one commit that a second modality can register, resolve, run and return through machinery built for one — before eight expansion plans commit to that assumption.
Output: five executable HippoRAG parts, HippoRAG's base wiring, two new store capabilities, a modality-agnostic wiring pool and seam, and a passing cross-modality run.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-hipporag-2-side-by-side/06-RESEARCH.md
@.planning/phases/06-hipporag-2-side-by-side/06-PATTERNS.md
@.planning/phases/06-hipporag-2-side-by-side/06-VALIDATION.md
@.claude/skills/spike-findings-rag-graph-vector-raw/SKILL.md
</context>

## Artifacts this phase produces

Symbols created by Phase 6 (excluded from drift verification — none of these existed before this phase):

**This plan (06-01):**
- `databasise/stores/graph.py`: `CozoGraphStore.export_to_igraph()`
- `databasise/stores/vector.py`: `FaissVectorStore.score_all()`
- `databasise/parts_core/hipporag/` package: `HIPPORAG_PARTS` tuple, `HIPPORAG_FACT_SCORER_PART`, `HIPPORAG_FACT_FILTER_PART`, `HIPPORAG_RESET_VECTOR_JOIN_PART`, `HIPPORAG_PPR_RETRIEVER_PART`, `HIPPORAG_RESULT_ASSEMBLER_PART`
- `databasise/wirings/hipporag/base.json`; `databasise/wirings/resolve.py`: `all_wirings()`, `load_wiring()`, `WIRING_NAMES`
- `databasise/seam/selectors.py`: `_registered_wiring_candidates()`
- `databasise/seam/engine.py`: `_declared_query_positions()`, `_declared_evidence_position()`, `_declared_store_namespaces()`
- Wiring top-level keys: `consumes_query`, `evidence_position`, `store_namespaces`
- Namespace constants: `hipporag-chunks`, `hipporag-entities`, `hipporag-facts`, `hipporag-graph`, `hipporag-text-chunks`

**Later plans in this phase:** `HIPPORAG_CHUNKER_EMBEDDER_PART`, `HIPPORAG_OPENIE_EXTRACTOR_PART`, `HIPPORAG_ENTITY_FACT_EMBEDDER_PART`, `HIPPORAG_FACT_EDGE_BUILDER_PART`, `HIPPORAG_PASSAGE_EDGE_BUILDER_PART`, `HIPPORAG_SYNONYMY_EDGE_BUILDER_PART`, `HIPPORAG_GRAPH_MATERIALIZER_PART`, `HIPPORAG_DPR_FALLBACK_PART`; `FaissVectorStore.self_knn()`; `databasise/seam/compare.py` (`Databasise.compare`, `ComparisonRequest`); `databasise/eval/bundle.py` (`EvalBundle`, `mint_version`, `BundleSplit`); `databasise/eval/calibration.py` (`calibrate_aa_floor`, `CalibrationResult`, `UnusableFloorError`); `compare` MCP tool; `POST /compare` REST route.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate for igraph, numpy and scipy</name>
  <read_first>
    - .planning/phases/06-hipporag-2-side-by-side/06-RESEARCH.md (## Package Legitimacy Audit)
    - .planning/phases/05-opaque-side-admission/05-LEARNINGS.md (the `mcp`/`python-multipart` precedent — the same sandbox blind spot)
    - databasise/pyproject.toml
  </read_first>
  <action>
Three packages carry `[SUS]` verdicts in 06-RESEARCH.md's Package Legitimacy Audit, every one on an `unknown-downloads` / `too-new` / `no-repository` signal rather than a slopsquat signature. This gate is mechanical and is not auto-approvable regardless of `workflow.auto_advance`. Do not add any of the three to `databasise/pyproject.toml` before the developer answers.

Present to the developer, for each package, the registry page to open and the exact claim to confirm:

- `igraph` — `https://pypi.org/project/igraph/` — confirm the project page links source repository `github.com/igraph/python-igraph`, and that `https://pypi.org/project/python-igraph/` describes itself as the legacy distribution of the same project. The name to depend on is `igraph`; `python-igraph` is the legacy alias and must not appear in `databasise`'s dependency list or `uv.lock`.
- `numpy` — `https://pypi.org/project/numpy/` — confirm source repository `github.com/numpy/numpy`. Note that this same monorepo already pins numpy at `v1/pyproject.toml`.
- `scipy` — `https://pypi.org/project/scipy/` — confirm source repository `github.com/scipy/scipy`. Needed by plans 06-04 and 06-06 for `scipy.stats.bootstrap`; cleared here so those plans carry no second checkpoint.

Also put Open Question 4 to the developer in the same turn: research recommends all three go in `[project.dependencies]` (core), not a new optional extra, because HippoRAG 2 is a first-class modality of this milestone rather than an optional transport like `rest`/`mcp`. Record the answer. Absent a contrary answer, proceed with core dependencies.

Record every answer verbatim in `.planning/phases/06-hipporag-2-side-by-side/06-CHECKPOINT-ANSWERS.md`, mirroring `04-CHECKPOINT-ANSWERS.md`'s format.
  </action>
  <what-built>Nothing is built yet. This gate runs before the first line of Task 2, because the package-legitimacy protocol forbids adding a `[SUS]`-verdicted package to `databasise/pyproject.toml` before a human has confirmed it. The three verdicts and their reasons are already written up in `06-RESEARCH.md`'s Package Legitimacy Audit; this gate is where a human checks them against the live registry.</what-built>
  <how-to-verify>
    1. Open `https://pypi.org/project/igraph/` and confirm the project links source repository `github.com/igraph/python-igraph`. Then open `https://pypi.org/project/python-igraph/` and confirm it describes itself as the legacy distribution of the same project.
    2. Open `https://pypi.org/project/numpy/` and confirm source repository `github.com/numpy/numpy`.
    3. Open `https://pypi.org/project/scipy/` and confirm source repository `github.com/scipy/scipy`.
    4. Answer Open Question 4: do `igraph`, `numpy` and `scipy` go in `[project.dependencies]` (core), or behind a new optional extra? Research recommends core, because HippoRAG 2 is a first-class modality of this milestone rather than an optional transport like `rest` or `mcp`.
  </how-to-verify>
  <acceptance_criteria>
    - `.planning/phases/06-hipporag-2-side-by-side/06-CHECKPOINT-ANSWERS.md` exists and contains one dated verdict line per package for `igraph`, `numpy`, `scipy`
    - The file records the Open Question 4 answer (core dependencies vs an optional extra) as a stated decision, not a restated recommendation
    - No edit to `databasise/pyproject.toml` was committed before this checkpoint was answered
  </acceptance_criteria>
  <resume-signal>Type "approved" (all three legitimate, dependencies go in core), or name the package to drop and the placement to use instead.</resume-signal>
  <done>Three package verdicts and the dependency-placement decision are recorded in 06-CHECKPOINT-ANSWERS.md.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: End-to-end "HippoRAG answers a query through the seam" — one path only</name>
  <files>databasise/pyproject.toml, databasise/stores/graph.py, databasise/stores/vector.py, databasise/parts_core/hipporag/__init__.py, databasise/parts_core/hipporag/fact_score.py, databasise/parts_core/hipporag/fact_filter.py, databasise/parts_core/hipporag/reset_vector_join.py, databasise/parts_core/hipporag/ppr.py, databasise/parts_core/hipporag/assemble_result.py, databasise/parts/registry.py, databasise/wirings/hipporag/__init__.py, databasise/wirings/hipporag/base.json, databasise/wirings/resolve.py, databasise/wirings/lightrag/base.json, databasise/seam/engine.py, databasise/seam/selectors.py, databasise/parity/run_arm.py, databasise/tests/parts_core/hipporag/__init__.py, databasise/tests/parts_core/hipporag/conftest.py, databasise/tests/seam/test_cross_modality_run.py</files>
  <read_first>
    - docs/system-model/PARTS.md (`## §H` — the thirteen-node table, the `Recipe:`/`Arms:`/`Artifact scopes:` fields, and the handed/does/returns/when statement of the whole-graph algorithm requirement)
    - docs/system-model/wirings/hipporag-base.json (the governing node ids, kinds, effects, deps, config and guards this port must match)
    - databasise/parts_core/lightrag/chunk_vector.py (the Part module shape: module docstring citing upstream_ref, `_NAME_AT_VERSION`, one async `_<name>_body(ctx)`, one `Part(...)` at module bottom)
    - databasise/parts_core/lightrag/join_roundrobin.py (the `kind="join"` Part that already executes through the existing dispatch; the arity-check-and-refuse and fixed-lookup-by-node-id patterns)
    - databasise/parts_core/lightrag/keywords.py (the `calls_llm` client-call shape `fact-filter` mirrors)
    - databasise/stores/graph.py (`CozoGraphStore._run`, `get_node_edges`, the module docstring's Cozo frozen-bug mitigations and read-your-writes rule)
    - databasise/stores/vector.py (`FaissVectorStore.query`, `iter_vectors`, `MultiNamespaceVectorStore.select`)
    - databasise/seam/engine.py (`_inject_query`, `_build_stores`, `_EVIDENCE_RETRIEVAL_NODE_ID`, `_execute`, `_select_answer`)
    - databasise/seam/selectors.py (`_capability_candidates`, `_match_capability`, `_resolve_default`, `_is_default_eligible`)
    - databasise/wirings/resolve.py (`load_base`, `resolve_arm`, `_ARM_NAMES`)
    - databasise/wirings/lightrag/base.json (the top-level key shape: `wiring_id`, `nodes`, `recipe`, `harnesses`, `provides`)
    - databasise/parts/registry.py (`default_registry`, `PartRegistry.register`, `dispatch`)
    - databasise/tests/seam/conftest.py (`synthetic_naive_store` — the fixture shape the HippoRAG fixture mirrors)
    - .planning/phases/06-hipporag-2-side-by-side/06-PATTERNS.md
  </read_first>
  <behavior>
    - Given a seeded HippoRAG index (facts in `hipporag-facts`, chunks in `hipporag-chunks`, a small weighted graph in `hipporag-graph`) and a stub LLM/embedding client, `Databasise.query(QueryObject(text=...), Selector(capability=[...]))` returns a `ResponseEnvelope` whose `evidence` refs carry `namespace="hipporag-chunks"`.
    - `CozoGraphStore.export_to_igraph()` on a graph of N nodes and M edges returns an `igraph.Graph` with `vcount() == N`, `ecount() == M`, `vs["name"]` equal to the stored node ids, and `es["weight"]` equal to the stored edge weights — built from exactly two Cozo queries whatever N and M are.
    - `FaissVectorStore.score_all(vector)` returns one scored entry per stored vector, never a top-k truncation: length equals the store's own entry count.
    - `reset-vector-join` handed two branch mappings over disjoint vertex refs returns their sum over the shared dense vertex space, order-insensitively; handed a branch count disagreeing with a declared `arity` it raises rather than merging what arrived.
    - `ppr` zeroes NaN and negative reset entries before the call, and reads back only the passage-node vertex partition, sorted by descending score.
    - `resolve_selector(Selector(capability=["calls_rerank"]), ...)` still resolves a LightRAG arm; `resolve_selector(None, ...)` still resolves `naive`.
  </behavior>
  <action>
Wire ONE path from a caller's query through every layer this phase touches, to the far end of the stack and back. No second call site, no batching, no node beyond the five named below.

**Dependencies and packaging.** Add `igraph>=0.11,<2.0` and `numpy>=1.24,<3.0` to `databasise/pyproject.toml`'s `[project] dependencies`, placed per Task 1's recorded decision. Pin the distribution name `igraph`, never `python-igraph`. Do not add `scipy` here — plans 06-04 and 06-06 own it. In the same edit, add `databasise.parts_core.hipporag` and `databasise.wirings.hipporag` to `[tool.setuptools] packages`, and widen `[tool.setuptools.package-data]`'s `"databasise.wirings"` entry to include `hipporag/*.json` — 05-04's own recorded rule is that a package directory is named in the same commit that creates it, and the existing `package-data` entry has already been widened once for exactly the failure of a wiring shipping in a source checkout and vanishing from an installed package. Run `uv sync` in `databasise/` after the edit.

**Store capability 1 — §14.2 Graph bulk-export.** Add `async def export_to_igraph(self, *, weight_attr: str = "weight") -> "igraph.Graph"` to `CozoGraphStore`. Import `igraph` function-scoped inside the method, mirroring 05-06's lazy-optional-import discipline. Issue exactly one Cozo query for the node relation and one for the edge relation through the existing `self._run` dispatcher — never `pycozo` directly, never one call per node. Consult the pending write buffers first exactly as `get_node`/`has_node` do, so a graph written in this run and not yet flushed is exported. Build the vertex-name to dense-index map once, construct one undirected `igraph.Graph`, set `vs["name"]` to the node ids and `es["weight"]` to the resolved weight attribute. Where the stored edge attrs carry no `weight_attr` key, default that edge to weight 1.0 and state that default in the method docstring rather than dropping the edge. The docstring cites `CONTRACT.md §14.2`'s bulk-export row and `PARTS.md ## §H`'s "single `weight` edge attribute collapsing all three edge types" clause.

**Store capability 2 — §14.2 `score_all`.** Add `async def score_all(self, vector: Any) -> list[dict[str, Any]]` to `FaissVectorStore`. This is an exhaustive dense inner product against every stored vector with no top-k cutoff: reuse the class's own normalisation and its existing Faiss index object with `k` set to the index's own `ntotal`, or a direct matrix product over `iter_vectors()` — either way one batched operation. Return the same entry shape `query()` returns (`id`, `score`, plus the stored metadata), sorted by descending score with `id` as the deterministic tie-break, matching `chunk_vector.py`'s own sort key. Do not compose `query()` in a loop.

**Five HippoRAG parts**, one module each under `databasise/parts_core/hipporag/`, every one following `chunk_vector.py`'s module shape and opening with a docstring that names the `PARTS.md ## §H` row it implements and the upstream `HippoRAG.py` line range that row cites. Every part takes `structural_depth="opaque"` and `upstream_ref="hipporag/src/hipporag/HippoRAG.py"`.

- `fact_score.py` — `hipporag/fact-scorer@0.1.0`, `kind="retriever"`, `effects=["reads_vector", "calls_embedding"]`. Embeds `ctx.config["query"]` through `ctx.clients` (the same client handle `embedder_query.py` uses), then calls `ctx.stores["vector"].select("hipporag-facts").score_all(vector)`. Emits `{"items": [...]}` of `graph_fact`-kind scored items. `PARTS.md ## §H` declares this node's effects as `reads_vector` alone; `calls_embedding` is added here because the node has no upstream embedder dep in the governing wiring (`deps: []`) and must therefore embed the query itself. Record that addition in this plan's SUMMARY as an additive effects discrepancy against `## §H`, in the same shape 05-06 recorded `mutates_store` on the codebase-memory-mcp part — never silently resolved in either direction.
- `fact_filter.py` — `hipporag/fact-filter@0.1.0`, `kind="ranker-reranker"`, `effects=["calls_llm"]`. Takes `fact-score`'s items, keeps the top `config["linking_top_k"]` by score, and asks the LLM client to drop facts unrelated to the query, returning the surviving subset in input order. Emits `{"items": [...], "guard_fired": bool}` where `guard_fired` is true when zero facts survive — the `zero_surviving_facts_dpr_fallback` guard `docs/system-model/wirings/hipporag-base.json` declares under `§19.8`, evaluated at this node. In this plan the guard's true branch has no consumer; plan 06-07 wires `dpr-fallback` to it. Report token spend through the same `TokenAccounting` shape `keywords.py` reports.
- `reset_vector_join.py` — `hipporag/reset-vector-join@0.1.0`, `kind="join"`, `effects=["reads_vector", "calls_embedding", "reads_kv"]`. Builds the two dense weight arrays `PARTS.md ## §H` names — phrase weights from the surviving facts' scores divided by their chunk count, and passage weights from min-max-normalised dense passage retrieval scores multiplied by `config["passage_node_weight"]` — over the same dense vertex-index space `export_to_igraph` produces, then sums them. The sum is disjoint-support and order-insensitive; entity-prefixed and chunk-prefixed refs resolve to disjoint vertex indices. Emit `{"reset_vector": [...], "vertex_names": [...]}`. Copy `join_roundrobin.py`'s `config["arity"]` check-and-refuse rule verbatim in intent: an input count disagreeing with a declared arity raises `ValueError`, never a best-effort merge. Do not copy its round-robin interleave body — the merge semantics here are a vector sum. Note in the module docstring, as a verified finding, that `join_roundrobin.py` already runs today as a plain `Part` with `kind="join"` because `runner/scheduler.py`'s `_run_node` calls `dispatch(part, ctx)` uniformly and branches on kind only inside `derive_execution_mode`; no new scheduler dispatch is built by this plan.
- `ppr.py` — `hipporag/ppr-retriever@0.1.0`, `kind="retriever"`, `effects=["reads_graph"]`. Calls `ctx.stores["graph"].select("hipporag-graph").export_to_igraph()`, aligns the reset vector to the exported graph's own vertex order by `vertex_names`, replaces every NaN and every negative reset entry with 0 before the call, and invokes `personalized_pagerank` with `vertices=range(graph.vcount())`, `damping=config.get("damping", 0.5)`, `directed=False`, `weights="weight"`, `reset=<the zeroed dense array>`, `implementation="prpack"`. The damping default is 0.5 — HippoRAG's own default, deliberately not igraph's 0.85; state that in the docstring. Read back only the passage-node vertex partition (vertex names carrying the chunk prefix), sort descending by score, and emit `{"items": [...]}` of `text_chunk`-kind scored items carrying `id` and `score`.
- `assemble_result.py` — `hipporag/result-assembler@0.1.0`, `kind="assembler"`, `effects=["reads_kv"]`. Hydrates each `ppr` item's chunk text from `ctx.stores["kv"]`, orders by the declared `config["order_policy"]` value `hipporag-doc-score-order`, and emits `{"items": [...], "completion": <the ordered context, joined>}`. HippoRAG 2's base wiring has no generator position among its thirteen — `completion` therefore carries assembled context, not a generated answer. State that in the module docstring as a named limitation; it is a content difference at the `answer` field, not an envelope-field difference, and plan 06-03 records it against F-14.

`databasise/parts_core/hipporag/__init__.py` exports a `HIPPORAG_PARTS` tuple of the five, mirroring `parts_core/lightrag/__init__.py`'s `LIGHTRAG_PARTS`. `default_registry()` in `databasise/parts/registry.py` registers `HIPPORAG_PARTS` alongside the existing tuples.

**The wiring, and a modality-agnostic pool.** Create `databasise/wirings/hipporag/base.json` carrying `wiring_id: "hipporag-base"`, the five nodes above with the ids, kinds, effects, config and deps `docs/system-model/wirings/hipporag-base.json` declares for them (`fact-score` deps `[]`, `fact-filter` deps `["fact-score"]`, `reset-vector-join` deps `["fact-filter"]`, `ppr` deps `["reset-vector-join"]`, `assemble-result` deps `["ppr"]`), `recipe` `{"chunker": "chunk-embed", "embedding": "chunk-embed", "extraction": "openie"}`, `harnesses: []`, `provides: ["assemble-result"]`. Plans 06-02, 06-05 and 06-07 add the remaining eight nodes to this same file; 06-07 pins the final node id set against the governing document. HippoRAG has no arms — do not create a patch file.

In `databasise/wirings/resolve.py`, add `load_wiring(wiring_name)` reading `<wiring_name>/base.json` under the wirings package, a `WIRING_NAMES` tuple naming both modalities, and `all_wirings()` returning `(candidate_name, resolved_dict)` for every LightRAG arm plus every non-LightRAG base wiring. Keep `load_base`, `resolve_arm`, `resolved_node_ids` and `declared_node_ids` unchanged so every existing caller and conformance test keeps working. In `databasise/seam/selectors.py`, replace `_capability_candidates`'s hardcoded five-arm list with a call into `all_wirings()`; leave `_match_capability`'s smallest-resolved-wiring-wins rule, the `_ARM_NAMES` tie-break order and `_is_default_eligible`'s provides-node opaque exclusion exactly as they are. The candidate pool is now over registered wirings, of which LightRAG's five arms are one modality's entries — the pool's identity is generalised, not extended alongside a still-LightRAG-only primary.

**Three seam couplings, made modality-agnostic.** All three follow the shape `provides` already establishes: a declared top-level array or object on the wiring, read by the seam, never a node id written into seam source.

- `consumes_query`: add `"consumes_query": ["keywords", "embedder-query", "generate"]` to `databasise/wirings/lightrag/base.json` — the same three ids `_inject_query` hardcodes today, so LightRAG's behaviour is byte-identical. HippoRAG's base declares `["fact-score", "reset-vector-join"]`. Rewrite `_inject_query` to read this array off the resolved dict, defaulting to an empty list, keeping its existing no-op-for-an-absent-node behaviour so the arm patches that remove nodes still work.
- `evidence_position`: add `"evidence_position": {"node": "chunk-vector", "namespace": "chunks"}` to LightRAG's base and `{"node": "ppr", "namespace": "hipporag-chunks"}` to HippoRAG's. In `_execute`, read the node id and namespace from the resolved dict instead of `_EVIDENCE_RETRIEVAL_NODE_ID` and the module-level `CHUNKS_NAMESPACE` import, preserving today's behaviour for a wiring that declares neither (empty evidence list, never a refusal) and preserving `mint_evidence_refs`' no-re-sort rule.
- `store_namespaces`: add `"store_namespaces": {"kv": "text_chunks", "graph": "chunk_entity_relation"}` to LightRAG's base — the values `_TEXT_CHUNKS_KIND` and `_GRAPH_KIND` already hold, so no on-disk state moves — and `{"kv": "hipporag-text-chunks", "graph": "hipporag-graph"}` to HippoRAG's. Give `_build_stores` an optional resolved-wiring argument and read the two namespaces from it, falling back to the existing constants when absent so `ingest`, `delete_document` and `resolve_evidence` keep their current directories. Make `databasise/parity/run_arm.py`'s own `_build_stores` read the same declaration so the parity harness and the seam cannot drift apart. The `vector` key stays `MultiNamespaceVectorStore`: vector isolation is already a side effect of each part selecting its own namespace by name.

Deriving these namespaces from `derive_namespace()` per RIG §RUN.1 is the eventual form and is deliberately not done here: it would relocate every existing LightRAG store directory, which `databasise/namespaces.py`'s own D-07 note classifies as a migration of on-disk state rather than a config edit. Record that as a stated planner assumption in the SUMMARY.

**The fixture and the end-to-end test.** `databasise/tests/parts_core/hipporag/conftest.py` provides `seeded_hipporag_store`, mirroring `tests/seam/conftest.py`'s `synthetic_naive_store` exactly: writes through the stores' own public write paths only (`FaissVectorStore.upsert` into `hipporag-facts` and `hipporag-chunks`, `SqliteKVStore.upsert` into `hipporag-text-chunks`, `CozoGraphStore.upsert_node`/`upsert_edge` into `hipporag-graph`), never a file copy and never a v1 artifact. Seed a graph small enough to hand-check: three entity vertices, two chunk (passage) vertices, weighted edges between them. `databasise/tests/seam/test_cross_modality_run.py` drives `Databasise` against that fixture with a stub client dict and asserts the `<behavior>` block above.
  </action>
  <verify>
    <automated>cd databasise && uv sync</automated>
    <fails_when>non-zero exit, or the resolution log names `python-igraph` rather than `igraph`</fails_when>
    <automated>cd databasise && uv run pytest -q tests/parts_core/hipporag/ tests/seam/test_cross_modality_run.py -x</automated>
    <fails_when>non-zero exit, or "no tests ran" in the summary line</fails_when>
    <automated>cd databasise && uv run pytest -q</automated>
    <fails_when>non-zero exit, or the summary reports fewer passed+skipped than the 745 recorded at Phase 5 close plus this plan's own new tests</fails_when>
    <automated>cd databasise && uv run python -m databasise.tools.check_import_boundary</automated>
    <fails_when>non-zero exit, or any reported import of `lightrag` or `hipporag` from a module outside the named subprocess-entry-point exclusion list</fails_when>
  </verify>
  <acceptance_criteria>
    - `databasise/pyproject.toml` `[project] dependencies` contains an `igraph` requirement and contains no `python-igraph` requirement; `databasise/uv.lock` contains an `igraph` entry
    - `databasise/stores/graph.py` defines `export_to_igraph` and its body contains `implementation` nowhere — the prpack argument belongs to the `ppr` node, not the store
    - `databasise/parts_core/hipporag/ppr.py` contains `implementation="prpack"`, `damping`, `directed=False` and `weights="weight"` in one `personalized_pagerank` call
    - `databasise/parts_core/hipporag/__init__.py` exports `HIPPORAG_PARTS` with exactly five members, and `default_registry()` returns a registry whose `keys()` contains all five `hipporag/` names
    - Every member of `HIPPORAG_PARTS` has `structural_depth == "opaque"`; `pytest tests/parts_core/hipporag/test_registration.py -x` proves it
    - `databasise/wirings/hipporag/base.json` parses through `parse_wiring(..., default_registry())` with `report.ok` true
    - `grep -v '^#' databasise/seam/engine.py | grep -c 'chunk-vector'` returns 0 — the evidence position is read from the wiring, not written into seam source
    - `resolve_selector(None, registry=default_registry(), store_root=...)` returns a wiring whose `wiring_id` is a LightRAG one, and `_is_default_eligible` returns False for the HippoRAG base wiring
    - `pytest tests/seam/test_cross_modality_run.py -x` exits 0 with at least one test asserting `evidence[0].namespace == "hipporag-chunks"`
  </acceptance_criteria>
  <done>One query resolves to HippoRAG's own wiring through a capability selector, runs its five-node chain over its own isolated stores and a real igraph prpack call, and returns a §18.2 envelope — committed, with the full suite green.</done>
  <reversibility rating="costly">The three declared top-level wiring keys (`consumes_query`, `evidence_position`, `store_namespaces`) enter every wiring's RFC 8785 canonicalisation and therefore every `wiring_instance_hash`; renaming them later rehashes every committed wiring and every evidence record keyed on one.</reversibility>
</task>

<task type="auto">
  <name>Task 3: The capability and isolation guarantees the happy path does not prove</name>
  <files>databasise/tests/stores/test_graph_bulk_export.py, databasise/tests/stores/test_vector_score_all.py, databasise/tests/seam/test_store_isolation.py, databasise/tests/parts_core/hipporag/test_registration.py, databasise/tests/parts_core/hipporag/test_ppr.py</files>
  <read_first>
    - databasise/stores/graph.py (as written by Task 2)
    - databasise/stores/vector.py (as written by Task 2)
    - databasise/parts_core/hipporag/ppr.py (as written by Task 2)
    - databasise/tests/stores/test_graph_frozen_bugs.py (the store-test shape and the negative-control convention)
    - databasise/tests/parity/test_arm_conformance.py (the resolved-wiring conformance shape)
    - docs/system-model/CONTRACT.md (§14.2's bulk-export and self-KNN rows, including the refusal-rather-than-emulate rule)
    - docs/system-model/RIG.md (§RUN.1 and §RUN.2 — the per-arm isolation and artifact-overlap rules)
  </read_first>
  <action>
Four properties the happy path leaves unproven. Every assertion here fails red if the property breaks; each carries a negative control where an equality is being asserted, per this codebase's own "negative control proving a comparison has teeth" convention.

`test_graph_bulk_export.py` — build a Cozo graph of 3 nodes / 3 edges and one of 30 nodes / 60 edges in the same test, count `CozoGraphStore._run` invocations across each export by wrapping the bound method with a counter, and assert the count is 2 for both sizes. That is the §14.2 no-pointwise-emulation rule made observable: a loop-based implementation would scale with node count. Also assert the round trip — every stored node id appears in `vs["name"]`, every stored edge weight appears in `es["weight"]` — and add the negative control that mutating one stored weight changes the exported `es["weight"]`.

`test_vector_score_all.py` — seed 25 vectors, assert `len(score_all(v)) == 25` (never a top-k truncation), assert descending score order with the `id` tie-break, and assert against the same store that `query(v, top_k=5)` returns 5, so the two methods are visibly different capabilities rather than one wrapping the other.

`test_store_isolation.py` — resolve LightRAG's `naive` arm and HippoRAG's base wiring against one `store_root` and one `workspace`, build each one's stores through the seam's own `_build_stores`, and assert the resolved graph and KV directory paths are disjoint between the two. Then pin the regression: LightRAG's resolved graph directory ends in `chunk_entity_relation` and its KV directory ends in `text_chunks` — the values in use before this plan — so a future change that relocates LightRAG's on-disk state fails here rather than silently orphaning the Phase 3 imported index. Add the cross-read refusal: reading HippoRAG's graph namespace after seeding only LightRAG's returns no nodes, and the reverse.

`test_registration.py` — assert every `HIPPORAG_PARTS` member's `name_at_version`, `kind`, `effects` and `structural_depth`, table-driven against a pinned mapping in the test module, and assert `derive_execution_mode(part.effects, part.kind) == "in-process"` for all five (none is `opaque`-kinded, none declares `net`/`fs`/`self_storage`/`mutates_store`, so none needs an admission record). Assert `effective_depth` over the parsed HippoRAG wiring is `opaque` at every node.

`test_ppr.py` — drive `_ppr_body` directly with a hand-built reset vector containing one NaN and one negative entry and assert both are zeroed before the call; assert the readback contains only passage-partition vertices and is sorted descending; assert the scores equal an independently-constructed `igraph.Graph.personalized_pagerank` call made in the test with the same pinned keyword arguments; and add the negative control that changing `damping` from 0.5 to 0.85 produces a different score vector, so the pinned-argument assertion has teeth.
  </action>
  <verify>
    <automated>cd databasise && uv run pytest -q tests/stores/test_graph_bulk_export.py tests/stores/test_vector_score_all.py tests/seam/test_store_isolation.py tests/parts_core/hipporag/ -x</automated>
    <fails_when>non-zero exit, or "no tests ran" in the summary line</fails_when>
    <automated>cd databasise && uv run pytest -q</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `test_graph_bulk_export.py` asserts the Cozo query count equals 2 for both a 3-node and a 30-node graph
    - `test_vector_score_all.py` asserts `score_all` returns one entry per stored vector and `query` returns `top_k`, against the same seeded store
    - `test_store_isolation.py` asserts LightRAG's resolved graph directory basename is `chunk_entity_relation` and HippoRAG's is `hipporag-graph`, and that the two path sets are disjoint
    - `test_registration.py` asserts `structural_depth == "opaque"` and `derive_execution_mode(...) == "in-process"` for all five parts
    - `test_ppr.py` contains a negative control that changes `damping` and asserts the resulting scores differ
    - Full suite `cd databasise && uv run pytest -q` exits 0
  </acceptance_criteria>
  <done>Bulk-export cannot regress into a pointwise loop, `score_all` cannot regress into a top-k call, the two modalities' store directories cannot collide, and every HippoRAG part's declared shape is pinned — all proven by failing-red tests.</done>
</task>

</tasks>

<verification>
- `cd databasise && uv run pytest -q` exits 0
- `cd databasise && uv run python -m databasise.tools.check_import_boundary` exits 0
- One real seam call resolves to the HippoRAG wiring and returns evidence from `hipporag-chunks`
- No LightRAG store directory moved
</verification>

<success_criteria>
HippoRAG 2's five-node query-side chain answers a query end to end through the §18 seam over its own isolated stores, reached by a caller-supplied capability selector, with whole-graph PPR served by one native prpack call over a two-query bulk export.
</success_criteria>

<flagged_assumptions>
- **MODAL-04 edge behaviour is unclassified.** The deterministic edge probe returned `unclassified` for MODAL-04 and it is carried here as `unresolved`, not auto-resolved. The frozen record states what `ppr` is handed, does and returns, but names no acceptance boundary for a degenerate graph (zero passage vertices, an all-zero reset vector, a disconnected graph). This plan's behaviour on those inputs is whatever `igraph` returns; nothing here asserts it is correct.
- **The upstream `hipporag` package is not installed and is not the oracle in this plan.** `test_ppr.py` compares the node's output against a direct `igraph` call with pinned arguments, which pins the transcription risk (damping 0.5 not 0.85, undirected, prpack, NaN/negative zeroing, partitioned readback) without a `torch==2.5.1` dependency. Plan 06-09 records what an upstream-package parity run would additionally settle and why it is not run in this phase.
- **Store namespaces are declared per wiring, not derived via `derive_namespace()`.** RIG §RUN.1's derivation is the eventual form; adopting it now would relocate every existing LightRAG store directory, which `databasise/namespaces.py`'s D-07 note classifies as an on-disk migration.
</flagged_assumptions>

<output>
Create `.planning/phases/06-hipporag-2-side-by-side/06-01-SUMMARY.md` when done
</output>
