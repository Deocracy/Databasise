---
phase: 06-hipporag-2-side-by-side
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - databasise/parts_core/hipporag/chunk_embed.py
  - databasise/parts_core/hipporag/openie.py
  - databasise/parts_core/hipporag/entity_fact_embed.py
  - databasise/parts_core/hipporag/__init__.py
  - databasise/wirings/hipporag/base.json
  - databasise/tests/parts_core/hipporag/test_index_side_extraction.py
  - databasise/tests/parts_core/hipporag/conftest.py
autonomous: true
requirements: [MODAL-04]

estimate:
  tokens: 35000
  raw_tokens: 70000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "Given three source documents and stub clients, the `chunk-embed` -> `openie` -> `entity-fact-embed` chain lands chunk vectors in `hipporag-chunks`, chunk text in `hipporag-text-chunks`, and entity and fact vectors in `hipporag-entities` and `hipporag-facts`."
    - "`openie` makes two LLM calls per chunk — named-entity recognition, then triple extraction — as one fused operation with no external branch between them, matching the `## §H` row for this node."
    - "Dispatched with no documents configured, every one of the three index-side nodes makes zero client calls and returns an empty result rather than raising, so a query-time run of the base wiring costs nothing at these positions."
    - "Token spend from every LLM and embedding call in this chain reaches the run record through the same `TokenAccounting` channel the LightRAG parts use, never a fabricated zero."
  artifacts:
    - databasise/parts_core/hipporag/chunk_embed.py
    - databasise/parts_core/hipporag/openie.py
    - databasise/parts_core/hipporag/entity_fact_embed.py
    - databasise/tests/parts_core/hipporag/test_index_side_extraction.py
  key_links:
    - "chunk-embed's emitted chunk ids -> openie's per-chunk extraction -> entity-fact-embed's fact and entity ids -> the vertex refs reset-vector-join and ppr resolve"
  prohibitions:
    - "MUST NOT report a token count of zero for an LLM or embedding call whose spend the client did not expose — the codebase already distinguishes a real zero (`counted_by=\"none\"`) from an unexposed one (`unbudgetable`), and collapsing the two fabricates a fact."
    - "MUST NOT fold two of the thirteen `## §H` node positions into one module because their bodies look similar — the position count is the requirement, and a quiet merge reduces the decomposition while reporting it complete."
---

<objective>
Port the first three of HippoRAG 2's seven index-side node positions: `chunk-embed`, `openie` and `entity-fact-embed` — the chunking-and-embedding fusion, the two-call OpenIE extraction, and the entity/fact embedding into two vector namespaces.

Purpose: these three produce every identifier the graph-construction nodes (06-05) and the query side (06-01, 06-07) resolve against. Without them the seeded test fixture is the only index HippoRAG has.
Output: three registered executable parts, three more nodes in the HippoRAG base wiring, and a stub-client integration test proving the chain end to end.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-hipporag-2-side-by-side/06-RESEARCH.md
@.planning/phases/06-hipporag-2-side-by-side/06-PATTERNS.md
@.planning/phases/06-hipporag-2-side-by-side/06-01-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols created by this plan: `databasise/parts_core/hipporag/chunk_embed.py` (`HIPPORAG_CHUNKER_EMBEDDER_PART`), `openie.py` (`HIPPORAG_OPENIE_EXTRACTOR_PART`), `entity_fact_embed.py` (`HIPPORAG_ENTITY_FACT_EMBEDDER_PART`). Wiring node ids `chunk-embed`, `openie`, `entity-fact-embed`. Fixture `seeded_hipporag_source_documents`.

Phase-level symbol list: see `06-01-PLAN.md` § "Artifacts this phase produces".

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: chunk-embed — the fused chunk-store-and-embed position</name>
  <files>databasise/parts_core/hipporag/chunk_embed.py, databasise/parts_core/hipporag/__init__.py, databasise/wirings/hipporag/base.json, databasise/tests/parts_core/hipporag/conftest.py</files>
  <read_first>
    - docs/system-model/PARTS.md (`## §H`, the `chunk-embed` row: §13.4 type 2 `embedder`, fused chunk-store + embed with no external cut point per §19.4, `HippoRAG.py:282`; and the `Recipe:` field naming `chunk-embed` as both the chunker and embedding role)
    - docs/system-model/wirings/hipporag-base.json (the `chunk-embed` node's governing kind, effects, note and deps)
    - databasise/parts_core/lightrag/embedder_index.py (the index-side no-op-when-unconfigured precedent, the `ctx.clients["embedding"].embed(...)` call shape and the `tokens`/`resolved_model_identity` passthrough)
    - databasise/parts_core/hipporag/fact_score.py (this package's own established module shape, as landed by 06-01)
    - databasise/stores/vector.py (`FaissVectorStore.upsert` signature: `ids`, `embeddings`, `metadatas`)
    - databasise/stores/kv.py (the KV upsert signature)
    - databasise/parts_core/hipporag/__init__.py
  </read_first>
  <behavior>
    - Test 1: dispatched with `config["documents"]` naming three source documents, emits one chunk record per produced chunk, each carrying a stable `chunk_id`, and writes those chunk vectors into the `hipporag-chunks` vector namespace and their text into the `hipporag-text-chunks` KV namespace.
    - Test 2: dispatched with no `documents` key, makes zero calls on the injected embedding client and returns an empty chunk list.
    - Test 3: chunk ids are deterministic — running the same document twice produces the same ids.
    - Test 4: the emitted `tokens` value is the embedding client's own reported `TokenAccounting`, unmodified.
  </behavior>
  <action>
Create `databasise/parts_core/hipporag/chunk_embed.py` registering `HIPPORAG_CHUNKER_EMBEDDER_PART` as `hipporag/chunker-embedder@0.1.0`, `kind="embedder"`, `structural_depth="opaque"`, `upstream_ref="hipporag/src/hipporag/HippoRAG.py"`.

The module docstring names the `## §H` row this implements, quotes its "fused chunk-store + embed, no external cut point per §19.4" clause, and states that chunking and embedding are deliberately one node because the upstream `EmbeddingStore.insert_strings` call fuses them with no knob at that boundary — this is the reason the wiring's `recipe` names `chunk-embed` for both the `chunker` and `embedding` roles.

Body: read `config["documents"]` as a list of `{"document_id", "text"}` mappings; absent or empty, return `{"chunks": []}` with no client call, following `embedder_index.py`'s own unconfigured-run precedent. Otherwise split each document into chunks using the chunk size and overlap named in `config["chunk_size"]` / `config["chunk_overlap"]` (defaulting to values stated in the module docstring), derive each `chunk_id` as a SHA-256 over the document id and the chunk's own ordinal so ids are stable across reruns, embed every chunk text in one batched `ctx.clients["embedding"].embed(texts)` call rather than one call per chunk, upsert the vectors into `ctx.stores["vector"].select("hipporag-chunks")` and the chunk text into `ctx.stores["kv"]`, and emit `{"chunks": [...], "tokens": result.tokens, "resolved_model_identity": result.resolved_model_identity}` where each chunk record carries `chunk_id`, `document_id`, `ordinal` and `text`.

Declared effects are `["calls_embedding", "writes_vector", "writes_kv"]`. `## §H` declares `calls_embedding` alone for this row; `writes_vector` and `writes_kv` are added because `CapabilityScopedStores.require` hands a body no store handle for an undeclared effect, and this node's own `## §H` description ("fused chunk-store + embed") is a store write. Record both additions in the SUMMARY as additive effects discrepancies against `## §H`, in the shape 05-06 used for `mutates_store` on the codebase-memory-mcp part — recorded, not silently resolved either direction.

Add the `chunk-embed` node to `databasise/wirings/hipporag/base.json` with `component: "hipporag/chunker-embedder@0.1.0"`, `kind: "embedder"`, the effects above, `deps: []`, and the note field the governing document carries. Register the part in `HIPPORAG_PARTS`.

Add `seeded_hipporag_source_documents` to `databasise/tests/parts_core/hipporag/conftest.py`: three short documents whose entities overlap, so 06-05's shared-entity edge cases have real data.
  </action>
  <verify>
    <automated>cd databasise && uv run pytest -q tests/parts_core/hipporag/test_index_side_extraction.py -x -k chunk_embed</automated>
    <fails_when>non-zero exit, or "no tests ran" in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - `databasise/parts_core/hipporag/chunk_embed.py` defines `HIPPORAG_CHUNKER_EMBEDDER_PART` with `name_at_version="hipporag/chunker-embedder@0.1.0"`, `kind="embedder"`, `structural_depth="opaque"`
    - Its `effects` list is exactly `["calls_embedding", "writes_vector", "writes_kv"]`
    - `HIPPORAG_PARTS` now has six members and `default_registry()` resolves `hipporag/chunker-embedder@0.1.0`
    - `databasise/wirings/hipporag/base.json` contains a `chunk-embed` node with `deps` equal to the empty list
    - A test asserts that a second dispatch of the same document yields identical `chunk_id` values
    - A test asserts zero calls on the injected embedding client when `documents` is absent
  </acceptance_criteria>
  <done>Source documents become stable, embedded, stored chunks at the `chunk-embed` position; an unconfigured run costs nothing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: openie — the two-call NER-then-triple extraction position</name>
  <files>databasise/parts_core/hipporag/openie.py, databasise/parts_core/hipporag/__init__.py, databasise/wirings/hipporag/base.json</files>
  <read_first>
    - docs/system-model/PARTS.md (`## §H`, the `openie` row: §13.4 type 3 `extractor`, `calls_llm`, `List[text_chunk]` → `List[derived_finding]`, "NER then triple extraction, `HippoRAG.py:299`, one fused two-call operation with no external branch"; and the `Recipe:` field naming `openie` as the extraction role)
    - databasise/parts_core/lightrag/keywords.py (the `ctx.clients["llm"].chat(...)` call shape, prompt construction, JSON-repair-tolerant response parsing and `TokenAccounting` reporting)
    - databasise/parts_core/hipporag/chunk_embed.py (as written by Task 1 — the chunk record shape this node consumes)
    - databasise/parts_core/hipporag/__init__.py
    - reference/hipporag2-2502.14802.pdf (the OpenIE step as the paper describes it, for the prompt's intent — never for a line citation)
  </read_first>
  <behavior>
    - Test 1: given two chunks and a stub LLM client, exactly four `chat` calls are made — two per chunk, in NER-then-triples order — and the second call for a chunk carries that chunk's own NER result.
    - Test 2: emits one `derived_finding` per extracted triple, each carrying `subject`, `predicate`, `object`, the source `chunk_id`, and a stable `fact_id`.
    - Test 3: a chunk whose triple-extraction response is malformed JSON yields zero findings for that chunk and does not abort the other chunk's findings.
    - Test 4: with an empty `chunks` input, zero `chat` calls are made.
  </behavior>
  <action>
Create `databasise/parts_core/hipporag/openie.py` registering `HIPPORAG_OPENIE_EXTRACTOR_PART` as `hipporag/openie-extractor@0.1.0`, `kind="extractor"`, `structural_depth="opaque"`, `effects=["calls_llm"]`, `upstream_ref="hipporag/src/hipporag/HippoRAG.py"`.

The module docstring names the `## §H` row, quotes its "one fused two-call operation with no external branch" clause, and states explicitly that the two LLM calls are internals of this one node position rather than two positions — that fusion is what `## §H` records, and splitting it would change the node count the requirement pins.

Body: read `ctx.inputs["chunk-embed"]["chunks"]`. For each chunk, issue the named-entity-recognition `chat` call, then the triple-extraction `chat` call carrying the recognised entities from the first, mirroring `keywords.py`'s prompt construction and its tolerant response parsing (this repository already depends on `json_repair` behaviour in v1; parse defensively and treat an unparseable response as zero findings for that chunk with the reason recorded on the emitted output, never an aborted run for the other chunks). Derive each `fact_id` as a SHA-256 over the canonicalised triple so the same triple from two chunks resolves to one fact. Emit `{"findings": [...], "tokens": <summed TokenAccounting across every call>, "resolved_model_identity": ...}`.

Sum token accounting across all calls through the same `TokenAccounting` addition the runner's budget metering already understands — never report a bare zero for a node that made calls. Where the client exposes no usage for a call, propagate the `unbudgetable` sentinel the seam already handles rather than substituting a number.

Both prompts live as module-level constants in this file, named `_NER_PROMPT` and `_TRIPLE_PROMPT`. The module docstring records that the OpenIE prompt text is carried over as this component's internals, exactly as `PARTS.md ## §P`'s HippoRAG row states ("The OpenIE extraction prompts ... carried inside the component").

Add the `openie` node to `databasise/wirings/hipporag/base.json` with `deps: ["chunk-embed"]` and register the part in `HIPPORAG_PARTS`.
  </action>
  <verify>
    <automated>cd databasise && uv run pytest -q tests/parts_core/hipporag/test_index_side_extraction.py -x -k openie</automated>
    <fails_when>non-zero exit, or "no tests ran" in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - `databasise/parts_core/hipporag/openie.py` defines `_NER_PROMPT` and `_TRIPLE_PROMPT` as module-level constants
    - `HIPPORAG_OPENIE_EXTRACTOR_PART.effects` is exactly `["calls_llm"]`
    - A test counts exactly two stub-client `chat` calls per input chunk and asserts the NER call precedes the triple call for that chunk
    - A test asserts one malformed triple response yields zero findings for its own chunk and non-zero findings for the sibling chunk in the same dispatch
    - `databasise/wirings/hipporag/base.json`'s `openie` node has `deps` equal to `["chunk-embed"]`
  </acceptance_criteria>
  <done>Chunks become triples at one node position through two fused LLM calls, with malformed output degrading per-chunk rather than per-run.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: entity-fact-embed — two vector namespaces from one extraction</name>
  <files>databasise/parts_core/hipporag/entity_fact_embed.py, databasise/parts_core/hipporag/__init__.py, databasise/wirings/hipporag/base.json, databasise/tests/parts_core/hipporag/test_index_side_extraction.py</files>
  <read_first>
    - docs/system-model/PARTS.md (`## §H`, the `entity-fact-embed` row: §13.4 type 2 `embedder`, `calls_embedding`, `List[derived_finding]` → `List[graph_entity]` and `List[graph_fact]` with `Score` attached, `HippoRAG.py:317,320`, "two Vector namespaces: entities, facts", declared capability `§14.2 namespaces`)
    - docs/system-model/wirings/hipporag-base.json (the `entity-fact-embed` node's `config.namespaces` declaration)
    - databasise/parts_core/hipporag/openie.py (as written by Task 2 — the finding record shape)
    - databasise/parts_core/hipporag/fact_score.py (the `hipporag-facts` namespace and the fact record shape the query side reads)
    - databasise/stores/vector.py (`MultiNamespaceVectorStore.select`, `FaissVectorStore.upsert`)
    - databasise/parts_core/hipporag/__init__.py
  </read_first>
  <behavior>
    - Test 1: given findings covering four distinct entities and three distinct facts, upserts four vectors into `hipporag-entities` and three into `hipporag-facts`, and no vector into `hipporag-chunks`.
    - Test 2: the fact vector ids equal `openie`'s own `fact_id` values, so `fact-score`'s scored items resolve back to findings.
    - Test 3: the entity vector ids carry the entity-vertex ref prefix `ppr` and `reset-vector-join` partition on, distinct from the chunk-vertex prefix.
    - Test 4: an entity appearing in two findings is embedded once, not twice.
  </behavior>
  <action>
Create `databasise/parts_core/hipporag/entity_fact_embed.py` registering `HIPPORAG_ENTITY_FACT_EMBEDDER_PART` as `hipporag/entity-fact-embedder@0.1.0`, `kind="embedder"`, `structural_depth="opaque"`, `effects=["calls_embedding", "writes_vector"]`, `upstream_ref="hipporag/src/hipporag/HippoRAG.py"`. `## §H` declares `calls_embedding` for this row; `writes_vector` is the same additive-declaration case as Task 1's and is recorded the same way in the SUMMARY.

The module docstring names the `## §H` row and its declared `§14.2 namespaces` capability, and states which two namespaces this node writes and why they are separate: `fact-score` scores against facts exhaustively while `synonymy-edges` runs self-KNN over entities, and a single namespace would make one of the two read the other's records.

Body: read `ctx.inputs["openie"]["findings"]`. Deduplicate entities by normalised surface form and facts by `fact_id`. Embed the deduplicated entity strings and the deduplicated fact strings in two batched `ctx.clients["embedding"].embed(...)` calls, never one call per item. Upsert entity vectors into `ctx.stores["vector"].select("hipporag-entities")` keyed by the entity vertex ref, and fact vectors into `ctx.stores["vector"].select("hipporag-facts")` keyed by `fact_id`, carrying the source `chunk_id` list in each fact's metadata so `reset-vector-join`'s fact-score-over-chunk-count scaling has its divisor. Emit `{"entities": [...], "facts": [...], "tokens": ..., "resolved_model_identity": ...}`.

The vertex-ref prefixes are the node identity space `export_to_igraph`, `reset-vector-join` and `ppr` all share. Define them once as module-level constants in this file — an entity prefix and a chunk prefix — and import them from here in 06-05's graph-construction nodes rather than re-spelling the strings. State in the docstring that entity-prefixed and chunk-prefixed refs resolve to disjoint vertex indices, which is what makes `reset-vector-join`'s sum disjoint-support and order-insensitive per `## §H`'s `arm_precedence` declaration.

Add the `entity-fact-embed` node to `databasise/wirings/hipporag/base.json` with `deps: ["openie"]` and `config: {"namespaces": ["entities", "facts"]}` matching the governing document. Register the part in `HIPPORAG_PARTS`.

Write `databasise/tests/parts_core/hipporag/test_index_side_extraction.py` covering all three tasks' `<behavior>` blocks, plus one chain test that dispatches `chunk-embed` then `openie` then `entity-fact-embed` in sequence against `seeded_hipporag_source_documents` and stub clients, and asserts that the fact ids landing in `hipporag-facts` are exactly the ids `openie` emitted.
  </action>
  <verify>
    <automated>cd databasise && uv run pytest -q tests/parts_core/hipporag/test_index_side_extraction.py -x</automated>
    <fails_when>non-zero exit, or "no tests ran" in the summary line</fails_when>
    <automated>cd databasise && uv run pytest -q</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `HIPPORAG_PARTS` has exactly eight members after this plan; `default_registry()` resolves all eight `hipporag/` names
    - `databasise/parts_core/hipporag/entity_fact_embed.py` defines the entity and chunk vertex-ref prefix constants at module level
    - A test asserts an entity present in two findings produces exactly one upserted entity vector
    - A test asserts the number of `embed` calls across a full three-document chain run is bounded — one batched call at `chunk-embed`, two at `entity-fact-embed` — never one per item
    - The chain test asserts `hipporag-facts` holds exactly the fact ids `openie` emitted
    - Full suite `cd databasise && uv run pytest -q` exits 0
  </acceptance_criteria>
  <done>Extracted findings become entity and fact vectors in two isolated namespaces, keyed so the query side and the graph builders both resolve against the same vertex-ref space.</done>
</task>

</tasks>

<verification>
- `cd databasise && uv run pytest -q` exits 0
- `cd databasise && uv run python -m databasise.tools.check_import_boundary` exits 0
- The HippoRAG base wiring now declares eight nodes and still parses clean against `default_registry()`
</verification>

<success_criteria>
Three of HippoRAG 2's seven index-side positions run as registered primitive parts, producing stable chunk, entity and fact identifiers in their own isolated namespaces, with every client call's spend accounted and an unconfigured dispatch costing nothing.
</success_criteria>

<output>
Create `.planning/phases/06-hipporag-2-side-by-side/06-02-SUMMARY.md` when done
</output>
