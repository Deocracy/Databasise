---
phase: 06-hipporag-2-side-by-side
plan: 07
type: execute
wave: 4
depends_on: ["06-05"]
files_modified:
  - databasise/parts_core/hipporag/dpr_fallback.py
  - databasise/parts_core/hipporag/fact_filter.py
  - databasise/parts_core/hipporag/assemble_result.py
  - databasise/parts_core/hipporag/__init__.py
  - databasise/wirings/hipporag/base.json
  - databasise/evidence/HIPPORAG-PORT-RECORD.md
  - databasise/tests/parts_core/hipporag/test_dpr_fallback_guard.py
  - databasise/tests/parts_core/hipporag/test_thirteen_positions.py
  - databasise/tests/evidence/test_hipporag_port_record.py
autonomous: true
requirements: [MODAL-04]

estimate:
  tokens: 35000
  raw_tokens: 70000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "The HippoRAG base wiring declares exactly thirteen node positions whose ids and component names equal the thirteen `PARTS.md ## §H` names, and every one resolves to a registered executable part."
    - "No HippoRAG node is declared with the `opaque` structural kind — the port leaves no opaque core behind, and the registry holds no admission record for any `hipporag/` part."
    - "When zero facts survive `fact-filter`, `dpr-fallback` executes and `assemble-result` returns its items; when facts survive, the `reset-vector-join`/`ppr` path supplies them."
    - "The guard's firing is observable per query through the run record's existing per-node `guards_fired` field, reached via the envelope's trace reference — no new envelope field is minted and the run is not labelled degraded for a guard that fired as declared."
    - "Every effect this port declares beyond what `PARTS.md ## §H` lists for that node is enumerated in one committed record with a per-row reason, rather than resolved silently in either direction."
  artifacts:
    - databasise/parts_core/hipporag/dpr_fallback.py
    - databasise/evidence/HIPPORAG-PORT-RECORD.md
    - databasise/tests/parts_core/hipporag/test_thirteen_positions.py
  key_links:
    - "fact-filter's zero-surviving-facts condition -> the declared §19.8 guard -> NodeTrace.guards_fired -> the envelope's trace reference"
    - "the thirteen registered parts -> effective_depth opaque at every position -> exclusion from the default selector"
  prohibitions:
    - "MUST NOT register the DPR fallback as a second arm or a second wiring — §19.8's equal-node-set rule makes it one arm carrying a declared guard, and a second arm would make every comparison between them a comparison of arms differing in which guards fired."
    - "MUST NOT mark a run degraded because a declared guard fired as designed — `degraded` and `partial` are a required-together pair naming an unhealthy run, and using them for a normal declared variant would make a real degradation indistinguishable from an ordinary fallback."
    - "MUST NOT close out the port while any node's declared effects differ from `## §H` without that difference being written down — an undocumented divergence between the governing record and the built part is exactly the contract erosion the opaque-boundary rule exists to prevent."
---

<objective>
Complete HippoRAG 2's decomposition: port `dpr-fallback`, declare the `§19.8` zero-surviving-facts guard on the node that evaluates it, wire the fallback into `assemble-result`, and pin the thirteen-position node set against the governing record.

Purpose: MODAL-04's claim is thirteen positions with no opaque core left behind. This plan is where that claim becomes checkable rather than asserted.
Output: the thirteenth part, a declared and observable data guard, a conformance test against `PARTS.md ## §H`, and one committed record of every declared-effect divergence from it.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-hipporag-2-side-by-side/06-RESEARCH.md
@.planning/phases/06-hipporag-2-side-by-side/06-01-SUMMARY.md
@.planning/phases/06-hipporag-2-side-by-side/06-02-SUMMARY.md
@.planning/phases/06-hipporag-2-side-by-side/06-05-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols created by this plan: `HIPPORAG_DPR_FALLBACK_PART`, wiring node id `dpr-fallback`, the `zero_surviving_facts_dpr_fallback` guard declaration, `databasise/evidence/HIPPORAG-PORT-RECORD.md`.

Phase-level symbol list: see `06-01-PLAN.md` § "Artifacts this phase produces".

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: dpr-fallback — dense passage retrieval on the guarded branch</name>
  <files>databasise/parts_core/hipporag/dpr_fallback.py, databasise/parts_core/hipporag/__init__.py, databasise/wirings/hipporag/base.json</files>
  <read_first>
    - docs/system-model/PARTS.md (`## §H`, the `dpr-fallback` row: §13.4 type 4 `retriever`, effects `calls_embedding` and `reads_vector`, query object → `List[ScoredItem]` of kind `text_chunk`, "calls the same `dense_passage_retrieval` helper `HippoRAG.py:1467-1499` `retrieve_dpr`'s separate top-level ablation entry point also calls, never itself wired here"; and the `Arms:` field's point 3 stating `retrieve_dpr` is explicitly not wired)
    - docs/system-model/wirings/hipporag-base.json (the `dpr-fallback` node's governing kind, effects, note and its `deps: ["fact-filter"]`)
    - databasise/parts_core/hipporag/fact_score.py (the query-embedding call shape and the `hipporag-chunks` namespace)
    - databasise/parts_core/hipporag/reset_vector_join.py (the passage-weight construction, which computes min-max-normalised dense passage retrieval scores — the same helper this node's output is drawn from, so the two must not diverge)
    - databasise/parts_core/hipporag/__init__.py
  </read_first>
  <behavior>
    - Test 1: given a query and a seeded `hipporag-chunks` namespace, emits `text_chunk`-kind scored items for every stored chunk sorted by descending score — a full dot product over the passage matrix, not a top-k store query.
    - Test 2: the scores it emits for a given query are the same scores `reset-vector-join` derives its passage-weight component from, so the two positions cannot silently diverge on what dense passage retrieval means.
    - Test 3: dispatched when the upstream guard did not fire, it still runs and emits its items — the wiring dispatches it unconditionally and `assemble-result` chooses; nothing in this body reads a guard.
    - Test 4: with an empty chunk namespace, emits an empty item list rather than raising.
  </behavior>
  <action>
Create `databasise/parts_core/hipporag/dpr_fallback.py` registering `HIPPORAG_DPR_FALLBACK_PART` as `hipporag/dpr-fallback@0.1.0`, `kind="retriever"`, `structural_depth="opaque"`, `effects=["calls_embedding", "reads_vector"]`, `upstream_ref="hipporag/src/hipporag/HippoRAG.py"`. These are exactly the effects `## §H` declares for this row; no additive declaration is needed here.

Body: embed the query through `ctx.clients["embedding"]`, then take a full dot product against every stored chunk vector in `hipporag-chunks` via the `score_all` capability 06-01 landed — `## §L.1`'s own `core/`-namespace check already records the distinction that makes this a different declared sub-capability from a top-k store query, and using `query(top_k=...)` here would silently make it the same one.

Factor the dense-passage-retrieval scoring into a module-level helper in this file and have `reset_vector_join.py` import it for its own passage-weight component, so the two positions cannot diverge on what dense passage retrieval means. That mirrors the upstream shape `## §H` describes — `dpr-fallback` and the reset construction call the same helper — and it is why Test 2 is checkable at all.

The module docstring states, in `## §H`'s own terms, that upstream's `retrieve_dpr` is a separate top-level ablation entry point reached by no path `retrieve()` takes, and is deliberately not wired here — stated explicitly rather than omitted, because an omission reads to a later reader as an oversight.

Add the `dpr-fallback` node to `databasise/wirings/hipporag/base.json` with `deps: ["fact-filter"]` and the note field the governing document carries, and register the part in `HIPPORAG_PARTS`.
  </action>
  <verify>
    <automated>cd databasise && uv run pytest -q tests/parts_core/hipporag/test_dpr_fallback_guard.py -x -k dpr</automated>
    <fails_when>non-zero exit, or "no tests ran" in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - `HIPPORAG_DPR_FALLBACK_PART.effects` is exactly `["calls_embedding", "reads_vector"]`
    - `databasise/parts_core/hipporag/dpr_fallback.py` calls `score_all` and contains no `top_k` argument
    - `databasise/parts_core/hipporag/reset_vector_join.py` imports the dense-passage-retrieval helper from `dpr_fallback.py` rather than computing its own
    - A test asserts the two positions produce the same scores for one query against one seeded namespace
    - `databasise/wirings/hipporag/base.json`'s `dpr-fallback` node has `deps` equal to `["fact-filter"]`
  </acceptance_criteria>
  <done>The thirteenth position runs, sharing one dense-passage-retrieval implementation with the reset-vector construction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the §19.8 guard — declared, evaluated, and observable through the trace</name>
  <files>databasise/parts_core/hipporag/fact_filter.py, databasise/parts_core/hipporag/assemble_result.py, databasise/wirings/hipporag/base.json, databasise/tests/parts_core/hipporag/test_dpr_fallback_guard.py</files>
  <read_first>
    - docs/system-model/PARTS.md (`## §H`'s `Arms:` field, point 1 in full — the zero-surviving-facts DPR fallback as a runtime data-guard, declared under §19.8, with `fact-filter` as the evaluating node, the socket value emitted when the guard does not fire, the §19.8 equal-node-set rule's outcome (1) making it one arm and not two, and the D10 reconciliation stating its firing is observable through §18.2's existing degradation-path envelope field with no new field minted)
    - docs/system-model/wirings/hipporag-base.json (the top-level `guards` array's `zero_surviving_facts_dpr_fallback` entry — every field it declares)
    - databasise/runner/guards.py (the whole module — `GuardDeclaration`, the mandatory `per-query`/`per-instance` granularity, `evaluate_guards`, and the module docstring's rule that a guard declaration is carried on the guarded node's own wiring `config.guards` so `config_hash` already covers it)
    - databasise/runner/scheduler.py (where `evaluate_guards(guards_by_node[node_id], results)` runs and how its result reaches `NodeTrace.guards_fired`)
    - databasise/runner/trace.py (the module docstring's honesty invariant: `partial` and `degraded` are a required-together pair and `RunRecord.__post_init__` refuses a record where they disagree)
    - databasise/parts_core/hipporag/fact_filter.py (as landed by 06-01 — the `guard_fired` output it already emits)
    - databasise/parts_core/hipporag/assemble_result.py (as landed by 06-01)
    - databasise/seam/engine.py (`resolve_trace` — how a caller reaches the node-by-node trace through the envelope's opaque reference)
  </read_first>
  <behavior>
    - Test 1: with facts surviving the filter, `assemble-result` returns items derived from `ppr`, and the run's `fact-filter` node trace reports an empty `guards_fired`.
    - Test 2: with zero facts surviving, `assemble-result` returns `dpr-fallback`'s items, and the run's `fact-filter` node trace reports `zero_surviving_facts_dpr_fallback` in `guards_fired`.
    - Test 3: in the guard-fired case the envelope reports `degraded` false and `partial` false — a declared guard firing as designed is not an unhealthy run.
    - Test 4: the guard's firing is reachable by a caller through `resolve_trace` on the envelope's trace reference, and is not present as a field on the envelope itself.
    - Test 5: the guard declaration's `granularity` is `per-query`, and a declaration omitting granularity raises `GuardDeclarationError` at wire time.
  </behavior>
  <action>
Declare the guard where this codebase already carries guards: on the guarded node's own wiring `config.guards`, per `databasise/runner/guards.py`'s own rule that this placement makes `config_hash` cover the declaration so two wirings differing only in a guard are two identities. The governing `docs/system-model/wirings/hipporag-base.json` carries its `guards` entry at the top level of that illustrative document; the built wiring uses the codebase's own `config.guards` placement. Record that shape difference in Task 3's port record — it is a placement difference between an illustrative document and the built form, not a semantic one, and it should be written down rather than left for a reader to notice.

The declaration on `fact-filter`'s `config.guards` carries the guard name `zero_surviving_facts_dpr_fallback`, `fact-filter` as the evaluating node, `granularity: "per-query"` (mandatory, and the correct one: firing is knowable only from the run record after the query executes, which is exactly what §5's tenth refusal condition reads), and the socket value emitted when the guard does not fire — the `reset-vector-join`/`ppr` arm's own resolved output, as the governing document states.

Confirm `fact_filter.py`'s existing `guard_fired` output is the value `evaluate_guards` reads, and adjust its key or shape only if the guards module requires a different one. Do not add a second guard-evaluation mechanism.

Update `assemble-result` to `deps: ["ppr", "dpr-fallback"]` matching the governing wiring, and update `assemble_result.py`'s body to select between the two inputs on the guard's outcome: `ppr`'s items when the guard did not fire, `dpr-fallback`'s when it did. Read the outcome from `fact-filter`'s emitted value rather than from the trace — a node reading the run record mid-run is exactly what §11's control-channel-is-a-read rule forbids.

Observability, resolved and stated: `## §H` says the firing is observable per query through §18.2's existing envelope machinery with no new field minted. `databasise/runner/trace.py`'s honesty invariant makes `degraded` unusable for this — `degraded` and `partial` are a required-together pair naming an unhealthy run, and `RunRecord` refuses a record where they disagree, so labelling a completed guarded run degraded would either be refused or would make a real degradation indistinguishable from an ordinary declared fallback. The observability channel is therefore the run record's own per-node `guards_fired` field, which RIG §TR.1 already requires and the scheduler already stamps, reached by a caller through the envelope's existing trace reference. That satisfies "no new envelope field is minted" exactly. Write this reasoning into `assemble_result.py`'s module docstring and into Task 3's port record, so the next reader finds the resolution rather than re-deriving it.

Write `databasise/tests/parts_core/hipporag/test_dpr_fallback_guard.py` covering the `<behavior>` block, driving both branches through a real `Databasise.query` against the seeded HippoRAG fixture with a stub LLM client configured to return zero surviving facts in one case and surviving facts in the other.
  </action>
  <verify>
    <automated>cd databasise && uv run pytest -q tests/parts_core/hipporag/test_dpr_fallback_guard.py -x</automated>
    <fails_when>non-zero exit, or "no tests ran" in the summary line</fails_when>
    <automated>cd databasise && uv run pytest -q tests/seam/ tests/runner/ -x</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `databasise/wirings/hipporag/base.json`'s `fact-filter` node carries a `config.guards` entry named `zero_surviving_facts_dpr_fallback` with `granularity` equal to `per-query`
    - The wiring declares no second arm and no patch file for the fallback branch
    - A test asserts the guard-fired run reports `degraded` false and `partial` false on the envelope
    - A test asserts `zero_surviving_facts_dpr_fallback` appears in the `fact-filter` node's `guards_fired` when reached through `resolve_trace`
    - A test asserts the guard name appears nowhere in the serialised envelope itself
    - `assemble_result.py` reads the guard outcome from its input, and contains no read of the run record
  </acceptance_criteria>
  <done>The declared data guard fires on the zero-facts branch, routes to the fallback, and is observable through the trace without minting an envelope field or mislabelling a healthy run.</done>
</task>

<task type="auto">
  <name>Task 3: thirteen positions pinned, and every divergence from ## §H written down</name>
  <files>databasise/tests/parts_core/hipporag/test_thirteen_positions.py, databasise/evidence/HIPPORAG-PORT-RECORD.md, databasise/tests/evidence/test_hipporag_port_record.py</files>
  <read_first>
    - docs/system-model/PARTS.md (`## §H` in full — the node table's thirteen rows with their component names, §13.4 types, effects and declared capabilities; the `Node set:` sentence; the `effects[]:` union sentence; the `Artifact scopes:` field; the `Recipe:` field; the `Arms:` field; the `Admission:` field's `n/a — decomposed` verdict; and the `Port cost class:` row)
    - docs/system-model/wirings/hipporag-base.json (the governing node id set, deps and recipe)
    - databasise/parts_core/hipporag/__init__.py (`HIPPORAG_PARTS` as it stands after Task 1)
    - databasise/wirings/hipporag/base.json (as it stands after Task 2)
    - databasise/tests/parity/test_arm_conformance.py (the resolved-node-id-set conformance shape this test mirrors)
    - databasise/evidence/ADMISSION-CODEBASE-MEMORY-MCP.md (the precedent for recording a declared-effect discrepancy rather than resolving it silently — 05-06's `mutates_store` case)
    - .planning/phases/06-hipporag-2-side-by-side/06-01-SUMMARY.md, 06-02-SUMMARY.md, 06-05-SUMMARY.md (each plan's own recorded additive-effects discrepancies, collected here)
  </read_first>
  <action>
`databasise/tests/parts_core/hipporag/test_thirteen_positions.py` pins the port's completeness claim against the governing record, deriving both sides at test time rather than from a literal list in the test file wherever that is possible.

Assert: the built wiring's resolved node id set equals the thirteen ids `docs/system-model/wirings/hipporag-base.json` declares; every node's `component` resolves in `default_registry()` to a `Part` with a non-`None` body — thirteen executable positions, no declaration-only placeholder left; no `hipporag/` part has `kind == "opaque"` and none carries an `admission` record, which is `## §H`'s `Admission: n/a — decomposed` verdict made checkable and MODAL-04's "no opaque core left behind" clause made checkable with it; the union of the thirteen parts' declared effects equals the union `## §H`'s own `effects[]:` sentence names, plus exactly the additive set the port record enumerates and no more; `effective_depth` over the parsed wiring is `opaque` at all thirteen positions, the taint rule holding; the wiring's `recipe` equals `## §H`'s `Recipe:` field; the wiring declares no arms and no patch file exists for this modality; and `derive_execution_mode` returns `in-process` for all thirteen.

Write `databasise/evidence/HIPPORAG-PORT-RECORD.md` in this codebase's evidence house format — dated header, the claim under test quoted verbatim from MODAL-04, a findings table with per-row `[code-verified]`/`[docs-verified]`/`[inference]` tags, a verdict, method and limits.

Its central content is the declared-effects reconciliation: one row per node where the built part's `effects` differ from what `## §H`'s node table lists, each carrying the `## §H` value, the built value, and the reason for the difference. Collect the rows the earlier plans recorded — `fact-score`'s `calls_embedding`, `chunk-embed`'s `writes_vector` and `writes_kv`, `entity-fact-embed`'s `writes_vector`, `graph-augment-persist`'s `writes_graph` and `writes_artifact` — plus any further one those plans surfaced. Every reason traces to the same mechanical cause: `CapabilityScopedStores.require` hands a body no store handle for an undeclared effect, so a node `## §H` describes as writing a store must declare the write. Follow 05-06's own precedent and record the discrepancy rather than resolving it in either direction: do not edit `docs/system-model/PARTS.md`, which is a verbatim frozen mirror, and do not narrow a built part's effects below what its body actually reaches.

Also record: the guard-placement difference from Task 2 (top-level `guards` in the illustrative document versus `config.guards` in the built wiring, with the `config_hash`-coverage reason); the guard-observability resolution from Task 2 (`guards_fired` on the node trace rather than `degraded`, with the `RunRecord` honesty-invariant reason); and `## §H`'s two "nearest signature" typings (`fact-score`/`ppr`/`dpr-fallback` typed `retriever` by nearest signature, and `graph-augment-persist` having no §13.4 row at all), which is one of the two reasons the index side's effective depth stays `opaque`.

Under limits, state the second reason plainly: `§6`'s parity-not-gain rule and `§5`'s determinism-is-verified rule together mean a fresh port earns `stage`-eligibility only by measurement against a trusted baseline, and no such measurement has been run for HippoRAG 2. Name what one would consist of — the real upstream `hipporag` package in its own pinned, isolated interpreter, run end to end on the same corpus and queries, compared at the retrieval level exactly as Phase 3 did for LightRAG — and state that it is deliberately not run in this phase, because MODAL-04's own text requires the index-side effective depth to stay `opaque` until parity is shown, and keeping it `opaque` is what this phase delivers. Plan 06-09 carries the deferral forward with its trigger.

`databasise/tests/evidence/test_hipporag_port_record.py` parses the document structurally and asserts the effects-reconciliation table covers exactly the set of nodes whose built effects differ from `## §H`'s — computed at test time by comparing the registry against a pinned transcription of `## §H`'s node table in the test module — so a future part that quietly gains an effect fails this test as an unrecorded divergence rather than passing by omission.
  </action>
  <verify>
    <automated>cd databasise && uv run pytest -q tests/parts_core/hipporag/test_thirteen_positions.py tests/evidence/test_hipporag_port_record.py -x</automated>
    <fails_when>non-zero exit, or "no tests ran" in the summary line</fails_when>
    <automated>cd databasise && uv run pytest -q</automated>
    <fails_when>non-zero exit</fails_when>
    <automated>cd databasise && uv run python -m databasise.tools.check_import_boundary</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `HIPPORAG_PARTS` has exactly thirteen members, every one with a non-`None` body
    - A test asserts no `hipporag/` part has `kind == "opaque"` and none carries an `admission` record
    - A test asserts the built wiring's node id set equals the thirteen ids in `docs/system-model/wirings/hipporag-base.json`
    - A test asserts `effective_depth` is `opaque` at all thirteen positions
    - `databasise/evidence/HIPPORAG-PORT-RECORD.md` has an effects-reconciliation table with one row per divergent node, each carrying the `## §H` value, the built value and a reason
    - The record contains the guard-placement row, the guard-observability row, and the nearest-signature typing row
    - The limits section names the unrun upstream-package parity measurement and states why it is deferred
    - `test_hipporag_port_record.py` computes the divergent-node set at test time from the registry and a pinned `## §H` transcription
  </acceptance_criteria>
  <done>Thirteen executable positions, no opaque core, the taint rule holding at every node, and every divergence from the governing record written down with its reason.</done>
</task>

</tasks>

<verification>
- `cd databasise && uv run pytest -q` exits 0
- `cd databasise && uv run python -m databasise.tools.check_import_boundary` exits 0
- The HippoRAG base wiring declares thirteen nodes matching the governing document, all executable, all opaque-depth, none opaque-kinded
</verification>

<success_criteria>
MODAL-04's decomposition claim is complete and checkable: thirteen fitted node positions with no opaque core left behind, whole-graph PPR through the declared bulk-export capability, OpenIE prompts and reset-vector scaling carried as node internals, index-side effective depth `opaque` under the taint rule, and every declared-effect divergence from the frozen record enumerated with its reason.
</success_criteria>

<output>
Create `.planning/phases/06-hipporag-2-side-by-side/06-07-SUMMARY.md` when done
</output>
