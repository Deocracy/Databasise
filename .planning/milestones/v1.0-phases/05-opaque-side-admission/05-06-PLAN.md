---
phase: 05-opaque-side-admission
plan: 06
type: execute
wave: 4
depends_on: ["05-01", "05-04"]
files_modified:
  - databasise/parts/admission.py
  - databasise/parts_core/declared_only.py
  - databasise/parts_core/codebase_memory_mcp.py
  - databasise/foreign/codebase_memory_mcp_adapter.py
  - databasise/wirings/codebase-memory-mcp.json
  - databasise/evidence/ADMISSION-CODEBASE-MEMORY-MCP.md
  - databasise/evidence/FALSIFIER-4-EVIDENCE.md
  - databasise/pyproject.toml
  - databasise/tests/parts_core/test_codebase_memory_mcp_admission.py
  - databasise/tests/evidence/test_falsifier4_evidence.py
  - .planning/phases/05-opaque-side-admission/COVERAGE.md
autonomous: true
requirements: [MODAL-03]

estimate:
  tokens: 88000
  raw_tokens: 88000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`codebase-memory-mcp@0.1.0` carries an `AdmissionRecord` with exactly eleven `ConditionVerdict` entries whose text is a machine-readable projection of `PARTS.md ## §X`'s already-run verdict table — not a re-derivation — and the three rows §X names as not-clean-yeses (condition 1 as evidence for the rule, condition 3 as an open question, conditions 7/8 and the counting half of 9 as machine-side obligations) are recorded as such, never rounded up to a satisfied verdict (MODAL-03)."
    - "The admission record's `wall_clock_ceiling_seconds` is derived from a measured `index_repository` run against a real repository, with the measurement and the margin recorded — the engine self-reports no ceiling, and none is invented (§8 condition 4, ROADMAP criterion 2)."
    - "The version the eleven verdicts were code-verified against (`codebase-memory-mcp@61b3b1b2`, `PARTS.md ## §X`) and the version actually admitted and run (the installed binary's own reported version) are both recorded, and the admission states plainly whether they are the same build; a drift between them is named, not assumed away."
    - "Every item the adapter returns from the foreign engine is normalized to a member of §4's `ItemKind` union at the adapter boundary before it reaches any machine code — an untyped pass-through is refused by name (§17's evidence-normalization rule)."
    - "An interrupted or timed-out foreign-engine call records the placement of both endpoints and the failure cause on the node's trace and is labeled halted or degraded per §9 — never silently dropped from the record (edge MODAL-03/concurrency, §17's health-and-lifecycle rule)."
    - "Falsifier 4's native-chunking leg is actually run against a real repository and its result recorded; the machine-chunks leg carries a recorded disposition stating whether it proved runnable, with `PARTS.md ## §X.5`'s structural finding — that a filesystem-path-only ingestion API may not distinguish two code paths at all — carried forward verbatim rather than restated as a plumbing problem."
    - "The admission records that no OS-level network-namespace isolation is built this phase, and carries `PARTS.md ## §R` row `N12`'s open question as the admission's own caveat rather than as a closed condition (§8 condition 3, 05-RESEARCH.md Open Question 3)."
  prohibitions:
    - "An admission verdict, a survey row, or a falsifier leg must never be recorded as a clean yes, a discharged status, or a completed run when it is an open question, a machine-side obligation, or unrun — an unsettled item is recorded as unsettled."
  artifacts:
    - path: "databasise/parts_core/codebase_memory_mcp.py"
      provides: "The executable body and eleven-verdict AdmissionRecord for codebase-memory-mcp@0.1.0"
      contains: "CODEBASE_MEMORY_MCP_ADMISSION"
    - path: "databasise/foreign/codebase_memory_mcp_adapter.py"
      provides: "The §17 foreign-part adapter for the MCP-served engine: process lifecycle, the RPC channel, health, and evidence normalization to §4's ItemKind union"
      contains: "def normalise_to_item_kind"
    - path: "databasise/evidence/ADMISSION-CODEBASE-MEMORY-MCP.md"
      provides: "The code-inspected admission manifest: the eleven verdicts, the measured ceiling, the version drift, and the network-namespace caveat"
    - path: "databasise/evidence/FALSIFIER-4-EVIDENCE.md"
      provides: "Falsifier 4 run twice: the native-chunking leg's real result and the machine-chunks leg's recorded disposition"
    - path: ".planning/phases/05-opaque-side-admission/COVERAGE.md"
      provides: "The external-API capability matrix for the surfaces this phase consumes"
  key_links:
    - from: "databasise/parts_core/codebase_memory_mcp.py"
      to: "databasise/foreign/codebase_memory_mcp_adapter.py"
      via: "the Part body calls the adapter's tool-invocation entry point; it never speaks the protocol itself"
      pattern: "codebase_memory_mcp_adapter"
    - from: "databasise/foreign/codebase_memory_mcp_adapter.py"
      to: "databasise/seam/evidence.py"
      via: "every returned item is normalized to a §4 ItemKind member before it leaves the adapter"
      pattern: "ItemKind|item_kind"
    - from: "databasise/parts_core/declared_only.py"
      to: "databasise/parts_core/codebase_memory_mcp.py"
      via: "CODEBASE_MEMORY_MCP_PART gains its body and admission record"
      pattern: "CODEBASE_MEMORY_MCP_ADMISSION"
---

<objective>
Admit codebase-memory-mcp whole-engine — a second, differently-shaped foreign engine — through the
same admission machinery the LightRAG ingest core went through, and run Falsifier 4 twice.

Purpose: `CONTRACT §17` names this engine the verified precedent for the foreign-part adapter and
the black-box extreme the contract commits to covering. `PARTS.md ## §X` already ran all eleven
admission verdicts at the source level against a pinned clone; this phase's job is to encode that
verdict table as executable machine data, measure the one value the engine cannot self-report, and
actually run the falsifier rather than leaving it declared.
Output: an admitted, executable second opaque part; the §17 adapter; two evidence documents; and
this phase's `COVERAGE.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-opaque-side-admission/05-RESEARCH.md
@.planning/phases/05-opaque-side-admission/05-01-SUMMARY.md
@.planning/phases/04-the-seam/COVERAGE.md
@.planning/phases/03-lightrag-query-side/COVERAGE.md
</context>

## Artifacts this phase produces

Symbols created by **this plan** (new to the codebase — not drift):

| Symbol | Kind | File |
|---|---|---|
| `CODEBASE_MEMORY_MCP_ADMISSION` | constant | `databasise/parts_core/codebase_memory_mcp.py` |
| `CBM_WALL_CLOCK_CEILING_SECONDS` | constant | `databasise/parts_core/codebase_memory_mcp.py` |
| `codebase_memory_mcp_body` | async function | `databasise/parts_core/codebase_memory_mcp.py` |
| `call_tool` | function | `databasise/foreign/codebase_memory_mcp_adapter.py` |
| `list_tools` | function | `databasise/foreign/codebase_memory_mcp_adapter.py` |
| `normalise_to_item_kind` | function | `databasise/foreign/codebase_memory_mcp_adapter.py` |
| `UntypedForeignItemError` | exception | `databasise/foreign/codebase_memory_mcp_adapter.py` |
| `ForeignEngineUnavailableError` | exception | `databasise/foreign/codebase_memory_mcp_adapter.py` |
| `databasise/wirings/codebase-memory-mcp.json` | wiring document | new file |
| `databasise/evidence/ADMISSION-CODEBASE-MEMORY-MCP.md` | evidence document | new file |
| `databasise/evidence/FALSIFIER-4-EVIDENCE.md` | evidence document | new file |
| `.planning/phases/05-opaque-side-admission/COVERAGE.md` | coverage record | new file |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The §17 adapter — process lifecycle, RPC, health, and evidence normalization</name>
  <files>databasise/foreign/codebase_memory_mcp_adapter.py, databasise/tests/parts_core/test_codebase_memory_mcp_admission.py</files>
  <precondition>`codebase-memory-mcp` is executable on this machine (installed at `/home/chris/.local/bin/codebase-memory-mcp`, reporting version 0.10.8 as of 2026-09-08). If absent, every live-engine test in this plan must skip with a message naming the missing binary — never pass on an assumed surface.</precondition>
  <read_first>
    - `docs/system-model/CONTRACT.md` §17 in full — the node-kind choice (`external-tool` hosted `opaque`), Transport, Corpus-feed handoff, Declared effects, Evidence normalization, Health and lifecycle, Storage and scope, and the Falsifier-6 framing
    - `docs/system-model/CONTRACT.md` §4 — the seven frozen `ItemKind` union members, the open-registry rule, the "kind MUST NOT be a bare scalar field" prohibition, and the tier-stamping rule that caps an unresolvable ref
    - `docs/system-model/PARTS.md` §X — the whole section: the wiring fragment, declared capabilities, the eleven-condition table, the mutation-surface scan, and §X.5's Falsifier-4 finding
    - `databasise/foreign/v1_corpus_adapter.py` — the sibling adapter from plan 05-01: the shape this one mirrors (named refusals, an explicit timeout, non-zero exit as a hard refusal), which is the concrete evidence for or against Falsifier 6's fixed-cost claim
    - `databasise/seam/evidence.py` — `EvidenceRef`'s real field set and `mint_evidence_refs`, and the Phase 4 FA-03 note that it is narrower than §4's `ChunkRef`
    - `databasise/parts/schema.py` — `Effect`'s seventeen members and `ArtifactScope`'s three values
    - `databasise/pyproject.toml` — the `mcp` extra added by plan 05-04
    - `databasise/evidence/wirings/w2-codebase-memory-mcp.json` and `databasise/evidence/FALSIFIER-2-EVIDENCE.md` lines 66-90 — the Falsifier-2 evidence that pins this part's currently-declared effects
  </read_first>
  <behavior>
    - `list_tools()` against the installed binary returns the engine's own tool names; the adapter never hardcodes them.
    - `call_tool(name, arguments, timeout=...)` returns the tool's result; a timeout raises a named refusal carrying the tool name and the ceiling; a non-running or non-existent binary raises `ForeignEngineUnavailableError` naming the resolved path.
    - `normalise_to_item_kind(raw_item, tool_name)` returns a record whose kind is a member of §4's union; a raw item the adapter cannot type raises `UntypedForeignItemError` carrying the tool name and the offending item's own keys — never a pass-through with a bare label.
    - A ref the adapter cannot resolve is tier-capped below `T1` rather than stamped optimistically.
    - The adapter's own module holds no admission logic and no verdict data: process lifecycle, RPC, health and normalization only.
  </behavior>
  <action>
Build the foreign-part adapter for the second engine shape, deliberately reusing the first adapter's structure so Falsifier 6's fixed-cost claim can be judged from the diff.

**A. Determine the client API before writing against it.** `05-RESEARCH.md` records `mcp` 2.2.0 as the SDK but did not fetch its docs. Run `cd databasise && uv run --extra mcp python -c "import mcp, inspect; print(mcp.__version__ if hasattr(mcp,'__version__') else 'n/a'); print([n for n in dir(mcp) if not n.startswith('_')])"` and read the installed package's own stdio-client and session modules to establish the exact import path and call shape. Write against what is installed, not against remembered API. Record the resolved import path in the adapter's module docstring.

**B. `databasise/foreign/codebase_memory_mcp_adapter.py` (new).** Module docstring: this is `CONTRACT §17`'s foreign-part adapter for the MCP-served engine shape — process lifecycle, the RPC channel, and health — and it is deliberately structured like `v1_corpus_adapter.py` so the fixed-cost claim §17 makes lineup-wide is checkable against two real, differently-shaped engines rather than asserted. Provide:
   - `DEFAULT_CBM_BINARY` resolved from `shutil.which("codebase-memory-mcp")` falling back to the known install path, and `ForeignEngineUnavailableError` raised by name when it resolves to nothing executable.
   - `list_tools(*, binary=None, timeout: float)` — start the server over stdio, complete the MCP initialize handshake, enumerate tools, terminate the process, return the tool records. Terminate in a `finally` block so a failed call never leaves a child process behind.
   - `call_tool(name, arguments, *, binary=None, timeout: float)` — the same lifecycle for one tool call. A timeout raises a named refusal carrying tool name and ceiling; any transport-level failure raises a named refusal carrying the failure cause **and the placement of both endpoints** (the machine-side caller and the foreign process), because §17 requires a cross-process failure be diagnosable from the trace alone.
   - `normalise_to_item_kind(raw_item, tool_name)` — map each tool's raw output to a §4 `ItemKind` member: code/snippet-returning tools to `code_snippet`, graph/path-returning tools to `graph_path`, and any computed-from-the-data result to `derived_finding` carrying its `derived_from` refs. Raise `UntypedForeignItemError` for anything that matches none — §4 forbids an untyped pass-through and §17 names this boundary as exactly where it gets reintroduced. Where a returned ref is not machine-resolvable, cap its tier below `T1` and say why in the returned record.
   - No admission data, no verdict text, no ceiling constant in this module — those belong to Task 2's part module.

**C. `databasise/tests/parts_core/test_codebase_memory_mcp_admission.py` (new, adapter half).** One test per `<behavior>` bullet. Every live-engine test is guarded by a skip that names the missing binary explicitly. The normalization tests run without the binary, against captured raw payloads recorded from a real run and committed as fixtures, so the typing rules stay tested even where the engine is absent. The untyped-item test asserts the refusal's own named attributes.
  </action>
  <verify>
    <automated>cd databasise && uv sync --extra mcp</automated>
    <fails_when>non-zero exit, or the resolver reports an unresolvable requirement for `mcp`</fails_when>
    <automated>cd databasise && uv run --extra mcp pytest -q tests/parts_core/test_codebase_memory_mcp_admission.py -x -rs</automated>
    <fails_when>non-zero exit, or the `-rs` skip report names a live-engine test while `codebase-memory-mcp` is executable on PATH, or the summary line reports "no tests ran"</fails_when>
    <automated>cd databasise && uv run python -m databasise.tools.check_import_boundary</automated>
    <fails_when>non-zero exit, or any stdout line reporting an `[import-boundary]` violation</fails_when>
  </verify>
  <acceptance_criteria>
    - `cd databasise && uv run --extra mcp python -c "from databasise.foreign import codebase_memory_mcp_adapter as a; print(sorted(t['name'] if isinstance(t,dict) else t.name for t in a.list_tools(timeout=60)))"` prints a non-empty sorted tool-name list read from the running engine.
    - `grep -c 'ConditionVerdict\|AdmissionRecord\|wall_clock_ceiling' databasise/foreign/codebase_memory_mcp_adapter.py` is 0 — the adapter holds no admission data.
    - `normalise_to_item_kind` maps every tool represented in the committed raw-payload fixtures to one of the seven §4 union members, asserted per fixture.
    - The adapter terminates the child process on both the success and the failure path — asserted by a test that provokes a timeout and then confirms no child process of the test process remains.
    - The transport-failure refusal carries both endpoint placements on named attributes, asserted by the test.
  </acceptance_criteria>
  <done>A second, differently-shaped foreign engine is reachable through an adapter built to the same shape as the first, and nothing it returns enters machine code without a §4 kind.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The eleven verdicts as executable data, the measured ceiling, and the version drift</name>
  <files>databasise/parts_core/codebase_memory_mcp.py, databasise/parts_core/declared_only.py, databasise/parts/admission.py, databasise/wirings/codebase-memory-mcp.json, databasise/pyproject.toml, databasise/evidence/ADMISSION-CODEBASE-MEMORY-MCP.md, databasise/tests/parts_core/test_codebase_memory_mcp_admission.py</files>
  <reversibility rating="costly">Admitting a foreign engine mints its `name@version` admission record and its TTL clock; withdrawing it later means retracting a recorded admission other phases (Phase 6's side-by-side run) build on.</reversibility>
  <read_first>
    - `docs/system-model/PARTS.md` §X's eleven-condition table — read every row and its evidence cell; this task projects it, it does not re-derive it
    - `docs/system-model/PARTS.md` §X's mutation-surface section and the D-14 correction note — `manage_adr` and `delete_project` are the two active mutators, `ingest_traces` is a stub at the pinned SHA
    - `docs/system-model/PARTS.md` §R row `N12` — condition 3's unresolved scoping question, which the admission carries forward as a caveat
    - `docs/system-model/CONTRACT.md` §8 conditions 1-11 and §17's Storage-and-scope paragraph
    - `databasise/parts/admission.py` — `AdmissionRecord`, `ConditionVerdict`, `validate_admission`, `cross_check_conditions`
    - `databasise/parts_core/declared_only.py` — `CODEBASE_MEMORY_MCP_PART`'s current declaration
    - `databasise/parts_core/lightrag/full_ingest.py` — the sibling admission record's structure and its environment-hash computation
    - `databasise/evidence/wirings/w2-codebase-memory-mcp.json` and `databasise/evidence/FALSIFIER-2-EVIDENCE.md` — the Falsifier-2 evidence pinning this part's declared effects, which constrains what may be changed
    - `.planning/phases/05-opaque-side-admission/05-RESEARCH.md` Open Question 3 and Pitfall 3
  </read_first>
  <behavior>
    - `CODEBASE_MEMORY_MCP_PART.admission` carries exactly eleven verdicts, conditions 1 through 11, each with non-empty evidence citing `PARTS.md ## §X`'s own evidence cell.
    - Condition 1's verdict records that the engine's own tool annotations are demonstrably imprecise and that the manifest is built from code inspection — it is not recorded as a satisfied condition.
    - Condition 3's verdict records an open question with `PARTS.md ## §R` row `N12` named, and the record's `network_namespace` field states that no OS-level isolation is built.
    - Conditions 7, 8, and the counting half of 9 are recorded as machine-side obligations with no source evidence either way.
    - `wall_clock_ceiling_seconds` is greater than the measured `index_repository` duration recorded in the evidence document, and the record's ceiling-basis string names that measurement.
    - `CODEBASE_MEMORY_MCP_PART.effects` contains `mutates_store` and retains `self_storage` and `fs`; `artifact_scope` remains `self_storage`.
    - Registering the part with its body and record succeeds; `validate_admission` and `cross_check_conditions` both pass for it.
    - `databasise/evidence/falsifier2.py`'s regenerated output for `w2-codebase-memory-mcp` is byte-identical to the committed `FALSIFIER-2-EVIDENCE.md` section, or the difference is recorded and the document regenerated by running the script — never hand-edited.
  </behavior>
  <action>
Turn §X's already-run analysis into machine data, measure the one value the engine cannot supply, and record what the analysis rests on.

**A. Measure the ceiling first.** Run `index_repository` through the adapter against a real repository — use this repository's own `databasise/` directory, which is small, local, and already the subject of the codebase graph — and record the wall-clock duration. Set `CBM_WALL_CLOCK_CEILING_SECONDS` to that measurement with a stated margin (state the multiple and why). §8 condition 4 requires the ceiling be declared by the admitting machine; `PARTS.md ## §X` records that the engine self-reports none, so a measurement is the only honest basis.

**B. Record the version drift.** Read the installed binary's own reported version. `PARTS.md ## §X`'s eleven verdicts are code-verified against the pinned clone `codebase-memory-mcp@61b3b1b2`. Compare, and put both values in the admission record (`environment_hash`'s input dict carries the binary's SHA-256 and its reported version; the manifest document states the pinned-clone SHA the verdicts came from). If the two are not demonstrably the same build, say so in the manifest and in the affected verdicts' evidence strings: a verdict inherited from a different build is inherited, not re-verified, and must read that way.

**C. Confirm the tool surface live.** Call `list_tools()` and compare the returned tool set against the fifteen `PARTS.md ## §X` names. Record the comparison in the manifest: which tools match, which are new, which are gone. A changed surface directly affects conditions 6, 9 and 11, whose verdicts rest on "none of the fifteen tools' schemas carries an `as_of`" and similar exhaustive claims — re-check each such claim against the live schema set and mark any verdict that can no longer be asserted exhaustively.

**D. `databasise/parts_core/codebase_memory_mcp.py` (new).** `CBM_WALL_CLOCK_CEILING_SECONDS`, `CODEBASE_MEMORY_MCP_ADMISSION` (the eleven verdicts, `storage="self-contained"`, `feed_tier` recording that this engine consumes no machine-produced feed at all, `ttl_days=90`, `network_namespace` recording the open question and the no-isolation scope decision, `environment_hash`, `manifest_source=MANIFEST_SOURCE_CODE_INSPECTED`), and `async def codebase_memory_mcp_body(ctx)` which reads a tool name and arguments from `ctx.config`, calls `await asyncio.to_thread(call_tool, ...)` under the ceiling, and returns items normalized through `normalise_to_item_kind`. Each verdict's `verdict` field must use a vocabulary that can express a non-yes — at minimum `satisfied`, `open-question`, `machine-side-obligation`, and `confirms-the-rule` — because three of these eleven rows are not yeses and a boolean field would force them to become one.

**E. `databasise/parts_core/declared_only.py`.** Give `CODEBASE_MEMORY_MCP_PART` its `body` and `admission`, and add `mutates_store` to its `effects` list additively (PARTS §X's X1 repair landed the effect and the `mutable-store` capability for `manage_adr`/`delete_project`). Keep `self_storage` and `fs`: `PARTS.md ## §X` declares `mutates_store` only, and the built stub declares three — record that discrepancy in the manifest rather than silently choosing. The two extra effects are over-declarations of the same self-contained-storage fact, which §2's deny-by-default rule permits, and they are retained because `databasise/evidence/wirings/w2-codebase-memory-mcp.json` and `FALSIFIER-2-EVIDENCE.md` pin them as committed Falsifier-2 evidence.

**F. `databasise/wirings/codebase-memory-mcp.json` (new).** A one-node wiring naming the part, so the admitted engine is reachable through the same `parse_wiring` → `run_wiring` path the LightRAG ports use. This is the concrete evidence for §17's fixed-cost claim: the same machinery hosts both engine shapes. In `databasise/pyproject.toml`, widen `[tool.setuptools.package-data]`'s `"databasise.wirings"` entry from `["lightrag/*.json"]` to also cover `*.json` at the package root, or the wiring ships in a source checkout and vanishes from an installed package — the same failure `[tool.setuptools.package-data]`'s existing entry was added to prevent for the Falsifier-2 evidence run.

**G. `databasise/evidence/ADMISSION-CODEBASE-MEMORY-MCP.md` (new).** The code-inspected admission manifest. Sections: entry path and node-kind choice (`external-tool` hosted `opaque`, §17); the eleven verdicts as a table, each with its verdict value and its evidence, with the three not-clean-yeses called out in their own short section so a reader cannot miss them; the measured ceiling and its margin; the version drift (pinned clone versus installed binary) and its consequence for inherited verdicts; the live tool-surface comparison; the declared-effects discrepancy between §X and the built Part; the network-namespace scope decision and `§R` row `N12` carried forward as the admission's caveat; and a closing statement of what this admission does and does not license.

**H. `databasise/tests/parts_core/test_codebase_memory_mcp_admission.py` (admission half).** One test per `<behavior>` bullet, asserting on the record's own fields. Include a test asserting that the verdict vocabulary actually contains a non-satisfied value in use — if all eleven read `satisfied`, the test fails, because §X says three of them are not.
  </action>
  <verify>
    <automated>cd databasise && uv run --extra mcp pytest -q tests/parts_core/test_codebase_memory_mcp_admission.py -x -rs</automated>
    <fails_when>non-zero exit, or the `-rs` skip report names a live-engine test while `codebase-memory-mcp` is executable on PATH</fails_when>
    <automated>cd databasise && uv run pytest -q tests/validator/ -x</automated>
    <fails_when>non-zero exit — the Falsifier-2 evidence suite reads this part's declared effects and must still pass after the additive change</fails_when>
    <automated>cd databasise && uv run pytest -q</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `cd databasise && uv run python -c "
from databasise.parts.registry import default_registry
p = default_registry().get('codebase-memory-mcp@0.1.0')
a = p.admission
assert sorted(v.condition for v in a.verdicts) == list(range(1,12))
assert all(v.evidence.strip() for v in a.verdicts)
assert len({v.verdict for v in a.verdicts}) > 1, 'all eleven verdicts identical'
assert 'mutates_store' in p.effects and p.artifact_scope == 'self_storage'
assert a.storage == 'self-contained'
print('ok')"` prints `ok`.
    - `databasise/evidence/ADMISSION-CODEBASE-MEMORY-MCP.md` contains the string `61b3b1b2` and the installed binary's reported version, and a section heading naming the drift.
    - The manifest contains the string `N12` and states that no OS-level network-namespace isolation is built this phase.
    - `CBM_WALL_CLOCK_CEILING_SECONDS` is strictly greater than the measured duration recorded in the manifest, checked by a test that reads both.
    - `cd databasise && uv run pytest -q tests/validator/test_falsifier2_evidence.py -x` exits 0.
  </acceptance_criteria>
  <done>A second engine is admitted through the same machinery as the first, its eleven verdicts are machine data rather than prose, its ceiling is measured, and the three things §X refused to round up are still not rounded up.</done>
</task>

<task type="auto">
  <name>Task 3: Falsifier 4, run twice — and this phase's COVERAGE.md</name>
  <files>databasise/evidence/FALSIFIER-4-EVIDENCE.md, databasise/tests/evidence/test_falsifier4_evidence.py, .planning/phases/05-opaque-side-admission/COVERAGE.md</files>
  <read_first>
    - `docs/system-model/PARTS.md` §X's "Falsifier 4's status" section in full — including the finding that the machine-chunks leg may not be executable as worded, and both reasons why
    - `docs/system-model/CONTRACT.md` §4's Falsifier text and the `ChunkRef(corpus_id, recipe@version, ordinal, content_hash)` shape the native leg checks against
    - `.planning/phases/05-opaque-side-admission/05-RESEARCH.md` Pitfall 3 — do not plan or attempt a `text_chunk` feed into `index_repository`
    - `databasise/evidence/FALSIFIER-2-EVIDENCE.md` — the house format for a falsifier evidence document
    - `.planning/phases/03-lightrag-query-side/COVERAGE.md` — the authoritative capability matrix for the model endpoint, and the format a real matrix takes
    - `.planning/phases/04-the-seam/COVERAGE.md` — the reasoned no-integration declaration format, and the §18.5 operations record
    - `databasise/foreign/codebase_memory_mcp_adapter.py` and `databasise/evidence/ADMISSION-CODEBASE-MEMORY-MCP.md` — as written by Tasks 1 and 2
  </read_first>
  <action>
Run the falsifier for real where it is runnable, record the disposition where it is not, and write the phase's coverage record.

**A. The native-chunking leg — actually run it.** Invoke `index_repository` through the adapter against this repository's own `databasise/` directory, then call the engine's evidence-returning tools (its graph search, code snippet, and code search tools, by whatever names `list_tools()` reports) and confirm each returned item normalizes to a §4 `ItemKind` member and maps to a `ChunkRef`-shaped reference: `corpus_id`, `recipe@version`, `ordinal`, `content_hash`. Record, per field, whether the engine supplies it directly, whether the adapter derives it (file path plus line range plus a computed content hash), or whether it cannot be supplied at all. A field the adapter cannot supply caps the item's evidence tier below `T1`; record the resulting tier rather than the tier that would have been convenient.

**B. The machine-chunks leg — record the disposition.** Do not attempt to feed `text_chunk` evidence into `index_repository`, and do not write chunks to a temporary directory and point `repo_path` at it. `PARTS.md ## §X.5` already records why both routes fail to test what the leg names: the first requires modifying a black-box part, the second collapses the leg back into the native leg by construction. Record the disposition as *not runnable as worded at this engine's ingestion API*, carry §X.5's structural finding forward in its own words — that for any opaque node whose only ingestion API is a filesystem path rather than a typed evidence parameter, the two-leg design may not distinguish two genuinely different code paths at all — and state what would have to change for the leg to become runnable. This is the finding, not a gap in the work.

**C. `databasise/evidence/FALSIFIER-4-EVIDENCE.md` (new).** Both legs, with the falsifier's own text quoted verbatim, the native leg's real per-field ChunkRef result table, the resulting evidence tier, the machine-chunks leg's disposition and its structural reason, and a verdict section stating what Falsifier 4 calibrated. Falsifier 4 calibrates the contract and does not halt the ladder — say so, and say which contract text it calibrates.

**D. `databasise/tests/evidence/test_falsifier4_evidence.py` (new).** Structural assertions over the document — both legs present, the native leg's table carries one row per `ChunkRef` field, the machine-chunks leg carries a disposition value, and a verdict section exists — plus a live re-run of the native leg guarded by a binary-present skip, so the recorded numbers are reproducible rather than a one-time transcription.

**E. `.planning/phases/05-opaque-side-admission/COVERAGE.md` (new).** This phase **consumes** an external surface, unlike Phase 4 which only exposed one — so this is a real capability matrix, not a no-integration declaration. State that decision and its reason in the opening paragraph: this phase's adapter calls a third-party engine's own served tool surface, and admits a second in-repo engine across a process boundary. Sections:
   1. **codebase-memory-mcp's tool surface** — one row per tool, enumerated from the live `list_tools()` call (never from memory or from a docs list), with `INTEGRATE` as the default decision and a one-line reason on every `OPT-OUT`. An opt-out with no reason is the undecided hole this matrix exists to close. Name the version the enumeration came from.
   2. **v1 LightRAG's corpus-side API** — the operations this phase consumes across the subprocess boundary (`apipeline_enqueue_documents`, `apipeline_process_enqueue_documents`, `adelete_by_doc_id`, `get_processing_status`, `aget_docs_by_track_id`, and whatever the driver's entity/health branches call), each `INTEGRATE`, plus the query-side operations deliberately not consumed here and why.
   3. **The OpenAI-compatible model endpoint** — not restated. Point at `.planning/phases/03-lightrag-query-side/COVERAGE.md` as the authoritative matrix, exactly as Phase 4's record does, and name any row whose *status in the product* this phase changes without changing the integration.
   4. **The §18.5 surface record** — extend Phase 4's operations table with this phase's new operations (ingest, document deletion, job status, health, corpus status, document counts), each with its transports, its plan, and the one-line reason it is an operation rather than a selector. Carry forward Phase 4's "Operations deliberately absent" table with the rows this phase discharges marked as discharged and the rest unchanged, and keep the per-modality-tool row marked `FORBIDDEN`.
  </action>
  <verify>
    <automated>cd databasise && uv run --extra mcp pytest -q tests/evidence/test_falsifier4_evidence.py -x -rs</automated>
    <fails_when>non-zero exit, or the `-rs` skip report names the live native-leg test while `codebase-memory-mcp` is executable on PATH</fails_when>
    <automated>cd databasise && uv run pytest -q</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `databasise/evidence/FALSIFIER-4-EVIDENCE.md` contains a section for each leg, and the machine-chunks section states a disposition value plus the structural reason drawn from `PARTS.md ## §X.5`.
    - The native leg's table has one row for each of `corpus_id`, `recipe@version`, `ordinal`, `content_hash`, each marked supplied, derived, or unavailable, and the document records the resulting evidence tier.
    - `.planning/phases/05-opaque-side-admission/COVERAGE.md` exists and its codebase-memory-mcp table row count equals the tool count `list_tools()` returned, asserted by reading both.
    - Every `OPT-OUT` row in `COVERAGE.md` carries a non-empty reason cell — checked by parsing the table, not by eye.
    - `COVERAGE.md` names `.planning/phases/03-lightrag-query-side/COVERAGE.md` as the authoritative record for the model endpoint and restates none of its rows.
    - `COVERAGE.md`'s §18.5 operations table lists every corpus-side operation plans 05-01, 05-03 and 05-04 added, each with a reason it is not expressible as a selector.
  </acceptance_criteria>
  <done>Falsifier 4 has been run twice in the only two forms that exist — once for real, once as a recorded disposition with its own structural finding — and the phase's coverage record enumerates a live tool surface rather than a remembered one.</done>
</task>

</tasks>

<verification>
- `cd databasise && uv run pytest -q` exits 0.
- `cd databasise && uv run --extra mcp --extra rest pytest -q` exits 0.
- `cd databasise && uv run python -m databasise.tools.check_import_boundary` exits 0.
- <human-check>Read the three not-clean-yes rows in `databasise/evidence/ADMISSION-CODEBASE-MEMORY-MCP.md` and the version-drift section. Confirm you accept admitting this engine on verdicts inherited from a different build than the one installed — or say which verdicts you want re-verified against 0.10.8 before the admission stands.</human-check>
- <human-check>Read `.planning/phases/05-opaque-side-admission/COVERAGE.md`'s opt-out reasons. Each one is a capability of a real engine this project chose not to use; confirm none of them is a capability you wanted.</human-check>
</verification>

<success_criteria>
- codebase-memory-mcp is admitted whole-engine with eleven machine-readable verdicts projected from §X, a measured ceiling, a recorded version drift, and the network-namespace open question carried as a caveat.
- Everything the engine returns is typed to §4's union at the adapter boundary.
- Falsifier 4 is run twice and both legs' outcomes are recorded, including the leg that is not runnable as worded.
- The phase's `COVERAGE.md` exists as a real capability matrix over a live-enumerated tool surface.
</success_criteria>

<output>
Create `.planning/phases/05-opaque-side-admission/05-06-SUMMARY.md` when done.
</output>
