You stopped, or your teacher labelling is failing, before the spike was complete. Nothing counts until it is on disk in your spike folder. Continue the same spike now, without asking questions.

Spike 003 specific, mandatory changes (evidence from logs/teacher-*.jsonl: 7 of 7 docs failed; "reasoning-only response (budget exhausted before content)" errors; when content arrives, 8/8 and 10/10 ops are rejected by Proof because the character-offset evidence spans do not contain the subject or value):
1. Evidence format: switch teacher AND student output to a quoted substring, `"quote": "<verbatim text from the section>"`, instead of `[start, end]` offsets. The harness resolves the quote to offsets by locating its first occurrence in the section after whitespace and case normalisation, then runs Proof's existing span check on the resolved span. Use a local schema copy `ops.v0.2.schema.json` in your folder (replace `evidence` with `quote`, string, 3 to 256 characters) and generate the grammar from it. Record this as a spec v0.2 deviation in your README; do not edit spike 001.
2. Teacher call: the pinned model is a reasoning model. Send `max_tokens` of at least 4096 and disable or minimise reasoning for the labeller (OpenRouter accepts `"reasoning": {"effort": "none"}` or `{"exclude": true}`; if the project's `OPENAI_LLM_EXTRA_BODY` sets an extra body, merge with it rather than dropping it). Keep temperature 0. If a response is still reasoning-only, retry once with a higher budget, then mark the doc failed.
3. Cap the labelling at 3 rounds per document as before; accept a document as labelled when Proof accepts at least one op; report the acceptance rate per document in the README.
4. Then run the student sweep on identical inputs across MiniCPM5-1B, MiniCPM5-2B, Qwen3-1.7B, Qwen3-4B, Qwen3-8B (Q8 GGUFs in `$MELODYSCRIBE_MODELS`), thinking off, grammar from the v0.2 schema, one model per process, every load under `flock /tmp/melodyscribe-gpu.lock`. Score routing accuracy (target match, subject/value match against teacher ops) and tokens per paragraph per size.
5. Finish README.md: Investigation Trail, Results with the size-versus-accuracy table, verdict in the frontmatter; `run.sh` reproduces everything.
Reply with one line: SPIKE DONE 003 verdict=<...>.
