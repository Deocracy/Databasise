# Requirements — Databasise 2.0

Requirements are grouped by category with stable REQ-IDs. v1 (this milestone) = the four §BP rungs plus the full standalone product surface. Traceability is filled by the roadmap.

Audited 2026-08-29 against SYSTEM-MODEL.md, CONTRACT.md, RIG.md, PARTS.md, ANATOMY.md, SELECTION.md by three independent reviewers; all findings applied below.

## v1 Requirements

### Build Gates (GATE)

- [x] **GATE-01**: Rung ordering is enforced: the phase implementing §BP rung N+1 never starts before rung N's gate passes. A Falsifier 2 or Falsifier 5 failure **halts the ladder** — it is a SELECTION.md-level reversal, not a repairable defect (SYSTEM-MODEL §BP Phase 1 gate)

### Machine Core (MACH)

- [x] **MACH-01**: Static validator implements CONTRACT §19's depth/execution_mode derivations from wiring + registry — `effective_depth` computed as the minimum over transitive deps, `execution_mode` derived from declared `effects[]`, with no self-declaration standing in for the computation — demonstrated on the three named wirings (decomposed lightrag-local query side, opaque codebase-memory-mcp, half-decomposed full LightRAG); the §19.10 boundary enumeration is recorded with the node set — **Falsifier 2 gate**
- [x] **MACH-02**: Eval bundle stood up per RIG §EV.1 with dev/holdout/sealed splits, containing questions, gold answers, a judge instance, a judge prompt hash, a corpus snapshot hash, and the determinism/concurrency setting; both §EV.2 target families are mandatory bundle content; versions are minted, never edited in place (opening `sealed` mints a new version); holdout-usage logging policy in force before decomposition work consults dev/holdout. Bootstrapped from a public benchmark corpus [Amended 2026-08-31: moved to Phase 3, point of first need Phase 3's parity comparison, superseding this requirement's original rung-1 timing per `.planning/phases/02-falsifier-gate/02-GATE-01-WAIVER.md`] [Amended 2026-08-31: moved a second time to Phase 6, point of first need Phase 6's cross-modality run, per `.planning/phases/03-lightrag-query-side/03-GATE-AMENDMENT.md`] [Measured 2026-09-09 (06-04-PLAN.md): `bundle@v1` minted and committed at `databasise/evidence/eval-bundles/`, carrying dev/holdout/sealed splits and both §EV.2 target families (gold-passage, answer-level); versioning enforced (content-addressed, a new version minted on any of §EV.1's five invalidating inputs). See `databasise/evidence/EVAL-BUNDLE-V1.md`. `judge_instance` is recorded as an explicit `"unresolved"` sentinel — no live judge call was made — a stated limit of `bundle@v1`, not an outstanding item against this requirement's own text, which asks only that the bundle stand up. Nothing outstanding.]
- [ ] **MACH-03**: First A/A calibration per RIG §AA.1: one A/A run at n questions producing n paired per-question differences, bootstrap-resampled, the resampled distribution's p95 taken as the calibrated floor at `(bundle@v, tier, metric)` inseparable from the run's declared determinism/concurrency setting; §AA.2 precondition holds (a null with unknown bypass status is not usable as a floor). **Pass criterion: per-tier null width at T1 materially narrower than T0** (SELECTION Falsifier 5) — **Falsifier 5 gate** [Amended 2026-08-31: moved to Phase 3, point of first need Phase 3's parity comparison, superseding this requirement's original rung-1 timing per `.planning/phases/02-falsifier-gate/02-GATE-01-WAIVER.md`] [Amended 2026-08-31: moved a second time to Phase 6, point of first need Phase 6's cross-modality run, per `.planning/phases/03-lightrag-query-side/03-GATE-AMENDMENT.md`] [Measured 2026-09-09 (06-06-PLAN.md): the calibration procedure itself landed and is fully tested against fixture data (`databasise/eval/calibration.py`, 15 passing tests) — the *instrument*, not a real run. The owner selected `defer-run` at the plan's own spend/judge-identity checkpoint: no real A/A calibration executed, no corpus ingested, no LLM call made. **BLOCKED, not discharged** — recorded honestly in `databasise/evidence/FALSIFIER-5-EVIDENCE.md`, which reports no floor value for either tier. Two preconditions remain outstanding: a resolved `judge_instance` in the bundle (unblocks the answer-level/T0 leg), and a cost-bounded indexing story for the 291-document eval-corpus (unblocks the gold-passage/T1 leg, currently reachable only through v1's `unbudgetable` `full-ingest` path). This row stays Pending until a real calibration run computes both floors.] [Declined 2026-09-10 (06-13-PLAN.md): the owner declined the real A/A calibration a second time at this plan's Task 2 `gate="blocking-human"` checkpoint, reasoning that Task 1's real HippoRAG run in this same session had just incurred spend and then died on a live-data edge case (the `fact-score` empty-string defect) that no test caught, and the same class of defect could waste the far more expensive T0 leg — so fixing `fact-score` first makes both spends cheaper to attempt. Both of this document's own preconditions are now closed in code by 06-12 (`databasise/eval/remint.py` resolves a real judge identity; `databasise/eval/corpus_ingest.py` gives a cost-bounded, dry-run-by-default ingest path). **Outstanding:** only the spend itself, gated behind fixing the `fact-score` empty-string defect first. See `databasise/evidence/FALSIFIER-5-EVIDENCE.md`'s `## Deferred again — 2026-09-10` section. This row stays Pending.] [Deferred 2026-09-11 (06-17-PLAN.md): the owner declined the real A/A calibration a third time, at this plan's own `gate="blocking-human"` checkpoint (`decline`), no free-text reason given beyond the selection itself. Both of this document's own preconditions (06-12) and the previously-missing A/A run driver (06-16, `databasise/eval/aa_run.py`) are now closed/built; the "materially narrower" threshold is pre-registered (`FALSIFIER-5-EVIDENCE.md`'s `## Pre-registered threshold — 2026-09-11` section, committed before any floor exists). **Outstanding:** only the spend itself, now against a fully-ready driver and a pre-registered threshold. See `databasise/evidence/FALSIFIER-5-EVIDENCE.md`'s `## Deferred a third time — 2026-09-11` section. This row stays Pending. Falsifier 5 remains open, not failed.] [Deferred 2026-09-11 (`.planning/phases/06-hipporag-2-side-by-side/06-GATE-AMENDMENT.md`): deferred a fourth time, from Phase 6's cross-modality run to the first gate-adjudicated promotion — the point of first need, since RIG §AA.1's floor is the gate's floor and Phase 7 builds only the operator-asserted path (RIG §PR.3, CONTRACT §7), which carries no verdict and consumes no floor. Under MACH-09's default posture that promotion lies outside this milestone. Phase 7 is authorised to start; Phase 6 stays `gaps_found`; this row stays Pending and the milestone cannot close until a real A/A run computes both floors. Falsifier 5 remains open, not failed.]
- [x] **MACH-04**: Injected-LLM-endpoint survey documented across the five sandbox-candidate engines named in the lineup (Falsifier 8, non-gating documentation pass)
- [x] **MACH-05**: Runner/scheduler executes wiring graphs with structured concurrency, metering spend at each node's declared boundary; the intra-node concurrency scheduling mechanism (§H1 handover, contract-settled at V-7) is decided as part of this design; **and the runner stamps the full RIG §TR.1 run-record field set the gate later reads — `determinism_setting`, `concurrency_setting`, `arm_execution_order`, per-node `cache_hit`, `realised_budget_share`, and `guards_fired` — validated against `docs/system-model/rig-trace.schema.json`**. *Run-record clause added 2026-08-30 per CONTEXT.md D-10: without these fields CONTRACT §5 can never evaluate refusal conditions 8, 10 or 11, and a confounded comparison then returns a normal verdict that looks settled rather than unsettled.*
- [x] **MACH-06**: Component and artifact identity per CONTRACT §0/§1: `name@version`, RFC 8785 + SHA-256 `config_hash`, content-addressed artifact registry with `upstream_ref` lineage; instance identity is `(name@version, config_hash, resolved_dependency_ids)`, never a wiring node id
- [ ] **MACH-07**: Append-only promote/rollback ledger per CONTRACT §7 + RIG §PR: the §7 field set with both `change_origin` (`human_edit` | `machine_mutation` — who authored the change) and `promotion_provenance` (`gate_adjudicated` | `operator_asserted` — who adjudicated it) required on every generation record, never defaulted, never inferable by absence (the two are orthogonal per CONTRACT §7), plus non-empty `promotion_trace_ids` on operator promotions; the ledger append is the decision; alias repoint is atomic; a semver is minted at promotion and only at promotion; tombstoned losers are never lifted; the active pointer is always a derived query, never a written field
- [x] **MACH-08**: Storage keying per RIG §RUN: KV shared only where CONTRACT §3's `shared` scope admits it (effective depth `stage`); an arm containing an `opaque` node writes `quarantined`, never `shared`; per-part graph/vector namespaces; artifact sharing iff index-recipe hashes are identical
- [x] **MACH-09**: Measurement posture for answer-level and index-side mutation classes defaults **off** per RIG §F3.2 and stays off in v1 unless §CM.3's A1–A4 cost-model inputs are replaced by measurements; fallback-ladder (degraded) runs are labelled via RIG §TR's `degraded`/`degradation_reason`
- [x] **MACH-10**: F-07 discharged: the snapshot/reset protocol CONTRACT §14.4 point 3 names for `mutable-store` components is defined, or the A/B exclusion is recorded as permanent [Measured 2026-09-10 (06-09-PLAN.md): every `mutates_store`-declaring component in `default_registry()` — `codebase-memory-mcp@0.1.0` (`CA-2`) and `lightrag/full-delete@0.1.0` (a fourth component not named in `MODEL-RED-TEAM.md`'s own three-entry roster) — plus the two catalogued-not-built roster entries (`MC-1`, `CA-7`) are each given the `permanent-exclusion` disposition in `databasise/evidence/F-07-MUTABLE-STORE-DISPOSITION.md`, enforced as an explicit refusal in the comparison path for the one component reachable there (`MutableStoreComparisonExcludedError`). **Outstanding:** the plan's own Task 1 `human-check` requires the owner's confirmation that `codebase-memory-mcp`'s permanent-exclusion disposition matches their intent before this row flips to Complete — not yet recorded. This row stays Pending until that confirmation lands.] [Confirmed 2026-09-10 (06-11-PLAN.md): the owner affirmed that `codebase-memory-mcp@0.1.0`'s (`CA-2`) `permanent-exclusion` disposition from §5 parity/determinism comparisons matches their intent. Recorded in `.planning/phases/06-hipporag-2-side-by-side/06-UAT.md` Test 1 (`result: pass`, `coverage_id: 06-09/D1`). The disposition itself is committed at `databasise/evidence/F-07-MUTABLE-STORE-DISPOSITION.md` and enforced by `MutableStoreComparisonExcludedError`. Reversal condition, carried verbatim from that record: a future A/B-testability requirement, at which point the snapshot/reset protocol is a file-copy-and-restore of the engine's SQLite DB plus its WAL/SHM files. Nothing outstanding.]
- [x] **MACH-11**: F-08 discharged: CONTRACT §18.2's envelope extended with the minimum seam-level event shape (`name@version`, spend, outcome) for the `mutates_store`-outside-`deps` exception class

### Modality Ports (MODAL)

- [ ] **MODAL-01**: LightRAG query side re-cut into primitive-part nodes — seventeen of the eighteen §L.1 positions (the eighteenth, `embedder-index`, is the one index-recipe node the port authors) — with N-run, variance-banded parity against the pre-decomposition original inside the A/A band, or named declared deviations (CONTRACT §5's parity-not-gain rule) [Measured 2026-09-06 (supersedes the 2026-09-02 note below — that measurement described a crash the source no longer produces): a fresh, consistent five-arm comparison ran against HEAD `cee485f` (03-13-PLAN.md), replacing all ten committed `parity_results/` records in one sitting. The prior graph-arm crash (`entity-hydrate-expand`/`relation-hydrate-expand` `NodeExecutionError`) is gone — 03-11/03-12 repopulated v1's knowledge graph and fixed the shared-vector-store wiring bug that fed it chunk records instead of entity/relation records. **All five arms now complete a real, non-degraded retrieval on both sides** — 03-VERIFICATION.md gap 3's core ask (a valid retrieval-level comparison for hybrid/local/global) is closed. `naive` still carries its two per-query tail-length excursions with an existing, grounded CONTRACT §5 cause (unchanged from before, still AI- not human-authored — see the outstanding item below). `bypass` has no retrieval to compare (a single `generate` node) and no excursion is applicable to it. `hybrid`/`local`/`global`, now that they actually retrieve, disagree substantially with the original arm on which entities/relations come back — chunk sym_diff 2-8 per query, entity sym_diff 20-76, relation sym_diff 30-78 (see `PARITY-EVIDENCE.md`'s per-arm table) — 18 excursions across the three arms' two queries and three diff fields, none yet carrying a CONTRACT §5 cause (`DECLARED-DEVIATIONS.md`'s "Outstanding" section names every one by arm/query/field, honestly, without blocking the rest of the document — see that document's own render fix). Two things remain outstanding, both owner-only: (1) all naive's and (once written) the graph arms' excursion causes must be replaced with the human owner's own reasoning, since CONTRACT §5 requires a human-authored cause and every cause on file today is AI-authored; (2) the human answer-substance spot-check for q1/q2 is still unrecorded. `.planning/phases/03-lightrag-query-side/03-UAT.md` gives the owner the exact commands and JSON fields for both. This traceability row stays Pending until both are recorded — a fully-measured comparison with no accepted cause is not the same as a validated parity claim.] [Measured 2026-09-02 (superseded above): the first completed five-arm comparison landed (`databasise/evidence/PARITY-EVIDENCE.md`, `DECLARED-DEVIATIONS.md`) — `naive` and `bypass` ran full pipelines end to end with exact chunk-set agreement past `ranking_agreement=1.000`; `naive`'s two per-query tail-length excursions (rank 11+ under v1's token-budget truncation vs the decomposed `chunk-vector` node's strict `top_k` cutoff) are carried as named declared deviations under CONTRACT §5, each with a grounded cause. N=5 keyword-variance bands recorded on `hybrid`/`local`/`global` with no cache-served runs. `hybrid`/`local`/`global`'s decomposed runs degraded before completing retrieval (`entity-hydrate-expand`/`relation-hydrate-expand` `NodeExecutionError` on every query) and the original arm's own answer for those six pairs also returned no context, so their measured zero diffs are not yet a validated retrieval-level match — a live defect surfaced by this comparison, not yet repaired. One condition remains outstanding: the human answer-substance spot-check for q1/q2 (`PARITY-EVIDENCE.md`'s "Human spot-check of answer substance" section, `03-VALIDATION.md`'s Manual-Only Verifications row) is not yet recorded. This traceability row flips to Complete once that judgment is recorded and the hybrid/local/global degradation is either repaired or itself named as a declared deviation.] [Deferred 2026-09-11 (`.planning/phases/06-hipporag-2-side-by-side/06-GATE-AMENDMENT.md`): the two owner-only items (03-UAT.md test 2 answer spot-check, test 3 human-authored CONTRACT §5 causes) stay on the owner's schedule. Their point of first need is the parity claim itself, which Phase 7 does not make — an operator-asserted promotion carries no verdict (RIG §PR.3). Phase 7 is authorised to start; Phase 3 stays `human_needed`; this row stays Pending until both judgments are recorded.]
- [x] **MODAL-02**: LightRAG's ~1,786-line ingest core admitted opaque under CONTRACT §8's conditions, its output artifacts under `quarantined` scope per PARTS §L.2's `embedder-index` treatment; at admission time an explicit inside-vs-across-boundary change rule is written (build-added discipline per PITFALLS research — not a model clause; prevents contract erosion at the opaque boundary)
- [x] **MODAL-03**: codebase-memory-mcp admitted whole-engine under §17/§8's eleven admission conditions verdict-by-verdict — including the three PARTS §X records as not-clean-yeses (machine-side obligations), network namespace denied with the machine acting as injected LLM provider (§8 condition 3), the wall-clock ceiling never self-declared (§8 condition 4), evidence normalized to §4's ItemKind union at the adapter (§17), and a code-inspected admission manifest. Run twice per Falsifier 4 — once consuming machine chunks, once chunking natively — carrying PARTS §X.5's finding that the machine-chunks leg may be unrunnable as worded (record the disposition either way; **Falsifier 4** calibrates the contract, it does not halt the ladder)
- [x] **MODAL-04**: HippoRAG 2 fully decomposed — thirteen node positions, no opaque core left behind — with whole-graph PPR reached via §14.2's Graph bulk-export declared capability and the native igraph/prpack call plus OpenIE/reset-vector-join scaling formulas carried over as node internals; index-side effective depth remains `opaque` under the taint rule until its parity is shown [Measured 2026-09-10 (06-07-PLAN.md): the decomposition half is satisfied and checkable — all thirteen `## §H` positions registered, executable, opaque-effective-depth, none opaque-kinded, no arms, no patch file. The parity half is deliberately not attempted this milestone; index-side effective depth correctly stays `opaque` under the taint rule, exactly as this requirement's own text requires absent a parity measurement. See `databasise/evidence/HIPPORAG-PORT-RECORD.md`, whose Limits section (extended 06-09) now carries the deferred parity measurement as a named, triggered deferral rather than an absence. Nothing outstanding against this requirement's own text.]
- [x] **MODAL-05**: LightRAG and HippoRAG 2 run side-by-side on one corpus under RIG §RUN (KV sharing per MACH-08's scope rules, isolated graph/vector stores), outputs structurally comparable — the milestone proof point. The first genuine seam call records F-14's outcome (the §18.3 seam-invariance falsifier) either way [Measured 2026-09-09 (06-08-PLAN.md): both the HippoRAG index-build harness (`databasise/parity/build_hipporag_index.py`) and the cross-modality comparison harness (`databasise/parity/run_cross_modality.py`) landed as genuine, runnable code against every real precondition a live run would need. The owner selected `defer-and-record-blocked` at the plan's own spend `gate="blocking-human"` checkpoint: neither harness was invoked for real — no LLM call, no embedding call, no ingestion. **BLOCKED, not discharged** — recorded honestly in `databasise/evidence/CROSS-MODALITY-EVIDENCE.md`, with no fabricated count/spend/duration/comparison-result anywhere. **Outstanding:** owner authorization of one real invocation of `build_hipporag_index` against the Phase 3 parity corpus using live `v1/.env.parity` credentials, followed by one real invocation of `run_cross_modality`. (F-14 itself already fired successfully on a separate, unrelated call — `databasise/evidence/F-14-SEAM-INVARIANCE.md`, 06-03-PLAN.md — but that single-query comparison is not the real side-by-side corpus run this requirement's own text names.) This row stays Pending until the real run lands.] [Attempted 2026-09-10 (06-13-PLAN.md): the owner authorized the real invocation at this plan's Task 1 `gate="blocking-human"` checkpoint (`approve`). `databasise.parity.build_hipporag_index` was invoked for real against live `v1/.env.parity` credentials and did not complete — the harness's own post-run verification refused, reporting `partial=True degraded=True` with `stop_reason` naming a `NodeExecutionError` at the `fact-score` node (a provider `400` validation error over an empty-string input). Real spend was incurred before the refusal; its exact size is not known because the harness's own token accounting was never assembled (the run never completed). `run_cross_modality` was not invoked because the index build never certified as complete, per this plan's own instruction to stop the branch on a harness refusal. See `databasise/evidence/CROSS-MODALITY-EVIDENCE.md`'s `## Real run attempted — refused — 2026-09-10` section. **Outstanding:** a code fix for the `fact-score` node's empty-string-input handling, then a fresh authorized invocation. This row stays Pending.] [Measured 2026-09-10 (06-15-PLAN.md): the owner re-authorized the real invocation (`approve`) now that 06-14 closed the `fact-score` empty-string defect. `build_hipporag_index` ran against live `v1/.env.parity` credentials and exited 0: `graph_node_count=229`, `graph_edge_count=460`, `chunk_vector_count=20`, `entity_vector_count=209`, `fact_vector_count=226`, `duration_seconds=1218.03`, `partial=False`, `degraded=False`. `run_cross_modality` then also exited 0: both of the corpus's 2 queries (`q1`, `q2`) ran through `Databasise.compare()` against both arms, `directories_disjoint=True`, `artifacts_overlap=False`, every arm on every query `partial=False`/`degraded=False`, identical envelope field sets on both arms. See `databasise/evidence/CROSS-MODALITY-EVIDENCE.md`'s `## Real run — 2026-09-10` section for the full harness-returned values. **Discharged — this milestone's core-value proof point is observed.** Nothing outstanding.]

### Product Surface (API)

- [x] **API-01**: Caller can ingest documents (raw upload and structured payload) with async job status polling
- [x] **API-02**: Caller can delete a document with graph-aware cleanup of entities/edges shared across documents
- [x] **API-03**: Caller queries with CONTRACT §18.1's query **object** (never a query string); selection via §18.4's four selectors (alias, capability, harness, default) with an unsatisfiable selector returning an explicit refusal naming what was missing, never a silent fallback; an unconsumable query-object member is refused by name; opaque nodes are excluded from the default selector (§8 condition 7); the response is the §18.2 closed envelope, invariant across modality swap
- [x] **API-04**: Caller can stream query responses
- [x] **API-05**: Every answer carries citations/provenance (evidence references per §18.2)
- [x] **API-06**: Caller can introspect health, corpus status, and document counts (bounded/paginated, never full dumps)
- [x] **API-07**: MCP surface exposes a curated intention-level tool set (ingest, query, delete, status, compare) with capability parity to REST; admission rule per §18.5 — a capability expressible as a §18.4 selector is never a new tool, and the surface never grows per-modality
- [x] **API-08**: Caller can run one query against two or more modalities on the same corpus in one call and receive per-arm results side-by-side, **keyed by the caller-supplied selectors** — never by arm_id, wiring name, node id, or modality name (§18.2/§18.3); comparison is inspection-only (per-arm outputs, traces, scores — no verdict) and is unaffected by §F3's default posture (RIG §RUN.4); one arm is a run, never a comparison (§RUN.3) [Measured 2026-09-09 (06-03-PLAN.md): `Databasise.compare()` landed on all three transports (in-process, REST `POST /compare`, MCP `compare` tool), proven against a real two-arm LightRAG-vs-HippoRAG call with no verdict token and no internal identity in the response; a single selector degenerates to a bare run, not a comparison. F-14 recorded its first outcome from that same real call: no consumer-visible envelope field changed across the modality swap (`databasise/evidence/F-14-SEAM-INVARIANCE.md`). 06-09-PLAN.md additionally closed §14.4 point 3's own explicit-refusal requirement in this path (`MutableStoreComparisonExcludedError`) — a narrowing of what a comparison may resolve to, not a change to this requirement's own text. Nothing outstanding.]
- [ ] **API-09**: Caller can promote and roll back a wiring via RIG §PR.3's operator path: `promotion_provenance: operator_asserted` with non-empty `promotion_trace_ids`, no `verdict` and no `tier-of-decision`; explicit caller-invoked action only; promote-next/promote-now remain unavailable for answer-level and index-side classes under the default posture (MACH-09)
- [x] **API-10**: Caller can retrieve the node-by-node execution trace for a query per RIG §TR's schema, reached through §18.2's trace reference (debug-flagged; trace content lives behind the reference — internal identities never enter the envelope itself)
- [x] **API-11**: Response envelope carries per-query budget-token spend with `counted_by` on every token number, an explicit refusal — never a substituted or estimated number — for any `unbudgetable` participant §8 admits, and spend never conflated with capacity (§9)

### Embeddable Product (EMBED)

- [x] **EMBED-01**: Databasise installs and runs as a single self-contained process tree on the local machine — importable as a Python library, embedded stores (Cozo pinned/vendored for graph, Faiss for vector, SQLite stdlib for KV/lexical/artifact-registry-index/ledger, filesystem for blob), no external DB servers, no Docker; node `execution_mode` remains derived per CONTRACT §3 (subprocess/confined-unit placements are legal inside the tree — only pure, non-iterative nodes may be hosted in-process). *Amended 2026-08-30 per CONTEXT.md D-05: the prior text named LanceDB, which was adopted from `.planning/research/STACK.md:26`'s factually wrong description of the incumbent. Cozo + Faiss are the fork's actual defaults (`v1/lightrag/lightrag.py:275-284`, `v1/lightrag/api/config.py:64-68`) and are owner-locked.*
- [x] **EMBED-02**: The REST + MCP server is a thin optional layer over the same seam the embedded library exposes — one seam, two transports

### Model Hardening (HARD) — folded in at first-touch

- [ ] **HARD-01**: Gate-script vacuous-pass sites fixed (parts-check.sh: 5 extraction sites guarding ~6 checks; anatomy-check.sh: 1 site guarding 3 checks) — first touched at rung 1 [Amended 2026-08-31: moved to Phase 7, point of first need Phase 7's hardening pass, superseding this requirement's original rung-1 timing per `.planning/phases/02-falsifier-gate/02-GATE-01-WAIVER.md`]
- [ ] **HARD-02**: The eight ANATOMY §F rows lacking landed-repair pointers linked to their PARTS Appendix A dispositions, **reconciling stale cross-document rows in the same pass** (e.g. DR-06: ANATOMY reads "Answered", PARTS Appendix A still reads "not reached") — doc pass alongside rung 1 [Amended 2026-08-31: moved to Phase 7, point of first need Phase 7's hardening pass, superseding this requirement's original rung-1 timing per `.planning/phases/02-falsifier-gate/02-GATE-01-WAIVER.md`]
- [x] **HARD-03**: DR-04 decided (per-chunk chunker/embedder provenance stamp on the stored chunk artifact: adopt the stamp, or record the two-covering rationale as sufficient) — first faced at rung 3
- [ ] **HARD-04**: Owner's own corpus layered into the eval bundle before any promotion decision is made on it

## v2 Requirements (Deferred)

- Self-improving/auto-promotion loop — the fitting enables it; explicit caller-invoked promotion only this milestone
- Third-modality port from the CATALOG protocol by a non-author (the porting-protocol generality test)
- Measured cost-model validation replacing RIG §CM.3's A1–A4 inference bands (build-or-later-spike per §H1); prerequisite for ever switching MACH-09's posture on
- DR-05 graph-store `validity` sub-capability (bi-temporal time-shift) — no v1 modality declares `temporal`; kept buildable per §14.4, deliberately deferred

## Out of Scope

- Any UI (v1 WebUI or new) — API only; every UI including Sourcerer is a client
- Docker/k8s deliverables — embeddable-first, local NixOS runtime
- Re-litigating the architecture — D4 One Machine ratified 2026-08-29; SELECTION.md governs
- Sourcerer-side applet work — §H2 assigns it to Sourcerer's own planning
- Falsifier 6's foreign-hosted-LightRAG experiment — no rung builds that deployment; accepted risk per §RK
- Full corpus/index dumps via API — bounded responses only (MCP anti-pattern and exfiltration risk)
- N1 (store kinds as NodeKind members) and N3 (`writes_artifact` differentiation) — inherited and left as stated per §H1's repair directions; each reopens only on its named forcing case, not by choice

## Traceability

Maps REQ-IDs to phases (see .planning/ROADMAP.md). Every v1 requirement maps to exactly one phase. Coverage: 34/34.

| REQ-ID | Phase | Status |
|--------|-------|--------|
| GATE-01 | Phase 2 | Complete |
| MACH-01 | Phase 2 | Complete |
| MACH-02 | Phase 6 | Complete |
| MACH-03 | Phase 6 | Pending |
| MACH-04 | Phase 5 | Complete |
| MACH-05 | Phase 1 | Complete |
| MACH-06 | Phase 1 | Complete |
| MACH-07 | Phase 7 | Pending |
| MACH-08 | Phase 1 | Complete |
| MACH-09 | Phase 2 | Complete |
| MACH-10 | Phase 6 | Complete |
| MACH-11 | Phase 4 | Complete |
| MODAL-01 | Phase 3 | Pending |
| MODAL-02 | Phase 5 | Complete |
| MODAL-03 | Phase 5 | Complete |
| MODAL-04 | Phase 6 | Complete |
| MODAL-05 | Phase 6 | Complete |
| API-01 | Phase 5 | Complete |
| API-02 | Phase 5 | Complete |
| API-03 | Phase 4 | Complete |
| API-04 | Phase 4 | Complete |
| API-05 | Phase 4 | Complete |
| API-06 | Phase 5 | Complete |
| API-07 | Phase 5 | Complete |
| API-08 | Phase 6 | Complete |
| API-09 | Phase 7 | Pending |
| API-10 | Phase 4 | Complete |
| API-11 | Phase 4 | Complete |
| EMBED-01 | Phase 1 | Complete |
| EMBED-02 | Phase 4 | Complete |
| HARD-01 | Phase 7 | Pending |
| HARD-02 | Phase 7 | Pending |
| HARD-03 | Phase 5 | Complete |
| HARD-04 | Phase 7 | Pending |

---
*Last updated: 2026-08-29 after roadmap creation (traceability filled)*
