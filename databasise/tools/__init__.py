"""Standalone runnable tools that operate over the ``databasise`` source tree itself — never
imported by ``databasise``'s own runtime path. ``check_import_boundary`` is the first: D-14's
v1 import boundary, enforced as a runnable check rather than a review convention.
"""

from __future__ import annotations
