---
title: System Model — the assembled entry point
status: ENTRY POINT — recommended, awaiting owner ratification
---

# RAG Modality-Swap System Model — Entry Point

This document is the reading order, the verdict summary, the ranked assumption/falsifier register, the handoff record, and the superseded-status table for the finished model. `CONTRACT.md`, `ANATOMY.md`, `PARTS.md`, `CATALOG.md`, and `RIG.md` remain the sole normative sources; this document duplicates none of their content and points at them instead. [inference] [CONTEXT D-11]

## Reading order

Read the five governing documents in this order, matching each document's own `## Reading order` block rather than inventing a competing sequence: [inference] [CONTEXT D-11]

1. **`CONTRACT.md`** — the fitting contract. Start at `## Conventions` and `## §0` (naming and versioning) regardless of role, per the document's own reading order, then the section your job touches. This is the normative base every other document is drawn from and states nothing of. [docs-verified] [CONTRACT §Reading order]
2. **`ANATOMY.md`** — the machine anatomy and component inventory, drawn from `CONTRACT.md`. Start at `## §D` (component inventory) and `## §C` (boundary types), per the document's own reading order. [docs-verified] [ANATOMY §Reading order]
3. **`PARTS.md`** — part anatomy on three stress modalities (LightRAG, HippoRAG 2, codebase-memory-mcp), drawn from `CONTRACT.md`. Start at `## §L`, `## §H`, `## §X`, per the document's own reading order. [docs-verified] [PARTS §Reading order]
4. **`CATALOG.md`** — the snap-in catalog and porting protocol, drawn from `CONTRACT.md` and projecting `PARTS.md`'s three deep modalities. Start at `## §T` (master table), per the document's own reading order. [docs-verified] [CATALOG §Reading order]
5. **`RIG.md`** — the comparison rig and versioning mechanics, specifying against `CONTRACT.md`. Start at `## §RUN` then `## §TR` for a builder, or `## §PR`/`## §AA` for an operator, per the document's own reading order. [docs-verified] [RIG §Reading order]
6. **`.planning/architectures/D-VARIANTS/SELECTION.md`** — the frozen source `CONTRACT.md` transcribes and elaborates; governs on any disagreement with any of the five documents above, including its own spike-005 amendment. Read this whenever a clause's authority is in question. [docs-verified] [CONTRACT §Status]
7. **`MODEL-RED-TEAM.md`** — the adversarial-pass record: the 26-row scoring table (`## §S`) against `ARCHITECTURE-RUBRIC.md`'s ten goals and `PROJECT.md`'s sixteen test dimensions, the attack write-ups (`## §A`) that forced any repair, and this phase's own `V`-family repair register (`## §R`). Start here for what the red team found, not in this document — this document points at it rather than restating its rows, per this document's own non-duplication boundary below. [inference] [CONTEXT D-11]

This reading order agrees with all five governing documents' own `## Reading order` blocks: each is reconciled above rather than restated, and none of the five is asked to yield to a Phase-6-invented sequence its own authors did not choose. [inference] [new-synthesis]

## What this document is not

This document duplicates no normative content from `CONTRACT.md`, `ANATOMY.md`, `PARTS.md`, `CATALOG.md`, or `RIG.md`. The five governing documents remain the sole normative sources for the fitting contract, the machine anatomy, the per-modality part anatomy, the snap-in catalog, and the comparison rig and versioning mechanics, respectively. Where this document and a governing document disagree, **the governing document controls** — the same one-way precedence `RIG.md` and `CATALOG.md` each already state for their own relationship to `CONTRACT.md`, extended here to this document's relationship to all five. [docs-verified] [CONTEXT D-11] [RIG §Status] [CATALOG §Status and authority]

This document is thin by construction: a reading order, a verdict, a ranked register, a handoff record, and a status table, each pointing into the governing documents by section id rather than restating their prose. A reader who needs a clause's actual text goes to the document that owns it; this document is where a reader starts, never where a reader's audit ends. [inference] [CONTEXT D-11]

## §VD — The verdict

**Status:** written (VERD-02: go/no-go with per-modality port costs restated from `PARTS.md ## §P` and `CATALOG.md ## §V`, per D-05).

**A conditional go is ruled on committing the Databasise build to this architecture — the fitting contract, machine anatomy, three worked part-cuts, snap-in catalog and comparison rig `CONTRACT.md`, `ANATOMY.md`, `PARTS.md`, `CATALOG.md` and `RIG.md` state as frozen at this phase's close — conditioned on Falsifier 2 (static depth computability) and Falsifier 5 (per-tier A/A null width) running and passing at or within the build's first phase, and the ruling recommends rather than decides, per D-06.** [inference, on the docs-verified evidence chain below]

What is committed to: the tagged-sum `NodeKind` vocabulary and the seventeen-member `effects[]` union (`§2`, `§14.1`, closing at `V-4`); the three artifact scopes — `shared`, `quarantined`, `self_storage` — and the blast-radius rule gating `shared` writes on effective depth `stage` (`§3`, the spike-005 repair `SELECTION.md`'s own AMENDMENT verifies); the budget-as-splittable-capability-token shape and its explicit exclusion of intra-node concurrency from its own scope (`§9`, `V-7`); the promote/rollback ledger discipline with `change_origin` now a required, never-inferable field (`§7`, `V-1`); and the admission machinery all three stress modalities — LightRAG, HippoRAG 2, codebase-memory-mcp — already exercise at three different entry rungs through the identical `§8`/`§17` mechanism (`PARTS.md ## §L`/`## §H`/`## §X`). [docs-verified] [CONTRACT §Conventions] [inference]

What is not committed to: the runner, scheduler or storage-keying implementation, RIG D-12's forward fence, owned by the build and stated, not designed, at `## §BP` below; a decision to switch the default measurement posture on for the answer-level or index-side mutation classes, `RIG.md ## §F3.2`'s own reversibility note naming this a build-time configuration act, not a Phase 5 or Phase 6 one; or a settled answer to `N1`'s or `N3`'s handed-over questions, both of which stay open until a build-phase forcing case neither this phase nor its two predecessors could construct actually arises. This verdict's own evidence chain is anchored throughout in `CONTRACT.md §Conventions`'s claim-tagging discipline — every clause below carries exactly the tag its own evidence supports. [inference] [CONTEXT D-13] [RIG §F3.2]

**The reasoning, in the order it actually carried weight:**

1. **The two falsifiers `SELECTION.md` itself ranks above the selection have both already run, and neither killed it.** Falsifier 1 — the record's own nominated decisive test — was executed as spike 005: the taint rule holds on the ingest-adapter's clean shared writes and correctly refuses the opaque-fed extractor's, and the one thing that was wrong (the ratchet's middle-state premise, argued rather than measured, per `SELECTION.md`'s own "Open questions" item 1) is repaired by the three-scope amendment, verified rather than merely re-argued: `shared` (effective depth `stage` only), `quarantined` (any depth, never shareable, GC'd with the instance), `self_storage` (private). Falsifier 3 — which `RIG.md ## §F3` states "outranks this document and the architecture selection itself" — is settled per mutation class, not rounded to one number: retrieval-side affordable and confirmed; answer-level and index-side affordable only inside `## §F3.2`'s stated default-off posture and `## §F3.3`'s priced fallback ladder. Both of the project's own two most decisive tests have run, and the selection stands after both — the same conclusion `S-D12` (Effort-vs-worth, `holds`) names as this row's own citation debt: "The go/no-go itself lands in `SYSTEM-MODEL.md ## §VD`." [docs-verified] [SELECTION §Falsifiers] [SELECTION §AMENDMENT] [RIG §F3.1] [MODEL-RED-TEAM §S S-D12]
2. **All twenty-six `## §S` rows are scored, and none blocks a go under `D-04`'s own test — including the row this pass's own most severe verdict landed on.** `S-G10` (Budget-obedient) scored `does-not-hold` at plan 06-02's close, the pass's single most severe verdict, for `N11`'s undeclared home for intra-node concurrency. That defect was repaired in place at `V-7` (plan 06-05): intra-node concurrency is declared out of `§9`'s scope by design, metered only through the node's own aggregate spend at its declared boundary. `## §S`'s own `S-G10` cell went unrewritten through plans 06-06 and 06-07 — a documentation-currency gap this verdict named plainly in the honesty-bar paragraph below rather than silently correcting or silently relying on it — and was corrected to `holds` at this phase's own closing sweep (plan 06-08), citing `V-7` in its own evidence cell. The same staleness affected four further cells (`S-G3`/`F-04`, `S-G4`/`F-05`, `S-D2`/`F-12`, `S-D5`/`F-13`), each closed at `V-3`, `V-6`, `V-5`/`V-6`, and `V-7` respectively, and each corrected at the same closing sweep. [docs-verified] [MODEL-RED-TEAM §S] [MODEL-RED-TEAM §R V-3, V-5, V-6, V-7]
3. **Every one of the seven inherited register rows carries a terminal disposition, closing the project's oldest-standing forward fence.** Five (`N2`, `N4`, `N7`, `N8`, `N11`) are repaired in place in `CONTRACT.md` with `ANATOMY.md` re-projections; two (`N1`, `N3`) are not exercised by any attack across three phases and are handed to the build with a stated, non-empty repair direction, per D-13's no-bare-deferrals rule. `N11` in particular — named at `RIG.md ## §RUN.3`, `## §AA.2` and `## §LC.3`, and carried since Phase 3 — is confirmed discharged by `RIG.md Appendix A`'s own updated text. [docs-verified] [MODEL-RED-TEAM §R] [RIG Appendix A]
4. **The remaining named risks are each narrow in blast radius, and none invalidates a `SELECTION.md`-level decision under `D-04`'s own criterion.** Nine risks — `F-02`, `F-03`, `F-06`, `F-07`, `F-08`, `F-14`, `F-15`, and `N1`/`N3`'s own handover directions — are classified individually below; every one lands `accepted`, none `blocking`. [inference] [CONTEXT D-04]
5. **Two of `SELECTION.md`'s own eight falsifiers remain unrun, and each, on the selection's own text, would reverse a `SELECTION.md`-level decision if it fires — this is why the verdict is conditional rather than unconditional, and why these two, not the other four still-unrun falsifiers, are the ones this verdict names as conditions.** Falsifier 2's own failure mode is stated in `SELECTION.md`'s own Condition 7: "if these two computations cannot be implemented, D4 is not implementable." Falsifier 5's own failure mode is stated in `SELECTION.md`'s own Falsifiers list: "the ladder buys no statistical power and is decoration; D1's refusal-instead-of-labeling becomes the cheapest correct design." Both experiments are named by `SELECTION.md` itself as cheap and independent of any built machine — "days, no execution" for Falsifier 2, "measurable before any architecture exists" for Falsifier 5 — which is why they gate the go decision itself rather than merely a later build phase, and why `S-D12`'s own text ("the go/no-go itself lands in `SYSTEM-MODEL.md ## §VD`") is discharged exactly here rather than left for a later section. [docs-verified] [SELECTION §Falsifiers] [MODEL-RED-TEAM §S S-D12]

**Conditions on this go.** Where this verdict is a conditional go, the conditions are the falsifiers that must run before the build commits — this set is identical to the `must-run-before-build-commits` set plan 06-07's ranked register inherits, named here so 06-07 does not re-derive it and diverge:

1. **Falsifier 2 (static depth computability) must run and pass at or within the build's first phase.** Mechanism: implement `§19`'s `depth`/`execution_mode` derivation against the three wirings `SELECTION.md`'s own experiment names — decomposed `lightrag-local` query side, opaque `codebase-memory-mcp`, half-decomposed full LightRAG — without any self-declaration standing in for the computation. Blast radius if it fires: `D4`'s central claim, "the kernel is a depth, not a place" (`SELECTION.md ## The verdict`, reasoning item 3), collapses, and the sandbox regime the selection ruled structurally unnecessary would be doing real work after all — a reversal at `SELECTION.md`'s own level, not a `CONTRACT.md` clause repair. [docs-verified] [SELECTION §Falsifiers 2] [SELECTION §The verdict]
2. **Falsifier 5 (per-tier A/A null width) must run and pass at or within the build's first phase.** Mechanism: the first A/A calibration run, per-tier null width measured at T0 against T1, per `RIG.md ## §AA.1`'s own procedure — no built architecture is a precondition, per `SELECTION.md`'s own experiment text. Blast radius if it fires: the depth ladder buys no statistical power and is decoration; `D1`'s refusal-instead-of-labeling design becomes the cheapest correct one — again a reversal at `SELECTION.md`'s own level. [docs-verified] [SELECTION §Falsifiers 5] [RIG §AA.1]

**What would change this verdict.** It changes to **no-go** if either Falsifier 2 or Falsifier 5 fails as described above — both are, on `SELECTION.md`'s own text, reversals of a settled architecture decision, not merely a repairable contract defect. It changes to **unconditional go** once both pass. It would need **re-litigating in a narrower way** if the default measurement posture (`RIG.md ## §F3.2`) is switched on for the answer-level or index-side classes without the corresponding measured validation of `## §CM.3`'s four residual `[inference]` cost-model inputs (`A1`-`A4`) — switching the posture on is a build-time act this document does not gate, but switching it on *without* that validation would be promoting on the same unmeasured basis `SELECTION.md`'s own Falsifier 3 named as the random-walk risk. It would need re-opening entirely if a future modality forces `N1`'s or `N3`'s handed-over question open — a store-instance-as-node case, or a real validation gap in an undifferentiated `writes_artifact` declaration, respectively — since neither handover direction was written as foreclosing the question, only as the record's own current, no-attack-forced-it state. [inference] [CONTEXT D-13] [RIG §F3.2]

**The honesty bar this verdict holds.** `RIG.md ## §F3.1`'s own closing line refused to round "affordable in a specific shape with a tighter margin than the record previously showed" down to a bare "affordable" — this verdict holds the same discipline in three places rather than one. First, on the two falsifiers this verdict conditions on: neither is rounded to "probably fine" because their own cheapest experiments are cheap — this verdict does not treat cheapness as a reason to skip them, only as a reason they can gate the build's very first phase rather than delaying it. Second, on the five `## §S` cells named in reasoning item 2 above (`S-G3`, `S-G4`, `S-G10`, `S-D2`, `S-D5`): this verdict does not claim the gap was never real — for the four plans between the underlying repair and the cell's own correction, the cell genuinely misstated the current state of the record, and `## §R`'s own more specific, more current rows were the governing fact throughout. That gap is now closed: this phase's own closing sweep (plan 06-08) rewrote all five cells to `holds`, each citing the `## §R` row that repaired it, so the literal `## §S` text and the governing register now agree. Third, on `F-15`'s "≈ days" custom-modality cost claim: this verdict does not restate it as a settled figure anywhere above — this section's own reasoning cites only the four-class ordinal scale and the structural reindex-planner `reuse` argument behind it, never the day count itself, since the day count carries `[inference]`, not a measured port, in its own source. No numeric score and no averaged confidence figure appears anywhere in this section. [docs-verified] [RIG §F3.1] [MODEL-RED-TEAM §A F-15]

**Every named risk, classified against `D-04`'s own test — blocks a go only if it invalidates a `SELECTION.md`-level settled decision; otherwise a condition or an accepted risk.** None of the nine below invalidates a `SELECTION.md`-level decision; none is therefore blocking.

| Risk | Origin | D-04 classification | Why |
|---|---|---|---|
| `F-02` — join-node effect widening at fan-in boundaries | `S-G1`, plan 06-02 | accepted | A deliberate, evidence-forced tradeoff already chosen and recorded (`PARTS.md ## §R D3`/`H2`); reopening it would reopen a choice `PARTS.md` already made, not repair a gap. |
| `F-03` — the `predicates` query-object member has no declared plain-data shape | `S-G2`, plan 06-02 | accepted | Scoped to one member of `§18.1`'s query object; a concrete fix direction is stated but forces no `SELECTION.md`-level reconsideration. |
| `F-06` — the cycle-legality clause's own justifying citation does not survive the catalog's later scrutiny | `S-G5`, plan 06-02 | accepted | A citation defect, not a mechanism defect — every catalogued loop already routes through `fixpoint`, the mechanism goal 5 actually needs; only the example text is wrong. |
| `F-07` — `mutable-store` components structurally excluded from goal 8's A/B mechanism, no snapshot/reset protocol defined | `S-G8`, plan 06-02; carried forward at 06-05 | accepted | Narrow blast radius, scoped to the `mutable-store` capability class (`CA-2`, `MC-1`, `CA-7`); does not invalidate `SELECTION.md`. Feeds 06-07's ranked register per 06-05's own disposition. |
| `F-08` — the `mutates_store`-outside-`deps` seam-level trace event has no defined field shape | `S-G9`, plan 06-02; carried forward at 06-05 | accepted | Narrow blast radius, scoped to observability completeness for one named exception class. Feeds 06-07's ranked register per 06-05's own disposition. |
| `F-14` — the seam-invariance rule's own named falsifier has never been run | `S-D9`, plan 06-04; carried forward at 06-05 | accepted | The structural argument is checked across all seventy-two `CATALOG.md ## §T` roster entries; the one experiment `§18.3`/`§18.5` themselves name as decisive is unrun because no wiring has run against a live seam anywhere in this design-documents project — build-phase work, not a `SELECTION.md`-level reversal. |
| `F-15` — the "≈ days" custom-modality cost claim rests on `[inference]`, not a measured port | `S-D10`, plan 06-04; carried forward at 06-05 | accepted | The structural basis (reindex-planner `reuse`, zero re-index cost) is real and code-verified; only the day figure itself is unmeasured, and this verdict never restates that figure as settled (see honesty-bar paragraph above). |
| `N1` — store kinds have no member position in `§2`'s tagged sum | inherited, `PARTS.md ## §R`; handed to the build at 06-05 | accepted | Low blast radius per 06-05's own determination; `§2`'s own text states the exclusion deliberately, and every one of `CATALOG.md`'s seventy-two roster entries accesses stores exclusively through `effects[]`. Reopens only on a future store-instance-as-node forcing case. |
| `N3` — `writes_artifact` stays undifferentiated by target store | inherited, `PARTS.md ## §R`; handed to the build at 06-05 | accepted | Low blast radius per 06-05's own determination; `§7`'s artifact-registry entry already recovers the target store type one layer downstream of the effect declaration, and `PARTS-04`'s own D1 repair deliberately declined the identical differentiation at the analogous layer. |

**Status:** recommended — awaiting owner ratification. This document recommends the conditional go stated above, with its full evidence chain and its own conditions; the act of committing the Databasise build to this architecture is the owner's, not this document's, and stands recommended, not decided, until the owner states otherwise. [inference] [CONTEXT D-06]

## §PC — Per-modality port cost

**Status:** written (a restatement table, not a re-derivation, per D-05).

Two of the four port-cost classes — `rewire` and `rebuild` — are defined by `CATALOG.md §V`'s own dominant-bucket rule but unexercised on this record: neither of the three modalities below anchors to either. A reader who sees only three table rows should not conclude the scale itself has three values. [docs-verified] [CATALOG §V]

| Modality | Class | Dominant bucket | What the port consists of | Citation |
|---|---|---|---|---|
| LightRAG | `recut` | `recut` | Seventeen of eighteen query-side node positions recut into the primitive-part graph over an opaque, carried-over ~1,786-line ingest core; three concrete store backends and the raw LLM/rerank wrapper functions rewired to the machine's own clients. | `PARTS.md ## §P` (LightRAG row); `CATALOG.md ## §V` (LightRAG calibration) |
| HippoRAG 2 | `recut` | `recut` | All thirteen node positions recut with no opaque core left behind; the native `igraph` `prpack` PPR call and the OpenIE/reset-vector-join scaling formulas carried over as node internals. | `PARTS.md ## §P` (HippoRAG 2 row); `CATALOG.md ## §V` (HippoRAG 2 calibration) |
| codebase-memory-mcp | `rehost` | `carry over` | The whole fifteen-tool-handler engine carried over wholesale as one opaque node's internals; nothing discarded, replaced, or recut — the real port cost sits in the adapter, the wall-clock ceiling, and the code-inspected admission manifest, outside the four-bucket ladder entirely. | `PARTS.md ## §P` (codebase-memory-mcp row); `CATALOG.md ## §V` (codebase-memory-mcp calibration) |

LightRAG and HippoRAG 2 share the `recut` class and remain two rows, per `PARTS.md ## §P`'s own "same class, still two rows" rule, carried forward verbatim by `CATALOG.md ## §V`: LightRAG's `recut` is anchored by seventeen positions recut over a carried-over opaque core, while HippoRAG 2's `recut` is anchored by all thirteen positions recut with the core itself carried inside the nodes rather than left opaque outside them — the same class label, two structurally different reasons, restated rather than collapsed. [docs-verified] [PARTS §P] [CATALOG §V]

No `[inference]`-tagged figure is restated in this table: `PARTS.md ## §P`'s own evidence rule (D-16) states a person-day figure appears only where tagged `[inference]`, and no modality row states one for the exact case this table restates — `codebase-memory-mcp`'s own entry names its person-day cost as explicitly "not sized," and the "≈ days" custom-modality figure `MODEL-RED-TEAM.md ## §A F-15` finds unmeasured belongs to a different case (a new query wiring, not a whole-engine port) this table does not restate. [docs-verified] [PARTS §P] [inference]

No cost number appears above that is not already stated in `PARTS.md ## §P` or `CATALOG.md ## §V`: this table's own Class and Dominant-bucket columns restate the two documents' own tokens verbatim, and the "What the port consists of" column restates each modality's own evidence cell in one line, without adding a figure neither source states. [inference] [new-synthesis]

## §BP — Databasise build phases

**Status:** written (VERD-03: the coarse 3-5 phase ladder, dependency-ordered, per D-09).

**The spike-005 decomposition order is binding, stated here as a constraint on this ladder, not as background.** `SELECTION.md`'s own AMENDMENT (spike 005) corrects Condition 1's original premise: the query side does not sit downstream of a clean ingest lane — it is already component-shaped and decomposes first (spike 001: "Query path is already a named 4-stage component pipeline"); the index side is the entangled, roughly-1,786-line side and stays opaque longest, because it is the hard side. **Any phase ordering below that assumed a clean ingest lane arrives before the query side decomposes would be falsified reasoning by spike 005's own measurement — stated here so a later reader cannot reintroduce it as an optimisation.** [docs-verified] [SELECTION §AMENDMENT] [SELECTION spike 001]

**Falsifiers 1 and 3 are already retired, before this ladder starts.** Falsifier 1 (the laundering test) closed at spike 005, the three-scope repair verified; Falsifier 3 (eval-bundle affordability) closed at `RIG.md §F3`, settled per mutation class. Neither needs a rung below. [docs-verified] [SELECTION §AMENDMENT] [RIG §F3.1]

**Where this ladder's first rung sits against `## §VD`'s own conditions — checked explicitly.** `## §VD`'s conditional go names Falsifier 2 and Falsifier 5 as the must-run set, both required "at or within the build's first phase." Phase 1 below is exactly where both are placed — neither is deferred to a later rung, and no rung before Phase 1 exists for either to be deferred past. This ladder's first rung therefore does not commit any work `## §VD` says must wait: Phase 1's own deliverables are the two falsifiers themselves, and Phase 2 does not start until both pass. Consistency holds. [inference] [new-synthesis]

**1. Validator, depth machinery, and the pre-build falsifier gate.**
Delivers: `§19`'s `depth`/`execution_mode` static derivation, implemented against the three wirings `SELECTION.md`'s own Falsifier 2 experiment names — decomposed `lightrag-local` query side, opaque `codebase-memory-mcp`, half-decomposed full LightRAG — with no self-declaration standing in for the computation; the eval bundle's dev/holdout/sealed splits stood up per `RIG.md ## §EV.1`; the first A/A calibration run at `(bundle@v, tier, metric)` per `RIG.md ## §AA.1`; and, as a documentation pass rather than a build artifact, a survey of injected-LLM-endpoint support across the sandbox-candidate engine set `SELECTION.md`'s own Falsifier 8 names.
Must be true before it starts: this verdict ratified by the owner, per `## §VD`'s own status line.
Retires: **Falsifier 2** and **Falsifier 5** (both gate this phase's own close, per `## §VD`'s Conditions above); **Falsifier 8** (the documentation pass, non-gating — cheap enough to run alongside the other two without delaying either).
Gate to Phase 2: Falsifier 2 and Falsifier 5 both pass. If either fails, the ladder halts here — per `## §VD`'s own "what would change this" statement, a failure here reverses a `SELECTION.md`-level decision, not merely a `CONTRACT.md` clause.

**2. First real re-cut: LightRAG's query side, in parity.**
Delivers: LightRAG's ~794-line query side — seventeen of eighteen `## §L.1` node positions — decomposed into primitive-part nodes against the opaque original, gated on `§5`'s own parity rule: a decomposition is gated on parity, not gain, and its difference against the pre-decomposition original must fall inside the A/A band Phase 1 established, or name its declared deviations.
Must be true before it starts: Phase 1's gate passed.
Retires: **Falsifier 7** (a parity decomposition brought inside the A/A band, by construction — this rung is the experiment `SELECTION.md`'s own text names: "the first real decomposition increment, which the build performs anyway"); register rows **`N7`/`N8`** (already repaired on paper at `V-5`/`V-6`, exercised for real here at the exact boundary — the `nano_vector_db_impl.py` embedder cut — two independent blind authors originally diverged on).
Gate to Phase 3: the decomposed query side's parity result lands inside the A/A band, or names its declared deviations, per `§5`'s own refusal list.

**3. Opaque-side admission: LightRAG's ingest core and codebase-memory-mcp.**
Delivers: LightRAG's ~1,786-line ingest core admitted as an opaque node under the `quarantined` scope — spike 005's own repair, exercised here in a real build rather than only the paper wiring walk spike 005 ran; `codebase-memory-mcp` admitted whole-engine under `§17`/`§8`'s eleven admission conditions, run twice per `SELECTION.md`'s own Falsifier 4 experiment — once consuming machine chunks, once chunking natively.
Must be true before it starts: Phase 2's gate passed. **This is the binding order stated above, applied**: the ingest core is admitted only after the query side is already real, never before it, and never on the assumption that a clean ingest lane could have arrived first.
Retires: **Falsifier 4** (opaque parts emit machine-resolvable refs, calibrated either way per `SELECTION.md`'s own text — a fired result recalibrates the contract, it does not by itself halt the ladder unless Falsifier 5 also fails, and Falsifier 5 already passed at Phase 1's gate); confirms the taint rule's blast-radius mechanism on a real opaque write, not only spike 005's paper wiring walk.
Gate to Phase 4: both admissions pass `§8`'s conditions with no refusal.

**4. HippoRAG 2, full decomposition, and the first side-by-side comparison.**
Delivers: HippoRAG 2's thirteen node positions — index and query side both, no opaque core left behind, per `## §H`'s own table — decomposed and run side-by-side against LightRAG's now-real wiring under `RIG.md ## §RUN`'s parallel-run model, the first genuine architecture-comparison-mode run (closing the question `RIG.md ## §R N9` names, per `## §AA.4`'s own two comparison modes) and the first time `ADV-1`/`ADV-4`/`ADV-6` are exercised across two structurally different modalities rather than argued on paper.
Must be true before it starts: Phase 3's gate passed.
Retires: **no falsifier by name** — Falsifier 6's own experiment needs a genuinely separate opaque, foreign-hosted LightRAG deployment this ladder's own LightRAG port never builds, since this port decomposes LightRAG rather than admitting it opaque; Falsifier 6 is retired by no rung in this ladder and is carried to the ranked register as accepted risk, stated so rather than assigned to a rung that does not test it. This rung does close `MODEL-RED-TEAM.md ## §A F-14` one way or the other — the first genuine seam call this ladder makes is the first opportunity `§18.3`'s own named falsifier has ever had to fire, in either direction.
Gate: none — this is the ladder's last coarse rung; the build's own detailed planning takes over from here, per D-09. No runner, scheduler, or storage-keying design is stated at any rung above — `RIG.md ## §RUN.3`'s own forward fence names all three as the build's, and this section states the build's phase order and nothing more, per D-09/D-12. [docs-verified] [RIG Appendix A] [CONTEXT D-09] [CONTEXT D-12]

## §RK — Ranked assumption and falsifier register

**Status:** written (VERD-04/VERD-05: the merged ranked register discharging both, per D-07/D-08).

**One register, two requirements.** `SELECTION.md`'s eight falsifiers mostly already are the model's riskiest assumptions with an experiment attached; a second, parallel list for VERD-04 would drift from the falsifier triage VERD-05 asks for. This register discharges both from one row set. Columns: the assumption, the falsifier id where one exists (`none — <reason>` where it does not), the smallest experiment that would test it, its cost class, its blast radius (which settled decisions or build phases its firing invalidates), and its disposition. [inference] [CONTEXT D-07]

**The ordering rule.** Rows are ordered by blast radius, widest first — the register reads top-down from "invalidates a `SELECTION.md`-level decision" through "scoped to one capability class or one clause's completeness" to "arithmetic precision on an already-settled figure" — with cost class as the tie-break within an equal blast radius. A reader can re-derive the row order from this rule alone; it is not an editorial arrangement. [inference] [CONTEXT D-07]

**Cost class, stated once.** Three bands, used consistently below: **cheap** — no corpus and no built architecture required; **moderate** — a real but small-scale execution (not corpus-scale); **expensive** — requires a real corpus, a judge implementation, or a build-phase run to settle. [inference] [new-synthesis]

**The tie-break-never-merges rule.** Two rows with equal blast radius are ordered by cost class and remain two rows — Falsifier 2 and Falsifier 5 below are the concrete case: both invalidate a `SELECTION.md`-level decision if they fire, both carry the same `cheap` cost class, and both stay two separate rows rather than collapsing into one, per the same same-class-still-two-rows precedent `PARTS.md ## §P` and `CATALOG.md ## §V` each already carry forward. Where blast radius and cost class both tie (as they do for Falsifier 2 and Falsifier 5), the two rows keep `SELECTION.md`'s own falsifier numbering as the final, stable order — a rule stated here rather than left as an unexplained residual ordering. [docs-verified] [PARTS §P] [CATALOG §V]

**The no-blank-cell rule.** Every cell below carries a value or the explicit alternative `none — <reason>`. A row with no attached falsifier id, or no experiment a falsifier framing would even apply to (the two handed-over `N`-rows), says so with a reason rather than leaving the cell empty. [inference] [new-synthesis]

**The zero-row honesty rule, stated in force from `RIG.md ## §R`.** A register with no rows would be a legitimate outcome, not a check failure — this register is not empty at this phase's close because real risk survives the model's own five phases of repair, but the rule is stated because its absence is what would make a thin register look complete by omission rather than by verification. [docs-verified] [RIG §R]

**The disposition vocabulary is closed to two values: `must-run-before-build-commits` and `accepted-as-stated-risk`.** Per D-08, the criterion is blast radius alone: a falsifier must run before the build commits if and only if its firing would invalidate a settled architecture decision or render a build phase's output worthless — the Falsifier 3 precedent, which outranked the selection itself precisely because its failure would have voided every promotion claim `RIG.md §CM.3` and `## §F3` since price against. Cost class orders the must-run set and tie-breaks ranking above; it never appears below as the reason a row lands where it does. **Expense is never a reason to accept a selection-invalidating risk** — the same rule `CATALOG.md`'s own port-cost-is-never-an-exclusion-reason disposition already states for a different question, restated here for this one. [inference] [CONTEXT D-08]

**Falsifiers 1 and 3 are not rows below — both are already discharged, and are named here so a reader does not conclude they were forgotten.** Falsifier 1 (the laundering test) closed at spike 005; see `SELECTION.md §AMENDMENT`, the three-scope repair verified rather than merely re-argued. Falsifier 3 (eval-bundle affordability) closed at `RIG.md §F3`, settled per mutation class — retrieval-side affordable and confirmed, answer-level and index-side affordable only inside `## §F3.2`'s stated default-off posture and `## §F3.3`'s priced fallback ladder. [docs-verified] [SELECTION §AMENDMENT] [RIG §F3.1]

| Assumption | Falsifier id | Smallest experiment | Cost class | Blast radius | Disposition |
|---|---|---|---|---|---|
| Depth (`{opaque, evidence, stage}`) can be computed statically from wiring + registry, with no self-declaration. | Falsifier 2 | Write the depth computation against the three named wirings' specs (decomposed `lightrag-local`, opaque `codebase-memory-mcp`, half-decomposed full LightRAG); days, no execution. | cheap | Widest — `SELECTION.md`'s own Condition 7: "if these two computations cannot be implemented, D4 is not implementable." D4's central claim ("the kernel is a depth, not a place") collapses and the sandbox regime the selection ruled unnecessary would be doing real work — a `SELECTION.md`-level reversal. | must-run-before-build-commits |
| Per-tier A/A null widths are materially narrower at T1 than T0, so the depth ladder buys real statistical power. | Falsifier 5 | First A/A calibration run at `(bundle@v, tier, metric)`, measurable before any built architecture exists, per `RIG.md ## §AA.1`. | cheap | Widest — `SELECTION.md`'s own Falsifiers list: if it fails, "the ladder buys no statistical power and is decoration; D1's refusal-instead-of-labeling becomes the cheapest correct design" — a `SELECTION.md`-level reversal. | must-run-before-build-commits |
| Opaque parts can emit machine-resolvable refs whether or not they consume machine-owned chunks. | Falsifier 4 | Run D2's falsifier 1 twice on `codebase-memory-mcp`: once consuming machine chunks, once chunking natively. | moderate | Narrower than Falsifiers 2/5 — `SELECTION.md`'s own text: "either result calibrates the contract" rather than reversing it; only combines into a `SELECTION.md`-level threat together with a Falsifier 5 failure, and Falsifier 5 is already gated above. | accepted-as-stated-risk |
| A parity decomposition of LightRAG's query side can be brought inside the A/A band. | Falsifier 7 | The first real decomposition increment (`## §BP` Phase 2), which the build performs anyway. | expensive | Scoped to LightRAG's own decomposition approach — `SELECTION.md`'s own text: if it fails, "promotion is a rewrite, not a refactor, and deserves D1's ceremony" for that one modality, not a reopening of the selection itself; retired by construction at `## §BP` Phase 2, not a precondition on the go decision. | accepted-as-stated-risk |
| The foreign-node adapter is a fixed lineup-wide cost, not a per-engine-shape cost. | Falsifier 6 | Implement the adapter for two differently-shaped engines (`codebase-memory-mcp`: no LLM, own storage; a LightRAG server: LLM-heavy, injectable endpoint). | moderate | Narrow — `SELECTION.md`'s own text: "F6 says the cost is fixed lineup-wide, so this corrects pricing more than ranking." Retired by no rung in `## §BP` (Falsifier 6's own experiment needs a genuinely separate opaque, foreign-hosted LightRAG deployment this ladder never builds). | accepted-as-stated-risk |
| The seam-invariance rule (`CONTRACT.md §18.3`) holds under a real call, not only under the 72-row structural check. | none — `CONTRACT.md §18.3`'s own named falsifier, not one of `SELECTION.md`'s eight | The first genuine seam call `## §BP` Phase 4 makes, closing `MODEL-RED-TEAM.md ## §A F-14` one way or the other. | expensive | Narrow — the structural argument is already checked across all 72 `CATALOG.md ## §T` roster entries; the one experiment `§18.3`/`§18.5` name as decisive is unrun because no wiring has run against a live seam anywhere in this design-documents project — build-phase work, not a `SELECTION.md`-level reversal. | accepted-as-stated-risk |
| `mutable-store` components (`CA-2`, `MC-1`, `CA-7`) can be varied, budgeted, and A/B-run against their original, per goal 8. | none — `MODEL-RED-TEAM.md ## §A F-07`, a rubric-goal-forced finding | Define the snapshot/reset protocol `CONTRACT.md §14.4` point 3 names as the condition for lifting the exclusion, or state the exclusion permanent. | cheap | Narrow — scoped to the `mutable-store` capability class alone; does not invalidate `SELECTION.md`. Carried forward from plan 06-05's own D-13 handover list. | accepted-as-stated-risk |
| The `mutates_store`-outside-`deps` seam-level trace event is fully observable, per goal 9. | none — `MODEL-RED-TEAM.md ## §A F-08`, a rubric-goal-forced finding | Extend `CONTRACT.md §18.2`'s envelope with a minimum seam-level-event shape (`name@version`, spend, outcome). | cheap | Narrow — scoped to observability completeness for one named exception class. Carried forward from plan 06-05's own D-13 handover list. | accepted-as-stated-risk |
| Store kinds need no member position in `§2`'s tagged sum — `effects[]` is a sufficient store-access mechanism. | none — `N1`, a handed-over design question, not a falsifiable claim | none — no attack across three phases (Phase 3's original finding, Phase 4's catalog work, this phase's own Cluster A) constructed a forcing case; reopens only if a future modality needs a store *instance* wired as an addressable node. | none — no experiment exists to cost; the row is a design handover | Low, per plan 06-05's own determination — `§2`'s own text states the exclusion deliberately, and every one of `CATALOG.md`'s 72 roster entries accesses stores exclusively through `effects[]`. | accepted-as-stated-risk |
| `writes_artifact` staying undifferentiated by target store causes no real validation gap. | none — `N3`, a handed-over design question, not a falsifiable claim | none — no attack across three phases constructed a forcing case; reopens only if a build-phase case shows the artifact registry's own namespace/`space_id` fields do not in fact recover the target store type downstream of the coarse effect declaration. | none — no experiment exists to cost; the row is a design handover | Low, per plan 06-05's own determination — `§7`'s artifact-registry entry already recovers the target store type one layer downstream, and `PARTS-04`'s own D1 repair deliberately declined the identical differentiation at the analogous layer. | accepted-as-stated-risk |
| The porting protocol (`CATALOG.md ## §C`), tested once against PathRAG, generalises to a system ported by someone other than this protocol's own author. | none — `CATALOG.md`'s own named residual risk, not one of `SELECTION.md`'s eight | Port a second, independent fork against `## §C`'s text alone, run by a party who did not write the protocol. | expensive | Narrow — the protocol's own three defects (`§D-1`–`§D-3`) are already repaired in place; the residual risk is confidence in generality from a single worked instance, not a known defect. | accepted-as-stated-risk |
| Engines accept an injected LLM endpoint in the common case, preserving token comparability. | Falsifier 8 | Survey the endpoint-injection surface of the five sandbox-candidate engines already named in the lineup; a documentation pass. | cheap | Narrowest of the falsifiers — a non-gating documentation pass placed in `## §BP` Phase 1 alongside the two gating falsifiers; failure "collapses to the shallowest-denominator problem the selection exists to fix" for engine comparability specifically, not a `SELECTION.md`-level reversal. | accepted-as-stated-risk |
| The "≈ days" custom-modality cost figure is close enough to a measured port to plan against. | none — `MODEL-RED-TEAM.md ## §A F-15` | Measure a real custom-modality port and compare against the reindex-planner `reuse` argument's day estimate. | expensive | Narrowest — the structural basis (reindex-planner `reuse`, zero re-index cost) is code-verified; only the day figure itself is `[inference]`, and `## §VD`/`## §PC` never restate it as settled. | accepted-as-stated-risk |
| A2 (LLM call output size, 400–1,500 tokens) is the dominant lever on the index-side cost multiplier, and its band is wide enough to plan against without a measured value. | none — `RIG.md ### §CM.3` residual cost-model input | Measure LLM call output size across real gleaning calls on a real corpus, replacing the 400–1,500-token band. | moderate | Narrowest — moving A2 2x moves the per-chunk total by roughly +14% (low end) to +38% (high end); real, but the order-of-magnitude correction against `RT-selfimprove.md` comes from the code-verified fact that gleaning fires by default, not from A2's own uncertainty. | accepted-as-stated-risk |
| A1 (the ~4-chars/token heuristic) is accurate enough for the `cl100k`-family `gpt-4o-mini` tokenizer. | none — `RIG.md ### §CM.3` residual cost-model input | Measure the actual chars/token ratio against a real corpus sample, replacing the ±15-25% heuristic band. | cheap | Narrowest — a well-established approximation; its own stated uncertainty band is already disclosed rather than hidden. | accepted-as-stated-risk |
| A3 (realized query-time context utilization) tracks close enough to the 30,000-token ceiling to plan against without measurement. | none — `RIG.md ### §CM.3` residual cost-model input | Measure realized query-time context utilization on a real corpus, replacing the stated hard-upper-bound assumption. | expensive | Narrowest — corpus-parameterized, unresolved by design at this phase; affects only cost-figure precision, not any settled decision. | accepted-as-stated-risk |
| A4 (judge self-disagreement, ~5-15%, ~0.3M tokens/decision) tracks close enough to `RT-selfimprove.md`'s carried figures to plan against without a judge implementation. | none — `RIG.md ### §CM.3` residual cost-model input | Run a judge implementation against a small sample and measure self-disagreement, replacing the carried figure. | expensive | Narrowest — no judge implementation exists anywhere in this record to verify against; affects only cost-figure precision. | accepted-as-stated-risk |

**Reconciliation against `## §VD`'s conditions, checked and recorded.** The `must-run-before-build-commits` set above is `{Falsifier 2, Falsifier 5}` — exactly the two falsifiers `## §VD`'s Conditions section names, and no others. The two are the same set seen from two places: `## §VD` states them as the conditions on the go decision itself; this register states them as the two rows whose blast radius meets D-08's must-run test. No divergence found; none needed resolving. [inference] [CONTEXT D-08]

## §H1 — Handoff ledger 1: the Databasise build

**Status:** written (MODEL-03: inherits-as-settled vs. must-decide-itself, per D-16).

Two sides. The inherits-as-settled side is what the build does not get to reopen; the must-decide-itself side is every open question the build owns, each carrying an explicit owner tag and a citation, in the line shape `RIG.md ## Appendix A`'s own fence list uses: `- **<item>** — <what/why>. Owner: **<owner>**, per <citation>.` [inference] [CONTEXT D-16]

### Inherits as settled

Pointer-shaped by design — each entry names what is settled and where, and does not restate the settled text, per this document's own non-duplication boundary. [inference] [CONTEXT D-11]

- The architecture selection itself, including its spike-005 amendment (the three artifact scopes, the repaired laundering-test argument). Settled at `SELECTION.md`, including `SELECTION.md §AMENDMENT`.
- The frozen contract vocabulary all five governing documents carry: `NodeKind`'s tagged sum and the seventeen-member `effects[]` union, the three artifact scopes and the blast-radius rule, the budget-as-splittable-capability-token shape, the promote/rollback ledger discipline, the admission machinery all three stress modalities exercise. Settled at `CONTRACT.md §2`, `§3`, `§9`, `§7`, `§8`, `§17`, closing at `MODEL-RED-TEAM.md ## §R V-4`, `V-7`, `V-1`.
- Every question this project's five phases actually closed: the five-store-type vocabulary and the Lexical sub-capability gap (`V-2`, `V-3`), `§19.4`'s limb-(c) scope and fusion-neighbour absorption rule (`V-5`, `V-6`), and every terminal, non-handover disposition in `MODEL-RED-TEAM.md ## §R`.
- `## §VD`'s conditional-go verdict itself, and the falsifier reconciliation `## §RK` above confirms: `{Falsifier 2, Falsifier 5}` is the must-run set, checked and settled at this phase's close (subject to the build actually running and passing both, per `## §VD`'s own conditions — that act is the build's, not this ledger's).

### Must decide itself

Seeded from the record's own named fences, not fresh reasoning. Each entry is attached to the `## §BP` rung that first faces it, where one does. [inference] [CONTEXT D-16]

- **Runner, scheduler and storage-keying design** — the process model `RIG.md ## §RUN.3` deliberately does not design. Owner: **the build**, per `RIG.md Appendix A` and `CONTEXT D-12`. First faced at `## §BP` Phase 1 (the validator and depth machinery are the first rung to need a runner at all).
- **Intra-node concurrency's own scheduling mechanism** — `CONTRACT.md §9`'s `V-7` repair settles the *contract* question (a node's internal parallelism is out of `§9`'s scope, metered only at the node's declared boundary); it does not settle *how* the runner actually schedules or bounds that internal parallelism in a real process. `RIG.md Appendix A` still lists this fence, marked discharged at the contract level only — what remains is an implementation decision, folded into the runner-design fence directly above rather than invented as a separate, unowned item. Owner: **the build**, per `RIG.md Appendix A` and `MODEL-RED-TEAM.md ## §R V-7`. First faced at the same rung as runner design, `## §BP` Phase 1.
- **The default-posture switches for answer-level and index-side measurement** — a build-time configuration act, not a design decision this project makes, per `RIG.md ## §F3.2`'s own reversibility note: "switching a class on is a configuration act informed by `### §CM.2`'s numbers — a build-time decision, not a Phase 5 one." Owner: **the build**, per `RIG.md ## §F3.2` and `CONTEXT D-05`. No `## §BP` rung names it explicitly — it becomes relevant only once the build has real trace data to inform the switch, which no rung above states as its own deliverable; stated here as facing no specific rung rather than left silently unattached.
- **Measured validation of the cost model** — replacing `RIG.md ### §CM.3`'s four residual `[inference]` inputs (A1 through A4, also entered individually in `## §RK` above) with measurements against a real corpus, which this project's own no-runs fence (`CONTEXT D-10`) forbids here. Owner: **the build or a later spike**, per `RIG.md Appendix A` and `RIG.md §CM.3`. No `## §BP` rung names it explicitly, since every rung above is falsifier-triage or decomposition work, not cost-model remeasurement; the build's own detailed planning is where this lands.
- **A per-chunk chunker/embedder provenance stamp on the machine's own stored chunk artifact** — `ANATOMY.md ## §F`'s `DR-04` row records this question as narrowed, not closed, by Phase 3's D9 repair, and states its own narrowing argument there rather than here: what the record settles is per-item, query-side attribution on `ScoredItem.provenance`; what stays open is the stored chunk artifact itself, whose provenance today is `CONTRACT.md §7`'s per-run SA-2 sub-recipe stamp at the artifact-registry level together with `ANATOMY.md ## §C`'s per-ref `ChunkRef(corpus_id, recipe@version, ordinal, content_hash)`. The row's own two named options — a checkable per-chunk stamp on the artifact, or those two coverings alone — are left unselected between rather than superseded, so this fence carries no repair direction beyond the two options themselves, and the record says so rather than manufacturing a preference. Owner: **the build**, per `ANATOMY.md ## §F`'s `DR-04` row and `PARTS.md ## Appendix A`'s `DR-04` row. First faced at `## §BP` Phase 3, the first rung whose own deliverables name a machine-owned chunk artifact at all — `codebase-memory-mcp` admitted and run once consuming machine chunks, LightRAG's ingest core admitted opaque under `quarantined` — where Phase 1 delivers the validator and depth machinery and Phase 2 the query-side re-cut, neither of which produces the artifact this question asks about.
- **Intra-node concurrency's own name in `## §RK`, and every other register row handed to the build under D-13 in plan 06-05, each carried with its stated repair direction so the build has a starting point rather than only a problem statement:**
  - **`N1`** (store kinds excluded from `NodeKind`) — leave `§2`'s design as stated; reopen only if a future modality forces a store-instance-as-node case. Owner: **the build**, per `MODEL-RED-TEAM.md ## §R N1`. No `## §BP` rung names it; reopens only on a forcing case no rung above is built to produce.
  - **`N3`** (`writes_artifact` stays undifferentiated) — leave it undifferentiated; the artifact registry's namespace/`space_id` fields already recover the target store type downstream. Owner: **the build**, per `MODEL-RED-TEAM.md ## §R N3`. No `## §BP` rung names it.
  - **`F-07`** (`mutable-store` A/B exclusion) — define the snapshot/reset protocol `§14.4` point 3 names, or state the exclusion permanent. Owner: **the build**, per `MODEL-RED-TEAM.md ## §A F-07`. No `## §BP` rung names it — none of the four rungs exercises a `mutable-store` component.
  - **`F-08`** (seam-level trace shape undefined) — extend `§18.2`'s envelope with a minimum shape (`name@version`, spend, outcome). Owner: **the build**, per `MODEL-RED-TEAM.md ## §A F-08`. No `## §BP` rung names it.

**This ledger's own completeness rule.** Every fence in the record naming a Phase 6 owner has been discharged into one of the two sides above, and every question this project did not close appears on the must-decide side with an owner — no open question exits this project without appearing in `## §H1` or `## §H2`. [inference] [CONTEXT D-16]

## §H2 — Handoff ledger 2: Sourcerer across the seam

**Status:** written (MODEL-03: can-rely-on vs. must-change, per D-16).

Two sides again, but a different pair. The can-rely-on side is assembly over frozen vocabulary — cite `CONTRACT.md §18`, do not re-derive. The must-change side is the only genuinely new writing this phase does, grounded in Sourcerer's own surviving planning documents rather than in assumption about what Sourcerer does. [inference] [CONTEXT D-16]

**A note on the evidence path.** `06-CONTEXT.md`'s own stated path for Sourcerer's surviving `.planning/` docs (`/home/chris/Vibe Coding/Sourcerer/.planning/`) no longer resolves — the repository was renamed. The corrected, verified path is `/home/chris/Vibe Coding/Sourcerer-Archived/.planning/`, confirmed before this section was written to list `milestones/v2.0-REQUIREMENTS.md` and `milestones/v2.0-ROADMAP.md`. This ledger's must-change side is grounded in that corrected path; the archived directory itself is read-only evidence and was not modified by this task. [inference] [CONTEXT D-16]

### Can rely on

- **The response envelope's closed shape.** `CONTRACT.md §18.2`: evidence references, a trace reference, depth and tier labels, partial/degraded flags, and `counted_by` on any token number — and nothing else. An applet may therefore assume the field set it receives is fixed regardless of which modality answered, and will not have to revisit its own response-parsing code when a modality is swapped.
- **The invariance rule.** `CONTRACT.md §18.3`: "swapping the fitted modality MUST change no field a consumer sees." No modality-specific field, no modality-specific error distinct from the refusal vocabulary, no modality name anywhere in the envelope. An applet may assume its own error-handling and rendering logic stays correct across a modality swap without a code change on the applet side.
- **Modality selection without naming a wiring.** `CONTRACT.md §18.4`: a stable alias, a declared capability, a harness by name, or the default selector — never a wiring name, node id, or instance hash. An applet may assume it selects *what it wants*, never *which internal component answers it*, and this selection vocabulary does not change when a new modality is added.
- **The tool-surface-growth rule.** `CONTRACT.md §18.5`: the tool surface grows per genuinely new *part*, never per modality answering an operation an existing tool already exposes. An applet may assume the REST/MCP tool surface it already calls against does not grow simply because Databasise gains a second or third modality behind the same seam.

This side is assembly over `CONTRACT.md §18`'s already-frozen text, cited clause by clause rather than re-derived. [docs-verified] [CONTRACT §18]

### Must change

Grounded in Sourcerer's own v2.0 requirements at `/home/chris/Vibe Coding/Sourcerer-Archived/.planning/milestones/v2.0-REQUIREMENTS.md` and `v2.0-ROADMAP.md`, distinguishing what the contract forces from what Sourcerer's own plan already committed to independently of this model — conflating the two would overstate this project's own cost.

- **Contract-forced.** Sourcerer's v2.0 plan already commits to running the Databasise engine inside the substrate with REST and MCP answering on loopback (`v2.0-REQUIREMENTS.md ENG-01`: "Databasise engine runs inside the substrate; REST + MCP answer on loopback"). What this model's `§18` seam forces on top of that already-planned commitment: any applet consuming Databasise's responses today, before this seam exists, must be rewritten to consume the closed envelope shape (`§18.2`) rather than whatever ad hoc response shape the current stock-LightRAG-plus-Cozo engine returns — the envelope's closed-set rule (no wiring name, node id, or instance hash) is new discipline the pre-modality-swap engine's own responses were never required to honor.
- **Already committed by Sourcerer, not this model's cost.** The assistant harness relocating substrate-side with the `host.ai()` seam left unchanged applet-side (`v2.0-REQUIREMENTS.md ENG-02`) is Sourcerer's own v2.0 commitment, independent of whether a modality is swappable underneath Databasise — `host.ai()` is a different seam than `§18`'s public engine seam, and this model's ledger does not add work here.
- **Already committed by Sourcerer, and directly the applet this ledger's rely-on side is written for.** The wiki read-view applet rendering live Databasise data through the seam (`v2.0-REQUIREMENTS.md ENG-03`) is Sourcerer's own planned first engine-backed applet slice — the concrete instance of "an applet may assume the field set it receives is fixed" above, not new work this model's ledger creates.
- **Open question, Sourcerer named as owner.** Sourcerer's v2.0 documents commit to running the engine substrate-side and to a first applet consuming it, but are silent on whether any *other* existing Sourcerer applet (beyond the planned wiki read-view) already parses a pre-seam Databasise response shape that `§18.2`'s closed envelope would break. This model's record has no evidence either way — it is not filled by inference presented as fact. Owner: **Sourcerer's own v2.0 planning**, to be answered when Track 2 (Phase 13, the Databasise-repo track `v2.0-ROADMAP.md` names as running parallel to Phases 10/11) actually joins the shell track at Phase 14.

### Joint completeness

Between `## §H1` and `## §H2`, every open question exiting this project has a named owner. `## §H1` owns what the Databasise build itself must decide; `## §H2` owns what Sourcerer, as the seam's consumer, must change or leave as its own open question. Nothing exits either ledger unowned. [inference] [CONTEXT D-16]

## §ST — Document status table

**Status:** written (MODEL-02: `superseded` / `archived-with-standing` status for every document in `.planning/architectures/` and `.planning/architectures/D-VARIANTS/`, per D-14/D-15; each non-governing row's status is backed by a matching in-file banner, applied at plan 06-03 and cross-verified against this table by `model-check.sh`). [inference] [CONTEXT D-14] [CONTEXT D-15]

Status is drawn from a closed three-token set, stated once here rather than per row: **`governing`** — the five governing documents (`CONTRACT.md`, `ANATOMY.md`, `PARTS.md`, `CATALOG.md`, `RIG.md`), plus `D-VARIANTS/SELECTION.md`, `MODEL-RED-TEAM.md`, and this document itself; **`superseded`** — a document a later, named document fully replaces, carrying no residual value the record still needs; **`archived-with-standing`** — a document that lost the selection or completed its work but deliberately retains force (evidence a ruling cites, a design revisitable under a stated condition, a finding dispositioned elsewhere). The two non-governing statuses are two, not one: collapsing them would either strip standing from the variant record — treating D1/D2/D5's still-relevant conditions and the adversarial/compliance evidence as dead — or leave genuinely-dead drafts (`CANDIDATES.md`, `WIRING-SPEC-DRAFT.md`) unmarked as if still live. `D-VARIANTS/SELECTION.md` governs on any disagreement between this table and any other document in either directory, per `CONTRACT.md §Status`'s own precedence statement, and is never itself marked `superseded` or `archived-with-standing` — it remains the frozen source every governing document transcribes and elaborates. [inference] [CONTRACT §Status]

**Scope.** This table covers markdown documents in `.planning/architectures/` and `.planning/architectures/D-VARIANTS/` only. Non-markdown artifacts in those directories — the five gate scripts (`anatomy-check.sh`, `parts-check.sh`, `catalog-check.sh`, `rig-check.sh`, `model-check.sh`), `rig-trace.schema.json`, and the `wirings/` subdirectory — are out of this table's scope; they are tooling and worked instances, not documents carrying a governing/superseded/archived-with-standing status.

The row set below is derived from a directory listing of `.planning/architectures/*.md` and `.planning/architectures/D-VARIANTS/*.md` (18 files) rather than from memory, so a document nobody remembered cannot be silently omitted.

| Document | Status | What supersedes it / standing retained | Go instead to |
|---|---|---|---|
| `CONTRACT.md` | governing | — | (governing document) |
| `ANATOMY.md` | governing | — | (governing document) |
| `PARTS.md` | governing | — | (governing document) |
| `CATALOG.md` | governing | — | (governing document) |
| `RIG.md` | governing | — | (governing document) |
| `D-VARIANTS/SELECTION.md` | governing | — | (governs on any disagreement; never marked) |
| `MODEL-RED-TEAM.md` | governing | — | (governing document) |
| `SYSTEM-MODEL.md` | governing | — | (this document) |
| `CANDIDATES.md` | superseded | Candidates A, B and C superseded by `D-VARIANTS/SELECTION.md` (the D4 One Machine selection, with its spike-005 amendment) | `D-VARIANTS/SELECTION.md` |
| `WIRING-SPEC-DRAFT.md` | superseded | Discharged by `CONTRACT.md` — this draft's own "feeds step 8 (fitting contract spec)" is complete | `CONTRACT.md` |
| `ANATOMY-REVIEW.md` | archived-with-standing | Fully dispositioned in `ANATOMY.md ## Appendix A — ANATOMY-REVIEW disposition` | `ANATOMY.md ## Appendix A` |
| `D-VARIANTS/D1-two-kingdoms.md` | archived-with-standing | The design to revisit if the laundering middle state fails in practice — the `quarantined` artifact scope (`SELECTION.md`'s spike-005 amendment) is the mechanism whose failure would revive it | `D-VARIANTS/SELECTION.md ## Conditions on the selection` |
| `D-VARIANTS/D2-instrument-bus.md` | archived-with-standing | Losing variant, preserved for the conditions under which it would win again — contributed the evidence-tier ladder T0-T3, the pooling key, and the cross-tier veto-only rule (Rule 4) | `D-VARIANTS/SELECTION.md ## What the winner inherits from the losers` |
| `D-VARIANTS/D4-one-machine.md` | archived-with-standing | The selected variant's own draft — `SELECTION.md` carries the governing text, this draft does not | `D-VARIANTS/SELECTION.md` |
| `D-VARIANTS/D5-actor-kernel.md` | archived-with-standing | Losing variant, preserved for the conditions under which it would win again — contributed edge semantics as a named contract property, runtime-minted instance identity, and budget as a splittable capability token | `D-VARIANTS/SELECTION.md ## What the winner inherits from the losers` |
| `D-VARIANTS/RED-TEAM.md` | archived-with-standing | Evidence for `SELECTION.md`'s ruling — the cross-variant adversarial pass, not itself superseded | `D-VARIANTS/SELECTION.md` |
| `D-VARIANTS/COMPLIANCE.md` | archived-with-standing | Evidence for `SELECTION.md`'s ruling — the disqualifier compliance verification, not itself superseded | `D-VARIANTS/SELECTION.md` |
| `D-VARIANTS/BRIEF.md` | archived-with-standing | Evidence for `SELECTION.md`'s ruling — the binding drafting brief that produced the four compared variants, not itself superseded | `D-VARIANTS/SELECTION.md` |

## Appendix A — Requirement-to-section map

**Status:** written (one row per `VERD-01`..`VERD-05` and `MODEL-01`..`MODEL-03`, per D-07's shared-target reconciliation for `VERD-04`/`VERD-05`).

One row per requirement this phase discharges. Status vocabulary is the closed pair `written` / `pending — plan 06-NN`, matching `RIG.md ## Appendix A`'s own convention — a row reading `written` means the section it names is at final quality now, not merely present. Every row below was verified, not asserted: the section named in `Discharged by` was opened and its text checked to actually deliver what the requirement's own wording asks, per the Phase 5 discipline `RIG.md ## Appendix A`'s own close already carries forward — a scope fence is discharged only after its target section's text is read, never by pointer alone. [docs-verified] [RIG §Appendix A]

**`VERD-04` and `VERD-05` both name `## §RK` — stated once here so a reader does not read the shared target as a mapping error.** `## §RK`'s own text states this explicitly (D-07): one merged register, ordered by blast radius, discharges both the ranking `VERD-04` asks for and the triage `VERD-05` asks for from the same row set, because a second, parallel list for one of the two requirements would drift from the other's own triage rather than agreeing with it. [inference] [CONTEXT D-07]

| Requirement | Discharged by | How | Status |
|---|---|---|---|
| VERD-01 | `MODEL-RED-TEAM.md ## §S`, `## §A`, `## §R` | The adversarial pass itself — this document's `## Reading order` item 7 points here rather than restating it, per this document's own non-duplication boundary above; `MODEL-RED-TEAM.md ## Appendix A` carries the full discharge mechanism (twenty-six-row scoring table, every row attacked and closed-vocabulary-verdicted, the `V`-family register closed). | written |
| VERD-02 | `## §VD`, `## §PC` | `## §VD` states the go/no-go ruling (a conditional go, two named conditions); `## §PC` restates per-modality port costs from `PARTS.md ## §P` and `CATALOG.md ## §V` with no new number derived. | written |
| VERD-03 | `## §BP` | Four Databasise build phases in dependency order, each naming the falsifiers or register rows it retires or stating explicitly that it retires none; the spike-005 decomposition order stated as a binding constraint on the ladder, not background. | written |
| VERD-04 | `## §RK` | The merged register's own ordering rule (blast radius, widest first, cost class as tie-break) is the ranking this requirement asks for; every row carries the smallest experiment that would test it. | written |
| VERD-05 | `## §RK` | The same register's disposition column triages Falsifiers 2, 4, 5, 6, 7 and 8 into the closed two-value vocabulary (`must-run-before-build-commits` / `accepted-as-stated-risk`); Falsifiers 1 and 3 are named as already discharged via a prose pointer rather than a row, since the closed vocabulary has no third term for "already settled." | written |
| MODEL-01 | This document itself, `## Reading order` | `SYSTEM-MODEL.md` is the entry point named by this requirement; `## Reading order` states the order a reader follows through the five governing documents, `SELECTION.md`, and `MODEL-RED-TEAM.md`, reconciled against each document's own `## Reading order` block rather than inventing a competing sequence. | written |
| MODEL-02 | `## §ST`, plus in-file banners outside this table's own scope | `## §ST` marks `CANDIDATES.md` and `WIRING-SPEC-DRAFT.md` `superseded` and eight variant/evidence documents `archived-with-standing`, each cross-verified against a matching in-file banner (`model-check.sh` Checks 6/7, both directions). The spike-002 "shallowest-denominator" condition spike 003 rejected is marked at the site the condition itself is recorded — an inline rejection marker on spike 002's own README (plan 06-03) — since a spike document sits outside `## §ST`'s own stated scope (markdown files in `.planning/architectures/` and `.planning/architectures/D-VARIANTS/` only). | written |
| MODEL-03 | `## §H1`, `## §H2` | `## §H1` states what the Databasise build inherits as settled and what it must decide for itself; `## §H2` states what Sourcerer, as the seam's consumer, can rely on and what it must change, grounded in Sourcerer's own corrected archived-path v2.0 planning documents. | written |

---

*Phase: 06-red-team-verdict-assembled-model*
*Frame established: plan 06-01 (tracer). Sections filled across plans 06-02 through 06-08. This document's own frame is complete: every `## §` section and this appendix read `written`, no section reads `pending`.*
